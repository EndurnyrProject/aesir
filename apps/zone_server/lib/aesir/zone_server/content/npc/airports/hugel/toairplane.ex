defmodule Aesir.ZoneServer.Content.Npc.Airports.Hugel.Toairplane do
  @moduledoc """
  Transpiled from rAthena airports/hugel.txt:15 (toairplane#hugel).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "hugel",
        x: 178,
        y: 142,
        dir: 0,
        sprite: 45,
        name: "toairplane",
        scope: :shared,
        unique_name: "toairplane#hugel",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    {ctx, v1} =
      ctx
      |> mes("To use the airship, you are required to pay 1,200 zeny or a Free Airship Ticket.")
      |> mes("Would you like to use the service?")
      |> next()
      |> select(["Yes", "No"])

    ctx =
      if v1 == 1 do
        ctx =
          if count_item(ctx, 7311) > 0 do
            ctx = ctx |> delitem(7311, 1) |> warp("airplane", 244, 58)
            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          if zeny(ctx) >= 1200 do
            ctx = ctx |> pay_zeny(1200) |> warp("airplane", 244, 58)
            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("I am sorry, but you do not have enough money.")
          |> mes("Please remember, you are required to pay 1,200 zeny to use the service.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx |> mes("Thank you, please come again.") |> close()
  end
end
