defmodule Aesir.ZoneServer.Content.Npc.Airports.Airships.UmbalaLady do
  @moduledoc """
  Transpiled from rAthena airports/airships.txt:210 (Umbala Lady#ein_p).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "airplane",
        x: 66,
        y: 93,
        dir: 3,
        sprite: 783,
        name: "Umbala Lady",
        scope: :shared,
        unique_name: "Umbala Lady#ein_p"
      }
    ]

  @impl true
  def on_talk(ctx) do
    ctx = ctx |> emotion(:think) |> mes("[Lady]")

    ctx =
      if get_char_var(ctx, :event_umbala, 0) >= 3 do
        ctx
        |> mes("Shush...")
        |> mes("Honey, behave~")
        |> mes("Don't act so excited")
        |> mes("when we're out in a")
        |> mes("public place like this!")
      else
        ctx
        |> mes("Chooktu!")
        |> mes("Sacraup matii!")
        |> mes("Shaka gurftalfi")
        |> mes("huntiki manjoo!")
      end

    close(ctx)
  end
end
