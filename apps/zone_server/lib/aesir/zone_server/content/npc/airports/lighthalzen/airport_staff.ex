defmodule Aesir.ZoneServer.Content.Npc.Airports.Lighthalzen.AirportStaff do
  @moduledoc """
  Transpiled from rAthena airports/lighthalzen.txt:15 (Airport Staff#lhz_air1a::lhz_airport1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "lhz_airport",
        x: 143,
        y: 43,
        dir: 5,
        sprite: 90,
        name: "Airport Staff",
        unique_name: "lhz_airport1"
      },
      %{
        map: "lhz_airport",
        x: 158,
        y: 43,
        dir: 5,
        sprite: 90,
        name: "Airship Staff",
        unique_name: "Airship Staff#lhz_air1b"
      },
      %{
        map: "lhz_airport",
        x: 126,
        y: 43,
        dir: 5,
        sprite: 90,
        name: "Airship Staff",
        unique_name: "Airship Staff#lhz_air1c"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Airport Staff]")
      |> mes("Welcome to the")
      |> mes("Lighthalzen Airport,")
      |> mes("where we offer nonstop")
      |> mes("flights to Einbroch, Juno and Hugel.")
      |> next()
      |> select(["Board the Airship.", "Cancel."])

    ctx =
      if v1 == 1 do
        {ctx, v2} =
          ctx
          |> mes("[Airport Staff]")
          |> mes("The boarding fee is")
          |> mes("1,200 zeny, but you can")
          |> mes("waive the fee if you redeem")
          |> mes("a Free Ticket for Airship.")
          |> next()
          |> select(["Yes", "No"])

        ctx =
          if v2 == 1 do
            ctx =
              if count_item(ctx, 7311) > 0 do
                ctx = ctx |> delitem(7311, 1) |> warp("lhz_airport", 148, 51)
                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              if zeny(ctx) >= 1200 do
                ctx = ctx |> pay_zeny(1200) |> warp("lhz_airport", 148, 51)
                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              ctx
              |> mes("[Airship Staff]")
              |> mes("I'm sorry, but you don't")
              |> mes("have 1,200 zeny to pay")
              |> mes("for the boarding fee.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx
    |> mes("[Airport Staff]")
    |> mes("Thank you and")
    |> mes("please come again.")
    |> mes("Have a good day~")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
