defmodule Aesir.ZoneServer.Content.Npc.Airports.Yuno.DomesticBoarding do
  @moduledoc """
  Transpiled from rAthena airports/yuno.txt:84 (Domestic Boarding).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "y_airport",
        x: 145,
        y: 63,
        dir: 5,
        sprite: 91,
        name: "Domestic Boarding",
        scope: :shared
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Boarding Staff]")
      |> mes("Would you like to board the")
      |> mes("Airship that flies to Einbroch,")
      |> mes("Lighthalzen and Hugel? If so,")
      |> mes("please let me guide you to that")
      |> mes("Airship's boarding area.")
      |> next()
      |> select(["Yes", "No"])

    ctx =
      if v1 == 1 do
        ctx = warp(ctx, "yuno", 59, 244)
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Boarding Staff]")
    |> mes("Very well, then.")
    |> mes("Thank you for your")
    |> mes("patronage, and I hope")
    |> mes("you enjoy your travels~")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
