defmodule Aesir.ZoneServer.Content.Npc.Einbroch.VoteTimer3 do
  @moduledoc """
  Transpiled from rAthena kafras/dts_warper.txt:254 (Vote Timer3#ein).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []

  @impl true
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  def on_event("OnTimer60000", ctx), do: ev_ontimer60000(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_onenable(ctx) do
    initnpctimer(ctx)
  end

  def ev_ontimer60000(ctx) do
    ctx = stopnpctimer(ctx)

    ctx =
      if get_server_var(ctx, "DTS_PeriodCheck", 0) == 8640 do
        ctx =
          ctx
          |> set_server_var("DTS_PeriodCheck", 0)
          |> set_server_var("DTS_Result", 0)
          |> set_server_var("DTS_KafraVotes", 0)
          |> set_server_var("DTS_JondaVotes", 0)

        ctx = set_server_var(ctx, "DTS_JondaWins", get_server_var(ctx, "DTS_JondaWins", 0) - 1)

        ctx
        |> set_server_var("DTS_VoteCount", get_server_var(ctx, "DTS_VoteCount", 0) + 1)
        |> donpcevent("Vote Timer#ein::OnEnable")
      else
        ctx
        |> set_server_var("DTS_PeriodCheck", get_server_var(ctx, "DTS_PeriodCheck", 0) + 1)
        |> donpcevent("Vote Timer3#ein::OnEnable")
      end

    ctx
  end
end
