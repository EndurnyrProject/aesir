defmodule Aesir.ZoneServer.Content.Npc.Einbroch.ScrutinyAssociation24971 do
  @moduledoc """
  Transpiled from rAthena kafras/dts_warper.txt:1155 (Scrutiny Association#7).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []

  @impl true
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  def on_event("OnTimer1000", ctx), do: ev_ontimer1000(ctx)
  def on_event("OnTimer5000", ctx), do: ev_ontimer5000(ctx)
  def on_event("OnTimer10000", ctx), do: ev_ontimer10000(ctx)
  def on_event("OnTimer15000", ctx), do: ev_ontimer15000(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_onenable(ctx) do
    initnpctimer(ctx)
  end

  def ev_ontimer1000(ctx) do
    announce(
      ctx,
      "All the votes for the Dungeon Teleport Service Provider Election have been received and counted.",
      0,
      "0x70dbdb"
    )
  end

  def ev_ontimer5000(ctx) do
    announce(ctx, "The results are now in...", 0, "0x70dbdb")
  end

  def ev_ontimer10000(ctx) do
    announce(
      ctx,
      "This time, Kafra Corporation will be the Dungeon Teleport Service Provider.",
      0,
      "0x70dbdb"
    )
  end

  def ev_ontimer15000(ctx) do
    ctx
    |> announce("Many thanks to all of you who have voted and shown your support.", 0, "0x70dbdb")
    |> stopnpctimer()
  end
end
