defmodule Aesir.ZoneServer.Content.Npc.Einbroch.Mark do
  @moduledoc """
  Transpiled from rAthena cities/einbroch.txt:35 (Mark#ein).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "einbroch", x: 218, y: 198, dir: 5, sprite: 855, name: "Mark"},
      %{map: "einbroch", x: 173, y: 229, dir: 5, sprite: 855, name: "Oberu"},
      %{map: "einbroch", x: 176, y: 172, dir: 5, sprite: 855, name: "Khemko"}
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = set_local(ctx, :"npc_name$", strnpcinfo(ctx, 1))

    ctx =
      ctx
      |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
      |> mes("Good day~")

    ctx =
      ctx
      |> mes(Rathena.concat(Rathena.concat("I'm ", get_local(ctx, :"npc_name$", "")), ", your"))
      |> mes("guide to exploring")
      |> mes("the Einbroch Tower.")
      |> next()

    ctx =
      ctx
      |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
      |> mes("Einbroch Tower offers")
      |> mes("the best view of our city")
      |> mes("and it's a great place to")
      |> mes("meet with friends or take")
      |> mes("a date. The Einbroch Tower")
      |> mes("admission fee is 10 zeny.")
      |> next()

    {ctx, v1} =
      ctx
      |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
      |> mes("Right now, we're offering")
      |> mes("a special promotion called")
      |> mes("the Apple Combo Set for only")
      |> mes("20 zeny. This set includes")
      |> mes("Einbroch Tower admission")
      |> mes("and an Apple to snack on.")
      |> next()
      |> select(["Tower Admission Only", "Apple Combo Set", "Cancel"])

    ctx =
      case v1 do
        1 ->
          if zeny(ctx) < 10 do
            ctx =
              ctx
              |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
              |> mes("I'm sorry, but you")
              |> mes("don't have enough")
              |> mes("zeny. The Einbroch")
              |> mes("Tower Admission")
              |> mes("fee is 10 zeny.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx =
              ctx
              |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
              |> mes("Thank you for")
              |> mes("using our services.")
              |> mes("Let me guide you to")
              |> mes("the tower right away.")
              |> pay_zeny(10)
              |> close()
              |> warp("einbroch", 181, 196)

            throw({:script_end, ctx})
          end

        2 ->
          if zeny(ctx) < 20 do
            ctx =
              ctx
              |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
              |> mes("I'm sorry, but you don't")
              |> mes("have enough zeny. The")
              |> mes("Apple Combo Set is 20 zeny.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx =
              ctx
              |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
              |> mes("Before I guide you to")
              |> mes("the tower, let me check")
              |> mes("your status to insure")
              |> mes("your safety before I give")
              |> mes("you the Apple Combo Set.")
              |> next()

            if Rathena.truthy?(checkweight(ctx, [{512, 1}])) do
              ctx =
                ctx
                |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
                |> mes("Thank you for")
                |> mes("using our services.")
                |> mes("Let me guide you to")
                |> mes("the tower right away.")
                |> pay_zeny(20)
                |> give_item(512, 1)
                |> close()
                |> warp("einbroch", 174, 204)

              throw({:script_end, ctx})
            else
              ctx =
                ctx
                |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
                |> mes("I'm sorry, but you're carrying")
                |> mes(
                  "too many items with you. Please store some of your things in your Kafra Storage before purchasing"
                )
                |> mes("the Apple Combo Set.")
                |> close()

              throw({:script_end, ctx})
            end
          end

        3 ->
          ctx =
            ctx
            |> mes(Rathena.concat(Rathena.concat("[", get_local(ctx, :"npc_name$", "")), "]"))
            |> mes("I see.")
            |> mes("Feel free to")
            |> mes("come back any")
            |> mes("time. Thank you.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
