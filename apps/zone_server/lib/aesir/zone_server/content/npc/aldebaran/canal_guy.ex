defmodule Aesir.ZoneServer.Content.Npc.Aldebaran.CanalGuy do
  @moduledoc """
  Transpiled from rAthena cities/aldebaran.txt:126 (Canal Guy#alde).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "aldebaran", x: 46, y: 129, dir: 4, sprite: 97, name: "Canal Guy"}]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Panama]")
      |> mes(
        "Al De Baran is known world wide as the City of Canals. The waterways really add a sophisticated, romantic touch to our fair city."
      )
      |> next()
      |> select(["About the Canals", "End Conversation"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Panama]")
            |> mes("Well, a canal is an artificial waterway used for travel,")
            |> mes("shipping, or irrigation.")
            |> next()
            |> mes("[Panama]")
            |> mes(
              "However, the canals over here are just for show. If we needed to transport anything, we just use the Kafra Corporation Teleport service!"
            )
            |> close()

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Panama]")
            |> mes("I have that you will enjoy your stay in Al De Baran.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
