defmodule Aesir.ZoneServer.Content.Npc.Aldebaran.BellKeeper do
  @moduledoc """
  Transpiled from rAthena cities/aldebaran.txt:518 (Bell Keeper#A).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "aldebaran", x: 143, y: 136, dir: 4, sprite: 89, name: "Bell Keeper"}]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Bell Keeper]")
      |> mes(
        "I have been charged by the Committee of 'Heaven on Earth' to guard this entrance of the Clock Tower."
      )
      |> next()
      |> select(["About Clock Tower.", "Quit."])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("[Bell Keeper]")
          |> mes(
            "Every floor of this tower is connected to each other by a certain device we like to call 'Warp Gear.'"
          )
          |> next()
          |> mes("[Bell Keeper]")
          |> mes(
            "Even though there are interconnecting warps everywhere in the Clock Tower, beware the 'Random Warp.'"
          )
          |> next()
          |> mes("[Bell Keeper]")
          |> mes(
            "The 'Random Warp' will transport you to an unknown spot. Be advised if you don't want to suddenly be separated from your party..."
          )
          |> next()
          |> mes("[Bell Keeper]")
          |> mes(
            "Remember, Random Warps are shown in green on the mini-map. So keep your eyes peeled for that, as well as for those dangerous Clocks."
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Bell Keeper]")
    |> mes("Please take heed that this Clock Tower is filled with extremely dangerous monsters.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
