defmodule Aesir.ZoneServer.Content.Npc.Aldebaran.ShellGatheringLady do
  @moduledoc """
  Transpiled from rAthena cities/aldebaran.txt:96 (Shell Gathering Lady#ald).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "aldebaran", x: 81, y: 61, dir: 4, sprite: 101, name: "Shell Gathering Lady"}]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Joanne]")
      |> mes("I enjoy gathering shells from the sea. It's really fun and relaxing~")
      |> next()
      |> select(["Shell Gathering?", "End Conversation"])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("[Joanne]")
          |> mes(
            "When you see bubbles popping up from the sand or muddy puddles, try digging into the ground a bit. You might find some shells underneath the ground!"
          )
          |> next()
          |> mes("[Joanne]")
          |> mes("Have you heard")
          |> mes("of Ambernite?")
          |> mes("That shell monster")
          |> mes("is pretty tough~")
          |> next()
          |> mes("[Joanne]")
          |> mes(
            "It's usually seen at the beach near the west province of Prontera. If you ever try attacking it without being prepared, you might be in trouble."
          )
          |> next()
          |> mes("[Joanne]")
          |> mes("Ambernite is")
          |> mes("pretty strong!")
          |> mes("So look out for it!")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Joanne]")
    |> mes("Ambernite is")
    |> mes("pretty strong!")
    |> mes("So look out for it!")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
