defmodule Aesir.ZoneServer.Content.Npc.LhzAirport.ArrivalStaff do
  @moduledoc """
  Transpiled from rAthena airports/lighthalzen.txt:57 (Arrival Staff#lhz_air2a::lhz_airport2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "lhz_airport",
        x: 143,
        y: 49,
        dir: 3,
        sprite: 90,
        name: "Arrival Staff",
        unique_name: "lhz_airport2"
      },
      %{map: "lhz_airport", x: 126, y: 51, dir: 3, sprite: 90, name: "Arrival Staff"},
      %{map: "lhz_airport", x: 158, y: 50, dir: 3, sprite: 90, name: "Arrival Staff"}
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Arrival Staff]")
      |> mes("Welcome to Lighthalzen Airport.")
      |> mes("Please let me guide you to the")
      |> mes("main terminal if you are arriving from your flight. Otherwise, please")
      |> mes("board the departing Airship to reach your intended destination.")
      |> next()
      |> select(["Exit to main terminal.", "Cancel."])

    ctx =
      if v1 == 1 do
        {ctx, v2} =
          ctx
          |> mes("[Arrival Staff]")
          |> mes("Once you're in the main terminal, you will need to pay the fee again")
          |> mes("to board an Airship. You should")
          |> mes("only exit if Lighthalzen is your intended destination. Shall we")
          |> mes("proceed to the main terminal?")
          |> next()
          |> select(["Yes", "No"])

        ctx =
          if v2 == 1 do
            ctx = warp(ctx, "lhz_airport", 142, 40)
            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx
    |> mes("[Arrival Staff]")
    |> mes("Alright, thank you")
    |> mes("for your patronage")
    |> mes("and I hope you have")
    |> mes("a pleasant flight~")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
