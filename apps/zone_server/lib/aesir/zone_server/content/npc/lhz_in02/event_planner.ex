defmodule Aesir.ZoneServer.Content.Npc.LhzIn02.EventPlanner do
  @moduledoc """
  Transpiled from rAthena cities/lighthalzen.txt:3067 (Event Planner).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "lhz_in02", x: 40, y: 280, dir: 6, sprite: 833, name: "Event Planner"}]

  @impl true
  def on_talk(ctx) do
    ctx
    |> mes("[Jellarin]")
    |> mes("I don't like this.")
    |> mes("But I don't like that")
    |> mes("idea either. What will")
    |> mes("I do for a new event, eh?")
    |> next()
    |> mes("[Jellarin]")
    |> mes("I need something")
    |> mes("major, something that'll")
    |> mes("really shake up the world,")
    |> mes("something epochal, but what?")
    |> mes("Hey, do you have any ideas?")
    |> close()

    exit(:normal)
  end
end
