defmodule Aesir.ZoneServer.Content.Npc.Jawaii.Mariner122263 do
  @moduledoc """
  Transpiled from rAthena cities/jawaii.txt:78 (Mariner#toalbe).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "jawaii", x: 122, y: 263, dir: 5, sprite: 100, name: "Mariner"}]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Mariner]")
      |> mes("This ship")
      |> mes("is headed back")
      |> mes("towards ^003399Alberta^000000.")
      |> mes("Have you enjoyed your time in Jawaii? You should check to see")
      |> mes("if you forgot anything before we go.")
      |> next()
      |> mes("[Mariner]")
      |> mes("Now, are you")
      |> mes("ready to go back")
      |> mes("to Alberta?")
      |> next()
      |> select(["Go back.", "Cancel."])

    ctx =
      if v1 == 1 do
        ctx
        |> mes("[Mariner]")
        |> mes("Now, let me")
        |> mes("take you back")
        |> mes("to Alberta.")
        |> close()
        |> warp("alberta", 192, 157)

        exit(:normal)
      else
        ctx
      end

    ctx
    |> mes("[Mariner]")
    |> mes("Yeah...")
    |> mes("Try to enjoy your")
    |> mes("vacation as much")
    |> mes("as you can. We'll be")
    |> mes("ready to leave when")
    |> mes("you are.")
    |> close()

    exit(:normal)
  end
end
