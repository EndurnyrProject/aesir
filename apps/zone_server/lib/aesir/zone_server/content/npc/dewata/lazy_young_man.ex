defmodule Aesir.ZoneServer.Content.Npc.Dewata.LazyYoungMan do
  @moduledoc """
  Transpiled from rAthena re/cities/dewata.txt:177 (Lazy Young Man#dew).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "dewata",
        x: 114,
        y: 243,
        dir: 6,
        sprite: 536,
        name: "Lazy Young Man",
        unique_name: "Lazy Young Man#dew",
        trigger: {1, 1}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx =
      if checkweight(ctx, [{1201, 1}]) == 0 or
           get_char_var(ctx, :MaxWeight, 0) - weight(ctx) < 500 do
        ctx =
          ctx
          |> mes("[Lazy Young Man]")
          |> mes("Why are you carrying so much? Isn't it heavy?")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx =
      if Rathena.truthy?(count_item(ctx, 11534)) do
        {ctx, v1} =
          ctx
          |> mes("[Lazy Young Man]")
          |> mes("Wewt~!! Isn't that Palm Juice?")
          |> mes("Can you give me that? I just woke")
          |> mes("up, and my throat is parched!")
          |> next()
          |> select(["Give", "Do not give"])

        ctx =
          case v1 do
            1 ->
              ctx =
                ctx
                |> mes("[Lazy Young Man]")
                |> mes("Really? For me?? I'll give you this in return... Thanks... Hehe~")
                |> delitem(11534, 1)
                |> get_group_item(:giftbox)
                |> close()

              throw({:script_end, ctx})

            2 ->
              ctx = ctx |> mes("[Lazy Young Man]") |> mes("So be it~ Ok...") |> close()
              throw({:script_end, ctx})

            _ ->
              ctx
          end

        ctx
      else
        ctx
      end

    ctx
    |> mes("[Lazy Young Man]")
    |> mes("Ah~ Thristy~ Anyone have some Palm")
    |> mes("juice for me?~")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if count_item(ctx, 11534) == 0 do
        ctx =
          ctx
          |> mes("[Lazy Young Man]")
          |> mes("Arrgg~ I'm thirsty~~ so thirsty~~")
          |> mes("Give me Palm juice... Palm Juuiice~~")
          |> emotion(:fret)
          |> next()
          |> emotion(:fret)
          |> next()
          |> emotion(:fret)
          |> next()
          |> emotion(:fret)
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
  end
end
