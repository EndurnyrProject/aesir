defmodule Aesir.ZoneServer.Content.Npc.Mora.RelaxingTraveler do
  @moduledoc """
  Transpiled from rAthena re/cities/mora.txt:352 (Relaxing Traveler#ep14_1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "mora", x: 30, y: 128, dir: 4, sprite: 514, name: "Relaxing Traveler"}]

  @impl true
  def on_talk(ctx) do
    {ctx, _} =
      ctx
      |> mes("[Traveler]")
      |> mes("The Mora Inn is famous")
      |> mes("for its unusual pieces of furniture.")
      |> mes("Mushroom closets and apple dressers...")
      |> mes("Aren't they so innovative?")
      |> next()
      |> select(["... ... ..."])

    ctx
    |> mes("[Traveler]")
    |> mes("Sorry... maybe it's just me...")
    |> emotion(:sorry)
    |> close()
  end
end
