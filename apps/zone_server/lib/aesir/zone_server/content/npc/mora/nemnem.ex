defmodule Aesir.ZoneServer.Content.Npc.Mora.Nemnem do
  @moduledoc """
  Transpiled from rAthena re/cities/mora.txt:30 (Nemnem#p).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "mora", x: 160, y: 123, dir: 3, sprite: 517, name: "Nemnem"}]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Nemnem]")
      |> mes("My pain is getting worse and worse")
      |> mes("since the crevice appeared in Bifrost.")
      |> mes("Are you responsible for it, young one?")
      |> next()
      |> select(["Of course I am!", "That's an unfair accusation!"])

    ctx =
      case v1 do
        1 ->
          ctx = ctx |> mes("[Nemnem]") |> mes("Bra~vo!") |> emotion(:cool) |> close()
          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Nemnem]")
            |> mes("If not, never mind!")
            |> emotion(:profusely_sweat)
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
