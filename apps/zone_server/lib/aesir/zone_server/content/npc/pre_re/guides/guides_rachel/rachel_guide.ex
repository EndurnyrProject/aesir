defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesRachel.RachelGuide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_rachel.txt:22 (Rachel Guide).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "rachel",
        x: 138,
        y: 146,
        dir: 5,
        sprite: 934,
        name: "Rachel Guide",
        scope: :pre_renewal
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> mes("[Rachel Guide]")
    |> mes("Welcome to the capital of")
    |> mes("Arunafeltz, Rachel where a warm")
    |> mes("breath of goddess Freya reaches.")
    |> mes("If this is the first time for you")
    |> mes("to use the guide services, why")
    |> mes("don't you check the \"Notice\" menu first?")
    |> set_local(:loop1, 1)
    |> loop_1()
    |> close()
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, ["Cheshrumnir", "Inn", "Weapon Shop", "Tool Shop", "Airport", "Cancel"])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Rachel Guide]")
              |> mes(
                "Cheshrumnir is a holy ground where pope, the incarnation of goddess Freya stays."
              )
              |> mes("Take the road to the norh to find the building.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 150, 249, 1, 16_711_680)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Rachel Guide]")
              |> mes("You can rest your fatigue off the journey in the Inn.")
              |> mes("The left building next to me is the Inn of Rachel.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 115, 149, 2, 16_711_935)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Rachel Guide]")
              |> mes("Do you want to check out the weapons that are sold in Rachel?")
              |> mes("The weapon shop is located nearby the western gate.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 42, 87, 3, 10_092_543)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Rachel Guide]")
              |> mes("Rachel tool shop sells the best quality potions.")
              |> mes("It's located nearby the western gate.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 83, 78, 4, 255)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Rachel Guide]")
              |> mes("The Airport is located outside the eastern gate.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 273, 125, 5, 65280)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Rachel Guide]")
              |> mes("If you like to get rid of all the location marks on your Mini-Map,")
              |> mes("just ask me again, and choose \"Remove Marks from Mini-Map\" menu.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx
        |> next()
        |> select(["Village Guide", "Remove Marks from Mini-Map", "Notice", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Rachel Guide]")
              |> mes("I can tell you any building location as long as it is in Rachel.")
              |> mes("So where do you want to go?")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes", "No"])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 150, 249, 1, 16_711_680)
            |> viewpoint(2, 115, 149, 2, 16_711_935)
            |> viewpoint(2, 42, 87, 3, 10_092_543)
            |> viewpoint(2, 83, 78, 4, 255)
            |> viewpoint(2, 273, 125, 5, 65280)
            |> mes("[Rachel Guide]")
            |> mes(
              "Okay, they are gone now. If you have more locations to ask, just let me know."
            )

          3 ->
            ctx
            |> mes("[Rachel Guide]")
            |> mes("When you are using the ''Village Guide'' menu,")
            |> mes(
              "make sure that building locations will be marked on your mini-map at the upper right side of your screen."
            )
            |> mes(
              "If you cannot see your mini-map, use the short cut key ''ctrl+tab'' or press the ''Map'' button on your basic information windows, okay?"
            )
            |> mes(
              "And you can also zoom out your mini-map by using the ''-'' button in case you cannot view the entire map of the village."
            )

          4 ->
            ctx =
              ctx
              |> mes("[Rachel Guide]")
              |> mes("Hope you have a wonderfull journey")
              |> mes("in Arunafeltz.")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
