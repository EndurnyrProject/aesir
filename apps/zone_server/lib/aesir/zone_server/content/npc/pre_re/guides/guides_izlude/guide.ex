defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesIzlude.Guide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_izlude.txt:22 (Guide#iz).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "izlude",
        x: 121,
        y: 87,
        dir: 6,
        sprite: 105,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "Guide#iz"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("prt_soldier", 2)
    |> mes("[Izlude Guide]")
    |> mes("Welcome to Izlude,")
    |> mes("Prontera's satellite city.")
    |> mes("If you need any guidance")
    |> mes("around Izlude, feel free")
    |> mes("to ask me at anytime.")
    |> set_local(:loop1, 1)
    |> loop_1()
    |> cutin("prt_soldier", 255)
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, [
          "^FF0000Swordman Association^000000",
          "Swordman Hall",
          "Arena",
          "Izlude Marina",
          "Weapon Shop",
          "Tool Shop",
          "Cancel"
        ])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("The Swordman Association")
              |> mes("is located on an island that is")
              |> mes("in west Izlude. If you're thinking of changing jobs to Swordman,")
              |> mes("you should check it out.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 52, 140, 0, 16_711_680)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("The Swordman Hall")
              |> mes("is located in the eastern")
              |> mes("island connected to Izlude.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 214, 130, 1, 65280)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("Izlude's famous")
              |> mes("Arena is located at the")
              |> mes("northern end of Izlude.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 128, 225, 2, 65280)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("You can find the")
              |> mes("Marina in the northeast")
              |> mes("part of Izlude. There, you can")
              |> mes("ride a ship which will take you")
              |> mes("to Alberta or Byalan Island.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 200, 180, 3, 16_711_680)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("You can easily")
              |> mes("find the Weapon Shop")
              |> mes("in northwest Izlude.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 111, 149, 4, 16_711_935)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("The Tool Shop shouldn't")
              |> mes("be too hard to find in the")
              |> mes("northeast part of Izlude.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 148, 148, 5, 16_711_935)
              else
                ctx
              end

            ctx

          7 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("Please ask me to ''Remove")
              |> mes("Marks from Mini-Map'' if you")
              |> mes("no longer wish to have the")
              |> mes("location marks displayed")
              |> mes("on your Mini-Map.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx |> next() |> select(["City Guide", "Remove Marks from Mini-Map", "Notice.", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("Please select")
              |> mes("a location from")
              |> mes("the following menu.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes", "No"])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 237, 41, 0, 65280)
            |> viewpoint(2, 237, 41, 1, 255)
            |> viewpoint(2, 46, 345, 2, 65280)
            |> viewpoint(2, 175, 220, 3, 16_711_680)
            |> viewpoint(2, 134, 221, 4, 16_711_680)
            |> viewpoint(2, 204, 214, 5, 16_711_680)
            |> set_local(:compass_check, 0)

          3 ->
            ctx
            |> mes("[Izlude Guide]")
            |> mes("Advances in sorcery and")
            |> mes("technology have allowed")
            |> mes("us to update our information")
            |> mes("system, enabling up to mark")
            |> mes("locations on your Mini-Map")
            |> mes("for easier navigation.")
            |> next()
            |> mes("[Izlude Guide]")
            |> mes("Your Mini-Map is located")
            |> mes("in the upper right corner")
            |> mes("of the screen. If you can't")
            |> mes("see it, press the Ctrl + Tab")
            |> mes("keys or click the ''Map'' button in your Basic Info Window.")
            |> next()
            |> mes("[Izlude Guide]")
            |> mes("On your Mini-Map,")
            |> mes("click on the ''+'' and ''-''")
            |> mes("symbols to zoom in and")
            |> mes("our of your Mini-Map. We")
            |> mes("hope you enjoy your travels")
            |> mes("here in the city of Izlude.")

          4 ->
            ctx =
              ctx
              |> mes("[Izlude Guide]")
              |> mes("Okay then, feel")
              |> mes("free to come to me")
              |> mes("if you ever feel lost")
              |> mes("around Izlude, alright?")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
