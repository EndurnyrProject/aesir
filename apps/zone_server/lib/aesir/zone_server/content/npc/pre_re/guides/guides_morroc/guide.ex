defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesMorroc.Guide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_morroc.txt:21 (Guide#moc::MocGuide).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "morocc",
        x: 153,
        y: 286,
        dir: 6,
        sprite: 707,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "MocGuide"
      },
      %{
        map: "morocc",
        x: 54,
        y: 97,
        dir: 0,
        sprite: 707,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "Guide#2moc"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("moc_soldier", 2)
    |> mes("[Morocc Guide]")
    |> mes("Welcome to Morocc,")
    |> mes("the frontier town of the")
    |> mes("Rune-Midgarts Kingdom.")
    |> mes("Please ask me for help if")
    |> mes("you're having any trouble")
    |> mes("finding anything in town.")
    |> set_local(:loop1, 1)
    |> loop_1()
    |> cutin("moc_soldier", 255)
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, [
          "^FF0000Thief Guild^000000",
          "Weapon Shop",
          "Inn",
          "Pub",
          "Mercenary Guild",
          "Forge",
          "Cancel"
        ])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("The Thief Guild is")
              |> mes("in charge of all Job")
              |> mes("Changes to the Thief")
              |> mes("Class. From what I hear,")
              |> mes("you can find them inside")
              |> mes("the Pyramids nearby...")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 24, 297, 2, 16_711_680)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("The Weapon Shop")
              |> mes("is in the southeast")
              |> mes("end of Morocc.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 253, 56, 3, 16_711_935)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("There are Inns")
              |> mes("where you can rest")
              |> mes("at the southeast and")
              |> mes("northeast ends of Morocc.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                ctx
                |> viewpoint(1, 197, 66, 4, 16_711_935)
                |> viewpoint(1, 273, 269, 5, 16_711_935)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("You can find the")
              |> mes("Pub in northeast Morocc.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 52, 259, 6, 16_711_935)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("The Mercenary")
              |> mes("Guild is located")
              |> mes("in East Morocc.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 284, 171, 7, 65280)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("The Forge is")
              |> mes("located just")
              |> mes("southwest from")
              |> mes("the center of Morocc.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 47, 47, 7, 16_711_935)
              else
                ctx
              end

            ctx

          7 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("Please ask me to ''Remove")
              |> mes("Marks from Mini-Map'' if you")
              |> mes("no longer wish to have the")
              |> mes("location marks displayed")
              |> mes("on your Mini-Map.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx |> next() |> select(["City Guide", "Remove Marks from Mini-Map", "Notice", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("Please select")
              |> mes("a location from")
              |> mes("the following menu.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes.", "No."])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 237, 41, 2, 65280)
            |> viewpoint(2, 237, 41, 3, 255)
            |> viewpoint(2, 46, 345, 4, 65280)
            |> viewpoint(2, 175, 220, 5, 16_711_680)
            |> viewpoint(2, 175, 220, 6, 16_711_680)
            |> viewpoint(2, 175, 220, 7, 16_711_680)
            |> set_local(:compass_check, 0)

          3 ->
            ctx
            |> mes("[Morocc Guide]")
            |> mes("Advances in sorcery and")
            |> mes("technology have allowed")
            |> mes("us to update our information")
            |> mes("system, enabling up to mark")
            |> mes("locations on your Mini-Map")
            |> mes("for easier navigation.")
            |> next()
            |> mes("[Morocc Guide]")
            |> mes("Your Mini-Map is located")
            |> mes("in the upper right corner")
            |> mes("of the screen. If you can't")
            |> mes("see it, press the Ctrl + Tab")
            |> mes("keys or click the ''Map'' button in your Basic Info Window.")
            |> next()
            |> mes("[Morocc Guide]")
            |> mes("On your Mini-Map,")
            |> mes("click on the ''+'' and ''-''")
            |> mes("symbols to zoom in and")
            |> mes("our of your Mini-Map. We")
            |> mes("hope you enjoy your travels")
            |> mes("here in the city of Morocc.")

          4 ->
            ctx =
              ctx
              |> mes("[Morocc Guide]")
              |> mes("Alright then,")
              |> mes("try to stay out of")
              |> mes("too much trouble")
              |> mes("out there, adventurer.")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
