defmodule Aesir.ZoneServer.Content.Npc.PreRe.Jobs.M11.Mage.MixingMachine do
  @moduledoc """
  Transpiled from rAthena pre-re/jobs/1-1/mage.txt:287 (Mixing Machine).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "geffen_in",
        x: 164,
        y: 112,
        dir: 4,
        sprite: 111,
        name: "Mixing Machine",
        scope: :pre_renewal
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Mixing Machine]")
      |> mes(
        "This machine is the property of the Geffen Mage Guild and is used only for mixing solutions for magic purposes."
      )
      |> next()
      |> select(["Use Machine.", "Cancel."])

    ctx =
      if v1 == 1 do
        {ctx, v2} =
          ctx
          |> mes("[Mixing Machine]")
          |> mes("Choose the")
          |> mes("Solvent for")
          |> mes("the Solution.")
          |> next()
          |> select(["Payon Solution.", "Morocc Solution.", "No Solvent."])

        ctx =
          case v2 do
            1 ->
              ctx =
                if count_item(ctx, 1089) == 0 do
                  ctx =
                    ctx
                    |> mes("[Mixing Machine]")
                    |> mes("Error.")
                    |> mes("Cannot find the item.")
                    |> mes("Please check again.")
                    |> mes("Process Halting.")
                    |> close()

                  throw({:script_end, ctx})
                else
                  ctx
                end

              set_local(ctx, :mixitem2, 1)

            2 ->
              ctx =
                if count_item(ctx, 1088) == 0 do
                  ctx =
                    ctx
                    |> mes("[Mixing Machine]")
                    |> mes("Error.")
                    |> mes("Cannot find the item.")
                    |> mes("Please check again.")
                    |> mes("Process Halting.")
                    |> close()

                  throw({:script_end, ctx})
                else
                  ctx
                end

              set_local(ctx, :mixitem2, 2)

            3 ->
              ctx = set_local(ctx, :mixitem2, 0)
              ctx

            _ ->
              ctx
          end

        {ctx, v13} =
          ctx
          |> loop_2()
          |> mes("[Mixing Machine]")
          |> mes("Please enter the ")
          |> mes("Serial Number of")
          |> mes("the Magic Powder.")
          |> next()
          |> loop_9()
          |> mes("[Mixing Machine]")
          |> mes("Choose a")
          |> mes("Catalyst Stone.")
          |> next()
          |> select([
            "Yellow Gemstone.",
            "Red Gemstone.",
            "Blue Gemstone.",
            "1carat Diamond.",
            "Skip."
          ])

        ctx =
          case v13 do
            1 ->
              set_local(ctx, :mixitem3, 1)

            2 ->
              set_local(ctx, :mixitem3, 2)

            3 ->
              set_local(ctx, :mixitem3, 3)

            4 ->
              set_local(ctx, :mixitem3, 4)

            5 ->
              ctx

            _ ->
              ctx
          end

        ctx =
          ctx
          |> mes("[Mixing Machine]")
          |> mes("All Set.")
          |> mes("Initiating")
          |> mes("Mixing process.")
          |> mes("Please Wait.")
          |> next()
          |> mes("[Mixing Machine]")
          |> mes("- Proverb of the Day -")

        v14 = Enum.random(1..5)

        ctx =
          case v14 do
            1 ->
              mes(
                ctx,
                "An Eye for an Eye: When you take from a person, you must replace or repay in some way."
              )

            2 ->
              mes(
                ctx,
                "Credibility is a Man's Currency: There's a value in genuine trust that cannot be measured."
              )

            3 ->
              ctx
              |> mes(
                "What Goes Around Comes Around: Ultimately, you will be treated in the way you treat others."
              )
              |> mes(
                "It means 'When you harm Another you will be harmed by him in an unavoidable situation'."
              )

            4 ->
              mes(
                ctx,
                "A good neighbor is better than a distant brother: When you need help, you can count on those close to you."
              )

            _ ->
              mes(
                ctx,
                "Birds of a Feather Flock Together: You can look at a person's friends as an indicator of their character."
              )
          end

        ctx = next(ctx)

        ctx =
          if get_local(ctx, :mixitem1_1, 0) == 2 and get_local(ctx, :mixitem1_2, 0) == 3 and
               get_local(ctx, :mixitem1_3, 0) == 1 and get_local(ctx, :mixitem2, 0) == 1 and
               get_local(ctx, :mixitem3, 0) == 1 and get_local(ctx, :magic_powder, 0) == 1 do
            ctx
            |> mes("[Mixing Machine]")
            |> mes("Mage Test Solution No. 1.")
            |> give_item(1071, 1)
            |> next()
          else
            ctx =
              if get_local(ctx, :mixitem1_1, 0) == 3 and get_local(ctx, :mixitem1_2, 0) == 1 and
                   get_local(ctx, :mixitem1_3, 0) == 1 and get_local(ctx, :mixitem2, 0) == 0 and
                   get_local(ctx, :mixitem3, 0) == 2 and get_local(ctx, :magic_powder, 0) == 2 do
                ctx
                |> mes("[Mixing Machine]")
                |> mes("Mage Test Solution No. 2.")
                |> give_item(1085, 1)
                |> next()
              else
                ctx =
                  if get_local(ctx, :mixitem1_1, 0) == 6 and get_local(ctx, :mixitem1_2, 0) == 1 and
                       get_local(ctx, :mixitem1_3, 0) == 0 and get_local(ctx, :mixitem2, 0) == 1 and
                       get_local(ctx, :mixitem3, 0) == 3 and get_local(ctx, :magic_powder, 0) == 3 do
                    ctx
                    |> mes("[Mixing Machine]")
                    |> mes("Mage Test Solution No. 3.")
                    |> give_item(1086, 1)
                    |> next()
                  else
                    ctx =
                      if get_local(ctx, :mixitem1_1, 0) == 2 and
                           get_local(ctx, :mixitem1_2, 0) == 3 and
                           get_local(ctx, :mixitem1_3, 0) == 0 and
                           get_local(ctx, :mixitem2, 0) == 2 and get_local(ctx, :mixitem3, 0) == 4 and
                           get_local(ctx, :magic_powder, 0) == 4 do
                        ctx
                        |> mes("[Mixing Machine]")
                        |> mes("Mage Test Solution No. 4.")
                        |> give_item(1087, 1)
                        |> next()
                      else
                        ctx
                        |> mes("[Mixing Machine]")
                        |> mes("Unexpected")
                        |> mes("Error Occurred.")
                        |> give_item(1090, 1)
                        |> next()
                      end

                    ctx
                  end

                ctx
              end

            ctx
          end

        ctx =
          ctx
          |> mes("[Mixing Machine]")
          |> mes("Mixing Complete.")
          |> mes("Thank you.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    close(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp loop_6(ctx) do
    result =
      try do
        {ctx, v7} = input(ctx, :int)
        ctx = set_local(ctx, :input, v7)

        ctx =
          if get_local(ctx, :input, 0) > 10000 do
            ctx
            |> next()
            |> mes("[Mixing Machine]")
            |> mes("Error: Item limit exceeded. Please enter values less than 10,000 try again.")
            |> next()
          else
            ctx =
              if count_item(ctx, 909) > 0 do
                set_local(
                  ctx,
                  :mixitem1_1,
                  get_local(ctx, :mixitem1_1, 0) + get_local(ctx, :input, 0)
                )
              else
                ctx
              end

            ctx = set_local(ctx, :progress, 2)
            throw({:brk_6, ctx})
          end

        {:next, ctx}
      catch
        :throw, {:brk_6, ctx} -> {:done, ctx}
        :throw, {:cont_6, ctx} -> {:next, ctx}
      end

    case result do
      {:next, ctx} -> loop_6(ctx)
      {:done, ctx} -> ctx
    end
  end

  defp loop_7(ctx) do
    result =
      try do
        {ctx, v8} = input(ctx, :int)
        ctx = set_local(ctx, :input, v8)

        ctx =
          if get_local(ctx, :input, 0) > 10000 do
            ctx
            |> next()
            |> mes("[Mixing Machine]")
            |> mes("Error: Item limit exceeded. Please enter values less than 10,000 try again.")
            |> next()
          else
            ctx =
              if count_item(ctx, 914) > 0 do
                set_local(
                  ctx,
                  :mixitem1_2,
                  get_local(ctx, :mixitem1_2, 0) + get_local(ctx, :input, 0)
                )
              else
                ctx
              end

            ctx = set_local(ctx, :progress, 2)
            throw({:brk_7, ctx})
          end

        {:next, ctx}
      catch
        :throw, {:brk_7, ctx} -> {:done, ctx}
        :throw, {:cont_7, ctx} -> {:next, ctx}
      end

    case result do
      {:next, ctx} -> loop_7(ctx)
      {:done, ctx} -> ctx
    end
  end

  defp loop_8(ctx) do
    result =
      try do
        {ctx, v9} = input(ctx, :int)
        ctx = set_local(ctx, :input, v9)

        ctx =
          if get_local(ctx, :input, 0) > 10000 do
            ctx
            |> next()
            |> mes("[Mixing Machine]")
            |> mes("Error: Item limit exceeded. Please enter values less than 10,000 try again.")
            |> next()
          else
            ctx =
              if count_item(ctx, 519) > 0 do
                set_local(
                  ctx,
                  :mixitem1_3,
                  get_local(ctx, :mixitem1_3, 0) + get_local(ctx, :input, 0)
                )
              else
                ctx
              end

            ctx = set_local(ctx, :progress, 2)
            throw({:brk_8, ctx})
          end

        {:next, ctx}
      catch
        :throw, {:brk_8, ctx} -> {:done, ctx}
        :throw, {:cont_8, ctx} -> {:next, ctx}
      end

    case result do
      {:next, ctx} -> loop_8(ctx)
      {:done, ctx} -> ctx
    end
  end

  defp loop_4(ctx) do
    result =
      try do
        {ctx, v6} = select(ctx, ["Jellopy.", "Fluff.", "Milk.", "Ready to Mix."])

        ctx =
          case v6 do
            1 ->
              loop_6(ctx)

            2 ->
              loop_7(ctx)

            3 ->
              loop_8(ctx)

            4 ->
              ctx =
                if get_local(ctx, :progress, 0) != 2 do
                  set_local(ctx, :progress, 1)
                else
                  ctx
                end

              ctx = ctx |> set_local(:continue, 1) |> next()
              ctx

            _ ->
              ctx
          end

        ctx =
          if Rathena.truthy?(get_local(ctx, :continue, 0)) do
            throw({:brk_4, ctx})
          else
            ctx
          end

        {:next, ctx}
      catch
        :throw, {:brk_4, ctx} -> {:done, ctx}
        :throw, {:cont_4, ctx} -> {:next, ctx}
      end

    case result do
      {:next, ctx} -> loop_4(ctx)
      {:done, ctx} -> ctx
    end
  end

  defp loop_2(ctx) do
    result =
      try do
        ctx =
          if get_local(ctx, :progress, 0) == 2 do
            ctx = mes(ctx, "[Mixing Machine]")

            ctx =
              if get_local(ctx, :mixitem1_1, 0) != 0 do
                mes(
                  ctx,
                  Rathena.concat(
                    Rathena.concat("Jellopy: ", get_local(ctx, :mixitem1_1, 0)),
                    " ea."
                  )
                )
              else
                ctx
              end

            ctx =
              if get_local(ctx, :mixitem1_2, 0) != 0 do
                mes(
                  ctx,
                  Rathena.concat(
                    Rathena.concat("Fluff: ", get_local(ctx, :mixitem1_2, 0)),
                    " ea."
                  )
                )
              else
                ctx
              end

            ctx =
              if get_local(ctx, :mixitem1_3, 0) != 0 do
                mes(
                  ctx,
                  Rathena.concat(Rathena.concat("Milk: ", get_local(ctx, :mixitem1_3, 0)), " ea.")
                )
              else
                ctx
              end

            ctx =
              if get_local(ctx, :mixitem2, 0) == 0 do
                mes(ctx, "Solvent: None.")
              else
                ctx
              end

            ctx =
              if get_local(ctx, :mixitem2, 0) == 1 do
                mes(ctx, "Solvent: Payon Solution.")
              else
                ctx
              end

            ctx =
              if get_local(ctx, :mixitem2, 0) == 2 do
                mes(ctx, "Solvent: Morocc Solution.")
              else
                ctx
              end

            {ctx, v3} =
              ctx
              |> next()
              |> mes("[Mixing Machine]")
              |> mes(
                "Please choose if you wish to begin mixing, or to re-enter the number of items to be mixed."
              )
              |> next()
              |> select(["Begin Mixing.", "Re-Enter Number of Items.", "Reset."])

            ctx =
              try do
                case v3 do
                  1 ->
                    {ctx, v4} =
                      ctx
                      |> mes("[Mixing Machine]")
                      |> mes(
                        "Please place the items into the Mixing Receptacle. Make sure the item amounts are correct."
                      )
                      |> next()
                      |> mes("[Mixing Machine]")
                      |> mes(
                        "You cannot adjust or restore items once they are placed into the Mixing Receptacle."
                      )
                      |> next()
                      |> mes("[Mixing Machine]")
                      |> mes(
                        "If everything is correct, press the 'Mix' button when you are ready. Otherwise, press the 'Cancel' button."
                      )
                      |> next()
                      |> select(["Press 'Mix' Button.", "Press 'Cancel' Button."])

                    ctx =
                      if v4 == 1 do
                        ctx =
                          ctx
                          |> mes("[Mixing Machine]")
                          |> mes("Place items into the Mixing Receptacle now. Please wait.")
                          |> next()
                          |> mes("[Mixing Machine]")

                        ctx =
                          if count_item(ctx, 909) < get_local(ctx, :mixitem1_1, 0) do
                            ctx =
                              ctx
                              |> mes("Insufficient Jellopy.")
                              |> mes("Please Check again.")
                              |> mes("Process Halted.")
                              |> close()

                            throw({:script_end, ctx})
                          else
                            ctx =
                              if count_item(ctx, 914) < get_local(ctx, :mixitem1_2, 0) do
                                ctx =
                                  ctx
                                  |> mes("Insufficient Fluff.")
                                  |> mes("Please Check again.")
                                  |> mes("Process Halted.")
                                  |> close()

                                throw({:script_end, ctx})
                              else
                                ctx =
                                  if count_item(ctx, 519) < get_local(ctx, :mixitem1_3, 0) do
                                    ctx =
                                      ctx
                                      |> mes("Insufficient Milk.")
                                      |> mes("Please Check again.")
                                      |> mes("Process Halted.")
                                      |> close()

                                    throw({:script_end, ctx})
                                  else
                                    ctx =
                                      if get_local(ctx, :mixitem2, 0) == 1 or
                                           get_local(ctx, :mixitem2, 0) == 2 do
                                        ctx =
                                          if count_item(ctx, 1089) == 0 and
                                               count_item(ctx, 1088) == 0 do
                                            ctx =
                                              ctx
                                              |> mes("Solution not found.")
                                              |> mes("Please Check again.")
                                              |> mes("Process Halted.")
                                              |> close()

                                            throw({:script_end, ctx})
                                          else
                                            ctx
                                          end

                                        ctx
                                      else
                                        ctx
                                      end

                                    ctx
                                  end

                                ctx
                              end

                            ctx
                          end

                        ctx =
                          if get_local(ctx, :mixitem1_1, 0) != 0 do
                            delitem(ctx, 909, get_local(ctx, :mixitem1_1, 0))
                          else
                            ctx
                          end

                        ctx =
                          if get_local(ctx, :mixitem1_2, 0) != 0 do
                            delitem(ctx, 914, get_local(ctx, :mixitem1_2, 0))
                          else
                            ctx
                          end

                        ctx =
                          if get_local(ctx, :mixitem1_3, 0) != 0 do
                            delitem(ctx, 519, get_local(ctx, :mixitem1_3, 0))
                          else
                            ctx
                          end

                        ctx =
                          if get_local(ctx, :mixitem2, 0) == 1 do
                            delitem(ctx, 1089, 1)
                          else
                            ctx
                          end

                        ctx =
                          if get_local(ctx, :mixitem2, 0) == 2 do
                            delitem(ctx, 1088, 1)
                          else
                            ctx
                          end

                        ctx
                        |> mes("Items are Ready.")
                        |> mes("Close the Lid.")
                        |> set_local(:progress, 3)
                        |> next()
                      else
                        ctx
                      end

                    ctx

                  2 ->
                    ctx |> set_local(:continue, 0) |> next()

                  3 ->
                    {ctx, v5} =
                      ctx
                      |> set_local(:mixitem1_1, 0)
                      |> set_local(:mixitem1_2, 0)
                      |> set_local(:mixitem1_3, 0)
                      |> set_local(:progress, 0)
                      |> set_local(:continue, 0)
                      |> mes("[Mixing Machine]")
                      |> mes("Reset Complete.")
                      |> mes("Initiate again?")
                      |> next()
                      |> select(["Yes.", "No."])

                    ctx =
                      if v5 == 1 do
                        throw({:brk_3, ctx})
                      else
                        ctx
                      end

                    ctx =
                      ctx
                      |> mes("[Mixing Machine]")
                      |> mes("Process Halted.")
                      |> mes("Thank you.")
                      |> close()

                    throw({:script_end, ctx})

                  _ ->
                    ctx
                end
              catch
                :throw, {:brk_3, ctx} -> ctx
              end

            ctx =
              if get_local(ctx, :progress, 0) == 3 do
                throw({:brk_2, ctx})
              else
                ctx
              end

            ctx
          else
            ctx =
              if get_local(ctx, :progress, 0) == 1 do
                ctx |> mes("[Mixing Machine]") |> mes("Nothing found.") |> next()
              else
                ctx
              end

            ctx
          end

        ctx = ctx |> mes("[Mixing Machine]") |> mes("Select items to mix.") |> loop_4()
        {:next, ctx}
      catch
        :throw, {:brk_2, ctx} -> {:done, ctx}
        :throw, {:cont_2, ctx} -> {:next, ctx}
      end

    case result do
      {:next, ctx} -> loop_2(ctx)
      {:done, ctx} -> ctx
    end
  end

  defp loop_9(ctx) do
    result =
      try do
        {ctx, v10} = input(ctx, :int)
        ctx = set_local(ctx, :input, v10)

        ctx =
          if get_local(ctx, :input, 0) < 1000 or get_local(ctx, :input, 0) > 9999 do
            ctx = mes(ctx, "[Mixing Machine]")

            ctx =
              if get_local(ctx, :input, 0) == 0 do
                {ctx, v11} =
                  ctx
                  |> mes("Do you want to skip this Menu?")
                  |> next()
                  |> select(["Yes.", "No."])

                ctx =
                  if v11 == 1 do
                    throw({:brk_9, ctx})
                  else
                    ctx
                  end

                ctx
              else
                ctx |> mes("Invalid Serial Number.") |> mes("Please try again.") |> next()
              end

            ctx
          else
            ctx = mes(ctx, "[Mixing Machine]")

            {ctx, v12} =
              ctx
              |> mes(
                Rathena.concat(
                  Rathena.concat("The Serial Number is #", get_local(ctx, :input, 0)),
                  ", correct?"
                )
              )
              |> next()
              |> select(["Confirm.", "Cancel."])

            ctx =
              if v12 == 1 do
                ctx =
                  if get_local(ctx, :input, 0) == 8472 do
                    set_local(ctx, :magic_powder, 1)
                  else
                    ctx =
                      if get_local(ctx, :input, 0) == 3735 do
                        set_local(ctx, :magic_powder, 2)
                      else
                        ctx =
                          if get_local(ctx, :input, 0) == 2750 do
                            set_local(ctx, :magic_powder, 3)
                          else
                            ctx =
                              if get_local(ctx, :input, 0) == 5429 do
                                set_local(ctx, :magic_powder, 4)
                              else
                                set_local(ctx, :magic_powder, 5)
                              end

                            ctx
                          end

                        ctx
                      end

                    ctx
                  end

                ctx
              else
                ctx
              end

            throw({:brk_9, ctx})
          end

        {:next, ctx}
      catch
        :throw, {:brk_9, ctx} -> {:done, ctx}
        :throw, {:cont_9, ctx} -> {:next, ctx}
      end

    case result do
      {:next, ctx} -> loop_9(ctx)
      {:done, ctx} -> ctx
    end
  end
end
