defmodule Aesir.ZoneServer.Content.Npc.PreRe.Functions.FMerckafra do
  @moduledoc """
  Transpiled from rAthena pre-re/jobs/1-1/merchant.txt:1060 (F_MercKafra).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, _args) do
    ctx =
      if (get_char_var(ctx, :job_merchant_q, 0) == 6 or get_char_var(ctx, :job_merchant_q, 0) == 5) and
           (get_char_var(ctx, :job_merchant_q2, 0) == 8 or
              get_char_var(ctx, :job_merchant_q2, 0) == 7) do
        ctx =
          ctx
          |> mes("[Kafra Employee]")
          |> mes("Oh hello~")
          |> mes("Um, is there")
          |> mes("some special reason")
          |> mes("as to why you're here?")
          |> close()
          |> cutin("", 255)

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :job_merchant_q, 0) == 4 or
               get_char_var(ctx, :job_merchant_q, 0) == 3 do
            ctx =
              ctx
              |> mes("[Kafra Employee]")
              |> mes("A delivery from")
              |> mes("the Merchant Guild?")
              |> mes("Oh, yes, please set")
              |> mes("it down right over there...")

            ctx =
              if count_item(ctx, 1081) == 1 or count_item(ctx, 1082) == 1 or
                   count_item(ctx, 1091) == 1 do
                ctx |> mes("You must be really tired") |> mes("after carrying it for so long!")
              else
                ctx =
                  ctx
                  |> mes("W-wait. Didn't you bring it?")
                  |> mes("Where's the package?")
                  |> close()
                  |> cutin("", 255)

                throw({:script_end, ctx})
              end

            ctx =
              ctx
              |> next()
              |> mes("[Kafra Employee]")
              |> mes("Now, let me check")
              |> mes("the serial number...")

            ctx =
              if get_char_var(ctx, :job_merchant_q2, 0) == 7 and count_item(ctx, 1081) != 0 do
                ctx
                |> mes("3318702. Right, this is")
                |> mes("the one we ordered. Oh,")
                |> mes("and don't forget this receipt!")
                |> delitem(1081, 1)
                |> give_item(1079, 1)
              else
                ctx =
                  if get_char_var(ctx, :job_merchant_q2, 0) == 8 and count_item(ctx, 1082) != 0 do
                    ctx
                    |> mes("3543625. Right, this is")
                    |> mes("the one we ordered. Oh,")
                    |> mes("and don't forget this receipt!")
                    |> delitem(1082, 1)
                    |> give_item(1080, 1)
                  else
                    ctx =
                      ctx |> mes("Mmmm? Hold on. This is") |> mes("the wrong package. What we")

                    ctx =
                      if get_char_var(ctx, :job_merchant_q2, 0) == 7 do
                        mes(ctx, "ordered had the serial number 3318702. I'm sure it's not this.")
                      else
                        ctx =
                          if get_char_var(ctx, :job_merchant_q2, 0) == 8 do
                            mes(
                              ctx,
                              "ordered had the serial number 3543625. I'm sure it's not this."
                            )
                          else
                            mes(ctx, "ordered had the serial number 3318702 or 3543625.")
                          end

                        ctx
                      end

                    ctx = ctx |> close() |> cutin("", 255)
                    throw({:script_end, ctx})
                  end

                ctx
              end

            ctx =
              if get_char_var(ctx, :job_merchant_q, 0) == 4 do
                set_char_var(ctx, :job_merchant_q, 6)
              else
                ctx =
                  if get_char_var(ctx, :job_merchant_q, 0) == 3 do
                    set_char_var(ctx, :job_merchant_q, 5)
                  else
                    ctx
                  end

                ctx
              end

            ctx = next(ctx)

            ctx =
              if count_item(ctx, 1072) != 0 and get_char_var(ctx, :quest_alb_01, 0) == 0 do
                {ctx, _} = select(ctx, ["This is from Chief Mahnsoo of the Merchant Guild..."])

                ctx
                |> delitem(1072, 1)
                |> set_char_var(:quest_alb_01, 1)
                |> mes("[Kafra Employee]")
                |> mes("Oh~! A letter from")
                |> mes("Mahnsoo! Thank you")
                |> mes("so much, I've been dying")
                |> mes("to hear from him. How is")
                |> mes("he doing, is he alright?")
                |> mes("I can't wait to read it...")
                |> next()
                |> mes("[Kafra Employee]")
                |> mes("Oh, thank you for")
                |> mes("going through all the")
                |> mes("trouble of delivering all")
                |> mes("of this. This isn't anything")
                |> mes("special, but please take it.")
                |> mes("Well, see you again~")
                |> give_item(513, 3)
              else
                ctx
                |> mes("[Kafra Employee]")
                |> mes("Thanks again")
                |> mes("for going through")
                |> mes("all of that trouble~")
              end

            ctx = ctx |> close() |> cutin("", 255)
            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    {ctx, nil}
  end
end
