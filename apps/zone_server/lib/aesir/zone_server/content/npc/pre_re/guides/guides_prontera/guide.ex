defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesProntera.Guide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_prontera.txt:26 (Guide#prt::PrtGuide).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "prontera",
        x: 154,
        y: 187,
        dir: 4,
        sprite: 105,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "PrtGuide"
      },
      %{
        map: "prontera",
        x: 282,
        y: 208,
        dir: 2,
        sprite: 105,
        name: "East Gate-Guide",
        scope: :pre_renewal
      },
      %{
        map: "prontera",
        x: 29,
        y: 200,
        dir: 6,
        sprite: 105,
        name: "West Gate-Guide",
        scope: :pre_renewal
      },
      %{
        map: "prontera",
        x: 160,
        y: 29,
        dir: 0,
        sprite: 105,
        name: "South Gate-Guide",
        scope: :pre_renewal
      },
      %{
        map: "prontera",
        x: 151,
        y: 330,
        dir: 4,
        sprite: 105,
        name: "North Gate-Guide",
        scope: :pre_renewal
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("prt_soldier", 2)
    |> mes("[Prontera Guide]")
    |> mes("Welcome to Prontera,")
    |> mes("the beautiful capital of the")
    |> mes("Rune-Midgarts Kingdom. If")
    |> mes(
      "you have questions or need help finding something in the city, don't hesitate to ask."
    )
    |> set_local(:loop1, 1)
    |> loop_1()
    |> cutin("prt_soldier", 255)
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, [
          "Swordman Association",
          "^0000FFSanctuary^000000",
          "Prontera Chivalry",
          "Weapon Shop",
          "Tool Shop",
          "Inn",
          "Trading Post",
          "Pub",
          "Library",
          "Job Agency",
          "Prontera Castle",
          "City Hall",
          "Cancel"
        ])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Swordman Association,")
              |> mes("which handles Job Changes")
              |> mes("to the Swordman class, has")
              |> mes("moved to Izlude. This facility")
              |> mes("is just an empty building now.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 237, 41, 4, 65280)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Prontera Sanctuary")
              |> mes("handles Job Changes to")
              |> mes("the Acolyte class, and can")
              |> mes("be found in the northeast")
              |> mes("corner of Prontera.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 236, 316, 5, 16_711_680)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Prontera Chivralry,")
              |> mes("which is responsible for")
              |> mes("the safety of our capital, is")
              |> mes("in Prontera's northwest corner.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 46, 345, 6, 65280)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Weapon Shop")
              |> mes("is located northeast")
              |> mes("of the central fountain.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 175, 220, 7, 16_711_935)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Tool Shop")
              |> mes("is located northwest")
              |> mes("of the central fountain.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 134, 221, 8, 16_711_935)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Inns in Prontera are")
              |> mes("located both to the east")
              |> mes("and west of Prontera's")
              |> mes("central fountain area.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                ctx
                |> viewpoint(1, 204, 191, 9, 16_711_935)
                |> viewpoint(1, 107, 192, 10, 16_711_935)
              else
                ctx
              end

            ctx

          7 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Trading Post")
              |> mes("can be found southeast")
              |> mes("from the central fountain.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 179, 184, 11, 65280)
              else
                ctx
              end

            ctx

          8 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Pub is located")
              |> mes("southeast of the fountain,")
              |> mes("behind the Trading Post.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 208, 154, 12, 65280)
              else
                ctx
              end

            ctx

          9 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("If you head north from")
              |> mes("the central fountain, you'll")
              |> mes("find an empty area in which")
              |> mes("both branches of the Prontera")
              |> mes("Library can be accessed if you")
              |> mes("head towards the east or west.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                ctx |> viewpoint(1, 120, 267, 13, 65280) |> viewpoint(1, 192, 267, 14, 65280)
              else
                ctx
              end

            ctx

          10 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Job Agency is")
              |> mes("just southwest of the")
              |> mes("central fountain area.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 133, 183, 15, 65280)
              else
                ctx
              end

            ctx

          11 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The Prontera Castle is")
              |> mes("located at the northern")
              |> mes("sector of this city. You can")
              |> mes("go to the fields that are north")
              |> mes("of Prontera by going through")
              |> mes("the castle's rear exit.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 156, 360, 16, 65280)
              else
                ctx
              end

            ctx

          12 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("The City Hall")
              |> mes("is located in the")
              |> mes("southwest corner")
              |> mes("in our city of Prontera.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 75, 91, 17, 130_817)
              else
                ctx
              end

            ctx

          13 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("Please ask me to ''Remove")
              |> mes("Marks from Mini-Map'' if you")
              |> mes("no longer wish to have the")
              |> mes("location marks displayed")
              |> mes("on your Mini-Map.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx |> next() |> select(["City Guide.", "Remove Marks from Mini-Map", "Notice", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("Please select")
              |> mes("a location from")
              |> mes("the following menu.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes", "No"])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 237, 41, 4, 65280)
            |> viewpoint(2, 237, 41, 5, 255)
            |> viewpoint(2, 46, 345, 6, 65280)
            |> viewpoint(2, 175, 220, 7, 16_711_680)
            |> viewpoint(2, 134, 221, 8, 16_711_680)
            |> viewpoint(2, 204, 191, 9, 16_711_680)
            |> viewpoint(2, 107, 192, 10, 16_711_680)
            |> viewpoint(2, 179, 184, 11, 65280)
            |> viewpoint(2, 208, 154, 12, 65280)
            |> viewpoint(2, 120, 267, 13, 65280)
            |> viewpoint(2, 192, 267, 14, 65280)
            |> viewpoint(2, 133, 183, 15, 65280)
            |> viewpoint(2, 156, 360, 16, 65280)
            |> viewpoint(2, 75, 91, 17, 65280)
            |> set_local(:compass_check, 0)

          3 ->
            ctx
            |> mes("[Prontera Guide]")
            |> mes("Advances in sorcery and")
            |> mes("technology have allowed")
            |> mes("us to update our information")
            |> mes("system, enabling up to mark")
            |> mes("locations on your Mini-Map")
            |> mes("for easier navigation.")
            |> next()
            |> mes("[Prontera Guide]")
            |> mes("Your Mini-Map is located")
            |> mes("in the upper right corner")
            |> mes("of the screen. If you can't")
            |> mes("see it, press the Ctrl + Tab")
            |> mes("keys or click the ''Map'' button in your Basic Info Window.")
            |> next()
            |> mes("[Prontera Guide]")
            |> mes("On your Mini-Map,")
            |> mes("click on the ''+'' and ''-''")
            |> mes("symbols to zoom in and")
            |> mes("our of your Mini-Map. We")
            |> mes("hope you enjoy your travels")
            |> mes("here in the city of Prontera.")

          4 ->
            ctx =
              ctx
              |> mes("[Prontera Guide]")
              |> mes("Well, adventurer...")
              |> mes("I hope your journeys")
              |> mes("through Rune-Midgarts")
              |> mes("are both fun and safe.")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
