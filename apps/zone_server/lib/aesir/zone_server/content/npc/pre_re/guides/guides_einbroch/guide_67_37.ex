defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesEinbroch.Guide6737 do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_einbroch.txt:209 (Guide#4ein::EinGuide2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "einbech",
        x: 67,
        y: 37,
        dir: 4,
        sprite: 852,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "EinGuide2"
      },
      %{
        map: "einbech",
        x: 48,
        y: 214,
        dir: 4,
        sprite: 852,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "Guide#5ein"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("ein_soldier", 2)
    |> mes("[Einbech Guide]")
    |> mes("Welcome to Einbech,")
    |> mes("the Mining Town. We're")
    |> mes("here to assist tourists,")
    |> mes("so if you have any questions,")
    |> mes("please feel free to ask us.")
    |> set_local(:loop1, 1)
    |> loop_1()
    |> cutin("ein_soldier", 255)
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, ["Train Station", "Tavern", "Tool Shop", "Swordman Guild", "Mine", "Cancel"])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Einbech Guide]")
              |> mes("The Train Stations are")
              |> mes("located in the northwest")
              |> mes("and northeast parts of")
              |> mes("Einbech. There, you can")
              |> mes("take a train to Einbroch.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 43, 213, 2, 16_711_680)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Einbech Guide]")
              |> mes("The Tavern is located")
              |> mes("in the southern part of")
              |> mes("Einbech. It's a nice place")
              |> mes("to relax after a long day.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 142, 112, 3, 16_711_935)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Einbech Guide]")
              |> mes("You can find the Tool")
              |> mes("Shop in the center of")
              |> mes("Einbech. There, you can")
              |> mes("purchase any tools you")
              |> mes("might need for your travels.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 176, 136, 4, 16_711_935)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Einbech Guide]")
              |> mes("The Swordman Guild")
              |> mes("is located in the eastern")
              |> mes("outskirts of Einbech. It's")
              |> mes("under construction and they")
              |> mes("haven't started accepting")
              |> mes("applications.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 250, 110, 5, 16_711_935)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Einbech Guide]")
              |> mes("The Mine, which is")
              |> mes("Einbech's major industry,")
              |> mes("is located in the northern")
              |> mes("part of this town. It's where")
              |> mes("we get all our ores, although monsters get in the miners' way.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 138, 251, 6, 65280)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Einbech Guide]")
              |> mes("Please ask me to ''Remove")
              |> mes("Marks from Mini-Map'' if you")
              |> mes("no longer wish to have the")
              |> mes("location marks displayed")
              |> mes("on your Mini-Map.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx
        |> next()
        |> select(["City Guide.", "Remove Marks from Mini-Map.", "Notice.", "Cancel."])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Einbech Guide]")
              |> mes("Please select")
              |> mes("a location from")
              |> mes("the following menu.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes.", "No."])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 43, 213, 2, 16_711_680)
            |> viewpoint(2, 142, 112, 3, 16_711_935)
            |> viewpoint(2, 176, 136, 4, 16_711_935)
            |> viewpoint(2, 250, 110, 5, 16_711_935)
            |> viewpoint(2, 138, 251, 6, 65280)
            |> set_local(:compass_check, 0)
            |> mes("[Einbech Guide]")
            |> mes("Okay, the marks from")
            |> mes("your Mini-Map have been")
            |> mes("removed. If you need any")
            |> mes("guidance around Einbech,")
            |> mes("please let me or one of the")
            |> mes("other Einbech Guides know.")

          3 ->
            ctx
            |> mes("[Einbech Guide]")
            |> mes("Through the technology of")
            |> mes("the Schwarzwald Republic,")
            |> mes("we've upgraded to a digital")
            |> mes("information system that allows")
            |> mes("us to mark locations on your")
            |> mes("Mini-Map for easier navigation.")
            |> next()
            |> mes("[Einbech Guide]")
            |> mes("Your Mini-Map is located")
            |> mes("in the upper right corner")
            |> mes("of the screen. If you can't")
            |> mes("see it, press the Ctrl + Tab")
            |> mes("keys or click the ''Map'' button in your Basic Info Window.")
            |> next()
            |> mes("[Einbech Guide]")
            |> mes("On your Mini-Map,")
            |> mes("click on the ''+'' and ''-''")
            |> mes("symbols to zoom in and")
            |> mes("our of your Mini-Map. We")
            |> mes("hope you enjoy your travels")
            |> mes("here in Einbech, adventurer.")

          4 ->
            ctx =
              ctx
              |> mes("[Einbech Guide]")
              |> mes("We hope that you")
              |> mes("enjoy your travels")
              |> mes("here in Einbech.")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
