defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesPayon.Guide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_payon.txt:24 (Guide#pay).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "payon",
        x: 162,
        y: 67,
        dir: 4,
        sprite: 708,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "Guide#pay"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("pay_soldier", 2)
    |> mes("[Payon Guide]")
    |> mes("Welcome to the")
    |> mes("mountain city of Payon.")
    |> mes("If you're unfamiliar with this")
    |> mes("area, I can help you find what")
    |> mes("you're looking for around here.")
    |> set_local(:loop1, 1)
    |> loop_1()
    |> cutin("", 255)
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, [
          "^FF0000Archer Guild^000000",
          "Weapon Shop",
          "Tool Shop",
          "Pub",
          "Central Palace",
          "The Empress",
          "Palace Annex",
          "Royal Kitchen",
          "Forge",
          "Cancel"
        ])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Archer Guild handles")
              |> mes("Job Changes to the Archer")
              |> mes("Class. You'll need to enter")
              |> mes("the Archer Village which is")
              |> mes("to the northeast of Payon.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 227, 328, 2, 16_711_680)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Weapon Shop")
              |> mes("can be found in the")
              |> mes("northwest corner of")
              |> mes("the city of Payon.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 139, 159, 3, 16_711_935)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Tool Shop")
              |> mes("is located near")
              |> mes("the northwest")
              |> mes("corner of Payon.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 144, 85, 4, 16_711_935)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Pub can be")
              |> mes("found in the northeast")
              |> mes("part of Payon. It's the")
              |> mes("best place to relax after")
              |> mes("a long day of hunting.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 220, 117, 5, 16_711_935)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Central Palace")
              |> mes("is located to the north")
              |> mes("within the city of Payon.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 155, 245, 6, 65280)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Empress")
              |> mes("can be found to the")
              |> mes("northwest in Payon.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 107, 324, 7, 65280)
              else
                ctx
              end

            ctx

          7 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Palace Annex")
              |> mes("can be found in the")
              |> mes("western part of Payon.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 130, 204, 8, 65280)
              else
                ctx
              end

            ctx

          8 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Royal Kitchen")
              |> mes("is located near the")
              |> mes("northern end of Payon.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 154, 325, 9, 65280)
              else
                ctx
              end

            ctx

          9 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Forge is")
              |> mes("situated near")
              |> mes("the center of Payon.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 126, 169, 10, 16_776_960)
              else
                ctx
              end

            ctx

          10 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("Please ask me to ''Remove")
              |> mes("Marks from Mini-Map'' if you")
              |> mes("no longer wish to have the")
              |> mes("location marks displayed")
              |> mes("on your Mini-Map.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx |> next() |> select(["City Guide", "Remove Marks from Mini-Map", "Notice.", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("Please select")
              |> mes("a location from")
              |> mes("the following menu.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes.", "No."])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 237, 41, 2, 65280)
            |> viewpoint(2, 237, 41, 3, 255)
            |> viewpoint(2, 46, 345, 4, 16_711_935)
            |> viewpoint(2, 175, 220, 5, 16_711_680)
            |> viewpoint(2, 175, 220, 6, 16_711_680)
            |> viewpoint(2, 175, 220, 7, 16_711_680)
            |> viewpoint(2, 237, 41, 8, 255)
            |> viewpoint(2, 46, 345, 9, 65280)
            |> viewpoint(2, 175, 220, 10, 16_711_680)
            |> set_local(:compass_check, 0)

          3 ->
            ctx
            |> mes("[Payon Guide]")
            |> mes("Advances in sorcery and")
            |> mes("technology have allowed")
            |> mes("us to update our information")
            |> mes("system, enabling up to mark")
            |> mes("locations on your Mini-Map")
            |> mes("for easier navigation.")
            |> next()
            |> mes("[Payon Guide]")
            |> mes("Your Mini-Map is located")
            |> mes("in the upper right corner")
            |> mes("of the screen. If you can't")
            |> mes("see it, press the Ctrl + Tab")
            |> mes("keys or click the ''Map'' button in your Basic Info Window.")
            |> next()
            |> mes("[Payon Guide]")
            |> mes("On your Mini-Map,")
            |> mes("click on the ''+'' and ''-''")
            |> mes("symbols to zoom in and")
            |> mes("our of your Mini-Map. We")
            |> mes("hope you enjoy your travels")
            |> mes("here in the city of Payon.")

          4 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("Be safe in")
              |> mes("your travels,")
              |> mes("brave adventurer.")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
