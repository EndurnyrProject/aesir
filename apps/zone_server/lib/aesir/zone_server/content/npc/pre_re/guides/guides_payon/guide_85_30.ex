defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesPayon.Guide8530 do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_payon.txt:193 (Guide#2pay).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "pay_arche",
        x: 85,
        y: 30,
        dir: 2,
        sprite: 708,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "Guide#2pay"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("pay_soldier", 2)
    |> mes("[Payon Guide]")
    |> mes("Welcome to the")
    |> mes("mountain city of Payon.")
    |> mes("If you're unfamiliar with this")
    |> mes("area, I can help you find what")
    |> mes("you're looking for around here.")
    |> set_local(:loop1, 1)
    |> loop_1()
    |> cutin("", 255)
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, ["^FF0000Archer Guild^000000", "Tool Shop", "Payon Dungeon", "Cancel"])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The Archer Guild,")
              |> mes("found northeast in")
              |> mes("the Archer Village,")
              |> mes("handles Job Changes")
              |> mes("to the Archer Class.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 144, 164, 0, 16_776_960)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("You can find")
              |> mes("a Tool Shop in")
              |> mes("the northeast corner")
              |> mes("of the Archer Village.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 71, 156, 1, 16_776_960)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("The entrance to")
              |> mes("the Payon Dungeon")
              |> mes("is located at the west")
              |> mes("end of the village.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 34, 132, 2, 16_777_215)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes(
                "If you'd like to erase the marks on the mini-map, select menu, 'Wipe all indicators on the mini-map'."
              )
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx |> next() |> select(["City Guide", "Remove Marks from Mini-Map", "Notice", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx = ctx |> mes("[Payon Guide]") |> mes("Please, select a menu.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like to leave indicators on the mini-map?")
                  |> next()
                  |> select(["Yes.", "No."])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 237, 41, 0, 16_711_935)
            |> viewpoint(2, 237, 41, 1, 16_711_680)
            |> viewpoint(2, 46, 345, 2, 16_711_935)
            |> set_local(:compass_check, 0)

          3 ->
            ctx
            |> mes("[Payon Guide]")
            |> mes("Advances in sorcery and")
            |> mes("technology have allowed")
            |> mes("us to update our information")
            |> mes("system, enabling up to mark")
            |> mes("locations on your Mini-Map")
            |> mes("for easier navigation.")
            |> next()
            |> mes("[Payon Guide]")
            |> mes("Your Mini-Map is located")
            |> mes("in the upper right corner")
            |> mes("of the screen. If you can't")
            |> mes("see it, press the Ctrl + Tab")
            |> mes("keys or click the ''Map'' button in your Basic Info Window.")
            |> next()
            |> mes("[Payon Guide]")
            |> mes("On your Mini-Map,")
            |> mes("click on the ''+'' and ''-''")
            |> mes("symbols to zoom in and")
            |> mes("our of your Mini-Map. We")
            |> mes("hope you enjoy your travels")
            |> mes("here in the city of Payon.")

          4 ->
            ctx =
              ctx
              |> mes("[Payon Guide]")
              |> mes("Be safe in")
              |> mes("your travels,")
              |> mes("brave adventurer.")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
