defmodule Aesir.ZoneServer.Content.Npc.PreRe.Mobs.Dungeons.LhzDun.SummonBossLt do
  @moduledoc """
  Transpiled from rAthena pre-re/mobs/dungeons/lhz_dun.txt:221 (summon_boss_lt).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, scope: :pre_renewal, spawn: []

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnTimer6000000", ctx), do: ev_ontimer6000000(ctx)
  def on_event("OnTimer6300000", ctx), do: ev_ontimer6300000(ctx)
  def on_event("OnTimer6600000", ctx), do: ev_ontimer6600000(ctx)
  def on_event("OnTimer6900000", ctx), do: ev_ontimer6900000(ctx)
  def on_event("OnTimer7200000", ctx), do: ev_ontimer7200000(ctx)
  def on_event("OnTimer7500000", ctx), do: ev_ontimer7500000(ctx)
  def on_event("OnTimer7800000", ctx), do: ev_ontimer7800000(ctx)
  def on_event("OnSummon", ctx), do: ev_onsummon(ctx)
  def on_event("OnLhzMvPDead", ctx), do: ev_onlhzmvpdead(ctx)
  def on_event("OnMy99Dead", ctx), do: ev_onmy99dead(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    initnpctimer(ctx)
  end

  def ev_ontimer6000000(ctx) do
    ctx =
      if Enum.random(1..6) == 1 do
        ctx |> donpcevent("summon_boss_lt::OnSummon") |> stopnpctimer()
      else
        ctx
      end

    ctx
  end

  def ev_ontimer6300000(ctx) do
    ctx =
      if Enum.random(1..6) == 2 do
        ctx |> donpcevent("summon_boss_lt::OnSummon") |> stopnpctimer()
      else
        ctx
      end

    ctx
  end

  def ev_ontimer6600000(ctx) do
    ctx =
      if Enum.random(1..6) == 3 do
        ctx |> donpcevent("summon_boss_lt::OnSummon") |> stopnpctimer()
      else
        ctx
      end

    ctx
  end

  def ev_ontimer6900000(ctx) do
    ctx =
      if Enum.random(1..6) == 4 do
        ctx |> donpcevent("summon_boss_lt::OnSummon") |> stopnpctimer()
      else
        ctx
      end

    ctx
  end

  def ev_ontimer7200000(ctx) do
    ctx =
      if Enum.random(1..6) == 5 do
        ctx |> donpcevent("summon_boss_lt::OnSummon") |> stopnpctimer()
      else
        ctx
      end

    ctx
  end

  def ev_ontimer7500000(ctx) do
    ctx =
      if Enum.random(1..6) == 6 do
        ctx |> donpcevent("summon_boss_lt::OnSummon") |> stopnpctimer()
      else
        ctx
      end

    ctx
  end

  def ev_ontimer7800000(ctx) do
    ctx |> donpcevent("summon_boss_lt::OnSummon") |> stopnpctimer()
  end

  def ev_onsummon(ctx) do
    v1 = Enum.random(1..6)

    ctx =
      case v1 do
        1 ->
          ctx |> set_local(:x, 140) |> set_local(:y, 232)

        2 ->
          ctx |> set_local(:x, 75) |> set_local(:y, 138)

        3 ->
          ctx |> set_local(:x, 140) |> set_local(:y, 87)

        4 ->
          ctx |> set_local(:x, 205) |> set_local(:y, 140)

        5 ->
          ctx |> set_local(:x, 123) |> set_local(:y, 137)

        6 ->
          ctx = ctx |> set_local(:x, 175) |> set_local(:y, 137)
          ctx

        _ ->
          ctx
      end

    ctx = set_local(ctx, :mob, Enum.random(1646..1651))

    ctx =
      summon_mob(ctx,
        mob_id: get_local(ctx, :mob, 0),
        map: "lhz_dun03",
        at: {get_local(ctx, :x, 0), get_local(ctx, :y, 0)},
        event: "summon_boss_lt::OnLhzMvPDead"
      )

    v2 = Enum.random(1..6)

    ctx =
      case v2 do
        1 ->
          ctx |> set_local(:x2, 183) |> set_local(:y2, 97)

        2 ->
          ctx |> set_local(:x2, 97) |> set_local(:y2, 96)

        3 ->
          ctx |> set_local(:x2, 47) |> set_local(:y2, 139)

        4 ->
          ctx |> set_local(:x2, 231) |> set_local(:y2, 140)

        5 ->
          ctx |> set_local(:x2, 139) |> set_local(:y2, 211)

        6 ->
          ctx = ctx |> set_local(:x2, 139) |> set_local(:y2, 259)
          ctx

        _ ->
          ctx
      end

    ctx = set_local(ctx, :mob2, Enum.random(1640..1645))

    summon_mob(ctx,
      mob_id: get_local(ctx, :mob2, 0),
      map: "lhz_dun03",
      at: {get_local(ctx, :x2, 0), get_local(ctx, :y2, 0)},
      event: "summon_boss_lt::OnMy99Dead"
    )
  end

  def ev_onlhzmvpdead(ctx) do
    ctx |> killmonster("lhz_dun03", "summon_boss_lt::OnMy99Dead") |> initnpctimer()
  end

  def ev_onmy99dead(ctx) do
    ctx
  end
end
