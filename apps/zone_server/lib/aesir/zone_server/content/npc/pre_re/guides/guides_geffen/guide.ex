defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesGeffen.Guide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_geffen.txt:23 (Guide#gef::GefGuide).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "geffen",
        x: 203,
        y: 116,
        dir: 0,
        sprite: 705,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "GefGuide"
      },
      %{
        map: "geffen",
        x: 118,
        y: 62,
        dir: 0,
        sprite: 705,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "Guide#2gef"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("gef_soldier", 2)
    |> mes("[Geffen Guide]")
    |> mes("Welcome to Geffen,")
    |> mes("the City of Magic. If you")
    |> mes("need any guidance around")
    |> mes("the city, feel free to ask me")
    |> mes("and I'll do my best to assist you. ^FFFFFFcobo^000000")
    |> set_local(:loop1, 1)
    |> loop_1()
    |> cutin("gef_soldier", 255)
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, [
          "^FF0000Magic Acedemy^000000",
          "Forge Shop",
          "Weapon Shop",
          "Tool Shop",
          "Pub",
          "Inn",
          "Geffen Tower",
          "Cancel"
        ])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("The Magic Academy in")
              |> mes("northwest Geffen handles")
              |> mes("Job Changes to the Mage class.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 61, 180, 2, 16_711_680)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("The Forge Shop is")
              |> mes("located just southeast")
              |> mes("from the center of Geffen.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 182, 59, 3, 65280)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("The Weapon Shop")
              |> mes("can be found northwest")
              |> mes("from the center of Geffen.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 99, 140, 4, 16_711_935)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("You can find the")
              |> mes("Tool Shop by heading")
              |> mes("southwest from the")
              |> mes("center of Geffen.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 44, 86, 5, 16_711_935)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("The Pub can be")
              |> mes("found northeast")
              |> mes("from the Geffen Tower.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 138, 138, 6, 16_711_935)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("The Inn can be")
              |> mes("found by traveling")
              |> mes("northeast from the")
              |> mes("center of Geffen.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 172, 174, 7, 16_711_935)
              else
                ctx
              end

            ctx

          7 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("Geffen Tower is found")
              |> mes("in the center of the city.")
              |> mes("The Wizard Guild is at the")
              |> mes("top, and there's even a dungeon")
              |> mes("underneath it. There's many a")
              |> mes("mystery surrounding that tower...")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 120, 114, 8, 65280)
              else
                ctx
              end

            ctx

          8 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("Please ask me to ''Remove")
              |> mes("Marks from Mini-Map'' if you")
              |> mes("no longer wish to have the")
              |> mes("location marks displayed")
              |> mes("on your Mini-Map.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx |> next() |> select(["City Guide", "Remove Marks from Mini-Map", "Notice.", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("Please select")
              |> mes("a location from")
              |> mes("the following menu.")

            ctx =
              if get_char_var(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes", "No"])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 237, 41, 2, 16_711_680)
            |> viewpoint(2, 237, 41, 3, 65280)
            |> viewpoint(2, 46, 345, 4, 16_711_935)
            |> viewpoint(2, 175, 220, 5, 16_711_935)
            |> viewpoint(2, 134, 221, 6, 16_711_935)
            |> viewpoint(2, 204, 214, 7, 16_711_935)
            |> viewpoint(2, 204, 214, 8, 65280)
            |> set_local(:compass_check, 0)

          3 ->
            ctx
            |> mes("[Geffen Guide]")
            |> mes("Advances in sorcery and")
            |> mes("technology have allowed")
            |> mes("us to update our information")
            |> mes("system, enabling up to mark")
            |> mes("locations on your Mini-Map")
            |> mes("for easier navigation.")
            |> next()
            |> mes("[Geffen Guide]")
            |> mes("Your Mini-Map is located")
            |> mes("in the upper right corner")
            |> mes("of the screen. If you can't")
            |> mes("see it, press the Ctrl + Tab")
            |> mes("keys or click the ''Map'' button in your Basic Info Window.")
            |> next()
            |> mes("[Geffen Guide]")
            |> mes("On your Mini-Map,")
            |> mes("click on the ''+'' and ''-''")
            |> mes("symbols to zoom in and")
            |> mes("our of your Mini-Map. We")
            |> mes("hope you enjoy your travels")
            |> mes("here in the city of Geffen.")

          4 ->
            ctx =
              ctx
              |> mes("[Geffen Guide]")
              |> mes("Alright, adventurer.")
              |> mes("I wish you safety on")
              |> mes("your journeys through")
              |> mes("the lands you may travel...")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
