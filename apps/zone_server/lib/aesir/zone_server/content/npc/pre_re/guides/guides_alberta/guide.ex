defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesAlberta.Guide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_alberta.txt:24 (Guide#alb::AlbGuide).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "alberta",
        x: 23,
        y: 238,
        dir: 4,
        sprite: 105,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "AlbGuide"
      },
      %{
        map: "alberta",
        x: 120,
        y: 60,
        dir: 3,
        sprite: 105,
        name: "Guide",
        scope: :pre_renewal,
        unique_name: "Guide#2alb"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("prt_soldier", 2)
    |> mes("[Alberta Guide]")
    |> mes("Welcome to Alberta,")
    |> mes("the Port City. Feel free")
    |> mes("to ask me if you're having")
    |> mes("trouble finding anything in")
    |> mes("town, or if you just need")
    |> mes("guidance around the city.")
    |> set_local(:loop1, 1)
    |> loop_1()
    |> cutin("prt_soldier", 255)
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, [
          "^FF0000Merchant Guild^000000",
          "Weapon Shop",
          "Tool Shop",
          "Inn",
          "Forge",
          "Cancel"
        ])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Alberta Guide]")
              |> mes("The Merchant Guild")
              |> mes("handles Job Changes")
              |> mes("to the Merchant Class,")
              |> mes("and is located in the")
              |> mes("southwest corner")
              |> mes("of Alberta.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 33, 41, 2, 16_711_680)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Alberta Guide]")
              |> mes("The Weapon Shop")
              |> mes("can be found in the")
              |> mes("southern end of Alberta.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 117, 37, 3, 16_711_935)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Alberta Guide]")
              |> mes("The Tool Shop")
              |> mes("is kind of close")
              |> mes("to the center of")
              |> mes("Alberta. It shouldn't")
              |> mes("be too hard to find.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 98, 154, 4, 16_711_935)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Alberta Guide]")
              |> mes("There's an Inn")
              |> mes("at the northern")
              |> mes("end of Alberta")
              |> mes("where you can rest.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 65, 233, 5, 16_711_935)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Alberta Guide]")
              |> mes("The Forge in Alberta")
              |> mes("is in the same building")
              |> mes("as the Merchant Guild.")
              |> mes("It's to the southwest.")

            ctx =
              if Rathena.truthy?(get_local(ctx, :compass_check, 0)) do
                viewpoint(ctx, 1, 35, 41, 6, 16_711_935)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Alberta Guide]")
              |> mes("Please ask me to ''Remove")
              |> mes("Marks from Mini-Map'' if you")
              |> mes("no longer wish to have the")
              |> mes("location marks displayed")
              |> mes("on your Mini-Map.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx |> next() |> select(["City Guide", "Remove Marks from Mini-Map", "Notice", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Alberta Guide]")
              |> mes("Please select")
              |> mes("a location from")
              |> mes("the following menu.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes", "No"])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 237, 41, 2, 16_711_680)
            |> viewpoint(2, 237, 41, 3, 16_711_935)
            |> viewpoint(2, 46, 345, 4, 16_711_935)
            |> viewpoint(2, 175, 220, 5, 16_711_935)
            |> viewpoint(2, 175, 220, 6, 16_711_935)
            |> set_local(:compass_check, 0)

          3 ->
            ctx
            |> mes("[Alberta Guide]")
            |> mes("Advances in sorcery and")
            |> mes("technology have allowed")
            |> mes("us to update our information")
            |> mes("system, enabling up to mark")
            |> mes("locations on your Mini-Map")
            |> mes("for easier navigation.")
            |> next()
            |> mes("[Alberta Guide]")
            |> mes("Your Mini-Map is located")
            |> mes("in the upper right corner")
            |> mes("of the screen. If you can't")
            |> mes("see it, press the Ctrl + Tab")
            |> mes("keys or click the ''Map'' button in your Basic Info Window.")
            |> next()
            |> mes("[Alberta Guide]")
            |> mes("On your Mini-Map,")
            |> mes("click on the ''+'' and ''-''")
            |> mes("symbols to zoom in and")
            |> mes("our of your Mini-Map. We")
            |> mes("hope you enjoy your travels")
            |> mes("here in the city of Alberta.")

          4 ->
            ctx =
              ctx
              |> mes("[Alberta Guide]")
              |> mes("Be safe when you")
              |> mes("travel and don't hesitate")
              |> mes("to ask me if you have any")
              |> mes("questions about Alberta.")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
