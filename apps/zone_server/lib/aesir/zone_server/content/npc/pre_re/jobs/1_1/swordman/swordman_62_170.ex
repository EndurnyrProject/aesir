defmodule Aesir.ZoneServer.Content.Npc.PreRe.Jobs.M11.Swordman.Swordman62170 do
  @moduledoc """
  Transpiled from rAthena pre-re/jobs/1-1/swordman.txt:206 (Swordman#swd_2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "izlude_in",
        x: 62,
        y: 170,
        dir: 6,
        sprite: 85,
        name: "Swordman",
        scope: :pre_renewal,
        unique_name: "Swordman#swd_2"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Swordman]")

    if base_job(ctx) == :swordman do
      ctx =
        ctx |> mes("Sorry guy, but I can only allow Novices to enter the Test Hall.") |> close()

      throw({:script_end, ctx})
    else
      if base_job(ctx) != :novice do
        ctx =
          ctx
          |> mes(
            "Who the hell are you?! Nobody, other than Novices, is permitted to come in here!"
          )
          |> close()

        throw({:script_end, ctx})
      else
        v1 = can_change_job?(ctx)

        if not Rathena.truthy?(v1) do
          ctx =
            ctx
            |> mes(
              "Stop! I can't let you in until you learn all of the Basic Skills. The Test Hall isn't for goofing off!"
            )
            |> close()

          throw({:script_end, ctx})
        else
          if get_char_var(ctx, :job_sword_q, 0) == 4 do
            ctx =
              ctx
              |> mes("Hey. You need to talk to the Swordman in the center of the room, not me.")
              |> close()

            throw({:script_end, ctx})
          else
            if get_char_var(ctx, :job_sword_q, 0) == 0 do
              ctx =
                ctx
                |> mes(
                  "Stop! If you want to take the Swordman Test, you'll need to fill out an application first."
                )
                |> next()
                |> mes("[Swordman]")
                |> mes("The Swordman in the center of the room can help you with that, got it?")
                |> close()

              throw({:script_end, ctx})
            else
              ctx = ctx |> savepoint("izlude_in", 65, 165) |> warp("izlude_in", 39, 170)
              throw({:script_end, ctx})
            end
          end
        end
      end
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
