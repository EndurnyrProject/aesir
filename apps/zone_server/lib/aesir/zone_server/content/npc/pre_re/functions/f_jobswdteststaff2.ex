defmodule Aesir.ZoneServer.Content.Npc.PreRe.Functions.FJobswdteststaff2 do
  @moduledoc """
  Transpiled from rAthena pre-re/jobs/1-1/swordman.txt:361 (F_JobSwdTestStaff2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    ctx =
      ctx
      |> mapannounce(
        "job_sword1",
        Rathena.concat(
          Rathena.concat(
            Rathena.concat(Rathena.concat("Applicant ", char_name(ctx, 0)), ". Pass the "),
            Enum.at(args, 0, 0)
          ),
          " course."
        ),
        1
      )
      |> warp("job_sword1", Enum.at(args, 1, 0), Enum.at(args, 2, 0))

    throw({:script_end, ctx})
  end
end
