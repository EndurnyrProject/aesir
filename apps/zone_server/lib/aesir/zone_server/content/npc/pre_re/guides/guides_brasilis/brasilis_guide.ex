defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesBrasilis.BrasilisGuide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_brasilis.txt:18 (Brasilis Guide).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "brasilis",
        x: 219,
        y: 97,
        dir: 3,
        sprite: 478,
        name: "Brasilis Guide",
        scope: :pre_renewal
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Brasilis Guide]")
      |> mes("Welcome to ^8B4513Brasilis^000000, a country as passionate as the sun.")
      |> mes("If you have any questions, please ask me.")
      |> next()
      |> select(["Ask about locations", "Remove Marks from Mini-Map", "Cancel"])

    ctx =
      case v1 do
        1 ->
          {ctx, v2} =
            ctx
            |> mes("[Brasilis Guide]")
            |> mes("Where can I guide you?")
            |> next()
            |> select([
              "[ Hotel ]",
              "[ Jungle Cable ]",
              "[ Art Museum ]",
              "[ Market ]",
              "[ Verass Monument ]"
            ])

          ctx =
            case v2 do
              1 ->
                ctx =
                  ctx
                  |> mes("[Brasilis Guide]")
                  |> mes("The Brasilis Hotel is located just above, ^FF3355+^000000.")
                  |> mes("Is there anything else I can do for you?")
                  |> viewpoint(1, 274, 151, 2, 16_724_821)
                  |> close()

                throw({:script_end, ctx})

              2 ->
                ctx =
                  ctx
                  |> mes("[Brasilis Guide]")
                  |> mes("Do you want to go through the rough jungle? You can take a ")
                  |> mes("Jungle Cable here ^CE6300+^000000.")
                  |> mes("Is there anything else I can do for you?")
                  |> viewpoint(1, 308, 335, 3, 13_525_760)
                  |> close()

                throw({:script_end, ctx})

              3 ->
                ctx =
                  ctx
                  |> mes("[Brasilis Guide]")
                  |> mes(
                    "The pride of Brasilis, the world scale Art Museum is at ^A5BAAD+^000000."
                  )
                  |> mes("Is there anything else I can do for you?")
                  |> viewpoint(1, 137, 167, 4, 65280)
                  |> close()

                throw({:script_end, ctx})

              4 ->
                ctx =
                  ctx
                  |> mes("[Brasilis Guide]")
                  |> mes("You can buy items for hunting at the Market here ^55FF33+^000000.")
                  |> mes("Is there anything else I can do for you?")
                  |> viewpoint(1, 254, 248, 5, 5_635_891)
                  |> close()

                throw({:script_end, ctx})

              5 ->
                ctx =
                  ctx
                  |> mes("[Brasilis Guide]")
                  |> mes(
                    "The iconic monument of Brasilis, the Verass Monument stands at ^3355FF+^000000."
                  )
                  |> mes("Is there anything else I can do for you?")
                  |> viewpoint(1, 195, 235, 6, 3_364_351)
                  |> close()

                throw({:script_end, ctx})

              _ ->
                ctx
            end

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Brasilis Guide]")
            |> mes("I'll remove all marks from your mini-map.")
            |> mes("Is there anything else I can do for you?")
            |> viewpoint(0, 274, 151, 2, 65280)
            |> viewpoint(0, 308, 335, 3, 65280)
            |> viewpoint(0, 137, 167, 4, 65280)
            |> viewpoint(0, 254, 248, 5, 65280)
            |> viewpoint(0, 195, 235, 6, 65280)
            |> close()

          throw({:script_end, ctx})

        3 ->
          ctx =
            ctx
            |> mes("[Brasilis Guide]")
            |> mes("Wandering on your own is always the best way to explore. Anyway, take care.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
