defmodule Aesir.ZoneServer.Content.Npc.PreRe.Jobs.Novice.Novice.Trainer do
  @moduledoc """
  Transpiled from rAthena pre-re/jobs/novice/novice.txt:2647 (Trainer#nv1::NovHoffman).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "new_1-3",
        x: 95,
        y: 30,
        dir: 4,
        sprite: 84,
        name: "Trainer",
        scope: :pre_renewal,
        unique_name: "NovHoffman"
      },
      %{
        map: "new_2-3",
        x: 95,
        y: 30,
        dir: 4,
        sprite: 84,
        name: "Trainer",
        scope: :pre_renewal,
        unique_name: "Trainer#nv2"
      },
      %{
        map: "new_3-3",
        x: 95,
        y: 30,
        dir: 4,
        sprite: 84,
        name: "Trainer",
        scope: :pre_renewal,
        unique_name: "Trainer#nv3"
      },
      %{
        map: "new_4-3",
        x: 95,
        y: 30,
        dir: 4,
        sprite: 84,
        name: "Trainer",
        scope: :pre_renewal,
        unique_name: "Trainer#nv4"
      },
      %{
        map: "new_5-3",
        x: 95,
        y: 30,
        dir: 4,
        sprite: 84,
        name: "Trainer",
        scope: :pre_renewal,
        unique_name: "Trainer#nv5"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Hoffman]")
      |> mes("Hey there~")
      |> mes("I'm here to provide")
      |> mes("you with a little instruction.")
      |> next()
      |> mes("[Hoffman]")
      |> mes("These monsters are all weak")
      |> mes("and easy to kill. But be careful,")
      |> mes("a lot of them are aggressive")
      |> mes("and out for blood!")
      |> next()
      |> mes("[Hoffman]")
      |> mes(
        "If you think monsters here are too weak for you, I can send you to another training ground where the monsters are stronger than the ones over here."
      )
      |> next()
      |> mes("[Hoffman]")
      |> mes("But don't worry so much,")
      |> mes("They're not impossible for")
      |> mes("Novices. So would you")
      |> mes("like to try?")
      |> next()
      |> select(["I do want more of a challenge~", "I wanna fight tough monsters!", "Cancel"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Hoffman]")
            |> mes("I see, then let me guide")
            |> mes("you to a training ground that has stronger monsters. May God be with you...")
            |> next()

          ctx =
            if strnpcinfo(ctx, 2) == "nv1" do
              ctx =
                if Rathena.truthy?(:rand.uniform(2) - 1) do
                  warp(ctx, "new_2-3", 96, 21)
                else
                  warp(ctx, "new_3-3", 96, 21)
                end

              ctx
            else
              warp(ctx, "new_1-3", 96, 21)
            end

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Hoffman]")
            |> mes("You must like ")
            |> mes("rough challenges,")
            |> mes("don't you? Please")
            |> mes("be careful, it can get")
            |> mes("pretty difficult...")
            |> next()

          ctx =
            if strnpcinfo(ctx, 2) == "nv1" or strnpcinfo(ctx, 2) == "nv2" or
                 strnpcinfo(ctx, 2) == "nv3" do
              ctx =
                if Rathena.truthy?(:rand.uniform(2) - 1) do
                  warp(ctx, "new_4-3", 96, 21)
                else
                  warp(ctx, "new_5-3", 96, 21)
                end

              ctx
            else
              ctx =
                if Rathena.truthy?(:rand.uniform(2) - 1) do
                  warp(ctx, "new_2-3", 96, 21)
                else
                  warp(ctx, "new_3-3", 96, 21)
                end

              ctx
            end

          throw({:script_end, ctx})

        3 ->
          ctx =
            ctx
            |> mes("[Hoffman]")
            |> mes("Hmm...?")
            |> mes("Are you worried about going")
            |> mes(
              "to more challenging places? That's understandable, since you're still a new adventurer. Good luck~"
            )
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
