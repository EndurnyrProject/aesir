defmodule Aesir.ZoneServer.Content.Npc.PreRe.Functions.FJobswdmedic do
  @moduledoc """
  Transpiled from rAthena pre-re/jobs/1-1/swordman.txt:326 (F_JobSwdMedic).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    ctx =
      ctx
      |> percent_heal(hp: 100, sp: 0)
      |> mes("[Medic]")
      |> mes(
        Rathena.concat(
          Rathena.concat("This is the ", Enum.at(args, 0, 0)),
          " check point! Cheer up!"
        )
      )
      |> close()

    throw({:script_end, ctx})
  end
end
