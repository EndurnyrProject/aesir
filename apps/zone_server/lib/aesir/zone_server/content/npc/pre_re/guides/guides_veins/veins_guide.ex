defmodule Aesir.ZoneServer.Content.Npc.PreRe.Guides.GuidesVeins.VeinsGuide do
  @moduledoc """
  Transpiled from rAthena pre-re/guides/guides_veins.txt:18 (Veins Guide#1::ve_guide).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :pre_renewal,
    spawn: [
      %{
        map: "veins",
        x: 210,
        y: 345,
        dir: 5,
        sprite: 934,
        name: "Veins Guide",
        scope: :pre_renewal,
        unique_name: "ve_guide"
      },
      %{
        map: "veins",
        x: 189,
        y: 101,
        dir: 5,
        sprite: 934,
        name: "Veins Guide",
        scope: :pre_renewal,
        unique_name: "Veins Guide#2"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx
    |> mes("[Veins Guide]")
    |> mes("Desert City Veins welcomes adventurers seeking shelter from harsh sandstorms.")
    |> mes(
      "If this is the first time for you to use the guide services, why don't you check the..."
    )
    |> set_local(:loop1, 1)
    |> loop_1()
    |> close()
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop2, 0)) do
      ctx =
        if get_local(ctx, :wait_button_chk, 0) == 0 do
          set_local(ctx, :wait_button_chk, 1)
        else
          next(ctx)
        end

      {ctx, v3} =
        select(ctx, [
          "Temple",
          "Inn",
          "Weapon Shop",
          "Tool Shop",
          "Airship",
          "Tavern",
          "Geological Research Institute",
          "Cancel"
        ])

      ctx =
        case v3 do
          1 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("Our temple is located north,")
              |> mes("and always crowded with sincere followers of Goddess Freya.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 196, 258, 1, 16_711_680)
              else
                ctx
              end

            ctx

          2 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("You can rest your fatigue of the journey in the Inn.")
              |> mes("The left building next to me is the Inn of Veins.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 128, 266, 2, 16_711_935)
              else
                ctx
              end

            ctx

          3 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("Yes, you should protect yourself from danger on your own.")
              |> mes("Purchase high quality weapons at affordable prices.")
              |> mes("The Veins Weapon Shop is located to the West.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 150, 175, 3, 10_092_543)
              else
                ctx
              end

            ctx

          4 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("Have you packed enough necessities  for your adventure?")
              |> mes("If not, I suggest you check what the Veins in the Center can offer you.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 230, 161, 4, 255)
              else
                ctx
              end

            ctx

          5 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("Please be aware that Veins only operates cargo airships.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 273, 285, 5, 65280)
              else
                ctx
              end

            ctx

          6 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("If you'd like to make friends with")
              |> mes("the townspeople, I suggest you")
              |> mes("go have a drink at the tavern to")
              |> mes("the west.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 150, 217, 6, 65280)
              else
                ctx
              end

            ctx

          7 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("Are you interested in studying geology?")
              |> mes("Then you'd better go check out the")
              |> mes("Geological Research Institute on")
              |> mes("the 2nd floor of the weapon shop.")

            ctx =
              if get_local(ctx, :compass_check, 0) == 1 do
                viewpoint(ctx, 1, 150, 175, 7, 65280)
              else
                ctx
              end

            ctx

          8 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("If you like to get rid of all the location marks on your Mini-Map,")
              |> mes("just ask me again, and choose 'Remove Marks from Mini-Map' menu.")
              |> set_local(:loop2, 0)

            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop1, 0)) do
      {ctx, v1} =
        ctx
        |> next()
        |> select(["Village Guide", "Remove Marks from Mini-Map", "Notice", "Cancel"])

      ctx =
        case v1 do
          1 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("I can tell you any building location as long as it is in Veins.")
              |> mes("So where do you want to go?")

            ctx =
              if get_local(ctx, :compass_check, 0) == 0 do
                {ctx, v2} =
                  ctx
                  |> mes("Would you like me")
                  |> mes("to mark locations")
                  |> mes("on your Mini-Map?")
                  |> next()
                  |> select(["Yes", "No"])

                ctx =
                  if v2 == 1 do
                    set_local(ctx, :compass_check, 1)
                  else
                    ctx
                  end

                ctx
              else
                ctx
              end

            ctx |> set_local(:loop2, 1) |> loop_3()

          2 ->
            ctx
            |> viewpoint(2, 196, 258, 1, 16_711_680)
            |> viewpoint(2, 128, 266, 2, 16_711_935)
            |> viewpoint(2, 150, 175, 3, 10_092_543)
            |> viewpoint(2, 230, 161, 4, 255)
            |> viewpoint(2, 273, 285, 5, 65280)
            |> viewpoint(2, 150, 217, 6, 65280)
            |> viewpoint(2, 150, 175, 7, 65280)
            |> mes("[Veins Guide]")
            |> mes(
              "Okay, they are gone now. If you have more locations to ask, just let me know."
            )
            |> mes("Enjoy your stay in Veins.")

          3 ->
            ctx
            |> mes("[Veins Guide]")
            |> mes("When you are using the ''Village Guide'' menu,")
            |> mes(
              "make sure that building locations will be marked on your mini-map at the upper right side of your screen."
            )
            |> mes(
              "If you cannot see your mini-map, use the short cut key ''ctrl+tab'' or press the ''Map'' button on your basic information windows, okay?"
            )
            |> mes(
              "And you can also zoom out your mini-map by using the ''-'' button in case you cannot view the entire map of the village."
            )

          4 ->
            ctx =
              ctx
              |> mes("[Veins Guide]")
              |> mes("Enjoy your stay in Veins.")
              |> close()
              |> set_local(:loop1, 0)

            ctx

          _ ->
            ctx
        end

      loop_1(ctx)
    else
      ctx
    end
  end
end
