defmodule Aesir.ZoneServer.Content.Npc.Splendide.Fairy125227 do
  @moduledoc """
  Transpiled from rAthena cities/splendide.txt:45 (Fairy#ep13_3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "splendide", x: 125, y: 227, dir: 3, sprite: 444, name: "Fairy"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 do
      ctx
      |> mes("[Flowa]")
      |> mes("No offense, I was not looking at you with sympathy.")
      |> mes("I just wondered how you could walk with that heavy body...")
      |> close()

      exit(:normal)
    else
      ctx
      |> mes("[Flowa]")
      |> mes("AnuFuloUor Ko CyaWosnes Ha WosAnuAsh O WosDuAno O ")
      |> mes("FuloAndueo Ie WosGothLars Ee Tinarmaur Or AlahnahVa Or narAnuFulo So KoCya")
      |> close()

      exit(:normal)
    end
  end
end
