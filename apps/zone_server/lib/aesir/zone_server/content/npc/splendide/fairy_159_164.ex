defmodule Aesir.ZoneServer.Content.Npc.Splendide.Fairy159164 do
  @moduledoc """
  Transpiled from rAthena cities/splendide.txt:61 (Fairy#13_2_1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "splendide", x: 159, y: 164, dir: 3, sprite: 461, name: "Fairy"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 and get_char_var(ctx, :ep13_2_rhea, 0) == 100 do
      ctx
      |> mes("[Fairy]")
      |> mes("It is shame that I have to")
      |> mes("cope with those fat beasts.")
      |> mes("I am just too delicate to fight with them.")
      |> close()

      exit(:normal)
    else
      ctx
      |> mes("[Fairy]")
      |> mes("RiniHirDieb Ie nahImanMe Di Mush")
      |> mes("mahnarAsh So HirAnMod O Ras")
      |> mes("neaLoDath Ha KoRivehWha So Thusnea")
      |> close()

      exit(:normal)
    end
  end
end
