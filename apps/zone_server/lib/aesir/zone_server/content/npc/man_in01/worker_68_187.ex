defmodule Aesir.ZoneServer.Content.Npc.ManIn01.Worker68187 do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:518 (Worker#ep13bs1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "man_in01", x: 68, y: 187, dir: 0, sprite: 454, name: "Worker"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 do
      ctx
      |> mes("[Worker]")
      |> mes("Hmm, it smells delicious.")
      |> mes("It should be time to turn it around now.")
      |> next()
      |> mes("[Worker]")
      |> mes("Hardrock Mammoth steak should be eaten slightly raw!")
      |> close()

      exit(:normal)
    else
      ctx
      |> mes("[Tee]")
      |> mes("As woue dpi sha we")
      |> mes("Two psie bu le")
      |> next()
      |> mes("[Tee]")
      |> mes("Tr sdou powee wwee ")
      |> close()

      exit(:normal)
    end
  end
end
