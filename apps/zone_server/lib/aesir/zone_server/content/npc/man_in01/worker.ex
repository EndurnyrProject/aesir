defmodule Aesir.ZoneServer.Content.Npc.ManIn01.Worker do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:292 ( Worker#ep13bsg1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "man_in01", x: 346, y: 135, dir: 0, sprite: 454, name: " Worker"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 do
      ctx
      |> mes("[Worker]")
      |> mes("It is dangerous if the valve is not checked properly every day.")
      |> mes("In fact, there was an incident.")
      |> mes("It gives me the creeps just thinking about it.")
      |> close()

      exit(:normal)
    else
      ctx
      |> mes("[Worker]")
      |> mes("Gs df o aj ud pa")
      |> mes("N sd asw ewt jj ")
      |> mes("Ud aso pda s ")
      |> close()

      exit(:normal)
    end
  end
end
