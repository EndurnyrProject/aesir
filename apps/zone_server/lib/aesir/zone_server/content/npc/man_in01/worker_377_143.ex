defmodule Aesir.ZoneServer.Content.Npc.ManIn01.Worker377143 do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:324 (Worker#ep13bsg3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "man_in01", x: 377, y: 143, dir: 5, sprite: 454, name: "Worker"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 do
      ctx
      |> mes("[Worker]")
      |> mes("Hmm... It's working just fine... No problems at all...")
      |> close()

      exit(:normal)
    else
      ctx |> mes("[Worker]") |> mes("Mou ii ros oa d d ") |> close()
      exit(:normal)
    end
  end
end
