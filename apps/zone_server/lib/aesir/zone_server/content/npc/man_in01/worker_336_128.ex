defmodule Aesir.ZoneServer.Content.Npc.ManIn01.Worker336128 do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:350 (Worker#ep13bsg5).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "man_in01", x: 336, y: 128, dir: 5, sprite: 454, name: "Worker"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 do
      ctx |> mes("[Worker]") |> mes("Isn't this fabulous?") |> close()
      exit(:normal)
    else
      ctx |> mes("[Worker]") |> mes("R tt osj dj d") |> close()
      exit(:normal)
    end
  end
end
