defmodule Aesir.ZoneServer.Content.Npc.Louyang.Exit do
  @moduledoc """
  Transpiled from rAthena cities/louyang.txt:309 (Exit#lou).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "louyang", x: 84, y: 254, dir: 0, sprite: 111, name: "Exit"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("^3355FFThere is some sort")
      |> mes("of descent apparatus.")
      |> mes("Would you like to use it?^000000")
      |> next()
      |> select(["Yes.", "No."])

    ctx =
      if v1 == 1 do
        ctx =
          if Enum.random(1..100) == 34 do
            ctx = ctx |> percent_heal(hp: -99, sp: 0) |> warp("louyang", 86, 269)

            mapannounce(
              ctx,
              "louyang",
              Rathena.concat(
                Rathena.concat("", strnpcinfo(ctx, 0)),
                " : Oh God, I'm faaaaaaaaaaaalling~~!!!!"
              ),
              1
            )
          else
            warp(ctx, "lou_in01", 10, 18)
          end

        throw({:script_end, ctx})
      else
        ctx
      end

    close(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
