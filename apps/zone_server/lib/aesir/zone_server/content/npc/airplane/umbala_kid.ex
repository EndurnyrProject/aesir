defmodule Aesir.ZoneServer.Content.Npc.Airplane.UmbalaKid do
  @moduledoc """
  Transpiled from rAthena airports/airships.txt:192 (Umbala Kid#ein_p).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "airplane", x: 64, y: 94, dir: 1, sprite: 787, name: "Umbala Kid"}]

  @impl true
  def on_talk(ctx) do
    ctx = ctx |> emotion(:profusely_sweat) |> mes("[Kid]")

    ctx =
      if get_char_var(ctx, :event_umbala, 0) >= 3 do
        ctx
        |> mes("Wow, mom!")
        |> mes("L-look at this!")
        |> mes("We're flying! W-we're...")
        |> mes("We're in the freakin' sky!")
      else
        ctx
        |> mes("Makumalagu!")
        |> mes("Saampa joojimbo")
        |> mes("kaku na jedi Solo.")
        |> mes("Bwahahahahahahaah!")
      end

    close(ctx)
    exit(:normal)
  end
end
