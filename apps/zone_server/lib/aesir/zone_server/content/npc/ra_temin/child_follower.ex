defmodule Aesir.ZoneServer.Content.Npc.RaTemin.ChildFollower do
  @moduledoc """
  Transpiled from rAthena cities/rachel.txt:413 (Child Follower#in1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "ra_temin", x: 138, y: 64, dir: 5, sprite: 921, name: "Child Follower"}]

  @impl true
  def on_talk(ctx) do
    {ctx, _} =
      ctx
      |> mes("[Child Follower]")
      |> mes("No! My Mingming is dead!")
      |> next()
      |> select(["Mingming?"])

    ctx
    |> mes("[Child Follower]")
    |> mes("Mingming is a sick bird")
    |> mes("I found on the street, and")
    |> mes("I really wanted it to just")
    |> mes("rest and be healthy again")
    |> mes("but it died! Waaaaah!")
    |> emotion(:cry)
    |> next()
    |> mes("[Child Follower]")
    |> mes("I... I'm going to")
    |> mes("try to ask the pope!")
    |> mes("M-maybe she can bring")
    |> mes("Mingming back to life!")
    |> mes("Do you know how I can")
    |> mes("find our pope?")
    |> close()
  end
end
