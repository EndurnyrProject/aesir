defmodule Aesir.ZoneServer.Content.Npc.Functions.FMothermart do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/priest.txt:1756 (F_MotherMart).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, _args) do
    ctx =
      if get_char_var(ctx, :PRIEST_Q, 0) != 0 do
        ctx =
          if get_char_var(ctx, :PRIEST_Q, 0) == 1 do
            ctx =
              ctx
              |> mes("Hmm...")
              |> mes("It seems you're")
              |> mes("training to become")
              |> mes("a Priest.")
              |> next()
              |> mes("[Mother Mathilda]")
              |> mes(
                "However, at this point in your pilgrimage, I am not the person that you should be visiting."
              )
              |> next()
              |> mes("[Mother Mathilda]")
              |> mes(
                "Perhaps you should ask Bishop Paul or Sister Cecilia once again. Well, may God be with you..."
              )
              |> close()

            throw({:script_end, ctx})
          else
            ctx =
              if get_char_var(ctx, :PRIEST_Q, 0) == 2 do
                ctx =
                  ctx
                  |> mes("Ah, are you")
                  |> mes("a Priest trainee...?")
                  |> mes("Welcome!")
                  |> next()
                  |> mes("[Mother Mathilda]")
                  |> mes("We Priests are obliged to spread the message of God to")
                  |> mes("the peoples of the Earth.")
                  |> next()
                  |> mes("[Mother Mathilda]")
                  |> mes(
                    "After you become a Priest, your travels may take you to the Morocc Pyramids. I hope that you may release the Undead there from the evil to which they are bound."
                  )
                  |> next()
                  |> mes("[Mother Mathilda]")
                  |> mes("Well then...")
                  |> mes("I shall pray to")
                  |> mes("God for safety")
                  |> mes("on your journey.")
                  |> close()
                  |> savepoint("moc_fild07", 35, 355)
                  |> set_char_var(:PRIEST_Q, 3)

                throw({:script_end, ctx})
              else
                ctx =
                  if get_char_var(ctx, :PRIEST_Q, 0) == 3 do
                    ctx =
                      ctx
                      |> mes("Please leave soon, and")
                      |> mes("continue your training.")
                      |> close()

                    throw({:script_end, ctx})
                  else
                    ctx
                  end

                ctx
              end

            ctx
          end

        ctx =
          ctx
          |> mes("May I ask you the reason you came back? Please continue your training.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx = ctx |> mes("May God") |> mes("be with you...") |> close()
    throw({:script_end, ctx})
  end
end
