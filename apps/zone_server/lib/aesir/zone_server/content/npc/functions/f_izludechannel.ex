defmodule Aesir.ZoneServer.Content.Npc.Functions.FIzludechannel do
  @moduledoc """
  Transpiled from rAthena re/cities/izlude.txt:87 (F_IzludeChannel).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    {ctx, v1} =
      ctx
      |> mes("[Channel Warp Official]")
      |> mes("Izlude is a verly lively place is it not?")
      |> mes("I am here to help make Izlude feel more smooth!")
      |> mes("I can send you to another copy of Izlude.")
      |> mes("Do you want to go to another copy?")
      |> next()
      |> select(String.split(Rathena.concat(Enum.at(args, 0, 0), ":Never mind"), ":"))

    ctx = set_local(ctx, :i, v1)

    ctx =
      if get_local(ctx, :i, 0) < 6 do
        ctx =
          set_local(
            ctx,
            :"maps$",
            Rathena.put_at(get_local(ctx, :"maps$", []), 1 + 0, "izlude", 0)
          )

        ctx =
          set_local(
            ctx,
            :"maps$",
            Rathena.put_at(get_local(ctx, :"maps$", []), 1 + 1, "izlude_a", 0)
          )

        ctx =
          set_local(
            ctx,
            :"maps$",
            Rathena.put_at(get_local(ctx, :"maps$", []), 1 + 2, "izlude_b", 0)
          )

        ctx =
          set_local(
            ctx,
            :"maps$",
            Rathena.put_at(get_local(ctx, :"maps$", []), 1 + 3, "izlude_c", 0)
          )

        ctx =
          set_local(
            ctx,
            :"maps$",
            Rathena.put_at(get_local(ctx, :"maps$", []), 1 + 4, "izlude_d", 0)
          )

        ctx =
          savepoint(
            ctx,
            Enum.at(get_local(ctx, :"maps$", []), get_local(ctx, :i, 0), ""),
            128,
            98
          )

        ctx = warp(ctx, Enum.at(get_local(ctx, :"maps$", []), get_local(ctx, :i, 0), ""), 128, 98)
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx = close(ctx)
    throw({:script_end, ctx})
  end
end
