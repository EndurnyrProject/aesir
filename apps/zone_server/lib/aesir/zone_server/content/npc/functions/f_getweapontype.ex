defmodule Aesir.ZoneServer.Content.Npc.Functions.FGetweapontype do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:263 (F_GetWeaponType).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Todo

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    v1 = Todo.call!(:getiteminfo, [Enum.at(args, 0, 0), get_char_var(ctx, :ITEMINFO_VIEW, 0)])

    case v1 do
      1 ->
        {ctx, "Dagger"}

      2 ->
        {ctx, "One-handed Sword"}

      3 ->
        {ctx, "Two-handed Sword"}

      4 ->
        {ctx, "One-handed Spear"}

      5 ->
        {ctx, "Two-handed Spear"}

      6 ->
        {ctx, "One-handed Axe"}

      7 ->
        {ctx, "Two-handed Axe"}

      8 ->
        {ctx, "Mace"}

      10 ->
        {ctx, "Staff"}

      11 ->
        {ctx, "Bow"}

      12 ->
        {ctx, "Knuckle"}

      13 ->
        {ctx, "Instrument"}

      14 ->
        {ctx, "Whip"}

      15 ->
        {ctx, "Book"}

      16 ->
        {ctx, "Katar"}

      17 ->
        {ctx, "Revolver"}

      18 ->
        {ctx, "Rifle"}

      19 ->
        {ctx, "Gatling gun"}

      20 ->
        {ctx, "Shotgun"}

      21 ->
        {ctx, "Grenade Launcher"}

      22 ->
        {ctx, "Shuriken"}

      _ ->
        {ctx, "Unknown Weapon"}
    end
  end
end
