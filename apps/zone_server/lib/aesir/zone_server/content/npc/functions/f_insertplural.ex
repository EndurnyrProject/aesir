defmodule Aesir.ZoneServer.Content.Npc.Functions.FInsertplural do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:517 (F_InsertPlural).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    {ctx, v1} =
      Aesir.ZoneServer.Content.Npc.Functions.FGetplural.call(ctx, [
        Enum.at(args, 1, 0),
        Enum.at(args, 2, 0)
      ])

    {ctx,
     Rathena.format(Enum.at(args, 3, "%d %s"), [
       Enum.at(args, 0, 0),
       if(Enum.at(args, 0, 0) == 1, do: Enum.at(args, 1, 0), else: v1)
     ])}
  end
end
