defmodule Aesir.ZoneServer.Content.Npc.Functions.FInsertarticle do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:578 (F_InsertArticle).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    {ctx, v1} =
      Aesir.ZoneServer.Content.Npc.Functions.FGetarticle.call(ctx, [Enum.at(args, 0, 0)])

    ctx = set_local(ctx, :"article$", v1)

    {ctx,
     Todo.call!(:sprintf, [
       "%s %s",
       if(Rathena.truthy?(Enum.at(args, 1, 0)),
         do: Todo.call!(:replacestr, [get_local(ctx, :"article$", ""), "a", "A"]),
         else: get_local(ctx, :"article$", "")
       ),
       Enum.at(args, 0, 0)
     ])}
  end
end
