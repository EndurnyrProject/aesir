defmodule Aesir.ZoneServer.Content.Npc.Functions.FGetplural do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:448 (F_GetPlural).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    try do
      ctx = set_local(ctx, :"str$", Enum.at(args, 0, 0))

      ctx =
        if Rathena.truthy?(Rathena.countstr(get_local(ctx, :"str$", ""), " ")) do
          ctx =
            set_local(
              ctx,
              :"tmp$",
              Rathena.put_many(
                get_local(ctx, :"tmp$", []),
                0,
                Rathena.explode(get_local(ctx, :"str$", ""), " "),
                ""
              )
            )

          ctx = set_local(ctx, :size, length(get_local(ctx, :"tmp$", [])))

          ctx =
            if Rathena.truthy?(Rathena.compare(get_local(ctx, :"str$", ""), " of ")) or
                 Rathena.truthy?(Rathena.compare(get_local(ctx, :"str$", ""), " in ")) or
                 Rathena.truthy?(Rathena.compare(get_local(ctx, :"str$", ""), " on ")) do
              ctx |> set_local(:i, 1) |> loop_1(args)
            else
              set_local(ctx, :index, get_local(ctx, :size, 0) - 1)
            end

          ctx =
            set_local(
              ctx,
              :"str$",
              Enum.at(get_local(ctx, :"tmp$", []), get_local(ctx, :index, 0), "")
            )

          ctx =
            set_local(
              ctx,
              :"tmp$",
              Rathena.put_at(get_local(ctx, :"tmp$", []), get_local(ctx, :index, 0), "%s", "")
            )

          set_local(ctx, :"format$", Enum.join(get_local(ctx, :"tmp$", []), " "))
        else
          set_local(ctx, :"format$", "%s")
        end

      ctx = set_local(ctx, :strlen, Rathena.getstrlen(get_local(ctx, :"str$", "")))

      ctx =
        if get_local(ctx, :strlen, 0) < 3 do
          throw(
            {:script_return,
             {ctx,
              if(Rathena.truthy?(Enum.at(args, 1, 0)),
                do:
                  Rathena.strtoupper(
                    Rathena.format(get_local(ctx, :"format$", ""), [get_local(ctx, :"str$", "")])
                  ),
                else:
                  Rathena.format(get_local(ctx, :"format$", ""), [get_local(ctx, :"str$", "")])
              )}}
          )
        else
          ctx
        end

      ctx =
        set_local(ctx, :"suffix$", [
          Rathena.charat(get_local(ctx, :"str$", ""), get_local(ctx, :strlen, 0) - 1),
          Rathena.substr(
            get_local(ctx, :"str$", ""),
            get_local(ctx, :strlen, 0) - 2,
            get_local(ctx, :strlen, 0) - 1
          )
        ])

      ctx =
        if not Rathena.truthy?(
             Rathena.compare(
               "abcdefghijklmnopqrstuvwxyz",
               Enum.at(get_local(ctx, :"suffix$", []), 0, "")
             )
           ) do
          set_local(ctx, :"result$", get_local(ctx, :"str$", ""))
        else
          ctx =
            if Rathena.truthy?(
                 Rathena.compare(
                   "fish|glasses|sunglasses|clothes|boots|shoes|greaves|sandals|wings|ears",
                   get_local(ctx, :"str$", "")
                 )
               ) do
              set_local(ctx, :"result$", get_local(ctx, :"str$", ""))
            else
              ctx =
                if Enum.at(get_local(ctx, :"suffix$", []), 0, "") == "s" or
                     Enum.at(get_local(ctx, :"suffix$", []), 0, "") == "x" or
                     Enum.at(get_local(ctx, :"suffix$", []), 0, "") == "z" or
                     Enum.at(get_local(ctx, :"suffix$", []), 1, "") == "ch" or
                     Enum.at(get_local(ctx, :"suffix$", []), 1, "") == "sh" do
                  set_local(ctx, :"result$", Rathena.concat(get_local(ctx, :"str$", ""), "es"))
                else
                  ctx =
                    if (Enum.at(get_local(ctx, :"suffix$", []), 0, "") == "f" or
                          Enum.at(get_local(ctx, :"suffix$", []), 1, "") == "fe") and
                         Enum.at(get_local(ctx, :"suffix$", []), 1, "") != "ff" do
                      ctx =
                        if Rathena.truthy?(
                             Rathena.compare(
                               "belief|cliff|chief|dwarf|grief|gulf|proof|roof",
                               get_local(ctx, :"str$", "")
                             )
                           ) do
                          set_local(
                            ctx,
                            :"result$",
                            Rathena.concat(get_local(ctx, :"str$", ""), "s")
                          )
                        else
                          set_local(
                            ctx,
                            :"result$",
                            Rathena.concat(
                              Rathena.substr(
                                get_local(ctx, :"str$", ""),
                                0,
                                get_local(ctx, :strlen, 0) - 2 -
                                  Rathena.bool_int(
                                    Enum.at(get_local(ctx, :"suffix$", []), 1, "") == "fe"
                                  )
                              ),
                              "ves"
                            )
                          )
                        end

                      ctx
                    else
                      ctx =
                        if Enum.at(get_local(ctx, :"suffix$", []), 0, "") == "y" and
                             not Rathena.truthy?(
                               Rathena.compare(
                                 "aeiou",
                                 Rathena.charat(Enum.at(get_local(ctx, :"suffix$", []), 1, ""), 0)
                               )
                             ) do
                          set_local(
                            ctx,
                            :"result$",
                            Rathena.concat(
                              Todo.call!(:delchar, [
                                get_local(ctx, :"str$", ""),
                                get_local(ctx, :strlen, 0) - 1
                              ]),
                              "ies"
                            )
                          )
                        else
                          ctx =
                            if Enum.at(get_local(ctx, :"suffix$", []), 0, "") == "o" and
                                 Rathena.truthy?(
                                   Rathena.compare(
                                     "buffalo|domino|echo|grotto|halo|hero|mango|mosquito|potato|tomato|tornado|torpedo|veto|volcano",
                                     get_local(ctx, :"str$", "")
                                   )
                                 ) do
                              set_local(
                                ctx,
                                :"result$",
                                Rathena.concat(get_local(ctx, :"str$", ""), "es")
                              )
                            else
                              set_local(
                                ctx,
                                :"result$",
                                Rathena.concat(get_local(ctx, :"str$", ""), "s")
                              )
                            end

                          ctx
                        end

                      ctx
                    end

                  ctx
                end

              ctx
            end

          ctx
        end

      throw(
        {:script_return,
         {ctx,
          if(Rathena.truthy?(Enum.at(args, 1, 0)),
            do:
              Rathena.strtoupper(
                Rathena.format(get_local(ctx, :"format$", ""), [get_local(ctx, :"result$", "")])
              ),
            else: Rathena.format(get_local(ctx, :"format$", ""), [get_local(ctx, :"result$", "")])
          )}}
      )
    catch
      :throw, {:script_return, result} -> result
    end
  end

  defp loop_1(ctx, args) do
    if get_local(ctx, :i, 0) < get_local(ctx, :size, 0) do
      result =
        try do
          ctx =
            if Rathena.getstrlen(Enum.at(get_local(ctx, :"tmp$", []), get_local(ctx, :i, 0), "")) ==
                 2 and
                 Rathena.truthy?(
                   Rathena.compare(
                     "of|in|on",
                     Enum.at(get_local(ctx, :"tmp$", []), get_local(ctx, :i, 0), "")
                   )
                 ) do
              throw({:brk_1, ctx})
            else
              ctx
            end

          ctx = set_local(ctx, :index, get_local(ctx, :index, 0) + 1)
          {:next, ctx}
        catch
          :throw, {:brk_1, ctx} -> {:done, ctx}
          :throw, {:cont_1, ctx} -> {:next, ctx}
        end

      case result do
        {:next, ctx} ->
          ctx = set_local(ctx, :i, get_local(ctx, :i, 0) + 1)
          loop_1(ctx, args)

        {:done, ctx} ->
          ctx
      end
    else
      ctx
    end
  end
end
