defmodule Aesir.ZoneServer.Content.Npc.Functions.FInsertcomma do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:590 (F_InsertComma).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    ctx = set_local(ctx, :"str$", Enum.at(args, 0, 0))
    ctx = ctx |> set_local(:i, Rathena.getstrlen(get_local(ctx, :"str$", "")) - 3) |> loop_1(args)
    {ctx, get_local(ctx, :"str$", "")}
  end

  defp loop_1(ctx, args) do
    if get_local(ctx, :i, 0) > 0 do
      ctx =
        set_local(
          ctx,
          :"str$",
          Rathena.insertchar(get_local(ctx, :"str$", ""), ",", get_local(ctx, :i, 0))
        )

      ctx |> set_local(:i, get_local(ctx, :i, 0) - 3) |> loop_1(args)
    else
      ctx
    end
  end
end
