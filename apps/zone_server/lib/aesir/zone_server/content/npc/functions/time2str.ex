defmodule Aesir.ZoneServer.Content.Npc.Functions.Time2str do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:350 (Time2Str).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    ctx = set_local(ctx, :Time_Left, Enum.at(args, 0, 0) - gettimetick(ctx, 2))
    ctx = set_local(ctx, :Days, div(get_local(ctx, :Time_Left, 0), 86400))

    ctx =
      set_local(ctx, :Time_Left, get_local(ctx, :Time_Left, 0) - get_local(ctx, :Days, 0) * 86400)

    ctx = set_local(ctx, :Hours, div(get_local(ctx, :Time_Left, 0), 3600))

    ctx =
      set_local(ctx, :Time_Left, get_local(ctx, :Time_Left, 0) - get_local(ctx, :Hours, 0) * 3600)

    ctx = set_local(ctx, :Minutes, div(get_local(ctx, :Time_Left, 0), 60))

    ctx =
      ctx
      |> set_local(:Time_Left, get_local(ctx, :Time_Left, 0) - get_local(ctx, :Minutes, 0) * 60)
      |> set_local(:"Time$", "")

    ctx =
      if get_local(ctx, :Days, 0) > 1 do
        set_local(
          ctx,
          :"Time$",
          Rathena.concat(
            Rathena.concat(get_local(ctx, :"Time$", ""), get_local(ctx, :Days, 0)),
            " days, "
          )
        )
      else
        ctx =
          if get_local(ctx, :Days, 0) > 0 do
            set_local(
              ctx,
              :"Time$",
              Rathena.concat(
                Rathena.concat(get_local(ctx, :"Time$", ""), get_local(ctx, :Days, 0)),
                " day, "
              )
            )
          else
            ctx
          end

        ctx
      end

    ctx =
      if get_local(ctx, :Hours, 0) > 1 do
        set_local(
          ctx,
          :"Time$",
          Rathena.concat(
            Rathena.concat(get_local(ctx, :"Time$", ""), get_local(ctx, :Hours, 0)),
            " hours, "
          )
        )
      else
        ctx =
          if get_local(ctx, :Hours, 0) > 0 do
            set_local(
              ctx,
              :"Time$",
              Rathena.concat(
                Rathena.concat(get_local(ctx, :"Time$", ""), get_local(ctx, :Hours, 0)),
                " hour, "
              )
            )
          else
            ctx
          end

        ctx
      end

    ctx =
      if get_local(ctx, :Minutes, 0) > 1 do
        set_local(
          ctx,
          :"Time$",
          Rathena.concat(
            Rathena.concat(get_local(ctx, :"Time$", ""), get_local(ctx, :Minutes, 0)),
            " minutes, "
          )
        )
      else
        ctx =
          if get_local(ctx, :Minutes, 0) > 0 do
            set_local(
              ctx,
              :"Time$",
              Rathena.concat(
                Rathena.concat(get_local(ctx, :"Time$", ""), get_local(ctx, :Minutes, 0)),
                " minute, "
              )
            )
          else
            ctx
          end

        ctx
      end

    ctx =
      if get_local(ctx, :Time_Left, 0) > 1 or get_local(ctx, :Time_Left, 0) == 0 do
        set_local(
          ctx,
          :"Time$",
          Rathena.concat(
            Rathena.concat(get_local(ctx, :"Time$", ""), get_local(ctx, :Time_Left, 0)),
            " seconds"
          )
        )
      else
        ctx =
          if get_local(ctx, :Time_Left, 0) == 1 do
            set_local(
              ctx,
              :"Time$",
              Rathena.concat(
                Rathena.concat(get_local(ctx, :"Time$", ""), get_local(ctx, :Time_Left, 0)),
                " second"
              )
            )
          else
            ctx
          end

        ctx
      end

    {ctx, get_local(ctx, :"Time$", "")}
  end
end
