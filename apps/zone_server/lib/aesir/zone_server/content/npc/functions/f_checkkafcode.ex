defmodule Aesir.ZoneServer.Content.Npc.Functions.FCheckkafcode do
  @moduledoc """
  Transpiled from rAthena kafras/functions_kafras.txt:475 (F_CheckKafCode).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, _args) do
    ctx =
      if not Rathena.truthy?(get_account_var(ctx, "#kafra_code", 0)) do
        {ctx, nil}
      else
        ctx
      end

    {ctx, v1} = ctx |> mes("Enter your storage password:") |> input(:int)
    ctx = set_local(ctx, :code_, v1)

    ctx =
      if get_local(ctx, :code_, 0) != get_account_var(ctx, "#kafra_code", 0) do
        ctx |> dispbottom("Wrong storage password.") |> close() |> cutin("", 255)
      else
        ctx
      end

    ctx = set_temp_var(ctx, :kafcode_try, 0)
    {ctx, nil}
  end
end
