defmodule Aesir.ZoneServer.Content.Npc.Functions.FKafend do
  @moduledoc """
  Transpiled from rAthena kafras/functions_kafras.txt:448 (F_KafEnd).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    ctx = ctx |> set_local(:type, Enum.at(args, 0, 0)) |> mes("[Kafra Employee]")

    ctx =
      if Enum.at(args, 1, 0) == 1 do
        ctx
        |> mes("Your Respawn Point")
        |> mes("has been saved here")
        |> mes(Rathena.concat(Rathena.concat("", Enum.at(args, 2, 0)), "."))
        |> mes("Thank you for using")
        |> mes("the Kafra Services.")
      else
        ctx =
          if get_local(ctx, :type, 0) == 0 or get_local(ctx, :type, 0) == 5 do
            ctx
            |> mes("We, here at Kafra Corporation,")
            |> mes(
              "are always endeavoring to provide you with the best services. We hope that we meet your adventuring needs and standards of excellence."
            )
          else
            ctx =
              if get_local(ctx, :type, 0) == 1 do
                ctx
                |> percent_heal(hp: 0, sp: -25)
                |> mes("^666666Kaffffra n-never")
                |> mes("diiiiiiiiiiiiiies. On...")
                |> mes("On y-yooour siiiiide~^000000")
              else
                ctx =
                  if get_local(ctx, :type, 0) == 2 do
                    ctx |> mes("Saved.") |> mes("Thank you for your patronage.")
                  else
                    ctx
                  end

                ctx
              end

            ctx
          end

        ctx
      end

    ctx |> close() |> cutin("", 255)
  end
end
