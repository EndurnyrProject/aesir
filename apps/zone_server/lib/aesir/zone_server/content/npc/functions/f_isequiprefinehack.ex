defmodule Aesir.ZoneServer.Content.Npc.Functions.FIsequiprefinehack do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:403 (F_IsEquipRefineHack).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    ctx =
      ctx
      |> set_local(:refine_chk, Todo.call!(:getequiprefinerycnt, [Enum.at(args, 0, 0)]))
      |> set_local(:refine, Enum.at(args, 1, 0))

    ctx =
      if get_local(ctx, :refine, 0) != get_local(ctx, :refine_chk, 0) do
        ctx =
          todo(ctx, :logmes, [
            Rathena.concat(
              Rathena.concat(
                Rathena.concat(
                  Rathena.concat(
                    "Hack: Tried to swap equip with refine +",
                    get_local(ctx, :refine, 0)
                  ),
                  " for +"
                ),
                get_local(ctx, :refine_chk, 0)
              ),
              "."
            )
          ])

        {ctx, 1}
      else
        ctx
      end

    {ctx, 0}
  end
end
