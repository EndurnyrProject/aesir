defmodule Aesir.ZoneServer.Content.Npc.Functions.FIscharm do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:758 (F_IsCharm).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    ctx = set_local(ctx, :id, Enum.at(args, 0, 0))

    {ctx,
     Rathena.bool_int(
       (get_local(ctx, :id, 0) >= 4700 and get_local(ctx, :id, 0) <= 4999) or
         (get_local(ctx, :id, 0) >= 29000 and get_local(ctx, :id, 0) <= 29689) or
         (get_local(ctx, :id, 0) >= 310_000 and get_local(ctx, :id, 0) <= 311_091)
     )}
  end
end
