defmodule Aesir.ZoneServer.Content.Npc.Functions.FGetarticle do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:535 (F_GetArticle).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    try do
      ctx = set_local(ctx, :"str$", Rathena.strtolower(Enum.at(args, 0, 0)))

      ctx =
        if not Rathena.truthy?(Rathena.charisalpha(get_local(ctx, :"str$", ""), 0)) do
          throw({:script_return, {ctx, "a"}})
        else
          ctx
        end

      ctx =
        if Rathena.getstrlen(get_local(ctx, :"str$", "")) == 1 do
          if Rathena.strpos("aefhilmnorsx", get_local(ctx, :"str$", "")) > -1 do
            throw({:script_return, {ctx, "an"}})
          else
            throw({:script_return, {ctx, "a"}})
          end
        else
          ctx
        end

      ctx =
        if Rathena.truthy?(
             Rathena.preg_match("(euler|hour(?!i)|heir|honest|hono)", get_local(ctx, :"str$", ""))
           ) do
          throw({:script_return, {ctx, "an"}})
        else
          ctx
        end

      ctx =
        if Rathena.truthy?(Rathena.preg_match("^[^aeiouy]", get_local(ctx, :"str$", ""))) do
          throw({:script_return, {ctx, "a"}})
        else
          ctx
        end

      ctx =
        if Rathena.truthy?(Rathena.preg_match("^e[uw]", get_local(ctx, :"str$", ""))) or
             Rathena.truthy?(Rathena.preg_match("^onc?eb", get_local(ctx, :"str$", ""))) or
             Rathena.truthy?(Rathena.preg_match("^uni([^nmd]|mo)", get_local(ctx, :"str$", ""))) or
             Rathena.truthy?(
               Rathena.preg_match("^u[bcfhjkqrst][aeiou]", get_local(ctx, :"str$", ""))
             ) do
          throw({:script_return, {ctx, "a"}})
        else
          ctx
        end

      ctx =
        if Rathena.truthy?(Rathena.preg_match("^ut[th]", get_local(ctx, :"str$", ""))) do
          throw({:script_return, {ctx, "an"}})
        else
          ctx
        end

      ctx =
        if Rathena.truthy?(Rathena.preg_match("^[aeiou]", get_local(ctx, :"str$", ""))) do
          throw({:script_return, {ctx, "an"}})
        else
          ctx
        end

      throw({:script_return, {ctx, "a"}})
    catch
      :throw, {:script_return, result} -> result
    end
  end
end
