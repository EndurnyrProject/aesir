defmodule Aesir.ZoneServer.Content.Npc.Functions.FFatheryos do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/priest.txt:1806 (F_FatherYos).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, _args) do
    ctx =
      if get_char_var(ctx, :PRIEST_Q, 0) != 0 do
        ctx =
          if get_char_var(ctx, :PRIEST_Q, 0) == 1 do
            ctx =
              ctx
              |> mes("Hmm...")
              |> mes("A Priest trainee, eh?")
              |> next()
              |> mes("[Father Yosuke]")
              |> mes(
                "Well, you got the wrong person. If you're supposed to meet up with me at all, it'd be later."
              )
              |> next()
              |> mes("[Father Yosuke]")
              |> mes("But that's your own fault.")
              |> mes("Now, go back to Church, kid.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx =
              if get_char_var(ctx, :PRIEST_Q, 0) == 2 do
                ctx =
                  ctx
                  |> mes("Hmm...")
                  |> mes("A Priest trainee, eh?")
                  |> next()
                  |> mes("[Father Yosuke]")
                  |> mes(
                    "Well, you got the wrong person. If you're supposed to meet up with me at all, it'd be later."
                  )
                  |> next()
                  |> mes("[Father Yosuke]")
                  |> mes("But that's your own fault. Go back to Church.")
                  |> close()

                throw({:script_end, ctx})
              else
                ctx =
                  if get_char_var(ctx, :PRIEST_Q, 0) == 3 do
                    ctx =
                      ctx
                      |> mes("Hmm.")
                      |> mes("A Priest")
                      |> mes("trainee, eh? ")
                      |> mes("Welcome.")
                      |> next()
                      |> mes("[Father Yosuke]")
                      |> mes("I won't say")
                      |> mes("anything more.")
                      |> mes("Just devote your")
                      |> mes("life to God.")
                      |> next()
                      |> mes("[Father Yosuke]")
                      |> mes("Now go back to church.")
                      |> mes("Hereby, the first of")
                      |> mes("your trials is now")
                      |> mes("completed.")
                      |> close()
                      |> savepoint("prt_fild00", 206, 230)
                      |> set_char_var(:PRIEST_Q, 4)

                    throw({:script_end, ctx})
                  else
                    ctx =
                      if get_char_var(ctx, :PRIEST_Q, 0) == 4 do
                        ctx =
                          ctx
                          |> mes("I told you to go back to church.")
                          |> mes(
                            "Or do you want to live with me here for the rest of your life...?"
                          )
                          |> close()

                        throw({:script_end, ctx})
                      else
                        ctx
                      end

                    ctx
                  end

                ctx
              end

            ctx
          end

        ctx = ctx |> mes("Just go be a Priest. This isn't a playground for kids.") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx = ctx |> mes("...Acolyte, you don't have any business with me here.") |> close()
    throw({:script_end, ctx})
  end
end
