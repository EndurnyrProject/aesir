defmodule Aesir.ZoneServer.Content.Npc.Functions.FCleargarbage do
  @moduledoc """
  Transpiled from rAthena other/Global_Functions.txt:101 (F_ClearGarbage).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, _args) do
    ctx =
      if get_char_var(ctx, :TURTLE, 0) == 20 do
        set_char_var(ctx, :MISC_QUEST, :erlang.bor(get_char_var(ctx, :MISC_QUEST, 0), 65536))
      else
        ctx
      end

    ctx =
      if Rathena.truthy?(:erlang.band(get_char_var(ctx, :MISC_QUEST, 0), 65536)) do
        set_char_var(ctx, :TURTLE, 0)
      else
        ctx
      end

    ctx =
      ctx
      |> set_char_var(:ADV_QSK, 0)
      |> set_char_var(:ADV_QSK2, 0)
      |> set_char_var(:RES_SKILL, 0)
      |> set_char_var(:wizard_m2, 0)
      |> set_char_var(:NEW_MES_FLAG0, 0)
      |> set_char_var(:NEW_MES_FLAG1, 0)
      |> set_char_var(:NEW_MES_FLAG2, 0)
      |> set_char_var(:NEW_MES_FLAG3, 0)
      |> set_char_var(:NEW_MES_FLAG4, 0)
      |> set_char_var(:NEW_MES_FLAG5, 0)
      |> set_char_var(:NEW_LVUP0, 0)
      |> set_char_var(:NEW_LVUP1, 0)
      |> set_char_var(:NEW_JOBLVUP, 0)
      |> set_char_var(:dtseligible, 0)

    ctx =
      set_char_var(
        ctx,
        :MISC_QUEST,
        :erlang.band(get_char_var(ctx, :MISC_QUEST, 0), :erlang.bnot(128))
      )

    {ctx, nil}
  end
end
