defmodule Aesir.ZoneServer.Content.Npc.Umbala.UtanKid59243 do
  @moduledoc """
  Transpiled from rAthena cities/umbala.txt:366 (Utan Kid#1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "umbala", x: 59, y: 243, dir: 5, sprite: 787, name: "Utan Kid"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    if get_char_var(ctx, :event_umbala, 0) >= 3 do
      {ctx, v1} =
        ctx
        |> mes("[Kotan]")
        |> mes("............")
        |> mes(".........poop!")
        |> mes(".....me like Meat.")
        |> mes("....gimme a Meat.")
        |> emotion(:rock)
        |> next()
        |> select(["Give him Meat.", "Refuse."])

      ctx =
        if v1 == 1 do
          ctx =
            if count_item(ctx, 517) > 0 do
              ctx =
                ctx
                |> mes("[Kotan]")
                |> mes("Whoa, are you really giving me")
                |> mes("Meat? Thanks! I will pay you")
                |> mes("back with these.")
                |> delitem(517, 1)

              ctx =
                if Rathena.truthy?(checkre(ctx, 0)) do
                  v2 = Enum.random(1..4)

                  ctx =
                    case v2 do
                      1 ->
                        give_item(ctx, 909, 3)

                      2 ->
                        give_item(ctx, 914, 3)

                      3 ->
                        give_item(ctx, 705, 3)

                      4 ->
                        ctx = give_item(ctx, 705, 3)
                        ctx

                      _ ->
                        ctx
                    end

                  ctx
                else
                  ctx |> give_item(909, 2) |> give_item(914, 2) |> give_item(705, 2)
                end

              ctx = ctx |> emotion(:scissor) |> close()
              throw({:script_end, ctx})
            else
              ctx
            end

          ctx =
            ctx
            |> mes("[Kotan]")
            |> mes("Hah! You don't have Meat,")
            |> mes("but pretend that you do?!")
            |> mes("I hate people who lie")
            |> mes("to me!!")
            |> percent_heal(hp: -20, sp: 0)
            |> emotion(:fret)
            |> close()

          throw({:script_end, ctx})
        else
          ctx
        end

      ctx =
        ctx
        |> mes("[Kotan]")
        |> mes(".........")
        |> mes(".....hungwee.")
        |> mes(".....I want Meat.")
        |> percent_heal(hp: -1, sp: 0)
        |> emotion(:cry)
        |> close()

      throw({:script_end, ctx})
    else
      {ctx, v3} =
        ctx
        |> mes("[???]")
        |> mes("............")
        |> mes("........umbah.")
        |> mes(".......umbabah.")
        |> mes("......humbah.")
        |> emotion(:rock)
        |> next()
        |> select(["Umbah", "Umbaboo"])

      ctx =
        if v3 == 1 do
          ctx =
            if count_item(ctx, 517) > 0 do
              ctx =
                ctx
                |> mes("[???]")
                |> mes("Umbaumbaumbabababah.")
                |> mes("Umbababahum.")
                |> delitem(517, 1)

              ctx =
                if Rathena.truthy?(checkre(ctx, 0)) do
                  v4 = Enum.random(1..4)

                  ctx =
                    case v4 do
                      1 ->
                        give_item(ctx, 909, 3)

                      2 ->
                        give_item(ctx, 914, 3)

                      3 ->
                        give_item(ctx, 705, 3)

                      4 ->
                        ctx = give_item(ctx, 705, 3)
                        ctx

                      _ ->
                        ctx
                    end

                  ctx
                else
                  ctx |> give_item(909, 2) |> give_item(914, 2) |> give_item(705, 2)
                end

              ctx = ctx |> emotion(:scissor) |> close()
              throw({:script_end, ctx})
            else
              ctx
            end

          ctx =
            ctx
            |> mes("[???]")
            |> mes("Umbahumumhumbubabababah!!")
            |> mes("Umbahumbababah umbahumboo!")
            |> percent_heal(hp: -20, sp: 0)
            |> emotion(:fret)
            |> close()

          throw({:script_end, ctx})
        else
          ctx
        end

      ctx =
        ctx
        |> mes("[???]")
        |> mes("...........")
        |> mes("......woong bah.")
        |> mes("....umbabababah.")
        |> emotion(:cry)
        |> percent_heal(hp: -1, sp: 0)
        |> close()

      throw({:script_end, ctx})
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
