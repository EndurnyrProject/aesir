defmodule Aesir.ZoneServer.Content.Npc.Malaya.LittleKid do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:464 (Little Kid#ma05).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "malaya",
        x: 219,
        y: 92,
        dir: 6,
        sprite: 577,
        name: "Little Kid",
        unique_name: "Little Kid#ma05"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx =
          ctx
          |> mes("[Little Kid]")
          |> mes("My mom told me not to play outside because its dangerous. Why?")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx =
              ctx
              |> mes("[Little Kid]")
              |> mes("My mom told me I can play but only in Port Malaya.")
              |> next()
              |> mes("[Little Kid]")
              |> mes("He he. But I never thought of going outside of Port Malaya.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx =
      if Enum.random(1..3) == 2 do
        ctx =
          ctx
          |> mes("[Little Kid]")
          |> mes("I heard someone took Jejelings hat in Baryo Mahiwaga.")
          |> next()
          |> mes("[Little Kid]")

        ctx =
          ctx
          |> mes(
            Rathena.concat(
              Rathena.concat(
                "Why would someone steal a monster's hat? ",
                getd(ctx, Rathena.concat(Rathena.concat("$ma_name0", Enum.random(1..6)), "$"))
              ),
              " must be desperate."
            )
          )
          |> next()
          |> mes("[Little Kid]")
          |> mes(
            "Oh wait! This was suppose to be a secret. Shsh! Please pretend you didn't hear me."
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Little Kid]")
    |> mes("I think someone is stealing all the hats in the village.")
    |> next()
    |> mes("[Little Kid]")
    |> mes(
      "The hat my mom washed yesterday disappeared. Our neighbor's hat also disappeared a few days ago."
    )
    |> next()
    |> mes("[Little Kid]")
    |> mes("Hmm... do you think Jejeling took it?")
    |> next()
    |> mes("[Little Kid]")
    |> mes(
      "I visited a friend in Baryo Mahiwaga and he said he saw Jejeling wearing the missing hat..."
    )
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
