defmodule Aesir.ZoneServer.Content.Npc.Malaya.Woman do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:605 (Woman#ma08).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "malaya", x: 63, y: 185, dir: 4, sprite: 583, name: "Woman"}]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx = ctx |> mes("[Woman]") |> mes("I don't have anything to share with you.") |> close()
        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx =
              ctx
              |> mes("[Woman]")
              |> mes(
                "Im worried about the children. I hope they aren't terrified from whats going on in the village."
              )
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx =
      if Enum.random(1..3) == 2 do
        ctx =
          ctx
          |> set_local(
            :"name$",
            Todo.call!(:getd, [
              Rathena.concat(Rathena.concat("$ma_name0", Enum.random(1..6)), "$")
            ])
          )
          |> set_local(:rand, Enum.random(1..10))

        ctx =
          if get_local(ctx, :rand, 0) < 5 do
            ctx = mes(ctx, "[Woman]")

            ctx
            |> mes(
              Rathena.concat(
                Rathena.concat(
                  "I like fun stories. Not so long ago, ",
                  get_local(ctx, :"name$", "")
                ),
                " came and told me a funny story."
              )
            )
            |> next()
          else
            ctx
          end

        v1 = get_local(ctx, :rand, 0)

        ctx =
          case v1 do
            1 ->
              ctx |> mes("[Woman]") |> mes("'My aunt stepped on an ant.'") |> next()

            2 ->
              ctx |> mes("[Woman]") |> mes("'You eat chili on a chilly day.'") |> next()

            3 ->
              ctx
              |> mes("[Woman]")
              |> mes("'Why are you putting flour on that flower?'")
              |> next()

            4 ->
              ctx =
                ctx
                |> mes("[Woman]")
                |> mes(
                  "At the Tool Store, 'Ill buy the needle and thread! You buy the hay to lose the needle in.'"
                )
                |> next()
                |> mes("[Woman]")

              ctx
              |> mes(
                Rathena.concat(
                  get_local(ctx, :"name$", ""),
                  " said that and was kicked out of the Tool Store."
                )
              )
              |> next()

            5 ->
              ctx =
                ctx
                |> mes("[Woman]")
                |> mes("A Kafra Employee I'm friends with told me a story about a customer.")
                |> next()
                |> mes("[Woman]")
                |> mes(
                  "'Kafra, the wise never marry and when they marry, they become otherwise.'"
                )
                |> next()
                |> mes("[Woman]")
                |> mes("He he... I do like funny stories but a bit difficult for my taste.")
                |> close()

              throw({:script_end, ctx})

            _ ->
              ctx = mes(ctx, "[Woman]")

              ctx
              |> mes(
                Rathena.concat(
                  Rathena.concat(
                    "I like fun stories. Oh by the way! ",
                    get_local(ctx, :"name$", "")
                  ),
                  ", love is photogenic. Don't you agree?"
                )
              )
              |> next()
              |> mes("[Woman]")
              |> mes("It needs darkness to develop.")
              |> next()
          end

        ctx = ctx |> mes("[Woman]") |> mes("He he. Isn't it fun?") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, _} =
      ctx
      |> mes("[Woman]")
      |> mes(
        "If you hear a baby crying from the forest outside of the village, never go near it."
      )
      |> next()
      |> select(["Why?"])

    {ctx, _} =
      ctx
      |> mes("[Woman]")
      |> mes(
        "There is a monster named Tiucknuc that roams around outside of the village and cries after transforming himself into a baby."
      )
      |> next()
      |> mes("[Woman]")
      |> mes(
        "But when kind travelers pick up the baby, it turns back into its original form and attacks."
      )
      |> next()
      |> select(["Sounds like a sneaky monster."])

    ctx
    |> mes("[Woman]")
    |> mes("Yes, this monster is bad to trick the kindness of travelers but...")
    |> next()
    |> mes("[Woman]")
    |> mes(
      "There is rumor that the monster came from the soul of a baby that never been born. So sad."
    )
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
