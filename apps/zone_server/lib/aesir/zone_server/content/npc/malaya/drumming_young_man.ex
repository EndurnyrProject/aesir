defmodule Aesir.ZoneServer.Content.Npc.Malaya.DrummingYoungMan do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:326 (Drumming Young Man#ma02).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "malaya", x: 189, y: 263, dir: 4, sprite: 578, name: "Drumming Young Man"}]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx =
          ctx
          |> emotion(:huk)
          |> mes("[Drumming Young Man]")
          |> mes("Ugh... it's just like that time before...")
          |> next()
          |> emotion(:huk)
          |> mes("[Drumming Young Man]")
          |> mes("When the moon was swallowed. Argh!")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx =
              ctx
              |> mes("[Drumming Young Man]")
              |> mes("I think the village was saved by a nameless adventurer.")
              |> next()
              |> emotion(:ohno)
              |> mes("[Drumming Young Man]")
              |> mes("Phew... I thought Bakonawa appeared again.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx =
      if Enum.random(1..3) == 2 do
        ctx =
          ctx
          |> mes("[Drumming Young Man]")
          |> mes(
            Rathena.concat(
              Todo.call!(:getd, [
                Rathena.concat(Rathena.concat("$ma_name0", Enum.random(1..6)), "$")
              ]),
              " is said to have stopped Bakonawa from swallowing the moon. Isn't it awesome? I'm so relieved."
            )
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, _} =
      ctx
      |> mes("[Drumming Young Man]")
      |> mes("Bakonawa is also known as the 'monster that swallows the moon' in Port Malaya.")
      |> next()
      |> mes("[Drumming Young Man]")
      |> mes("But I know that Bakonawa could also 'swallow people', too.")
      |> next()
      |> select(["Huk! Then isn't it dangerous?"])

    {ctx, _} =
      ctx
      |> emotion(:kik)
      |> mes("[Drumming Young Man]")
      |> mes("Ha ha ha. There is a way to stop Bakonawa from swallowing a person.")
      |> next()
      |> mes("[Drumming Young Man]")
      |> mes("Yes, there is a weakness to even the worst monster that swallowed 6 moons already.")
      |> next()
      |> select(["Wow. What is it?"])

    ctx
    |> mes("[Drumming Young Man]")
    |> mes("He is sensitive to noise so when he's about to swallow the moon!!")
    |> next()
    |> mes("[Drumming Young Man]")
    |> mes(
      "That's your chance!! It doesn't matter what kind of noise!! Whether it's a caldron, symbol, drum, pot or fry pan, just keep on making noise."
    )
    |> next()
    |> mes("[Drumming Young Man]")
    |> mes("Then Bakonawa will get surprised, spit out the moon and run away!")
    |> next()
    |> mes("[Drumming Young Man]")
    |> mes("Carry a drum around with you. You'll find it handy.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
