defmodule Aesir.ZoneServer.Content.Npc.Malaya.OldMan do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:565 (Old Man #ma07).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "malaya",
        x: 41,
        y: 127,
        dir: 6,
        sprite: 574,
        name: "Old Man ",
        unique_name: "Old Man #ma07"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx =
          ctx
          |> mes("[Old Man]")
          |> mes(
            "Foreigners are not welcomed that much when our village is chaotic like these days."
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx =
              ctx
              |> mes("[Old Man]")
              |> mes(
                "You are out of luck visiting the village at a time like this and not being welcomed."
              )
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx =
      if Enum.random(1..3) == 2 do
        ctx =
          ctx
          |> mes("[Old Man]")
          |> mes(
            Rathena.concat(
              Todo.call!(:getd, [
                Rathena.concat(Rathena.concat("$ma_name0", Enum.random(1..6)), "$")
              ]),
              " is said to throw out Jellopy in this village."
            )
          )
          |> next()
          |> mes("[Old Man]")
          |> mes("Tsk, tsk... Must be a person that isn't worthy of a Jellopy.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, _} =
      ctx
      |> mes("[Old Man]")
      |> mes(
        "You must also look out to see if Bangungot lives in a tree you are about to cut down."
      )
      |> next()
      |> mes("[Old Man]")
      |> mes(
        "Because strange things happen if logs from trees that Bangungot lived in are used as building pillars."
      )
      |> next()
      |> select(["What happens?"])

    {ctx, _} =
      ctx
      |> mes("[Old Man]")
      |> mes("Well, for a Bangungot, it means losing its home so they get vengeful.")
      |> next()
      |> mes("[Old Man]")
      |> mes(
        "When you try to sleep near the pillar, it will keep awake by playing pranks at first."
      )
      |> next()
      |> mes("[Old Man]")
      |> mes(
        "But as time goes by and their vengeance grows, they sit on top of you until you suffocate."
      )
      |> next()
      |> select(["Oh, gosh..."])

    ctx
    |> mes("[Old Man]")
    |> mes("Ha ha ha. There are no Bangungots in normal pillars, so don't lose sleep over it.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
