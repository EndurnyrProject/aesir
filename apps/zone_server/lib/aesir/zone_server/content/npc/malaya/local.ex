defmodule Aesir.ZoneServer.Content.Npc.Malaya.Local do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:502 (Local#ma06).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "malaya",
        x: 363,
        y: 283,
        dir: 4,
        sprite: 582,
        name: "Local",
        unique_name: "Local#ma06"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx =
          ctx
          |> mes("[Local]")
          |> mes("Hmm... is it time to be careful of the witches' curse?")
          |> next()
          |> mes("[Local]")
          |> mes("Beware of Mongkukurums needle, foreigner.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx =
              ctx
              |> mes("[Local]")
              |> mes("Welcome to Port Malaya, foreigner..")
              |> next()
              |> mes("[Local]")
              |> mes(
                "The village is chaotic these days. If you see someone with red eyes, try not to stare at their eyes."
              )
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx =
      set_local(
        ctx,
        :"name$",
        Todo.call!(:getd, [Rathena.concat(Rathena.concat("$ma_name0", Enum.random(1..6)), "$")])
      )

    v1 = Enum.random(1..6)

    ctx =
      case v1 do
        1 ->
          ctx = mes(ctx, "[Local]")

          ctx =
            ctx
            |> mes(
              Rathena.concat(
                Rathena.concat(
                  "Be careful walking around the village at night. ",
                  get_local(ctx, :"name$", "")
                ),
                " was taken down by Wokwok."
              )
            )
            |> close()

          throw({:script_end, ctx})

        2 ->
          ctx = mes(ctx, "[Local]")

          ctx =
            ctx
            |> mes(
              Rathena.concat(
                get_local(ctx, :"name$", ""),
                " is said to have successfully tamed a Tikbalang. This person must be courageous. Wonder if I'll get to see this tamed Tikbalang?"
              )
            )
            |> close()

          throw({:script_end, ctx})

        3 ->
          ctx = mes(ctx, "[Local]")

          ctx =
            ctx
            |> mes(
              Rathena.concat(
                Rathena.concat(
                  "Have you ever seen Bongisungisu? I heard ",
                  get_local(ctx, :"name$", "")
                ),
                " is hunting down Bongisungisus."
              )
            )
            |> close()

          throw({:script_end, ctx})

        4 ->
          ctx = mes(ctx, "[Local]")

          ctx =
            ctx
            |> mes(
              Rathena.concat(
                Rathena.concat(
                  "If you plan to go out of the village, be careful of Tiucknuc. ",
                  get_local(ctx, :"name$", "")
                ),
                " is said to be tricked by Tiucknuc and had to go to the hospital."
              )
            )
            |> close()

          throw({:script_end, ctx})

        v when v in [5, 6] ->
          ctx

        _ ->
          ctx
      end

    {ctx, _} =
      ctx
      |> mes("[Local]")
      |> mes("The village is chaotic these days. Looks like Mongkukurum is back in the village.")
      |> next()
      |> select(["Mongkukurum?"])

    {ctx, _} =
      ctx
      |> mes("[Local]")
      |> mes("A monster that is also called witch. Looks like a person and also wears clothes.")
      |> next()
      |> mes("[Local]")
      |> mes("Mongkukurum makes a doll out of the people it sees and curses it with needles.")
      |> next()
      |> select(["Is there a way to recognize this monster?"])

    ctx
    |> mes("[Local]")
    |> mes("There is one way. All Mongkukurum have red eyes.")
    |> next()
    |> mes("[Local]")
    |> mes("You can recognize them by their eyes but! You must remember one thing.")
    |> next()
    |> mes("[Local]")
    |> mes("You will be captivated by Mongkukurum if you stare at their eyes too long.")
    |> next()
    |> mes("[Local]")
    |> mes("Never ever stare into their eyes for too long.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
