defmodule Aesir.ZoneServer.Content.Npc.Manuk.Piom169260 do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:113 (Piom#ep13_2_1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "manuk", x: 169, y: 260, dir: 3, sprite: 455, name: "Piom"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 and get_char_var(ctx, :ep13_2_rhea, 0) == 100 do
      ctx
      |> mes("[Piom]")
      |> mes("We, Saphas are always together!")
      |> mes("Wherever we are. We are always connected to each other.")
      |> mes("I don't know where you are from but, you should learn our spirits.")
      |> close()

      exit(:normal)
    else
      ctx
      |> mes("[Piom]")
      |> mes("Ng go oois yus dd")
      |> mes("You ii iaao nfb ud")
      |> mes("Wqq ifn isp did")
      |> mes("Uy ydf sd fs wee")
      |> mes("Mgg gf fs d ff")
      |> close()

      exit(:normal)
    end
  end
end
