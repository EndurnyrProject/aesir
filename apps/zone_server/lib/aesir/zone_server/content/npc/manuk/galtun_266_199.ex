defmodule Aesir.ZoneServer.Content.Npc.Manuk.Galtun266199 do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:160 (Galtun#ep13_2_2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "manuk", x: 266, y: 199, dir: 5, sprite: 450, name: "Galtun"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 and get_char_var(ctx, :ep13_2_rhea, 0) == 100 do
      ctx
      |> mes("[Galtun]")
      |> mes("I can relax now that we have those piles of Bradium.")
      |> mes("But I am also worried that we can spend them in a short time.")
      |> close()

      exit(:normal)
    else
      ctx
      |> mes("[Galtun]")
      |> mes("Bu iu bus sfi a sd")
      |> mes("Zsd dwo uf sh osad ")
      |> mes("Qdf aih fas io d hoas")
      |> mes("Nas d iy as di")
      |> close()

      exit(:normal)
    end
  end
end
