defmodule Aesir.ZoneServer.Content.Npc.DicIn01.SuspiciousPiom do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:240 (Suspicious Piom#a).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "dic_in01", x: 91, y: 113, dir: 5, sprite: 489, name: "Suspicious Piom"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Suspicious Piom]")

    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        {ctx, v1} =
          ctx
          |> mes("Have you heard of the")
          |> mes("rumor about Ahat?")
          |> next()
          |> select(["Nope!", "You first."])

        ctx =
          if v1 == 1 do
            ctx = ctx |> mes("[Suspicious Piom]") |> mes("Never mind.") |> close()
            throw({:script_end, ctx})
          else
            ctx
          end

        {ctx, v2} =
          ctx
          |> mes("[Suspicious Piom]")
          |> mes("Oh, you do seem to know?")
          |> mes("I need to collect information.")
          |> mes("You first.")
          |> mes("You heard of the rumor, right?")
          |> next()
          |> select(["Something is fishy?", "If you're not going to spill it, me neither!"])

        ctx =
          if v2 == 1 do
            ctx =
              ctx
              |> mes("[Suspicious Piom]")
              |> mes("I'm only a very curious")
              |> mes("ordinary Piom.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("[Suspicious Piom]")
          |> mes("I am not going to say anything, either!")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, v3} =
      ctx
      |> mes("?ó¢³¡? ?? ??????")
      |> mes("?? ?????? ????")
      |> next()
      |> select(["Can't tell what it means", "Is it a curse??"])

    ctx =
      if v3 == 2 do
        ctx = ctx |> mes("[Suspicious Piom]") |> mes("Too bad.") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx |> mes("[Suspicious Piom]") |> mes("Be on your") |> mes("way, shoo") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
