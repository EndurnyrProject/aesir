defmodule Aesir.ZoneServer.Content.Npc.Yuno.ScrutinyAssociation do
  @moduledoc """
  Transpiled from rAthena kafras/dts_warper.txt:1114 (Scrutiny Association#5).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  def on_event("OnTimer7200000", ctx), do: ev_ontimer7200000(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    _ = initnpctimer(ctx)
    exit(:normal)
  end

  def ev_onenable(ctx) do
    _ = initnpctimer(ctx)
    exit(:normal)
  end

  def ev_ontimer7200000(ctx) do
    ctx = stopnpctimer(ctx)

    ctx =
      if get_server_var(ctx, "DTS_Result", 0) == 0 or get_server_var(ctx, "DTS_Result", 0) == 3 do
        announce(
          ctx,
          "Currently, the Dungeon Teleport Service Provider Election is being held in all major cities. Your participation is appreciated.",
          0,
          "0x70dbdb"
        )
      else
        ctx
      end

    _ = donpcevent(ctx, "Scrutiny Association#5::OnEnable")
    exit(:normal)
  end
end
