defmodule Aesir.ZoneServer.Content.Npc.Airplane01.Npc do
  @moduledoc """
  Transpiled from rAthena airports/airships.txt:655 (#AirshipWarp-3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "airplane_01", x: 243, y: 73, dir: 0, sprite: 45, name: "", trigger: {1, 1}},
      %{map: "airplane_01", x: 243, y: 29, dir: 0, sprite: 45, name: "", trigger: {1, 1}}
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def on_event("OnInit", ctx) do
    ev_oninit(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def on_event("OnHide", ctx) do
    ev_onhide(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def on_event("OnUnhide", ctx) do
    ev_onunhide(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    v1 = get_server_temp_var(ctx, "airplanelocation2", 0)

    ctx =
      case v1 do
        0 ->
          ctx = warp(ctx, "ra_fild12", 292, 204)
          throw({:script_end, ctx})

        1 ->
          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              warp(ctx, "izlude", 200, 73)
            else
              warp(ctx, "izlude", 200, 56)
            end

          throw({:script_end, ctx})

        2 ->
          ctx = warp(ctx, "yuno", 12, 261)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  end

  def ev_oninit(ctx) do
    ctx
  end

  def ev_onhide(ctx) do
    ctx |> specialeffect(:bash) |> todo(:disablenpc, [])
  end

  def ev_onunhide(ctx) do
    ctx |> todo(:enablenpc, []) |> specialeffect(:summonslave)
  end
end
