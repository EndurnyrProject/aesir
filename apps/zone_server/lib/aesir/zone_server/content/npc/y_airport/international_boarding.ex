defmodule Aesir.ZoneServer.Content.Npc.YAirport.InternationalBoarding do
  @moduledoc """
  Transpiled from rAthena airports/yuno.txt:104 (International Boarding).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "y_airport", x: 140, y: 63, dir: 5, sprite: 91, name: "International Boarding"}
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Boarding Staff]")
      |> mes("Would you like to board")
      |> mes("the Airship which flies to")
      |> mes("Juno, Izlude and Rachel?")
      |> mes("If so, let me guide")
      |> mes("you to the boarding area.")
      |> next()
      |> select(["Yes", "No"])

    ctx =
      if v1 == 1 do
        _ = warp(ctx, "yuno", 47, 244)
        exit(:normal)
      else
        ctx
      end

    ctx
    |> mes("[Boarding Staff]")
    |> mes("Alright, then.")
    |> mes("Thank you for flying")
    |> mes("with us, and I hope you")
    |> mes("enjoy your travels on our")
    |> mes("state of the art Airships.")
    |> close()

    exit(:normal)
  end
end
