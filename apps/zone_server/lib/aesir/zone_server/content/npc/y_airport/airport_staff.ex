defmodule Aesir.ZoneServer.Content.Npc.YAirport.AirportStaff do
  @moduledoc """
  Transpiled from rAthena airports/yuno.txt:15 (Airport Staff#y_air1a::y_airport1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "y_airport",
        x: 143,
        y: 43,
        dir: 5,
        sprite: 90,
        name: "Airport Staff",
        unique_name: "y_airport1"
      },
      %{map: "y_airport", x: 158, y: 43, dir: 5, sprite: 90, name: "Airport Staff"},
      %{map: "y_airport", x: 126, y: 43, dir: 5, sprite: 90, name: "Airport Staff"}
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Airport Staff]")
      |> mes(
        "Welcome to Juno Airport where we offer domestic flights to Einbroch, Lighthalzen and Hugel,"
      )
      |> mes("and international flights to Izlude and Rachel.")
      |> mes("How may I be of service?")
      |> next()
      |> select(["Board the Airship.", "Cancel."])

    ctx =
      if v1 == 1 do
        {ctx, v2} =
          ctx
          |> mes("[Airport Staff]")
          |> mes("The boarding fee for all")
          |> mes("flights is 1,200 zeny. If you")
          |> mes("use a Free Ticket for Airship,")
          |> mes("the boarding fee will be waived.So would you like to depart?")
          |> next()
          |> select(["Yes", "No"])

        ctx =
          if v2 == 1 do
            ctx =
              if count_item(ctx, 7311) > 0 do
                ctx |> delitem(7311, 1) |> warp("y_airport", 148, 51)
                exit(:normal)
              else
                ctx
              end

            ctx =
              if zeny(ctx) >= 1200 do
                ctx |> pay_zeny(1200) |> warp("y_airport", 148, 51)
                exit(:normal)
              else
                ctx
              end

            ctx
            |> mes("[Airport Staff]")
            |> mes("I'm sorry, but you don't")
            |> mes("have 1,200 zeny to pay")
            |> mes("for the boarding fee.")
            |> close()

            exit(:normal)
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx |> mes("[Airport Staff]") |> mes("Thank you and") |> mes("have a nice day.") |> close()
    exit(:normal)
  end
end
