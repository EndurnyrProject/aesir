defmodule Aesir.ZoneServer.Content.Npc.YAirport.ArrivalStaff do
  @moduledoc """
  Transpiled from rAthena airports/yuno.txt:55 (Arrival Staff#y_air2a::y_airport2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "y_airport",
        x: 143,
        y: 49,
        dir: 3,
        sprite: 90,
        name: "Arrival Staff",
        unique_name: "y_airport2"
      },
      %{map: "y_airport", x: 126, y: 51, dir: 3, sprite: 90, name: "Arrival Staff"},
      %{map: "y_airport", x: 158, y: 50, dir: 3, sprite: 90, name: "Arrival Staff"}
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Airport Staff]")
      |> mes("Welcome to Juno Airport. If you've just arrived from your")
      |> mes("flight, let me guide you to the main terminal. Otherwise, please")
      |> mes("board the departing Airship to reach your intended destination.")
      |> next()
      |> select(["Exit to main terminal", "Cancel"])

    ctx =
      if v1 == 1 do
        {ctx, v2} =
          ctx
          |> mes("[Airport Staff]")
          |> mes("Once you're in the main terminal, you must pay the fee once again")
          |> mes("to board a departing Airship. You should only exit if your intended")
          |> mes("destination is Juno. Proceed to")
          |> mes("exit to the main terminal?")
          |> next()
          |> select(["Yes", "No"])

        ctx =
          if v2 == 1 do
            ctx = warp(ctx, "y_airport", 142, 40)
            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx
    |> mes("[Airport Staff]")
    |> mes("Alright, thank you")
    |> mes("for your patronage")
    |> mes("and I hope you have")
    |> mes("a pleasant flight~")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
