defmodule Aesir.ZoneServer.Content.Npc.Malangdo.CatTrainer do
  @moduledoc """
  Transpiled from rAthena re/cities/malangdo.txt:372 (Cat Trainer#mal1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "malangdo",
        x: 216,
        y: 168,
        dir: 3,
        sprite: 558,
        name: "Cat Trainer",
        trigger: {0, 8}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    {ctx, _} =
      ctx
      |> emotion(:cool)
      |> mes("[Cat Trainer]")
      |> mes("Ooo~ their soft puffy tails and soft fur~")
      |> mes("I can't stand it, this is heaven~~!!")
      |> next()
      |> emotion(:kik)
      |> mes("[Cat Trainer]")
      |> mes(
        "My hidden card, ^FF0000Silvervine Fruit^000000, can make all cats in Malangdo my slaves~ haha!"
      )
      |> next()
      |> select(["^FF0000Silvervine Fruit^000000?"])

    {ctx, _} =
      ctx
      |> mes("[Cat Trainer]")
      |> mes("It is such a rare item!")
      |> mes("I can barely get it.")
      |> mes("But these cudly cats can't resist it~")
      |> next()
      |> select(["Where can I get ^FF0000Silvervine Fruit^000000?"])

    ctx
    |> mes("[Cat Trainer]")
    |> mes("Why would I tell you when I can barely get it?")
    |> mes("I wouldn't even tell you for all the zeny in the world~!")
    |> next()
    |> emotion(:kik)
    |> mes("[Cat Trainer]")
    |> mes("All the Malangdo cats are mine~!")
    |> mes("Muahahahaha~")
    |> mes("With enough ^FF0000Silvervine Fruit^000000 I can be a queen in Malangdo~ ohhhh!!!")
    |> close()
  end

  def ev_ontouch(ctx) do
    emotion(ctx, :spark)
  end
end
