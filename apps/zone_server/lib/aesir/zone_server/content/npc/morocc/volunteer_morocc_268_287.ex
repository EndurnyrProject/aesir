defmodule Aesir.ZoneServer.Content.Npc.Morocc.VolunteerMorocc268287 do
  @moduledoc """
  Transpiled from rAthena cities/morocc.txt:83 (Volunteer - Morocc#04).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "morocc", x: 268, y: 287, dir: 3, sprite: 727, name: "Volunteer - Morocc"}]

  @impl true
  def on_talk(ctx) do
    ctx
    |> mes("[Morocc Volunteer]")
    |> mes("As bad as it seems, we can't even ask for more support.")
    |> mes("This sure must be the worst thing ever happened in Rune-Midgarts' history.")
    |> next()
    |> mes("[Morocc Volunteer]")
    |> mes(
      "I wish I knew how bad the damage is, but we can't even estimate it. It's like shovelling sand against the tide.."
    )
    |> close()

    exit(:normal)
  end
end
