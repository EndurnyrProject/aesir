defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Priest.Zombie11 do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/priest.txt:1193 (Zombie1_1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  def on_event("OnDisable", ctx), do: ev_ondisable(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    disablenpc(ctx, "Zombie1_1")
  end

  def ev_ontouch(ctx) do
    ctx =
      if base_job(ctx) == :acolyte do
        ctx |> donpcevent("Zombie_Generator#prst::Onm1") |> donpcevent("Zombie1_1::OnDisable")
      else
        ctx
      end

    ctx
  end

  def ev_onenable(ctx) do
    enablenpc(ctx, "Zombie1_1")
  end

  def ev_ondisable(ctx) do
    disablenpc(ctx, "Zombie1_1")
  end
end
