defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Knight.SirWindsor do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/knight.txt:1058 (Sir Windsor#knt).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prt_in",
        x: 79,
        y: 94,
        dir: 4,
        sprite: 733,
        name: "Sir Windsor",
        unique_name: "Sir Windsor#knt"
      }
    ]

  @impl true
  def on_talk(ctx) do
    ctx =
      ctx
      |> mes("[Sir Windsor]")
      |> mes("...")
      |> next()
      |> mes("[Sir Windsor]")
      |> mes("...")
      |> mes("......")
      |> next()
      |> mes("[Sir Windsor]")

    ctx =
      if base_job(ctx) != :swordman do
        ctx =
          if base_job(ctx) == :knight do
            mes(ctx, "Protect.")
          else
            ctx =
              if base_class(ctx) == :novice do
                ctx |> mes("...Go play") |> mes("outside.")
              else
                mes(ctx, "...Hmpf.")
              end

            ctx
          end

        ctx = close(ctx)
        throw({:script_end, ctx})
      else
        ctx
      end

    if get_char_var(ctx, :KNIGHT_Q, 0) == 0 do
      ctx = ctx |> mes("...What?") |> close()
      throw({:script_end, ctx})
    else
      if get_char_var(ctx, :KNIGHT_Q, 0) >= 1 and get_char_var(ctx, :KNIGHT_Q, 0) <= 5 do
        {ctx, v1} =
          ctx
          |> mes("...What?")
          |> next()
          |> select(["I would like to take the test to change jobs.", "Oh, nothing."])

        ctx =
          if v1 == 1 do
            ctx =
              ctx
              |> mes("[Sir Windsor]")
              |> mes("...")
              |> next()
              |> mes("[Sir Windsor]")
              |> mes("...")
              |> mes("......")
              |> next()
              |> mes("[Sir Windsor]")
              |> mes("...It's not my turn.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx = ctx |> mes("[Sir Windsor]") |> mes("...") |> close()
        throw({:script_end, ctx})
      else
        if get_char_var(ctx, :KNIGHT_Q, 0) == 6 or get_char_var(ctx, :KNIGHT_Q, 0) == 7 do
          ctx =
            if get_char_var(ctx, :KNIGHT_Q, 0) == 6 do
              ctx
              |> set_local(:"mes$", "Sir Siracuse sent me to you.:Oh, nothing.")
              |> mes(".....What?")
              |> next()
            else
              ctx |> set_local(:"mes$", "I want to try again!:...") |> next()
            end

          {ctx, v2} = select(ctx, String.split(get_local(ctx, :"mes$", ""), ":"))

          ctx =
            if v2 == 1 do
              ctx =
                ctx |> mes("[Sir Windsor]") |> mes("...") |> next() |> set_char_var(:KNIGHT_Q, 7)

              ctx =
                if checkquest(ctx, 9004) != -1 do
                  changequest(ctx, 9004, 9005)
                else
                  ctx
                end

              ctx =
                ctx
                |> mes("[Sir Windsor]")
                |> mes("...")
                |> mes("......")
                |> next()
                |> mes("[Sir Windsor]")

              ctx =
                if get_char_var(ctx, :KNIGHT_Q, 0) == 6 do
                  mes(ctx, "...Follow me.")
                else
                  ctx |> mes("...Fine.") |> next() |> mes("[Sir Windsor]") |> mes("...This way.")
                end

              ctx = close(ctx)

              ctx =
                if checkquest(ctx, 9006) == -1 do
                  changequest(ctx, 9005, 9006)
                else
                  ctx
                end

              ctx = warp(ctx, "job_knt", 89, 101)
              throw({:script_end, ctx})
            else
              ctx
            end

          ctx = ctx |> mes("[Sir Windsor]") |> mes("...") |> close()
          throw({:script_end, ctx})
        else
          if get_char_var(ctx, :KNIGHT_Q, 0) == 14 do
            ctx = ctx |> mes("...Talk to") |> mes("the captain.") |> close()
            throw({:script_end, ctx})
          else
            ctx = ctx |> mes("...You're") |> mes("done here.") |> close()
            throw({:script_end, ctx})
          end
        end
      end
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
