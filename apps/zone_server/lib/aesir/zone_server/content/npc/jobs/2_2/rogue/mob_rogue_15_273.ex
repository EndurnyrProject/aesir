defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22.Rogue.MobRogue15273 do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/rogue.txt:1685 (mob_rogue#2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, scope: :shared, spawn: []

  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  def on_event("OnDisable", ctx), do: ev_ondisable(ctx)
  def on_event("OnMyMobDead", ctx), do: ev_onmymobdead(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if base_job(ctx) == :thief do
        ctx
        |> summon_mob(
          mob_id: 1041,
          map: "in_rogue",
          at: {15, 276},
          event: "mob_rogue#2::OnMyMobDead"
        )
        |> summon_mob(
          mob_id: 1041,
          map: "in_rogue",
          at: {13, 276},
          event: "mob_rogue#2::OnMyMobDead"
        )
        |> summon_mob(
          mob_id: 1041,
          map: "in_rogue",
          at: {11, 276},
          event: "mob_rogue#2::OnMyMobDead"
        )
      else
        warp(ctx, "mag_dun02", 181, 176)
      end

    ctx
  end

  def ev_ondisable(ctx) do
    killmonster(ctx, "in_rogue", "mob_rogue#2::OnMyMobDead")
  end

  def ev_onmymobdead(ctx) do
    ctx
  end
end
