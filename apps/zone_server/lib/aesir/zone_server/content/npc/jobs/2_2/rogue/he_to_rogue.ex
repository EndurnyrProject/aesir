defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22.Rogue.HeToRogue do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/rogue.txt:1278 (he_to_rogue#rg).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "in_rogue",
        x: 270,
        y: 130,
        dir: 0,
        sprite: 45,
        name: "he_to_rogue",
        scope: :shared,
        unique_name: "he_to_rogue#rg",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    {ctx, v1} =
      ctx
      |> mes(
        "^3355FFThe door is locked. You'll need to enter the four number combination to open it.^000000"
      )
      |> next()
      |> input(:int)

    ctx = set_local(ctx, :input, v1)

    if get_local(ctx, :input, 0) < 1 or get_local(ctx, :input, 0) > 10000 do
      ctx =
        if get_char_var(ctx, :ROGUE_Q, 0) == 12 do
          mes(ctx, "^3355FFIt didn't work. Please re-enter the four number combination.^000000")
        else
          mes(ctx, "^3355FFPlease enter a combination of four numbers.^000000")
        end

      ctx = close(ctx)
      throw({:script_end, ctx})
    else
      if get_local(ctx, :input, 0) == 3019 do
        if get_char_var(ctx, :ROGUE_Q, 0) == 12 do
          ctx =
            ctx
            |> mes("^3355FFThe door")
            |> mes("has opened.^000000")
            |> close()
            |> warp("in_rogue", 10, 21)
            |> set_char_var(:ROGUE_Q, 12)

          throw({:script_end, ctx})
        else
          ctx =
            ctx
            |> mes("[HermanthornJr.]")
            |> mes("Well...")
            |> mes("Didn't I tell you")
            |> mes("that I changed the")
            |> mes("password? *Wink Wink*")
            |> close()

          throw({:script_end, ctx})
        end
      else
        ctx = ctx |> mes("^3355FFThe door") |> mes("is still locked.^000000") |> close()
        throw({:script_end, ctx})
      end
    end
  end
end
