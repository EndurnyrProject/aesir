defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22.Rogue.Warp304180 do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/rogue.txt:1158 (Warp#3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "cmd_fild04",
        x: 304,
        y: 180,
        dir: 0,
        sprite: 45,
        name: "Warp",
        unique_name: "Warp#3",
        trigger: {1, 1}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[???]")
      |> mes("Who's there?!")
      |> mes("Who would dare")
      |> mes("intrude my territory?")
      |> next()
      |> select(["Anntonio", "Aragham", "Antonio", "Hollgrehenn"])

    ctx =
      case v1 do
        1 ->
          ctx
          |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
          |> mes("Anntonio")

        2 ->
          ctx
          |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
          |> mes("Aragham")

        3 ->
          ctx =
            ctx
            |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
            |> mes("Antonio")

          set_local(ctx, :rogue_t, get_local(ctx, :rogue_t, 0) + 10)

        4 ->
          ctx =
            ctx
            |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
            |> mes("Hollgrehenn")

          ctx

        _ ->
          ctx
      end

    {ctx, v2} = select(ctx, ["enjoys", "doesn't enjoy", "likes", "doesn't like"])

    ctx =
      case v2 do
        1 ->
          mes(ctx, "enjoys")

        2 ->
          ctx = mes(ctx, "doesn't enjoy")
          set_local(ctx, :rogue_t, get_local(ctx, :rogue_t, 0) + 10)

        3 ->
          mes(ctx, "likes")

        4 ->
          ctx = mes(ctx, "doesn't like")
          ctx

        _ ->
          ctx
      end

    {ctx, v3} = select(ctx, ["damaging", "destroying", "fixing", "forging"])

    ctx =
      case v3 do
        1 ->
          mes(ctx, "damaging")

        2 ->
          ctx = mes(ctx, "destroying")
          set_local(ctx, :rogue_t, get_local(ctx, :rogue_t, 0) + 10)

        3 ->
          mes(ctx, "fixing")

        4 ->
          ctx = mes(ctx, "forging")
          ctx

        _ ->
          ctx
      end

    {ctx, v4} =
      select(ctx, [
        "forging item.",
        "refining items.",
        "upgrade items.",
        "refined items.",
        "upgraded items.",
        "forged items."
      ])

    ctx =
      case v4 do
        1 ->
          mes(ctx, "forging item.")

        2 ->
          mes(ctx, "refining items.")

        3 ->
          ctx = mes(ctx, "upgrade items.")
          set_local(ctx, :rogue_t, get_local(ctx, :rogue_t, 0) + 10)

        4 ->
          mes(ctx, "refined items.")

        5 ->
          mes(ctx, "upgraded items.")

        6 ->
          ctx = mes(ctx, "forged items.")
          ctx

        _ ->
          ctx
      end

    ctx = next(ctx)

    if get_local(ctx, :rogue_t, 0) > 30 do
      ctx =
        ctx
        |> mes("^3355FF*Creeeeak*")
        |> mes("The door slowly opens.^000000")
        |> close()
        |> warp("in_rogue", 164, 106)

      throw({:script_end, ctx})
    else
      ctx = ctx |> mes("[???]") |> mes(".....Get lost!") |> close()
      throw({:script_end, ctx})
    end
  end
end
