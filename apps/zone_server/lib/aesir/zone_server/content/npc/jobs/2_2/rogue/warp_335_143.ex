defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22.Rogue.Warp335143 do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/rogue.txt:1088 (Warp#2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "cmd_fild09",
        x: 335,
        y: 143,
        dir: 0,
        sprite: 45,
        name: "Warp",
        unique_name: "Warp#2",
        trigger: {1, 1}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[???]")
      |> mes("Who's there?!")
      |> mes("Who would dare")
      |> mes("intrude my territory?")
      |> next()
      |> select(["My father", "Aragham", "Aragon", "Legolas"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
            |> mes("My father")

          set_local(ctx, :rogue_t, get_local(ctx, :rogue_t, 0) + 10)

        2 ->
          ctx
          |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
          |> mes("Aragham")

        3 ->
          ctx |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]")) |> mes("Aragon")

        4 ->
          ctx =
            ctx
            |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
            |> mes("Legolas")

          ctx

        _ ->
          ctx
      end

    {ctx, v2} = select(ctx, ["did not", "didn't", "never", "ever"])

    ctx =
      case v2 do
        1 ->
          mes(ctx, "did not")

        2 ->
          mes(ctx, "didn't")

        3 ->
          ctx = mes(ctx, "never")
          set_local(ctx, :rogue_t, get_local(ctx, :rogue_t, 0) + 10)

        4 ->
          ctx = mes(ctx, "ever")
          ctx

        _ ->
          ctx
      end

    {ctx, v3} = select(ctx, ["hoard", "takes", "hide", "took", "hoarded"])

    ctx =
      case v3 do
        1 ->
          mes(ctx, "hoard")

        2 ->
          mes(ctx, "takes")

        3 ->
          mes(ctx, "hide")

        4 ->
          mes(ctx, "took")

        5 ->
          ctx = mes(ctx, "hoarded")
          ctx = set_local(ctx, :rogue_t, get_local(ctx, :rogue_t, 0) + 10)
          ctx

        _ ->
          ctx
      end

    {ctx, v4} =
      select(ctx, ["upgrade items.", "forging items.", "refining item.", "upgrade item."])

    ctx =
      case v4 do
        1 ->
          ctx = mes(ctx, "upgrade items.")
          set_local(ctx, :rogue_t, get_local(ctx, :rogue_t, 0) + 10)

        2 ->
          mes(ctx, "forging items.")

        3 ->
          mes(ctx, "refining item.")

        4 ->
          ctx = mes(ctx, "upgrade item.")
          ctx

        _ ->
          ctx
      end

    ctx = next(ctx)

    if get_local(ctx, :rogue_t, 0) > 30 do
      ctx =
        ctx
        |> mes("^3355FF*Creeeeak*")
        |> mes("The door slowly opens.^000000")
        |> close()
        |> warp("in_rogue", 169, 34)

      throw({:script_end, ctx})
    else
      ctx = ctx |> mes("[???]") |> mes("What the...?") |> mes("Get lost!") |> close()
      throw({:script_end, ctx})
    end
  end
end
