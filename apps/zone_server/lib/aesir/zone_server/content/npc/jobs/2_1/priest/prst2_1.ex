defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Priest.Prst21 do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/priest.txt:1601 (prst2_1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "job_prist", x: 168, y: 180, dir: 4, sprite: 45, name: "prst2_1", trigger: {3, 3}}
    ]

  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if base_job(ctx) == :priest do
        warp(ctx, "job_prist", 98, 40)
      else
        ctx =
          if base_class(ctx) == :acolyte do
            ctx |> warp("job_prist", 98, 40) |> donpcevent("Mummy_Generator::OnEnable")
          else
            ctx
          end

        ctx
      end

    ctx
  end
end
