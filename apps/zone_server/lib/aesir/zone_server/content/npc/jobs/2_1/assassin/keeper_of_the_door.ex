defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Assassin.KeeperOfTheDoor do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/assassin.txt:1449 (Keeper of the Door#ASN).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "in_moc_16",
        x: 87,
        y: 137,
        dir: 0,
        sprite: 45,
        name: "Keeper of the Door",
        unique_name: "Keeper of the Door#ASN",
        trigger: {2, 1}
      }
    ]

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  def on_event("OnDisable", ctx), do: ev_ondisable(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    disablenpc(ctx, "Keeper of the Door#ASN")
  end

  def ev_ontouch(ctx) do
    ctx = donpcevent(ctx, "Thomas#ASNTEST::OnDisable")

    ctx =
      if get_char_var(ctx, :ASSIN_Q, 0) == 3 do
        set_char_var(ctx, :ASSIN_Q, 3)
      else
        set_char_var(ctx, :ASSIN_Q, 4)
      end

    ctx |> warp("in_moc_16", 87, 102) |> savepoint("in_moc_16", 16, 13)
  end

  def ev_onenable(ctx) do
    ctx
    |> mapannounce(
      "in_moc_16",
      "The door to the next room, at coordinates 87 137, has opened.",
      1
    )
    |> enablenpc("Keeper of the Door#ASN")
  end

  def ev_ondisable(ctx) do
    disablenpc(ctx, "Keeper of the Door#ASN")
  end
end
