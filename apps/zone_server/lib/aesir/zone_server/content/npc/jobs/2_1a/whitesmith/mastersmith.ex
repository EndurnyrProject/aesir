defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21a.Whitesmith.Mastersmith do
  @moduledoc """
  Transpiled from rAthena jobs/2-1a/WhiteSmith.txt:18 (MasterSmith#Valkyrie).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "valkyrie",
        x: 44,
        y: 50,
        dir: 5,
        sprite: 731,
        name: "MasterSmith",
        scope: :shared,
        unique_name: "MasterSmith#Valkyrie"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :ADVJOB, 0) == 0 or get_char_var(ctx, :Upper, 0) != 1 do
        ctx = set_local(ctx, :karma_d, Enum.random(1..10))

        ctx =
          if get_local(ctx, :karma_d, 0) > 4 do
            ctx =
              ctx
              |> mes("[MasterSmith]")
              |> mes("Congratulations.")
              |> mes("Honor to the warriors!")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("[MasterSmith]")
          |> mes("Ah...")
          |> mes("The pinnacle")
          |> mes("of craftsmanship.")
          |> mes("That's the work of")
          |> mes("a MasterSmith.")
          |> next()
          |> mes("[MasterSmith]")
          |> mes("Once you're the")
          |> mes("the best of the best,")
          |> mes("you begin forging with")
          |> mes("the goal of discovering")
          |> mes("new and better ways of")
          |> mes("crafting...")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :ADVJOB, 0) == :whitesmith and class(ctx) == :merchant_high and
               job_level(ctx) > 39 do
            {ctx, v1} =
              ctx
              |> mes("[MasterSmith]")
              |> mes("The time has come!")
              |> mes("Our world needs brave,")
              |> mes("hard-working adventurers")
              |> mes("like you...")
              |> next()
              |> mes("[MasterSmith]")
              |> mes("Would you like to")
              |> mes("become a MasterSmith?")
              |> next()
              |> select(["No.", "Yes."])

            ctx =
              if v1 == 1 do
                ctx =
                  ctx
                  |> mes("[MasterSmith]")
                  |> mes("When you're ready,")
                  |> mes("feel free to come back.")
                  |> mes("Honor to the warriors!")
                  |> close()

                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              if Rathena.truthy?(get_char_var(ctx, :SkillPoint, 0)) do
                ctx =
                  ctx
                  |> mes("[MasterSmith]")
                  |> mes("It is still possible for you to learn more skills. Please use")
                  |> mes("all of your remaining Skill Points before returning to me.")
                  |> close()

                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              ctx
              |> jobchange(:whitesmith)
              |> set_char_var(:ADVJOB, 0)
              |> mes("[MasterSmith]")
              |> mes("Congratulations!")
              |> mes("As a MasterSmith,")
              |> mes("I hope you will forge")
              |> mes("a path towards a brighter")
              |> mes("future for Midgard.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx
    |> mes("[MasterSmith]")
    |> mes("Welcome")
    |> mes("to Valhalla,")
    |> mes("the Hall of Honor.")
    |> next()
    |> mes("[MasterSmith]")
    |> mes("Please make")
    |> mes("yourself comfortable")
    |> mes("while you are here.")
    |> mes("Honor to the warriors!")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
