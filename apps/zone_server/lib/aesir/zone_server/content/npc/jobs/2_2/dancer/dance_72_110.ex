defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22.Dancer.Dance72110 do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/dancer.txt:1274 (dance#return#3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, scope: :shared, spawn: []

  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  def on_event("OnDisable", ctx), do: ev_ondisable(ctx)
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_ontouch(ctx) do
    ctx |> set_char_var(:DANC_Q, 9) |> warp("comodo", 188, 162)
  end

  def ev_ondisable(ctx) do
    disablenpc(ctx, "dance#return#3")
  end

  def ev_onenable(ctx) do
    ctx
    |> enablenpc("dance#return#3")
    |> donpcevent("Bijou#dance_timer::OnDisable")
    |> donpcevent("Waiting Room#dance::OnEnable")
  end
end
