defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22.Dancer.WaitingRoom do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/dancer.txt:898 (Waiting Room#dance).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "job_duncer",
        x: 32,
        y: 154,
        dir: 1,
        sprite: 66,
        name: "Waiting Room",
        scope: :shared,
        unique_name: "Waiting Room#dance"
      }
    ]

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnStartArena", ctx), do: ev_onstartarena(ctx)
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    ctx
    |> waitingroom("Waiting Room", 20, "Waiting Room#dance::OnStartArena", 1)
    |> enablewaitingroomevent()
  end

  def ev_onstartarena(ctx) do
    ctx
    |> disablenpc("dance#up")
    |> disablenpc("dance#down")
    |> disablenpc("dance#left")
    |> disablenpc("dance#right")
    |> disablenpc("dance#cen")
    |> donpcevent("dance#return::OnDisable")
    |> warpwaitingpc("job_duncer", 69, 110, 1)
    |> donpcevent("Bijou#dance_timer::OnEnable")
    |> disablewaitingroomevent()
  end

  def ev_onenable(ctx) do
    enablewaitingroomevent(ctx)
  end
end
