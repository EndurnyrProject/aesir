defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22e.Soullinker.SoulLinkerVar do
  @moduledoc """
  Transpiled from rAthena jobs/2-2e/SoulLinker.txt:668 (Soul Linker Var).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "sec_in02",
        x: 35,
        y: 153,
        dir: 0,
        sprite: 871,
        name: "Soul Linker Var",
        scope: :shared
      }
    ]

  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx =
      ctx
      |> todo(:callfunc, ["F_GM_NPC"])
      |> mes("[Soul Linker Var]")
      |> mes("I can reset the Soul Linker")
      |> mes("NPCs if a Soul Linker candidate")
      |> mes("encounters a problem during the")
      |> mes("end of the job quest. Please do")
      |> mes("not use this function if players are still in the Quest Map.")
      |> next()

    v1 = Todo.call!(:callfunc, ["F_GM_NPC", 1854, 0])

    ctx =
      if v1 < 1 do
        ctx =
          ctx |> mes("[Soul Linker Var]") |> mes("Password") |> mes("is incorrect.") |> close()

        throw({:script_end, ctx})
      else
        {ctx, v2} =
          ctx
          |> mes("[Soul Linker Var]")
          |> mes("Would you like to")
          |> mes("reset the Soul Linker")
          |> mes("Global Variable?")
          |> next()
          |> select(["Reset", "Cancel"])

        ctx =
          case v2 do
            1 ->
              ctx =
                ctx
                |> mes("[Soul Linker Var]")
                |> mes("The Soul Linker")
                |> mes("Job Quest NPCs")
                |> mes("have been reset.")
                |> set_npc_var_of("SoulLinkerTest", "Kid#link1", 0)
                |> close()

              throw({:script_end, ctx})

            2 ->
              ctx =
                ctx
                |> mes("[Soul Linker Var]")
                |> mes("You have canceled")
                |> mes("this command.")
                |> close()

              throw({:script_end, ctx})

            _ ->
              ctx
          end

        ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
