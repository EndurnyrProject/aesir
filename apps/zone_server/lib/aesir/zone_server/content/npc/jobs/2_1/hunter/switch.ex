defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Hunter.Switch do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/hunter.txt:1201 (Switch#hnt).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "job_hunte",
        x: 93,
        y: 101,
        dir: 1,
        sprite: 723,
        name: "Switch",
        scope: :shared,
        unique_name: "Switch#hnt",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def on_event("OnDisable", ctx) do
    ev_ondisable(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def on_event("OnEnable", ctx) do
    ev_onenable(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    {ctx, v1} =
      ctx
      |> mes("^3355FFThere are 3 buttons")
      |> mes("on the escape switch.^000000")
      |> set_char_var(:HNTR_Q, 15)
      |> next()
      |> select(["Escape", "Cancel", "Re-test"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("^3355FFThe Escape Warp Portal")
            |> mes("has now been activated.^000000")
            |> close()
            |> mapannounce("job_hunte", " !! Escape Warp Portal activation complete. !! ", 1)
            |> enablenpc("exit#hnttest")

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("^3355FFCanceling")
            |> mes("Operation.^000000")
            |> close()
            |> mapannounce("job_hunte", " !! Operation has been cancelled. !! ", 1)

          throw({:script_end, ctx})

        3 ->
          ctx =
            ctx
            |> mapannounce("job_hunte", " !! Cancellation warp activating... !! ", 1)
            |> mes("^3355FFYou will soon be")
            |> mes("returned to the")
            |> mes("waiting room.^000000")
            |> close()
            |> set_char_var(:HNTR_Q, 13)
            |> warp("job_hunte", 176, 22)
            |> donpcevent("Manager#hnt::OnReset")
            |> donpcevent("Waiting Room#hnt::OnStart")

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  end

  def ev_ondisable(ctx) do
    ctx |> disablenpc("exit#hnttest") |> disablenpc("Switch#hnt")
  end

  def ev_onenable(ctx) do
    enablenpc(ctx, "Switch#hnt")
  end
end
