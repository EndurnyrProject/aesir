defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Hunter.WaitingRoom do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/hunter.txt:1021 (Waiting Room#hnt).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "job_hunte",
        x: 178,
        y: 38,
        dir: 1,
        sprite: 66,
        name: "Waiting Room",
        scope: :shared,
        unique_name: "Waiting Room#hnt"
      }
    ]

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnStartArena", ctx), do: ev_onstartarena(ctx)
  def on_event("OnStart", ctx), do: ev_onstart(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    ctx
    |> waitingroom("Waiting Room", 10, "Waiting Room#hnt::OnStartArena", 1)
    |> enablewaitingroomevent()
  end

  def ev_onstartarena(ctx) do
    ctx
    |> warpwaitingpc("job_hunte", 90, 67)
    |> donpcevent("Manager#hnt::OnEnable")
    |> disablewaitingroomevent()
  end

  def ev_onstart(ctx) do
    enablewaitingroomevent(ctx)
  end
end
