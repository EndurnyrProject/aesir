defmodule Aesir.ZoneServer.Content.Npc.Jobs.M11e.Ninja.Akagi do
  @moduledoc """
  Transpiled from rAthena jobs/1-1e/ninja.txt:21 (Akagi).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [%{map: "alberta", x: 30, y: 65, dir: 3, sprite: 730, name: "Akagi", scope: :shared}]

  @impl true
  def on_talk(ctx) do
    if class(ctx) == :novice do
      if job_level(ctx) == 10 do
        {ctx, v1} =
          ctx
          |> mes("[Akagi]")
          |> mes("Hmmm...")
          |> mes("You must have come,")
          |> mes("sensing that someone")
          |> mes("is waiting for you here.")
          |> mes("Tell me, do you seek")
          |> mes("the path of patience?")
          |> next()
          |> select(["No", "Yes"])

        ctx =
          if v1 == 1 do
            ctx =
              ctx
              |> mes("[Akagi]")
              |> mes("I see.")
              |> mes("To each his own,")
              |> mes("I suppose. Take")
              |> mes("care of yourself.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("[Akagi]")
          |> mes("Very well.")
          |> mes("Then, let me")
          |> mes("set you on that")
          |> mes("path right away...")
          |> close()

        v2 = :rand.uniform(3) - 1

        case v2 do
          1 ->
            ctx = warp(ctx, "amatsu", 170, 229)
            throw({:script_end, ctx})

          2 ->
            ctx = warp(ctx, "amatsu", 216, 188)
            throw({:script_end, ctx})

          _ ->
            ctx = warp(ctx, "amatsu", 178, 176)
            throw({:script_end, ctx})
        end
      else
        ctx =
          ctx
          |> mes("[Akagi]")
          |> mes("Hm? I cannot be")
          |> mes("of any service to")
          |> mes("you until you grow")
          |> mes("a little stronger...")
          |> close()

        throw({:script_end, ctx})
      end
    else
      ctx =
        ctx
        |> mes("[Akagi]")
        |> mes("Hmm...")
        |> mes("You and I...")
        |> mes("We are fairly equal in")
        |> mes("terms of combat ability.")
        |> mes("Perhaps we can spar")
        |> mes("together sometime.")
        |> close()

      throw({:script_end, ctx})
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
