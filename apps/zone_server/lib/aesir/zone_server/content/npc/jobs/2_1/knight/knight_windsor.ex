defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Knight.KnightWindsor do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/knight.txt:1153 (Knight Windsor#knt).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "job_knt",
        x: 89,
        y: 106,
        dir: 4,
        sprite: 733,
        name: "Knight Windsor",
        unique_name: "Knight Windsor#knt"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Sir Windsor]")
      |> mes("...")
      |> next()
      |> mes("[Sir Windsor]")
      |> mes("...")
      |> mes("......")
      |> next()
      |> mes("[Sir Windsor]")
      |> mes("...Question?")
      |> next()
      |> select([
        "What kind of test is this?",
        "How do I take the test?",
        "I'd like to leave.",
        "No."
      ])

    ctx = ctx |> set_local(:i, v1) |> mes("[Sir Windsor]") |> mes("...")

    ctx =
      if get_local(ctx, :i, 0) == 4 do
        ctx = close(ctx)
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx = ctx |> next() |> mes("[Sir Windsor]") |> mes("...") |> mes("......")
    v2 = get_local(ctx, :i, 0)

    ctx =
      case v2 do
        1 ->
          ctx =
            ctx
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...You fight monsters.")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...Kill them all.")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...Three stages.")
            |> mes("Beat them all.")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("......3 minutes")
            |> mes("for each stage.")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("..........")
            |> close()

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...Go in the")
            |> mes("waiting room.")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...Then it")
            |> mes("will begin.")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...You have to wait")
            |> mes("if someone is testing.")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...You can go in")
            |> mes("after that person.")
            |> next()
            |> mes("[Sir Windsor]")
            |> mes("...")
            |> close()

          throw({:script_end, ctx})

        3 ->
          ctx = ctx |> close() |> warp("prt_in", 80, 100)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
