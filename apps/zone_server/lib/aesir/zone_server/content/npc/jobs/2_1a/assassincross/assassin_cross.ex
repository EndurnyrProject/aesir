defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21a.Assassincross.AssassinCross do
  @moduledoc """
  Transpiled from rAthena jobs/2-1a/AssassinCross.txt:18 (Assassin Cross#Valkyrie).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "valkyrie",
        x: 44,
        y: 58,
        dir: 5,
        sprite: 725,
        name: "Assassin Cross",
        scope: :shared,
        unique_name: "Assassin Cross#Valkyrie"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :ADVJOB, 0) == 0 or get_char_var(ctx, :Upper, 0) != 1 do
        ctx = set_local(ctx, :karma_d, Enum.random(1..10))

        ctx =
          if get_local(ctx, :karma_d, 0) > 4 do
            ctx =
              ctx
              |> mes("[Assassin Cross]")
              |> mes("Congratulations...")
              |> next()
              |> mes("[Assassin Cross]")
              |> mes("...")
              |> next()
              |> mes("[Assassin Cross]")
              |> mes("...")
              |> mes("......")
              |> next()
              |> mes("[Assassin Cross]")
              |> mes("...")
              |> mes("......")
              |> mes("Honor to")
              |> mes("the warriors.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("[Assassin Cross]")
          |> mes("We are the warriors")
          |> mes("of the desert. Nobody")
          |> mes("looks down upon us.")
          |> mes("Nobody...")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :ADVJOB, 0) == :assassin_cross and class(ctx) == :thief_high and
               job_level(ctx) > 39 do
            {ctx, v1} =
              ctx
              |> mes("[Assassin Cross]")
              |> mes("The time has come.")
              |> mes("The world needs you...")
              |> mes("More than ever.")
              |> next()
              |> mes("[Assassin Cross]")
              |> mes(
                "I ask that you continue to live in the shadows, but as an even greater Assassin with a new appearance."
              )
              |> next()
              |> mes("[Assassin Cross]")
              |> mes("Will you become")
              |> mes("an Assassin Cross?")
              |> next()
              |> select(["No.", "Yes."])

            ctx =
              if v1 == 1 do
                ctx =
                  ctx
                  |> mes("[Assassin Cross]")
                  |> mes("When you are")
                  |> mes("ready, come back.")
                  |> next()
                  |> mes("[Assassin Cross]")
                  |> mes("Honor to")
                  |> mes("the warriors.")
                  |> close()

                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              if Rathena.truthy?(get_char_var(ctx, :SkillPoint, 0)) do
                ctx =
                  ctx
                  |> mes("[Assassin Cross]")
                  |> mes("You still haven't")
                  |> mes("learned everything")
                  |> mes("that you can.")
                  |> next()
                  |> mes("[Assassin Cross]")
                  |> mes("Use all your")
                  |> mes("Skill Points")
                  |> mes("and then come back.")
                  |> close()

                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              ctx
              |> jobchange(:assassin_cross)
              |> set_char_var(:ADVJOB, 0)
              |> mes("[Assassin Cross]")
              |> mes("Congratulations.")
              |> mes("As an Assassin Cross,")
              |> mes("I hope that you fight for a brighter future within the darkness.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx
    |> mes("[Assassin Cross]")
    |> mes("Welcome")
    |> mes("to Valhalla,")
    |> mes("the Hall of Honor.")
    |> next()
    |> mes("[Assassin Cross]")
    |> mes("Please make")
    |> mes("yourself comfortable")
    |> mes("while you are here.")
    |> mes("Honor to the warriors.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
