defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22.Monk.DoorKeeper do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/monk.txt:1851 (Door Keeper#mk).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "prt_monk",
        x: 199,
        y: 169,
        dir: 3,
        sprite: 746,
        name: "Door Keeper",
        scope: :shared,
        unique_name: "Door Keeper#mk"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      ctx
      |> mes("[Keeper Chorip]")
      |> mes("....this place is for those")
      |> mes(" in testing for becoming a monk.")
      |> next()

    if get_char_var(ctx, :MONK_Q, 0) == 14 do
      ctx =
        ctx
        |> mes("[Keeper Chorip]")
        |> mes("Huh? Did you just say Boohae?")
        |> next()
        |> mes("[Keeper Chorip]")
        |> mes(
          "Boohae... tends to hide in some quite places, so you might not be able to find him. For instance... a corner..."
        )
        |> close()

      throw({:script_end, ctx})
    else
      if get_char_var(ctx, :MONK_Q, 0) > 14 and get_char_var(ctx, :MONK_Q, 0) < 25 do
        ctx = mes(ctx, "[Keeper Chorip]")

        {ctx, v1} =
          ctx
          |> mes(Rathena.concat(Rathena.concat("Is your name ", char_name(ctx, 0)), "?"))
          |> next()
          |> select(["Yes.", "No."])

        ctx =
          if v1 == 1 do
            ctx =
              ctx
              |> mes("[Keeper Chorip]")
              |> mes("Alright you're cool... go on in. Your test is waiting for you. Good luck.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("[Keeper Chorip]")
          |> mes("Yeah right, I know who you are... get in there... your test is ready.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx = ctx |> mes("[Keeper Chorip]") |> mes("...please be quiet inside.") |> close()
        throw({:script_end, ctx})
      end
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
