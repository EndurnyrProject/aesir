defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Priest.Mummy31 do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/priest.txt:1687 (Mummy3_1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, scope: :shared, spawn: []

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  def on_event("OnDisable", ctx), do: ev_ondisable(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    disablenpc(ctx, "Mummy3_1")
  end

  def ev_ontouch(ctx) do
    ctx =
      if base_job(ctx) == :acolyte do
        ctx |> donpcevent("Mummy_Generator::Onm3") |> donpcevent("Mummy3_1::OnDisable")
      else
        ctx
      end

    ctx
  end

  def ev_onenable(ctx) do
    enablenpc(ctx, "Mummy3_1")
  end

  def ev_ondisable(ctx) do
    disablenpc(ctx, "Mummy3_1")
  end
end
