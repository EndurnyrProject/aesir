defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Priest.Prst31 do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/priest.txt:1708 (prst3_1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "job_prist",
        x: 98,
        y: 105,
        dir: 4,
        sprite: 45,
        name: "prst3_1",
        scope: :shared,
        trigger: {3, 3}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if base_job(ctx) == :priest do
        ctx = warp(ctx, "prt_church", 15, 36)
        throw({:script_end, ctx})
      else
        ctx =
          if base_class(ctx) == :acolyte do
            ctx = set_char_var(ctx, :PRIEST_Q, 7)

            ctx =
              if checkquest(ctx, 8012) != -1 do
                changequest(ctx, 8012, 8013)
              else
                ctx
              end

            ctx |> warp("prt_church", 16, 37) |> donpcevent("Mummy_Generator::OnDisable")
          else
            ctx
          end

        ctx
      end

    ctx
  end
end
