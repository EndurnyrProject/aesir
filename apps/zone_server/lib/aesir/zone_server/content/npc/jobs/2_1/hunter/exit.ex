defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Hunter.Exit do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/hunter.txt:1244 (exit#hnttest).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "job_hunte",
        x: 89,
        y: 139,
        dir: 0,
        sprite: 45,
        name: "exit",
        scope: :shared,
        unique_name: "exit#hnttest",
        trigger: {2, 2}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    disablenpc(ctx, "exit#hnttest")
  end

  def ev_ontouch(ctx) do
    ctx =
      ctx
      |> donpcevent("Manager#hnt::OnReset")
      |> donpcevent("Waiting Room#hnt::OnStart")
      |> set_char_var(:HNTR_Q, 16)
      |> changequest(4011, 4012)
      |> savepoint("payon", 104, 99)

    ctx =
      if Rathena.truthy?(:rand.uniform(2) - 1) do
        warp(ctx, "payon_in02", 21, 27)
      else
        warp(ctx, "payon_in03", 128, 7)
      end

    ctx
  end
end
