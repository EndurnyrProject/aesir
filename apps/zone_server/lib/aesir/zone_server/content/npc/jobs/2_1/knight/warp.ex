defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Knight.Warp do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/knight.txt:1993 (Warp#knt).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "job_knt",
        x: 143,
        y: 57,
        dir: 1,
        sprite: 107,
        name: "Warp",
        scope: :shared,
        unique_name: "Warp#knt",
        trigger: {22, 22}
      }
    ]

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    disablenpc(ctx, "Warp#knt")
  end

  def ev_ontouch(ctx) do
    ctx |> set_char_var(:KNIGHT_Q, 12) |> changequest(9010, 9011) |> warp("prt_in", 80, 100)
  end
end
