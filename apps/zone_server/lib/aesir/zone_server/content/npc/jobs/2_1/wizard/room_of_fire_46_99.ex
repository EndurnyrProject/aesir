defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Wizard.RoomOfFire4699 do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/wizard.txt:1614 (Room of Fire#Failed).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, scope: :shared, spawn: []

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    disablenpc(ctx, "Room of Fire#Failed")
  end

  def ev_ontouch(ctx) do
    warp(ctx, "geffen", 120, 110)
  end
end
