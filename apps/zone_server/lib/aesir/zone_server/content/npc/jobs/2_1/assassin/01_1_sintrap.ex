defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Assassin.M011Sintrap do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/assassin.txt:1353 (01_1::SinTrap).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []
  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_npc_var_of(ctx, "DisableTraps", "Beholder#ASNTEST", 0) < 1 do
        ctx
        |> mapannounce(
          "in_moc_16",
          Rathena.concat(char_name(ctx, 0), ", you're trapped. You will be sent back."),
          1
        )
        |> set_char_var(:ASSIN_Q, 2)
        |> warp("in_moc_16", 19, 161)
        |> donpcevent("Beholder#ASNTEST::OnResetmob")
        |> donpcevent("Standby Room#ASNTEST::OnStart")
      else
        ctx
      end

    ctx
  end
end
