defmodule Aesir.ZoneServer.Content.Npc.Jobs.M21.Assassin.MazeAssistant do
  @moduledoc """
  Transpiled from rAthena jobs/2-1/assassin.txt:1601 (Maze Assistant).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "in_moc_16",
        x: 182,
        y: 169,
        dir: 0,
        sprite: 45,
        name: "Maze Assistant",
        trigger: {1, 1}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :ASSIN_Q, 0) == 5 or get_char_var(ctx, :ASSIN_Q, 0) == 6 do
        ctx = warp(ctx, "in_moc_16", 181, 183)
        ctx = set_char_var(ctx, :ASSIN_Q, get_char_var(ctx, :ASSIN_Q, 0) + 1)

        ctx =
          if not Rathena.truthy?(isbegin_quest(ctx, 8006)) do
            changequest(ctx, 8005, 8006)
          else
            ctx
          end

        ctx
      else
        ctx =
          ctx
          |> mapannounce(
            "in_moc_16",
            Rathena.concat(char_name(ctx, 0), " has entered 'Guildmaster's room.'"),
            1
          )
          |> savepoint("in_moc_16", 181, 183)
          |> donpcevent("Guildmaster#ASN1::OnCast")
          |> warp("in_moc_16", 167, 113)

        throw({:script_end, ctx})
      end

    ctx
  end
end
