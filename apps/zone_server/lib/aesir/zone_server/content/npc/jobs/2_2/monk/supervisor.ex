defmodule Aesir.ZoneServer.Content.Npc.Jobs.M22.Monk.Supervisor do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/monk.txt:1970 (Supervisor#race_monk).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "monk_test",
        x: 387,
        y: 350,
        dir: 0,
        sprite: 45,
        name: "Supervisor",
        scope: :shared,
        unique_name: "Supervisor#race_monk",
        trigger: {2, 2}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :MONK_Q, 0) >= 15 and get_char_var(ctx, :MONK_Q, 0) <= 23 do
        ctx =
          ctx
          |> set_char_var(:MONK_Q, get_char_var(ctx, :MONK_Q, 0) + 1)
          |> warp("monk_test", 385, 388)

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :MONK_Q, 0) == 24 do
            ctx =
              ctx
              |> set_char_var(:MONK_Q, 25)
              |> changequest(3028, 3029)
              |> mapannounce(
                "monk_test",
                "Now! This is the last lap!! If you make it you need to go visit Tomoon for the next test!",
                1
              )
              |> warp("monk_test", 385, 388)

            throw({:script_end, ctx})
          else
            ctx =
              if get_char_var(ctx, :MONK_Q, 0) == 25 do
                ctx =
                  ctx
                  |> mes("[Supervisor]")
                  |> mes("Now...you may go visit Tomoon.")
                  |> mes("Tomoon is in the deepest room inside a building near this abbey.")

                ctx =
                  ctx
                  |> mapannounce(
                    "monk_test",
                    Rathena.concat(
                      Rathena.concat("Congratulations!", char_name(ctx, 0)),
                      "!! You completed the marathon!"
                    ),
                    1
                  )
                  |> close()
                  |> warp("prt_monk", 194, 168)

                throw({:script_end, ctx})
              else
                ctx
              end

            ctx
          end

        ctx
      end

    ctx
  end
end
