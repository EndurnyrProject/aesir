defmodule Aesir.ZoneServer.Content.Npc.Re.Functions.FNverase do
  @moduledoc """
  Transpiled from rAthena re/jobs/novice/novice.txt:2448 (F_NvErase).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    ctx =
      if Rathena.truthy?(Enum.at(args, 0, 0)) do
        ctx =
          if checkquest(ctx, 7123) > -1 do
            erasequest(ctx, 7123)
          else
            ctx
          end

        ctx =
          if checkquest(ctx, 7124) > -1 do
            erasequest(ctx, 7124)
          else
            ctx
          end

        ctx =
          if checkquest(ctx, 7126) > -1 do
            erasequest(ctx, 7126)
          else
            ctx
          end

        ctx =
          if checkquest(ctx, 7127) > -1 do
            erasequest(ctx, 7127)
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx = nude(ctx)

    ctx =
      set_local(ctx, :manuals, [
        count_item(ctx, 2819),
        count_item(ctx, 2820),
        count_item(ctx, 2821),
        count_item(ctx, 2822),
        count_item(ctx, 2823),
        count_item(ctx, 2824)
      ])

    ctx =
      if Rathena.truthy?(Enum.at(get_local(ctx, :manuals, []), 0, 0)) do
        delitem(ctx, 2819, Enum.at(get_local(ctx, :manuals, []), 0, 0))
      else
        ctx
      end

    ctx =
      if Rathena.truthy?(Enum.at(get_local(ctx, :manuals, []), 1, 0)) do
        delitem(ctx, 2820, Enum.at(get_local(ctx, :manuals, []), 1, 0))
      else
        ctx
      end

    ctx =
      if Rathena.truthy?(Enum.at(get_local(ctx, :manuals, []), 2, 0)) do
        delitem(ctx, 2821, Enum.at(get_local(ctx, :manuals, []), 2, 0))
      else
        ctx
      end

    ctx =
      if Rathena.truthy?(Enum.at(get_local(ctx, :manuals, []), 3, 0)) do
        delitem(ctx, 2822, Enum.at(get_local(ctx, :manuals, []), 3, 0))
      else
        ctx
      end

    ctx =
      if Rathena.truthy?(Enum.at(get_local(ctx, :manuals, []), 4, 0)) do
        delitem(ctx, 2823, Enum.at(get_local(ctx, :manuals, []), 4, 0))
      else
        ctx
      end

    ctx =
      if Rathena.truthy?(Enum.at(get_local(ctx, :manuals, []), 5, 0)) do
        delitem(ctx, 2824, Enum.at(get_local(ctx, :manuals, []), 5, 0))
      else
        ctx
      end

    {ctx, nil}
  end
end
