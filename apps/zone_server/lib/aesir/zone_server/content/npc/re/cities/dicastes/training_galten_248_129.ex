defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dicastes.TrainingGalten248129 do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:123 (Training Galten#d).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "dicastes01",
        x: 248,
        y: 129,
        dir: 0,
        sprite: 450,
        name: "Training Galten",
        unique_name: "Training Galten#d"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Training Galten]")

    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        ctx =
          ctx
          |> mes("[Training Galten]")
          |> mes("I heard this isn't training")
          |> mes("equipment but a massager")
          |> mes("for trainees like me.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("?????? ?? ??????")
    |> mes("?????? ?? ?????? ??")
    |> next()
    |> mes("Don't know what it means.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
