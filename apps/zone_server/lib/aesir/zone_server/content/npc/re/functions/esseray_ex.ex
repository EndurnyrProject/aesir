defmodule Aesir.ZoneServer.Content.Npc.Re.Functions.EsserayEx do
  @moduledoc """
  Transpiled from rAthena re/jobs/novice/supernovice_ex.txt:18 (Esseray_Ex).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, _args) do
    try do
      if base_level(ctx) > 98 and job_level(ctx) > 98 do
        ctx =
          if get_char_var(ctx, :EXSUPNOV_Q, 0) > 0 and get_char_var(ctx, :EXSUPNOV_Q, 0) < 6 do
            ctx =
              ctx
              |> mes("[Esseray]")
              |> mes("A legend in the Novice world has disappeared to do something in Comodo.")
              |> mes("You should find him.")
              |> next()
              |> mes("[Esseray]")
              |> mes("He can guide you along")
              |> mes("the correct path...")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        {ctx, v1} =
          ctx
          |> mes("[Esseray]")
          |> mes("Oh? Are you one of our Novice Society?")
          |> mes("You're looking good.")
          |> next()
          |> mes("[Esseray]")
          |> mes("You look full of energy...")
          |> mes("You seem strong enough to endure the test.")
          |> next()
          |> mes("[Esseray]")
          |> mes("I know a way to become stronger. Are you willing you try it?")
          |> next()
          |> select(["No, I'm okay now.", "I want to be stronger!"])

        ctx =
          if v1 == 1 do
            ctx =
              ctx
              |> mes("[Esseray]")
              |> mes("If you are satisfied with mediocrity, I can't help you.")
              |> mes("Live a normal life then.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("[Esseray]")
          |> mes("Good!")
          |> mes("A legend in the Novice world has disappeared to do something in Comodo.")
          |> mes("You should find him.")
          |> next()
          |> setquest(5092)
          |> set_char_var(:EXSUPNOV_Q, 1)
          |> mes("[Esseray]")
          |> mes("He can guide you along")
          |> mes("the correct path...")
          |> close()

        throw({:script_end, ctx})
      else
        throw({:script_return, {ctx, nil}})
      end
    catch
      :throw, {:script_return, result} -> result
    end
  end
end
