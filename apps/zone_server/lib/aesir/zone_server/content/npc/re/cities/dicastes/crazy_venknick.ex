defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dicastes.CrazyVenknick do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:639 (Crazy Venknick#fihsing1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "dicastes01",
        x: 202,
        y: 197,
        dir: 4,
        sprite: 449,
        name: "Crazy Venknick",
        unique_name: "Crazy Venknick#fihsing1"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        {ctx, v1} =
          ctx
          |> mes("[Crazy Venknick]")
          |> mes("At only 5000 Zeny")
          |> mes("I will tell you the sad legend behind that statue.")
          |> next()
          |> select(["Give 5000 Zeny.", "Don't give 5000 Zeny."])

        ctx =
          if v1 == 2 do
            ctx =
              ctx |> mes("[Crazy Venknick]") |> mes("Don't come back with regrets.") |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          if zeny(ctx) < 5000 do
            ctx =
              ctx
              |> mes("[Crazy Venknick]")
              |> mes("I have")
              |> mes("no legend to share with")
              |> mes("the poor.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> pay_zeny(5000)
          |> mes("[Crazy Venknick]")
          |> mes("Thanks.")
          |> emotion(:kik)
          |> next()

        ctx =
          ctx
          |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
          |> mes("What is the sad legend?")
          |> next()
          |> emotion(:think)

        ctx =
          ctx
          |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
          |> mes("I asked what the sad legend is about!")
          |> next()
          |> mes("[Crazy Venknick]")
          |> mes("I don't believe in legends.")
          |> next()
          |> emotion(:think)

        ctx =
          ctx
          |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
          |> mes("Was I tricked?")
          |> sc_start(:sc_blind, 20000, 0)
          |> next()
          |> mes("You feel outraged at being tricked.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[?????? ?? ]")
    |> mes("?????? ?? ??????")
    |> mes("?????? ?? ?????? ?? ??????")
    |> next()
    |> mes("Don't know what it means.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
