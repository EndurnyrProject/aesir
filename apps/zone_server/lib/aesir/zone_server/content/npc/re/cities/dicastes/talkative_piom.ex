defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dicastes.TalkativePiom do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:466 (Talkative Piom#a).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "dic_in01",
        x: 335,
        y: 34,
        dir: 5,
        sprite: 490,
        name: "Talkative Piom",
        scope: :renewal,
        unique_name: "Talkative Piom#a"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Talkative Piom]")

    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        ctx =
          ctx
          |> mes("At my factory")
          |> mes("we not only refine Bradium,")
          |> mes("but we also produce various")
          |> mes("tools for daily use.")
          |> mes("We manufacture war supplies")
          |> mes("here too.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("?????? ??")
    |> mes("?????? ?? ????")
    |> mes("?????? ?? ?????? ??")
    |> mes("?????? ?? ??")
    |> mes("?????? ?? ?????? ?? ??")
    |> mes("?????? ?? ??????")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
