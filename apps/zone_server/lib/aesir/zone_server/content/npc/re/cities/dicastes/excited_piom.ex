defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dicastes.ExcitedPiom do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:546 (Excited Piom#a).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "dicastes01",
        x: 235,
        y: 245,
        dir: 4,
        sprite: 490,
        name: "Excited Piom",
        unique_name: "Excited Piom#a"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        ctx =
          ctx
          |> mes("[Excited Piom]")
          |> mes("Have you seen Ahat?")
          |> emotion(:delight)
          |> mes("His popularity here in El Discastes is phenomenal!")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Excited Piom]")
    |> mes("?????? ?? ?????? ??")
    |> emotion(:delight)
    |> mes("?????? ?? ?????? ?? ?????? ?? ?? ??")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
