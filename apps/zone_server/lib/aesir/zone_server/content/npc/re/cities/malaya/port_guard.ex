defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Malaya.PortGuard do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:379 (Port Guard#ma03).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "malaya",
        x: 270,
        y: 59,
        dir: 4,
        sprite: 570,
        name: "Port Guard",
        scope: :renewal,
        unique_name: "Port Guard#ma03"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx =
          ctx
          |> emotion(:profusely_sweat)
          |> mes("[Port Guard]")
          |> mes("The village is chaotic these days. Is it okay for me to be off like this?")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx =
              ctx
              |> emotion(:stare_about)
              |> mes("[Port Guard]")
              |> mes(
                "The village is somewhat stable now but you should still be careful walking around at night."
              )
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx =
      if Enum.random(1..3) == 2 do
        ctx = mes(ctx, "[Port Guard]")

        ctx =
          ctx
          |> mes(
            Rathena.concat(
              getd(ctx, Rathena.concat(Rathena.concat("$ma_name0", Enum.random(1..6)), "$")),
              " is said to escape from Buwaya alive after being captured. This person must be powerful."
            )
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, _} =
      ctx
      |> emotion(:huk)
      |> mes("[Port Guard]")
      |> mes("What!! Port all clear!!")
      |> next()
      |> mes("[Port Guard]")
      |> mes(
        "Huh? You're not the captain? You scared me. Shoot! Ah, right! I received an official document for travelers."
      )
      |> next()
      |> select(["What document?"])

    {ctx, v1} =
      ctx
      |> mes("[Port Guard]")
      |> mes(
        "It says that travelers should be careful of monsters when walking in the village at night."
      )
      |> next()
      |> mes("[Port Guard]")
      |> mes(
        "Buwaya don't come to the village often but there are cases when they snatch people in the boxes they carry at night."
      )
      |> next()
      |> mes("[Port Guard]")
      |> mes(
        "Huh?! And it says here that a monster named Wokwok especially visits the village often at night and should be avoided."
      )
      |> next()
      |> select(["I see.", "Is there any way to prevent them from coming?"])

    ctx =
      case v1 do
        1 ->
          ctx = ctx |> mes("[Port Guard]") |> mes("Be careful at night!") |> close()
          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Port Guard]")
            |> mes(
              "They say you can attack Buwaya's weak point inside the box they carry if you ever get caught in one."
            )
            |> next()
            |> mes("[Port Guard]")
            |> mes(
              "And you should run into the nearest building when you hear Wokwoks wings fluttering from afar."
            )
            |> next()
            |> emotion(:scratch)
            |> mes("[Port Guard]")
            |> mes(
              "But if the fluttering sound is small or if you don't hear anything, let's just say you should prepare for attack and wish for luck."
            )
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
