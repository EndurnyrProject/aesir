defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Mora.Drawer do
  @moduledoc """
  Transpiled from rAthena re/cities/mora.txt:1168 (Drawer#mora_warehouse).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "mora",
        x: 48,
        y: 128,
        dir: 0,
        sprite: 111,
        name: "Drawer",
        unique_name: "Drawer#mora_warehouse"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    v1 = Todo.call!(:callfunc, ["F_CanOpenStorage"])

    ctx =
      if not Rathena.truthy?(v1) do
        ctx = ctx |> mes("- You don't remember how to open the warehouse. -") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, v2} =
      ctx
      |> mes("- The figure 1 0 0 is written in small letters on the vegetable-shaped drawer. -")
      |> next()
      |> select(["Insert 100 zeny.", "Don't do anything."])

    ctx =
      case v2 do
        1 ->
          ctx =
            if zeny(ctx) < 100 do
              ctx = ctx |> mes("- You don't seem to have 100 zeny with you. -") |> close()
              throw({:script_end, ctx})
            else
              ctx
            end

          ctx = ctx |> pay_zeny(100) |> openstorage() |> close()
          throw({:script_end, ctx})

        2 ->
          ctx = ctx |> mes("- You gave up using the warehouse. -") |> close()
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
