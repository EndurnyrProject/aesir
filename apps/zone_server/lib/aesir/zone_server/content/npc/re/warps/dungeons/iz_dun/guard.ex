defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Dungeons.IzDun.Guard do
  @moduledoc """
  Transpiled from rAthena re/warps/dungeons/iz_dun.txt:46 (Guard#iz_dun).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "iz_dun05",
        x: 142,
        y: 190,
        dir: 5,
        sprite: 413,
        name: "Guard",
        unique_name: "Guard#iz_dun"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Gatekeeper]")
      |> mes("So I guess you want to go back to the fifth floor?")
      |> next()
      |> select(["Yes!", "No, I'll stay."])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Gatekeeper]")
            |> mes("Be safe, adventurer.")
            |> close()
            |> warp("iz_dun04", 129, 232)

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Gatekeeper]")
            |> mes("You're not going?")
            |> mes("You're braver than I thought.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
