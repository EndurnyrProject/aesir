defmodule Aesir.ZoneServer.Content.Npc.Re.Guides.GuidesPayon.Guide8633 do
  @moduledoc """
  Transpiled from rAthena re/guides/guides_payon.txt:193 (Guide#05payon).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "pay_arche",
        x: 86,
        y: 33,
        dir: 4,
        sprite: 708,
        name: "Guide",
        scope: :renewal,
        unique_name: "Guide#05payon"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("pay_soldier", 2)
    |> mes("[Payon Guide]")
    |> mes("Welcome to ^8B4513Archer Village^000000,")
    |> mes("a subsidiary village of Payon.")
    |> mes("Do you need help navigating the village?")
    |> todo(:F_Navi, ["[Payon Guide]"])
    |> next()
    |> set_local(:"str$", "Would you like to check any other locations?")
    |> loop_1()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp loop_1(ctx) do
    {ctx, v1} =
      select(ctx, [
        "[ Payon Dungeon ]",
        "[ Archer Guild ]",
        "[ Tool Shop ]",
        "[ Kafra Employee ]",
        "[ Mercenary Guildsman ]",
        "[ Alchemist Guildsman ]",
        "[ Eden Teleport Officer ]",
        "Remove Marks from Mini-Map",
        "Cancel"
      ])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Payon Guide]")
            |> mes("Let me mark the entrance of")
            |> mes(
              Rathena.concat(
                "the ",
                Todo.call!(:F_Navi, ["Payon Dungeon", "pay_arche,38,131", "^B9062F"])
              )
            )
            |> mes("on your mini-map.")

          ctx
          |> mes(get_local(ctx, :"str$", ""))
          |> viewpoint(1, 38, 131, 0, 16_711_680)
          |> next()

        2 ->
          ctx =
            ctx
            |> mes("[Payon Guide]")
            |> mes(
              Rathena.concat(
                "The ",
                Todo.call!(:F_Navi, ["Archer Guild", "pay_arche,143,164", "^0000FF"])
              )
            )
            |> mes("is where you can change")
            |> mes("your job to Archer.")

          ctx |> mes(get_local(ctx, :"str$", "")) |> viewpoint(1, 143, 164, 1, 688_895) |> next()

        3 ->
          ctx =
            ctx
            |> mes("[Payon Guide]")
            |> mes("Let me mark the location of the")
            |> mes(to_string(Todo.call!(:F_Navi, ["Tool Shop", "pay_arche,72,156", "^FF5400"])))
            |> mes("on your mini-map.")

          ctx
          |> mes(get_local(ctx, :"str$", ""))
          |> viewpoint(1, 72, 156, 2, 16_757_760)
          |> next()

        4 ->
          ctx =
            ctx
            |> mes("[Payon Guide]")
            |> mes("Let me mark the location of the ^006400Kafra Employee^000000")
            |> mes("on your mini-map.")

          ctx
          |> mes(get_local(ctx, :"str$", ""))
          |> viewpoint(1, 55, 123, 3, 11_206_400)
          |> next()

        5 ->
          ctx =
            ctx
            |> mes("[Payon Guide]")
            |> mes("Let me mark the location of the")
            |> mes(
              to_string(
                Todo.call!(:F_Navi, ["Mercenary Guildsman", "pay_arche,101,166", "^7F3300"])
              )
            )
            |> mes("on your mini-map.")

          ctx
          |> mes(get_local(ctx, :"str$", ""))
          |> viewpoint(1, 101, 166, 4, 13_789_470)
          |> next()

        6 ->
          ctx =
            ctx
            |> mes("[Payon Guide]")
            |> mes("Let me mark the location of the")
            |> mes(
              to_string(
                Todo.call!(:F_Navi, ["Alchemist Guildsman", "pay_arche,126,113,", "^800080"])
              )
            )
            |> mes("on your mini-map.")

          ctx
          |> mes(get_local(ctx, :"str$", ""))
          |> viewpoint(1, 127, 113, 5, 14_315_734)
          |> next()

        7 ->
          ctx =
            ctx
            |> mes("[Payon Guide]")
            |> mes(
              Rathena.concat(
                "The ",
                Todo.call!(:F_Navi, ["Eden Teleport Officer", "pay_arche,143,164", "^0000FF"])
              )
            )
            |> mes("is currently staying inside")
            |> mes("the Archer Guild building.")

          ctx |> mes(get_local(ctx, :"str$", "")) |> viewpoint(1, 143, 164, 6, 688_895) |> next()

        8 ->
          ctx =
            ctx |> mes("[Payon Guide]") |> mes("Sure, I'll remove all marks from your mini-map.")

          ctx
          |> mes(get_local(ctx, :"str$", ""))
          |> viewpoint(2, 1, 1, 0, 16_776_960)
          |> viewpoint(2, 1, 1, 1, 0)
          |> viewpoint(2, 1, 1, 2, 16_711_680)
          |> viewpoint(2, 1, 1, 3, 16_776_960)
          |> viewpoint(2, 1, 1, 4, 16_776_960)
          |> viewpoint(2, 1, 1, 5, 16_776_960)
          |> viewpoint(2, 1, 1, 6, 16_776_960)
          |> next()

        9 ->
          ctx =
            ctx |> mes("[Payon Guide]") |> mes("Enjoy your stay.") |> close() |> cutin("", 255)

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    loop_1(ctx)
  end
end
