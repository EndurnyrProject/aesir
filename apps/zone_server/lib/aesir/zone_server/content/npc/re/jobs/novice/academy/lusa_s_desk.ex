defmodule Aesir.ZoneServer.Content.Npc.Re.Jobs.Novice.Academy.LusaSDesk do
  @moduledoc """
  Transpiled from rAthena re/jobs/novice/academy.txt:8037 (Lusa's Desk#ac).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "iz_ac02",
        x: 131,
        y: 82,
        dir: 4,
        sprite: 844,
        name: "Lusa's Desk",
        unique_name: "Lusa's Desk#ac"
      },
      %{
        map: "iz_ac02_a",
        x: 131,
        y: 82,
        dir: 4,
        sprite: 844,
        name: "Lusa's Desk",
        unique_name: "Lusa's Desk#ac_a"
      },
      %{
        map: "iz_ac02_b",
        x: 131,
        y: 82,
        dir: 4,
        sprite: 844,
        name: "Lusa's Desk",
        unique_name: "Lusa's Desk#ac_b"
      },
      %{
        map: "iz_ac02_c",
        x: 131,
        y: 82,
        dir: 4,
        sprite: 844,
        name: "Lusa's Desk",
        unique_name: "Lusa's Desk#ac_c"
      },
      %{
        map: "iz_ac02_d",
        x: 131,
        y: 82,
        dir: 4,
        sprite: 844,
        name: "Lusa's Desk",
        unique_name: "Lusa's Desk#ac_d"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :nov_3_guns, 0) == 8 do
        ctx
        |> mes("[Lusa's Desk]")
        |> mes(
          "There are lots of files on the desk, they seem to be tasks from Prontera and Izlude residents."
        )
        |> next()
      else
        ctx =
          ctx |> mes("[Lusa's Desk]") |> mes("There is a stack of papers on the desk.") |> close()

        throw({:script_end, ctx})
      end

    loop_1(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp loop_1(ctx) do
    ctx = set_local(ctx, :gun_quest_01, checkquest(ctx, 5149, :hunting))
    ctx = set_local(ctx, :gun_quest_02, checkquest(ctx, 5150, :hunting))
    ctx = set_local(ctx, :gun_quest_03, checkquest(ctx, 5151, :hunting))
    ctx = set_local(ctx, :gun_quest_04, checkquest(ctx, 5152, :hunting))

    ctx =
      if get_local(ctx, :gun_quest_01, 0) == -1 do
        set_local(ctx, :"gque_01$", "^000000Available^000000")
      else
        ctx =
          if get_local(ctx, :gun_quest_01, 0) == 1 do
            set_local(ctx, :"gque_01$", "^0000FFIn Progress^000000")
          else
            ctx =
              if get_local(ctx, :gun_quest_01, 0) == 2 do
                set_local(ctx, :"gque_01$", "^FF0000Not Available^000000")
              else
                set_local(ctx, :"gque_01$", "^FF0000Error^000000")
              end

            ctx
          end

        ctx
      end

    ctx =
      if get_local(ctx, :gun_quest_02, 0) == -1 do
        set_local(ctx, :"gque_02$", "^000000Available^000000")
      else
        ctx =
          if get_local(ctx, :gun_quest_02, 0) == 1 do
            set_local(ctx, :"gque_02$", "^0000FFIn Progress^000000")
          else
            ctx =
              if get_local(ctx, :gun_quest_02, 0) == 2 do
                set_local(ctx, :"gque_02$", "^FF0000Not Available^000000")
              else
                set_local(ctx, :"gque_02$", "^FF0000Error^000000")
              end

            ctx
          end

        ctx
      end

    ctx =
      if get_local(ctx, :gun_quest_03, 0) == -1 do
        set_local(ctx, :"gque_03$", "^000000Available^000000")
      else
        ctx =
          if get_local(ctx, :gun_quest_03, 0) == 1 do
            set_local(ctx, :"gque_03$", "^0000FFIn Progress^000000")
          else
            ctx =
              if get_local(ctx, :gun_quest_03, 0) == 2 do
                set_local(ctx, :"gque_03$", "^FF0000Not Available^000000")
              else
                set_local(ctx, :"gque_03$", "^FF0000Error^000000")
              end

            ctx
          end

        ctx
      end

    ctx =
      if get_local(ctx, :gun_quest_04, 0) == -1 do
        set_local(ctx, :"gque_04$", "^000000Available^000000")
      else
        ctx =
          if get_local(ctx, :gun_quest_04, 0) == 1 do
            set_local(ctx, :"gque_04$", "^0000FFIn Progress^000000")
          else
            ctx =
              if get_local(ctx, :gun_quest_04, 0) == 2 do
                set_local(ctx, :"gque_04$", "^FF0000Not Available^000000")
              else
                set_local(ctx, :"gque_04$", "^FF0000Error^000000")
              end

            ctx
          end

        ctx
      end

    {ctx, v1} =
      select(
        ctx,
        Enum.flat_map(
          [
            Rathena.concat(
              Rathena.concat("Assignment A (", get_local(ctx, :"gque_01$", "")),
              ")"
            ),
            Rathena.concat(
              Rathena.concat("Assignment B (", get_local(ctx, :"gque_02$", "")),
              ")"
            ),
            Rathena.concat(
              Rathena.concat("Assignment C (", get_local(ctx, :"gque_03$", "")),
              ")"
            ),
            Rathena.concat(
              Rathena.concat("Assignment D (", get_local(ctx, :"gque_04$", "")),
              ")"
            ),
            "Cancel"
          ],
          &String.split(&1, ":")
        )
      )

    ctx =
      case v1 do
        1 ->
          ctx =
            if not Rathena.truthy?(get_local(ctx, :gun_quest_01, 0)) do
              {ctx, v2} =
                ctx
                |> mes("This is the <Assignment A> information.")
                |> mes("Client: Children")
                |> mes(
                  "On the outskirts of the village is a very bad Hornet, because of this, the children cannot play outside. Please get rid of 10 Hornets."
                )
                |> next()
                |> select(["Take Assignment", "Look at Other Assignments", "Cancel"])

              ctx =
                if v2 == 1 do
                  ctx
                  |> setquest(5149)
                  |> mes(
                    "You have received ^A2563E<Assignment A>^000000 details. Please press ALT + U to check them."
                  )
                  |> next()
                else
                  ctx
                end

              ctx
            else
              ctx =
                if get_local(ctx, :gun_quest_01, 0) == 1 do
                  ctx
                  |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                  |> mes(
                    "I am already doing this task currently. Lusa said if I ever wanted to abondon the assignment that I should talk to her."
                  )
                  |> next()
                else
                  ctx =
                    if get_local(ctx, :gun_quest_01, 0) == 2 do
                      ctx
                      |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                      |> mes(
                        "I have already completed this assignment. I should talk to Lusa for my reward."
                      )
                      |> next()
                    else
                      ctx
                    end

                  ctx
                end

              ctx
            end

          ctx

        2 ->
          ctx =
            if not Rathena.truthy?(get_local(ctx, :gun_quest_02, 0)) do
              {ctx, v3} =
                ctx
                |> mes("This is the <Assignment B> information.")
                |> mes("Client: Resident H")
                |> mes(
                  "Recently, Roda Frogs have been keeping me awake at night. Get rid of 10 Roda Frogs so I can get a good nights rest."
                )
                |> next()
                |> select(["Take Assignment", "Look at Other Assignments", "Cancel"])

              ctx =
                if v3 == 1 do
                  ctx
                  |> setquest(5150)
                  |> mes(
                    "You have received ^A2563E<Assignment B>^000000 details. Please press ALT + U to check them."
                  )
                  |> next()
                else
                  ctx
                end

              ctx
            else
              ctx =
                if get_local(ctx, :gun_quest_02, 0) == 1 do
                  ctx
                  |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                  |> mes(
                    "I am already doing this task currently. Lusa said if I ever wanted to abondon the assignment that I should talk to her."
                  )
                  |> next()
                else
                  ctx =
                    if get_local(ctx, :gun_quest_02, 0) == 2 do
                      ctx
                      |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                      |> mes(
                        "I have already completed this assignment. I should talk to Lusa for my reward."
                      )
                      |> next()
                    else
                      ctx
                    end

                  ctx
                end

              ctx
            end

          ctx

        3 ->
          ctx =
            if not Rathena.truthy?(get_local(ctx, :gun_quest_03, 0)) do
              {ctx, v4} =
                ctx
                |> mes("This is the <Assignment C> information.")
                |> mes("Client: Cute Pet Research Student")
                |> mes(
                  "I want a cute pet of my own to walk around with. Can you help me catch 10 Savage Babes?"
                )
                |> next()
                |> select(["Take Assignment", "Look at Other Assignments", "Cancel"])

              ctx =
                if v4 == 1 do
                  ctx
                  |> setquest(5151)
                  |> mes(
                    "You have received ^A2563E<Assignment C>^000000 details. Please press ALT + U to check them."
                  )
                  |> next()
                else
                  ctx
                end

              ctx
            else
              ctx =
                if get_local(ctx, :gun_quest_03, 0) == 1 do
                  ctx
                  |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                  |> mes(
                    "I am already doing this task currently. Lusa said if I ever wanted to abondon the assignment that I should talk to her."
                  )
                  |> next()
                else
                  ctx =
                    if get_local(ctx, :gun_quest_03, 0) == 2 do
                      ctx
                      |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                      |> mes(
                        "I have already completed this assignment. I should talk to Lusa for my reward."
                      )
                      |> next()
                    else
                      ctx
                    end

                  ctx
                end

              ctx
            end

          ctx

        4 ->
          ctx =
            if not Rathena.truthy?(get_local(ctx, :gun_quest_04, 0)) do
              {ctx, v5} =
                ctx
                |> mes("This is the <Assignment D> information.")
                |> mes("Client: Volunteer Bards")
                |> mes(
                  "I heard some Rockers were having a singing contest, but I got lost, can you help me catch 10 Rockers? One day Rockers will disappear from the world!"
                )
                |> next()
                |> select(["Take Assignment", "Look at Other Assignments", "Cancel"])

              ctx =
                if v5 == 1 do
                  ctx
                  |> setquest(5152)
                  |> mes(
                    "You have received ^A2563E<Assignment D>^000000 details. Please press ALT + U to check them."
                  )
                  |> next()
                else
                  ctx
                end

              ctx
            else
              ctx =
                if get_local(ctx, :gun_quest_04, 0) == 1 do
                  ctx
                  |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                  |> mes(
                    "I am already doing this task currently. Lusa said if I ever wanted to abondon the assignment that I should talk to her."
                  )
                  |> next()
                else
                  ctx =
                    if get_local(ctx, :gun_quest_04, 0) == 2 do
                      ctx
                      |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                      |> mes(
                        "I have already completed this assignment. I should talk to Lusa for my reward."
                      )
                      |> next()
                    else
                      ctx
                    end

                  ctx
                end

              ctx
            end

          ctx

        5 ->
          ctx

        _ ->
          ctx
      end

    ctx
    |> mes("[Lusa's desk]")
    |> mes(
      "There are lots of files on the desk, they seem to be tasks from Prontera and Izlude residents."
    )
    |> next()
    |> loop_1()
  end
end
