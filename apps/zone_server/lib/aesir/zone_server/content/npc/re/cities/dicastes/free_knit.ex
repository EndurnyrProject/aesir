defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dicastes.FreeKnit do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:498 (Free Knit#a).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "dicastes01",
        x: 282,
        y: 210,
        dir: 4,
        sprite: 453,
        name: "Free Knit",
        unique_name: "Free Knit#a"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Free Knit]")

    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        ctx =
          ctx
          |> mes("As long as we have a steady")
          |> mes("supply of Bradium")
          |> mes("Saphas can live forever.")
          |> mes("Our childhood lasts 10 years,")
          |> mes("which is short.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("?????? ?? ?????? ??")
    |> mes("?????? ?? ?????? ?? ?????? ?? ?????? ??")
    |> mes("?????? ?? ?????? ?? ??????")
    |> mes("?ó¢³¡? ?? ?????? ?? ????")
    |> mes("?ò¢²¡? ?? ????")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
