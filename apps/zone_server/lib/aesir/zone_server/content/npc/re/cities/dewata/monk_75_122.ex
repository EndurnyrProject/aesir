defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dewata.Monk75122 do
  @moduledoc """
  Transpiled from rAthena re/cities/dewata.txt:575 (Monk#dewt1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "dewata",
        x: 75,
        y: 122,
        dir: 6,
        sprite: 834,
        name: "Monk",
        unique_name: "Monk#dewt1"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Monk]")
      |> mes("You are a stranger here. What brings here to this quiet place?")
      |> next()
      |> select(["I'm sightseeing.", "No reason."])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Monk]")
            |> mes(
              "Oh, you are a tourist. This is the '^0000FFPagoda^000000' worshiping the great Buddha. Please be respectful of the monks praying here."
            )
            |> close()

          throw({:script_end, ctx})

        2 ->
          ctx = ctx |> mes("[Monk]") |> mes("Good day to you...") |> close()
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
