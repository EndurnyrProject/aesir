defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.Verus.Cafeteria324290 do
  @moduledoc """
  Transpiled from rAthena re/warps/other/verus.txt:626 (Cafeteria#G-2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "un_bk_q",
        x: 324,
        y: 290,
        dir: 0,
        sprite: 45,
        name: "Cafeteria",
        unique_name: "Cafeteria#G-2",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx = todo(ctx, :callfunc, ["F_time_limit_recorder", get_local(ctx, :cooldown_status_day, 0)])

    ctx =
      if Enum.at(get_local(ctx, :cooldown_status_day, []), 0, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 1, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 2, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 3, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 4, 0) == 0 do
        ctx =
          ctx
          |> emotion(6)
          |> mes("[Transportation Executive Ernon]")
          |> mes(
            "Hey~ This is a time to borrow even the hands of small children. Stop goofing around and come here."
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "un_bk_q", 340, 261)
  end
end
