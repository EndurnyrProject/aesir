defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Malaya.LittleGirl do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:432 (Little Girl#ma04).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "malaya",
        x: 88,
        y: 252,
        dir: 4,
        sprite: 576,
        name: "Little Girl",
        scope: :renewal,
        unique_name: "Little Girl#ma04"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx =
          ctx
          |> mes("[Little Girl]")
          |> mes("I'm scared but I have to visit the fairy in the forest.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx =
              ctx
              |> mes("[Little Girl]")
              |> mes(
                "He he. Father said I can visit the fairy in the forest when the village calms down."
              )
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx =
      if Enum.random(1..3) == 2 do
        ctx =
          ctx
          |> set_local(
            :"name$",
            getd(ctx, Rathena.concat(Rathena.concat("$ma_name0", Enum.random(1..6)), "$"))
          )
          |> mes("[Little Girl]")

        ctx =
          ctx
          |> mes(
            Rathena.concat(
              Rathena.concat("A little while ago ", get_local(ctx, :"name$", "")),
              " came and told me a fun story."
            )
          )
          |> next()
          |> mes("[Little Girl]")
          |> mes("'I planted a pair of pear trees in the yard.'")

        ctx =
          ctx
          |> mes(
            Rathena.concat(
              Rathena.concat("He he. Isn't it fun? ", get_local(ctx, :"name$", "")),
              " seems like a fun person."
            )
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, _} =
      ctx
      |> mes("[Little Girl]")
      |> mes(
        "Father said I won't catch skin diseases once I'm friends with the fairy from the forest."
      )
      |> next()
      |> select(["What is this fairy?"])

    ctx
    |> mes("[Little Girl]")
    |> mes(
      "Encan'to! Encan'to fairies live in big trees or rocks in the forest. There are boy fairies and girl fairies..."
    )
    |> next()
    |> mes("[Little Girl]")
    |> mes("But boy fairies are prettier. Why is that?")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
