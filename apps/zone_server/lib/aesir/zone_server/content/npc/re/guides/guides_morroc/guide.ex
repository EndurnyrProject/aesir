defmodule Aesir.ZoneServer.Content.Npc.Re.Guides.GuidesMorroc.Guide do
  @moduledoc """
  Transpiled from rAthena re/guides/guides_morroc.txt:18 (Guide#01morocc::GuideMorocc).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "morocc",
        x: 153,
        y: 286,
        dir: 6,
        sprite: 707,
        name: "Guide",
        scope: :renewal,
        unique_name: "GuideMorocc"
      },
      %{
        map: "morocc",
        x: 154,
        y: 38,
        dir: 6,
        sprite: 707,
        name: "Guide",
        scope: :renewal,
        unique_name: "Guide#02morocc"
      },
      %{
        map: "morocc",
        x: 296,
        y: 213,
        dir: 4,
        sprite: 707,
        name: "Guide",
        scope: :renewal,
        unique_name: "Guide#03morocc"
      },
      %{
        map: "morocc",
        x: 28,
        y: 170,
        dir: 5,
        sprite: 707,
        name: "Guide",
        scope: :renewal,
        unique_name: "Guide#04morocc"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("moc_soldier", 2)
    |> mes("[Morocc Guide]")
    |> mes("Welcome to ^8B4513Morocc^000000,")
    |> mes("the Desert Town.")
    |> mes("Do you need help navigating the town?")
    |> todo(:F_Navi, ["[Morocc Guide]"])
    |> next()
    |> loop_1()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp s_l_mark(ctx, args) do
    ctx =
      ctx
      |> mes("[Morocc Guide]")
      |> mes("Let me mark the location of the")
      |> mes(to_string(Enum.at(args, 0, 0)))
      |> mes("on your mini-map.")
      |> mes("Would you like to check any other locations?")

    {ctx, nil}
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop, 0)) do
      {ctx, v2} =
        select(ctx, [
          "[ Thief Guild ]",
          "[ Sphinx Dungeon ]",
          "[ Oasis Entrance ]",
          "[ Forge ]",
          "[ Weapon & Armor Shop ]",
          "[ Inn ]",
          "[ Tavern ]",
          "Previous Menu"
        ])

      ctx =
        case v2 do
          1 ->
            ctx
            |> mes("[Morocc Guide]")
            |> mes(
              Rathena.concat(
                Rathena.concat(
                  "The ",
                  Todo.call!(:F_Navi, ["Thief Guild", "morocc,26,294", "^B9062F"])
                ),
                " is located"
              )
            )
            |> mes("inside the ^B9062FPyramids^000000")
            |> mes("that is near the ^B9062FOasis^000000.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 26, 294, 0, 16_711_680)
            |> next()

          2 ->
            ctx
            |> mes("[Morocc Guide]")
            |> mes(
              Rathena.concat(
                Rathena.concat(
                  "The ",
                  Todo.call!(:F_Navi, ["Sphinx Dungeon", "morocc,26,164", "^0000FF"])
                ),
                " is"
              )
            )
            |> mes("right outside the western gate.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 26, 164, 1, 688_895)
            |> next()

          3 ->
            ctx
            |> mes("[Morocc Guide]")
            |> mes(
              Rathena.concat(
                "Let me mark the entrance to the ",
                Todo.call!(:F_Navi, ["Oasis", "morocc,26,294", "^FF5400"])
              )
            )
            |> mes("on your mini-map.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 26, 294, 2, 16_757_760)
            |> next()

          4 ->
            {ctx, _} = s_l_mark(ctx, [Todo.call!(:F_Navi, ["Forge", "morocc,47,47", "^006400"])])
            ctx |> viewpoint(1, 47, 47, 3, 11_206_400) |> next()

          5 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Weapon & Armor Shop", "morocc,253,56", "^7F3300"])
              ])

            ctx |> viewpoint(1, 253, 56, 4, 13_789_470) |> next()

          6 ->
            ctx
            |> mes("[Morocc Guide]")
            |> mes(
              Rathena.concat(
                "Currently the ",
                Todo.call!(:F_Navi, ["Inn", "morocc,273,269", "^800080"])
              )
            )
            |> mes("has been shut down until further notice,")
            |> mes("but I'll mark its location")
            |> mes("on your mini-map.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 273, 269, 5, 14_315_734)
            |> next()

          7 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Tavern", "morocc,52,259", "^B9062F"])])

            ctx |> viewpoint(1, 52, 259, 6, 16_711_680) |> next()

          8 ->
            ctx = set_local(ctx, :loop, 0)
            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_5(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop, 0)) do
      {ctx, v3} =
        select(ctx, [
          "[ Kafra Employee ]",
          "[ Poison Herb Salesman ]",
          "[ Dye Maker ]",
          "[ Seiyablem ]",
          "Previous Menu"
        ])

      ctx =
        case v3 do
          1 ->
            {ctx, _} = s_l_mark(ctx, ["^0000FFKafra Employees^000000"])

            ctx
            |> viewpoint(1, 156, 97, 7, 688_895)
            |> viewpoint(1, 160, 258, 8, 688_895)
            |> next()

          2 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Poison Herb Salesman", "morocc,191,94", "^006400"])
              ])

            ctx |> viewpoint(1, 190, 96, 9, 11_206_400) |> next()

          3 ->
            ctx
            |> mes("[Morocc Guide]")
            |> mes(
              Rathena.concat(
                Rathena.concat(
                  "The ",
                  Todo.call!(:F_Navi, ["Dye Maker", "morocc,273,269", "^7F3300"])
                ),
                " is"
              )
            )
            |> mes("currently staying")
            |> mes("on the 1st floor of the Inn.")
            |> mes("Let me mark his location")
            |> mes("on your mini-map.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 273, 269, 10, 13_789_470)
            |> next()

          4 ->
            ctx
            |> mes("[Morocc Guide]")
            |> mes(to_string(Todo.call!(:F_Navi, ["Seiyablem", "morocc,51,41", "^800080"])))
            |> mes("is a technician")
            |> mes("who can create ^0000FFslots^000000 on weapons and armors,")
            |> mes("but not all equipment")
            |> mes("can be slotted. Well, that's what he says.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 51, 41, 11, 14_315_734)
            |> next()

          5 ->
            ctx = set_local(ctx, :loop, 0)
            ctx

          _ ->
            ctx
        end

      loop_5(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    {ctx, v1} =
      select(ctx, [
        "[ Main Facilities ]",
        "[ Merchants & Helpers ]",
        "Remove Marks from Mini-Map",
        "Cancel"
      ])

    ctx =
      case v1 do
        1 ->
          ctx |> set_local(:loop, 1) |> loop_3()

        2 ->
          ctx |> set_local(:loop, 1) |> loop_5()

        3 ->
          ctx
          |> mes("[Morocc Guide]")
          |> mes("Sure, I'll remove all marks from your mini-map.")
          |> mes("Is there anything else I can do for you?")
          |> viewpoint(2, 1, 1, 0, 16_776_960)
          |> viewpoint(2, 1, 1, 1, 0)
          |> viewpoint(2, 1, 1, 2, 16_711_680)
          |> viewpoint(2, 1, 1, 3, 16_776_960)
          |> viewpoint(2, 1, 1, 4, 16_776_960)
          |> viewpoint(2, 1, 1, 5, 16_776_960)
          |> viewpoint(2, 1, 1, 6, 16_776_960)
          |> viewpoint(2, 1, 1, 7, 16_776_960)
          |> viewpoint(2, 1, 1, 8, 16_776_960)
          |> viewpoint(2, 1, 1, 9, 16_776_960)
          |> viewpoint(2, 1, 1, 10, 16_776_960)
          |> viewpoint(2, 1, 1, 11, 16_776_960)
          |> next()

        4 ->
          ctx =
            ctx
            |> mes("[Morocc Guide]")
            |> mes("Be safe on your travels.")
            |> close()
            |> cutin("moc_soldier", 255)

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    loop_1(ctx)
  end
end
