defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Cities.Prontera.WayToQroomLQroomLToWay do
  @moduledoc """
  Transpiled from rAthena re/warps/cities/prontera.txt:23 (way_to_qroom_L_qroom_L_to_way).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prt_cas",
        x: 152,
        y: 163,
        dir: 0,
        sprite: 45,
        name: "way_to_qroom_L_qroom_L_to_way",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :ep16_royal, 0) == 0 do
        ctx =
          npctalk(ctx, "That way is a royal villa for VIPs. Outsiders cannot access the place.",
            npc: "Royal Guardian Knight#02",
            target: :self
          )

        ctx = ctx |> todo(:setpcblock, [get_char_var(ctx, :PCBLOCK_NPC, 0), 1]) |> sleep2(3000)

        ctx =
          ctx
          |> todo(:setpcblock, [get_char_var(ctx, :PCBLOCK_NPC, 0), 0])
          |> npctalk(
            "If you are invited, a severnt should be there to greet you. Please be guided officially.",
            npc: "Royal Guardian Knight#02",
            target: :self
          )

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "prt_cas_q", 150, 8)
  end
end
