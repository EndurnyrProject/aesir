defmodule Aesir.ZoneServer.Content.Npc.Re.Quests.QuestsMorocc.SuspiciousCat do
  @moduledoc """
  Transpiled from rAthena re/quests/quests_morocc.txt:64 (Suspicious Cat#night).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "moc_prydb1",
        x: 103,
        y: 54,
        dir: 3,
        sprite: 547,
        name: "Suspicious Cat",
        scope: :renewal,
        unique_name: "Suspicious Cat#night"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Suspicious Cat]")
      |> mes(
        "I can't believe a mummy could do this to me. Oh man, it hurts. Nobody should find out about this...."
      )
      |> next()
      |> select(["Mummy? What happened?", "Pretend not to know anything."])

    ctx =
      if v1 == 2 do
        ctx = close(ctx)
        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, v2} =
      ctx
      |> mes("[Suspicious Cat]")
      |> mes(
        "You caught me! Darn it...I didn't expect my reputation as a tomb raider would get damaged like this..."
      )
      |> next()
      |> mes("[Suspicious Cat]")
      |> mes(
        "But come on, they're so strange! They're mummies! Just mummies, you know? They shouldn't be that strong!"
      )
      |> next()
      |> select(["Strong mummies? Where are they?", "End the conversation."])

    ctx =
      if v2 == 2 do
        ctx =
          ctx
          |> mes("[Suspicious Cat]")
          |> mes(
            "Whaaat~ you don't want to hear my story? Fine! You'd better not mention my name anywhere else!"
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, v3} =
      ctx
      |> mes("[Suspicious Cat]")
      |> mes("Haha, it's a secret.")
      |> mes("I found out while I was stealing... No, I mean, just walking through the pyramid.")
      |> next()
      |> mes("[Suspicious Cat]")
      |> mes(
        "When I saw a mummy coming toward me, I was like 'whatever' and didn't care. But then he knocked me down with a slap in the face! How could he do that to a cute cat like me! You bandaged freak... you just wait and see!"
      )
      |> next()
      |> select(["I would like to go, too.", "End the conversation."])

    ctx =
      if v3 == 2 do
        ctx = close(ctx)
        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, v4} =
      ctx
      |> mes("[Suspicious Cat]")
      |> mes("No way! I spent so much time finding that path!!")
      |> next()
      |> mes("[Suspicious Cat]")
      |> mes("Hmm... but if you insist, just give me 5000 Zeny. I'll lead you.")
      |> mes(
        "What~? Don't give me that look! Do you have any idea how hard it is for a cat to survive in Midgard!"
      )
      |> next()
      |> mes("[Suspicious Cat]")
      |> mes(
        "^FF0000But come to think of it, a weakling like you won't survive there. Maybe for a second if you're lucky?^000000"
      )
      |> mes("Well, it's none of my business. But I warned you anyway!")
      |> next()
      |> select(["Pay 5,000 zeny and set out.", "Forget it."])

    ctx =
      case v4 do
        1 ->
          ctx =
            if zeny(ctx) < 5000 do
              ctx =
                ctx
                |> mes("[Suspicious Cat]")
                |> mes("Where do you think you're going! You're so broke!")
                |> close()

              throw({:script_end, ctx})
            else
              ctx
            end

          ctx =
            ctx
            |> pay_zeny(5000)
            |> mes("[Suspicious Cat]")
            |> mes(
              "Bah, what's up with this old money... you don't have anything sparkly or shiny?!"
            )
            |> mes("Ahh, just follow me!")
            |> close()
            |> warp("moc_prydn1", 93, 96)

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Suspicious Cat]")
            |> mes("Gosh, you don't even have 5000 Zeny, do you?")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
