defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dewata.DietingLady do
  @moduledoc """
  Transpiled from rAthena re/cities/dewata.txt:1048 (Dieting Lady#dew).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "dewata",
        x: 204,
        y: 230,
        dir: 6,
        sprite: 537,
        name: "Dieting Lady",
        scope: :renewal,
        unique_name: "Dieting Lady#dew"
      }
    ]

  @impl true
  def on_talk(ctx) do
    ctx =
      if count_item(ctx, 11533) == 0 do
        ctx =
          ctx
          |> mes("[Dieting Lady]")
          |> mes("Ah~ I'm starving!!")
          |> mes("I should be on a diet now.")
          |> mes("Why am I still hungry?")
          |> next()
          |> mes("[Dieting Lady]")
          |> mes("Don't talk to me too much!!")
          |> mes("I want to try that grilled ^006400Satay^000000")
          |> mes("so much~!!!!!")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, v1} =
      ctx
      |> mes("[Dieting Lady]")
      |> mes("Is that mouth-watering")
      |> mes("^006400Satay^000000 for me?")
      |> next()
      |> select(["Yes, want a taste?", "No, it's for me!"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> delitem(11533, 1)
            |> mes("[Dieting Lady]")
            |> mes("Thanks so much!")
            |> mes("I've been so hungry~!!")
            |> mes("This smell of this ^006400Satay^000000")
            |> mes("is so good~!")
            |> next()
            |> give_item(11533, 1)
            |> mes("[Dieting Lady]")
            |> mes("Ah... gotta come back to my senses,")
            |> mes("too much of a good thing is bad...")
            |> mes("I should stick with my diet.")
            |> mes("Sorry, I'll give it back to you.")
            |> emotion(:cry)
            |> close()

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Dieting Lady]")
            |> mes("Arrgg.. You are so greedy!")
            |> mes("Since you don't want to share.")
            |> mes("Anyway, I shouldn't cheat on my diet")
            |> mes("even for something that looks and smells so good...")
            |> emotion(:hng)
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
