defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Malangdo.Wanderer do
  @moduledoc """
  Transpiled from rAthena re/cities/malangdo.txt:344 (Wanderer#mal).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "malangdo",
        x: 149,
        y: 120,
        dir: 5,
        sprite: 485,
        name: "Wanderer",
        unique_name: "Wanderer#mal",
        trigger: {5, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
    |> mes("[Wanderer]")
    |> mes("The cats living in here are so energetic.")
    |> mes("Is it weird that they make me want to dance all day?")
    |> mes("La di dah di dah~!")
    |> close()
  end

  def ev_ontouch(ctx) do
    v1 = :rand.uniform(5) - 1

    ctx =
      case v1 do
        0 ->
          soundeffect(ctx, "ring_of_nibelungen.wav", 0)

        1 ->
          soundeffect(ctx, "dont_forget_me_not.wav", 0)

        2 ->
          soundeffect(ctx, "humming.wav", 0)

        3 ->
          soundeffect(ctx, "assassin_of_sunset.wav", 0)

        4 ->
          ctx = ctx |> emotion(:delight) |> emotion(:delight)
          ctx

        _ ->
          ctx
      end

    ctx
  end
end
