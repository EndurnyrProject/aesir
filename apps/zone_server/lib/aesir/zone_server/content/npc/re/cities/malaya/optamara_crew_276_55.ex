defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Malaya.OptamaraCrew27655 do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:65 (Optamara Crew#malaya).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "malaya",
        x: 276,
        y: 55,
        dir: 4,
        sprite: 100,
        name: "Optamara Crew",
        unique_name: "Optamara Crew#malaya"
      }
    ]

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx
        |> mes("[Optamara Crew]")
        |> mes(
          "I think the village is still busy. Done with your visit? There is always a ship heading for Alberta."
        )
        |> next()
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx
            |> mes("[Optamara Crew]")
            |> mes(
              "I think a nameless adventurer helped take a load off the villagers' minds. Yahoo! Would you like to go back to Alberta?"
            )
            |> next()
          else
            ctx
            |> mes("[Optamara Crew]")
            |> mes("Would you like to go back to Alberta with me?")
            |> next()
          end

        ctx
      end

    {ctx, v1} = select(ctx, ["Return.", "Do not return."])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Optamara Crew]")
            |> mes("Great! Let's leave now for Alberta!!")
            |> close()
            |> warp("alberta", 192, 217)

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Optamara Crew]")
            |> mes("That's okay. Come here if you ever want to go back to Alberta.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
