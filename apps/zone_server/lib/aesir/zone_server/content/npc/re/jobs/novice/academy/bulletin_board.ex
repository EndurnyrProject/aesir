defmodule Aesir.ZoneServer.Content.Npc.Re.Jobs.Novice.Academy.BulletinBoard do
  @moduledoc """
  Transpiled from rAthena re/jobs/novice/academy.txt:1921 (Bulletin Board#iz).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "izlude",
        x: 45,
        y: 94,
        dir: 3,
        sprite: 858,
        name: "Bulletin Board",
        unique_name: "Bulletin Board#iz",
        trigger: {3, 3}
      },
      %{
        map: "izlude_a",
        x: 45,
        y: 94,
        dir: 3,
        sprite: 858,
        name: "Bulletin Board",
        unique_name: "Bulletin Board#iz_a",
        trigger: {3, 3}
      },
      %{
        map: "izlude_b",
        x: 45,
        y: 94,
        dir: 3,
        sprite: 858,
        name: "Bulletin Board",
        unique_name: "Bulletin Board#iz_b",
        trigger: {3, 3}
      },
      %{
        map: "izlude_c",
        x: 45,
        y: 94,
        dir: 3,
        sprite: 858,
        name: "Bulletin Board",
        unique_name: "Bulletin Board#iz_c",
        trigger: {3, 3}
      },
      %{
        map: "izlude_d",
        x: 45,
        y: 94,
        dir: 3,
        sprite: 858,
        name: "Bulletin Board",
        unique_name: "Bulletin Board#iz_d",
        trigger: {3, 3}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnInit", ctx) do
    ev_oninit(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx =
      set_local(
        ctx,
        :total,
        isbegin_quest(ctx, 7475) + isbegin_quest(ctx, 7476) + isbegin_quest(ctx, 7477)
      )

    if isbegin_quest(ctx, 7474) == 1 do
      if not Rathena.truthy?(isbegin_quest(ctx, 7477)) do
        ctx =
          ctx
          |> mes("==== Information ====")
          |> mes("Prontera Field.")
          |> mes("The city of Prontera is located to the north.")
          |> next()

        if get_local(ctx, :total, 0) == 1 do
          {ctx, _} =
            ctx
            |> mes("Except for the simple signboard, nothing can be found here.")
            |> next()
            |> select(["Where's the treasure?"])

          ctx =
            ctx
            |> mes("[Lumin]")
            |> mes("Yo.")
            |> mes("Sorry but I am quicker than you.")
            |> mes("There were Fly Wings here..")
            |> cutin("nov_lumin01.bmp", 2)
            |> next()
            |> mes("[Lumin]")
            |> mes("In fact, I don't like Fly's wings.")
            |> mes("Flies are so dirty.")
            |> cutin("nov_lumin02.bmp", 2)
            |> next()
            |> mes("[Lumin]")
            |> mes("By the way, why is one of the convenient transportations fly wing shaped?")
            |> mes("Perhaps you know?")
            |> next()
            |> mes("[Lumin]")
            |> mes("...you don't know.")
            |> cutin("nov_lumin01.bmp", 2)
            |> next()
            |> cutin("", 255)
            |> mes("Defeated by Lumin again.")
            |> mes("Keep calm and find the next location.")
            |> setquest(7477)
            |> viewpoint(2, 45, 94, 2, 16_711_680)
            |> close()

          throw({:script_end, ctx})
        else
          if get_local(ctx, :total, 0) > 1 do
            {ctx, v1} =
              ctx
              |> mes("[Lumin]")
              |> mes(".......")
              |> cutin("nov_lumin01.bmp", 2)
              |> next()
              |> mes("[Lumin]")
              |> mes("...Hello")
              |> cutin("nov_lumin03.bmp", 2)
              |> next()
              |> select(["Are you kidding me?!", "Where's the treasure?"])

            ctx =
              if v1 == 1 do
                {ctx, _} =
                  ctx
                  |> mes("[Lumin]")
                  |> mes("No...")
                  |> mes("Meeting three times in one day must be destiny.")
                  |> mes("You and I can be friends.")
                  |> cutin("nov_lumin05.bmp", 2)
                  |> next()
                  |> select(["Or Frenemies!"])

                ctx =
                  ctx
                  |> mes("[Lumin]")
                  |> mes("Frene-what? Oh well I'm done with the mission.")
                  |> mes("Take care.")
                  |> cutin("nov_lumin02.bmp", 2)
                  |> next()
                  |> cutin("", 255)

                ctx =
                  ctx
                  |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
                  |> mes("Are you even listening to me?!")
                  |> setquest(7477)
                  |> viewpoint(2, 45, 94, 2, 16_711_680)
                  |> next()
                  |> mes(
                    "^4d4dff...I have to report the result to the Information Staff...^000000."
                  )
                  |> close()

                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              ctx
              |> mes("[Lumin]")
              |> mes("Fly Wings.")
              |> mes(
                "If I hand them over to you, you'll just waste 'em so I think I'll keep them."
              )
              |> cutin("nov_lumin01.bmp", 2)
              |> next()
              |> mes("[Lumin]")
              |> mes("Come on this is just friendly competition.")
              |> cutin("nov_lumin05.bmp", 2)
              |> next()
              |> mes("[Lumin]")
              |> mes("I am doing this for you to make you stronger.")
              |> mes("Now, so long.")
              |> cutin("nov_lumin03.bmp", 2)
              |> next()
              |> cutin("", 255)
              |> mes("^4d4dff...I have to report of the result to Information staff...^000000.")
              |> setquest(7477)
              |> viewpoint(2, 45, 94, 2, 16_711_680)
              |> close()

            throw({:script_end, ctx})
          else
            {ctx, _} =
              ctx
              |> mes("Except for the simple signboard, nothing can be found.")
              |> next()
              |> select(["Why is there nothing?"])

            {ctx, _} =
              ctx
              |> mes("[Lumin]")
              |> mes("Of course.")
              |> mes("Fly's wings were here so I already packed them in my bag.")
              |> cutin("nov_lumin01.bmp", 2)
              |> next()
              |> select(["So can I have some?"])

            ctx =
              ctx
              |> mes("[Lumin]")
              |> mes("That's not fair, I was here first so they're mine.")
              |> mes("You better get to the next stop faster if you want the prize.")
              |> cutin("nov_lumin03.bmp", 2)
              |> next()
              |> mes("[Lumin]")
              |> mes("Well, then.....")
              |> next()
              |> cutin("", 255)
              |> mes("Lumin disappeared with his long hair in the air.")
              |> mes("This guy....")
              |> setquest(7477)
              |> viewpoint(2, 45, 94, 2, 16_711_680)
              |> close()

            throw({:script_end, ctx})
          end
        end
      else
        ctx =
          ctx
          |> mes("==== Information ====")
          |> mes("Prontera Field.")
          |> mes("The city of Prontera is located to the north.")
          |> close()

        throw({:script_end, ctx})
      end
    else
      ctx =
        ctx
        |> mes("==== Information ====")
        |> mes("Prontera Field.")
        |> mes("The city of Prontera is located to the north.")
        |> close()

      throw({:script_end, ctx})
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_oninit(ctx) do
    questinfo(ctx, 0, 0, fn ctx ->
      not Rathena.truthy?(isbegin_quest(ctx, 7477)) and isbegin_quest(ctx, 7474) == 1
    end)
  end

  def ev_ontouch(ctx) do
    ctx =
      if isbegin_quest(ctx, 7474) == 1 and not Rathena.truthy?(isbegin_quest(ctx, 7477)) do
        emotion(ctx, :surprise)
      else
        ctx
      end

    ctx
  end
end
