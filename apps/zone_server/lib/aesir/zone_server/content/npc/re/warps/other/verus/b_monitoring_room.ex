defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.Verus.BMonitoringRoom do
  @moduledoc """
  Transpiled from rAthena re/warps/other/verus.txt:221 (b_Monitoring room#L-1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "un_bk_q",
        x: 69,
        y: 167,
        dir: 0,
        sprite: 45,
        name: "b_Monitoring room",
        unique_name: "b_Monitoring room#L-1",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx = todo(ctx, :callfunc, ["F_time_limit_recorder", get_local(ctx, :cooldown_status_day, 0)])

    ctx =
      if Enum.at(get_local(ctx, :cooldown_status_day, []), 0, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 1, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 2, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 3, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 4, 0) == 0 do
        ctx =
          ctx
          |> mes("[Bunker Sayhu]")
          |> mes(
            "Hey!! Where do you think you're going when we're trying to get a headcount? Get over here."
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "un_bk_q", 57, 168)
  end
end
