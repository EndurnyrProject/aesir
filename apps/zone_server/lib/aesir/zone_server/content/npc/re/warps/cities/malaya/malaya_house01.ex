defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Cities.Malaya.MalayaHouse01 do
  @moduledoc """
  Transpiled from rAthena re/warps/cities/malaya.txt:81 (malaya_house01).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "malaya",
        x: 300,
        y: 211,
        dir: 0,
        sprite: 45,
        name: "malaya_house01",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx = ctx |> mes("Door is closed. It seems to be keeping its eyes on you.") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "ma_in01", 36, 152)
  end
end
