defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Dungeons.IzDun.Gatekeeper do
  @moduledoc """
  Transpiled from rAthena re/warps/dungeons/iz_dun.txt:19 (Gatekeeper#iz_dun).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "iz_dun04",
        x: 130,
        y: 234,
        dir: 5,
        sprite: 413,
        name: "Gatekeeper",
        unique_name: "Gatekeeper#iz_dun"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Gatekeeper]")
      |> mes("Halt!")
      |> mes("What brings you here, Adventurer?")
      |> mes("We've discovered a new floor of this underwater city.")
      |> next()
      |> mes("[Gatekeeper]")
      |> mes("Do you think you're up to the challenge?")
      |> mes("The monsters here are between levels 110 and 124.")
      |> mes("And you must beware of the Kraken!")
      |> next()
      |> select(["Release the Kraken!", "I'll stay here."])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Gatekeeper]")
            |> mes("Be cautious and good luck.")
            |> mes(
              "If you see the Kraken be sure you have your friends to help or do what we do and run."
            )
            |> close()
            |> warp("iz_dun05", 141, 187)

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Gatekeeper]")
            |> mes("It's always best to be prepared before exploring a new area.")
            |> mes("Come back when you're ready to go.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
