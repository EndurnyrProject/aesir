defmodule Aesir.ZoneServer.Content.Npc.Re.Jobs.Novice.Academy.Npc90208 do
  @moduledoc """
  Transpiled from rAthena re/jobs/novice/academy.txt:9493 (#acmageguide).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "gef_fild07",
        x: 90,
        y: 208,
        dir: 6,
        sprite: 111,
        name: "",
        unique_name: "#acmageguide",
        trigger: {7, 7}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if isbegin_quest(ctx, 9264) == 1 do
        ctx =
          ctx
          |> mes("[Magician's Guild Guide]")
          |> mes("It seems that you've come from Criatura Academy.")
          |> next()
          |> mes("[Magician's Guild Guide]")
          |> mes("You look a little exhausted...")
          |> mes("Though.. to become a magician is exhausting in a way...")
          |> next()
          |> mes("[Magician's Guild Guide]")
          |> mes("First off, my task is to guide you to the Mage's guild!")
          |> mes("Go straight to the right!")
          |> mes(
            "After ^8B4513crossing the bridge^000000, you will see the entrance to ^FF0000Geffen^000000!"
          )
          |> next()
          |> mes("[Magician's Guild Guide]")
          |> mes("Once there,")
          |> mes(
            "You should find our <NAVI>^4D4DFF[Magician's Guild Guide]^000000<INFO>geffen,43,123,</INFO></NAVI>."
          )
          |> mes("Speak to him, he will continue to guide you to the Magician's Guild!")
          |> set_char_var(:nov_3_magician, 2)
          |> completequest(9264)
          |> erasequest(9264)
          |> setquest(9265)
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
  end
end
