defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.BaMaison.Npc do
  @moduledoc """
  Transpiled from rAthena re/warps/other/ba_maison.txt:7 (#ep172_lab1_out).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "ba_in01",
        x: 12,
        y: 321,
        dir: 0,
        sprite: 45,
        name: "",
        unique_name: "#ep172_lab1_out",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :ep17_2_main, 0) < 7 do
        ctx =
          ctx
          |> mes("There is still some work to be done here. Let's look around more.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "ba_maison", 75, 146)
  end
end
