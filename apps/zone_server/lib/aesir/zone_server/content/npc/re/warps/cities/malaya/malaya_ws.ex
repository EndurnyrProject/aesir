defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Cities.Malaya.MalayaWs do
  @moduledoc """
  Transpiled from rAthena re/warps/cities/malaya.txt:48 (malaya_ws).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "malaya", x: 112, y: 212, dir: 0, sprite: 45, name: "malaya_ws", trigger: {1, 1}}
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 20 do
        ctx = ctx |> mes("Door is closed. It seems to be keeping its eyes on you.") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "ma_in01", 83, 16)
  end
end
