defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.Verus.Clinic do
  @moduledoc """
  Transpiled from rAthena re/warps/other/verus.txt:470 (Clinic#G-1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "un_bk_q",
        x: 269,
        y: 236,
        dir: 0,
        sprite: 45,
        name: "Clinic",
        unique_name: "Clinic#G-1",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx = todo(ctx, :callfunc, ["F_time_limit_recorder", get_local(ctx, :cooldown_status_day, 0)])

    ctx =
      if Enum.at(get_local(ctx, :cooldown_status_day, []), 0, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 1, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 2, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 3, 0) == 0 and
           Enum.at(get_local(ctx, :cooldown_status_day, []), 4, 0) == 0 do
        ctx =
          ctx
          |> emotion(6)
          |> mes("[Nurse Lapplad]")
          |> mes(
            "Look here?! Where are you trying to go? We have to do some tests so don't leave and wait here!!"
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx =
      if isbegin_quest(ctx, 5364) + isbegin_quest(ctx, 5365) == 1 do
        ctx =
          ctx
          |> emotion(6)
          |> mes("[Nurse Lapplad]")
          |> mes("Hello? You have to deal with it before you can go? Where do you want to go!?")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "un_bk_q", 255, 235)
  end
end
