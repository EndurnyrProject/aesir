defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Cities.Prontera.LibQToPprt do
  @moduledoc """
  Transpiled from rAthena re/warps/cities/prontera.txt:11 (lib_q_to_pprt).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "prt_lib_q", x: 9, y: 25, dir: 0, sprite: 45, name: "lib_q_to_pprt", trigger: {1, 1}}
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :ep16_royal, 0) < 21 do
        ctx = ctx |> mes("There are more to find out.") |> mes("Go back to Nillem.") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "prt_q", 155, 354)
  end
end
