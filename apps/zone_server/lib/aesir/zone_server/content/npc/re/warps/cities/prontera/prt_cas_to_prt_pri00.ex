defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Cities.Prontera.PrtCasToPrtPri00 do
  @moduledoc """
  Transpiled from rAthena re/warps/cities/prontera.txt:95 (prt_cas_to_prt_pri00).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prt_cas",
        x: 188,
        y: 258,
        dir: 0,
        sprite: 45,
        name: "prt_cas_to_prt_pri00",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :ep16_royal, 0) >= 2 do
        warp(ctx, "prt_pri00", 54, 134)
      else
        ctx =
          npctalk(ctx, "You shouldn't enter the place like that.",
            npc: "Guardian Knight#ep16_prigate01",
            target: :self
          )

        ctx = ctx |> todo(:setpcblock, [get_char_var(ctx, :PCBLOCK_NPC, 0), 1]) |> sleep2(2000)

        ctx
        |> todo(:setpcblock, [get_char_var(ctx, :PCBLOCK_NPC, 0), 0])
        |> warp("prt_cas", 182, 258)
      end

    ctx
  end
end
