defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Dungeons.MagDun.Mag02aMag03a do
  @moduledoc """
  Transpiled from rAthena re/warps/dungeons/mag_dun.txt:12 (mag02a_mag03a).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "mag_dun02",
        x: 247,
        y: 68,
        dir: 0,
        sprite: 45,
        name: "mag02a_mag03a",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if base_level(ctx) < 175 do
        ctx =
          npctalk(
            ctx,
            "Republic Guard: A dangerous place for adventurers. You must be at least level 175 to enter.",
            npc: "Republic Guard#mag03",
            target: :self
          )

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "mag_dun03", 120, 120)
  end
end
