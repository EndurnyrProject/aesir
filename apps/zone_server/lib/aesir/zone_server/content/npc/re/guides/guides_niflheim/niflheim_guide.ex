defmodule Aesir.ZoneServer.Content.Npc.Re.Guides.GuidesNiflheim.NiflheimGuide do
  @moduledoc """
  Transpiled from rAthena re/guides/guides_niflheim.txt:18 (Niflheim Guide#01nif).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "niflheim",
        x: 201,
        y: 187,
        dir: 3,
        sprite: 849,
        name: "Niflheim Guide",
        scope: :renewal,
        unique_name: "Niflheim Guide#01nif"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx =
      ctx
      |> mes("[Niflheim Guide]")
      |> mes("Welcome to.. ^8B4513Niflheim^000000...")
      |> mes("the Realm of the Dead...")
      |> mes("Do you need help navigating the realm...?")

    ctx =
      if Todo.call!(:F_Navi, []) == 0 do
        ctx
        |> next()
        |> mes("[Niflheim Guide]")
        |> mes(
          "When you click on the.. ^B9062F[location name]^000000... you'll receive the most advanced ^B9062FNavigation^000000 services...!"
        )
      else
        ctx
      end

    loop_1(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp s_l_mark(ctx, args) do
    ctx =
      ctx
      |> mes("[Niflheim Guide]")
      |> mes("Let me mark the location of the")
      |> mes(Rathena.concat(Enum.at(args, 0, 0), "^000000..."))
      |> mes("on your mini-map.")
      |> mes("Would you like to check any other locations?")

    {ctx, nil}
  end

  defp loop_1(ctx) do
    {ctx, v1} =
      ctx
      |> next()
      |> select([
        "[ Tool Shop ]",
        "[ Pub ]",
        "[ Weapon Shop ]",
        "[ Witch's Castle ]",
        "Remove Marks from Mini-Map",
        "Cancel"
      ])

    ctx =
      case v1 do
        1 ->
          {ctx, _} =
            s_l_mark(ctx, [Todo.call!(:F_Navi, ["Tool Shop", "niflheim,218,196", "^0000FF"])])

          viewpoint(ctx, 1, 218, 196, 0, 688_895)

        2 ->
          {ctx, _} = s_l_mark(ctx, [Todo.call!(:F_Navi, ["Pub", "niflheim,189,208", "^006400"])])
          viewpoint(ctx, 1, 189, 208, 1, 11_206_400)

        3 ->
          {ctx, _} =
            s_l_mark(ctx, [Todo.call!(:F_Navi, ["Weapon Shop", "niflheim,218,170", "^800080"])])

          viewpoint(ctx, 1, 218, 170, 2, 14_315_734)

        4 ->
          {ctx, _} =
            s_l_mark(ctx, [Todo.call!(:F_Navi, ["Witch's Castle", "niflheim,255,195", "^FF1493"])])

          viewpoint(ctx, 1, 255, 195, 3, 16_716_947)

        5 ->
          ctx
          |> mes("[Niflheim Guide]")
          |> mes("Sure, I'll remove all marks from your mini-map...")
          |> mes("Is there anything else I can do for you...?")
          |> viewpoint(2, 1, 1, 0, 16_777_215)
          |> viewpoint(2, 1, 1, 1, 16_777_215)
          |> viewpoint(2, 1, 1, 2, 16_777_215)
          |> viewpoint(2, 1, 1, 3, 16_777_215)

        6 ->
          ctx = ctx |> mes("[Niflheim Guide]") |> mes("......Bye~!..") |> close()
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    loop_1(ctx)
  end
end
