defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dicastes.FavorableKnit do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:561 (Favorable Knit#a).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "dicastes01",
        x: 284,
        y: 262,
        dir: 3,
        sprite: 453,
        name: "Favorable Knit",
        unique_name: "Favorable Knit#a"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Favorable Knit]")

    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        ctx =
          ctx
          |> mes("Unlike the Laphine, we Saphas")
          |> mes("are a very close community.")
          |> mes("We start by living together in common facilities after we are born.")
          |> mes("That is why all Saphas are like family.")
          |> mes("We never have any quarrels between classes.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("?????? ?? ?????? ??")
    |> mes("?????? ?? ?????? ?? ??")
    |> mes("?????? ?? ?????? ?? ??????")
    |> mes("?????? ?? ?????? ?? ????")
    |> mes("?????? ?? ?????? ?? ?????? ????")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
