defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Cities.Prontera.QroomNeriusInQroomNeriusOut do
  @moduledoc """
  Transpiled from rAthena re/warps/cities/prontera.txt:64 (qroom_nerius_in_qroom_nerius_out).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prt_cas_q",
        x: 130,
        y: 56,
        dir: 0,
        sprite: 45,
        name: "qroom_nerius_in_qroom_nerius_out",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :ep16_royal, 0) > 2 and get_char_var(ctx, :ep16_royal, 0) < 15 and
           get_char_var(ctx, :ep16_royal, 0) != 7 do
        ctx = ctx |> mes("It is locked.") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx =
      if get_char_var(ctx, :ep16_royal, 0) == 15 do
        ctx =
          if isbegin_quest(ctx, 7697) < 1 do
            ctx =
              ctx
              |> mes("Let's first reply to the servant that the invitation will be accepted.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      else
        ctx
      end

    warp(ctx, "prt_cas_q", 30, 128)
  end
end
