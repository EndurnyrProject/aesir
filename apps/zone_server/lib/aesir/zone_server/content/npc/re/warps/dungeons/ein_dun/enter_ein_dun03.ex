defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Dungeons.EinDun.EnterEinDun03 do
  @moduledoc """
  Transpiled from rAthena re/warps/dungeons/ein_dun.txt:7 (enter_ein_dun03).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "ein_dun02",
        x: 16,
        y: 250,
        dir: 0,
        sprite: 45,
        name: "enter_ein_dun03",
        trigger: {1, 1}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if base_level(ctx) < 180 do
        ctx =
          todo(ctx, :unittalk, [
            getcharid(ctx, 3),
            Rathena.concat(
              Rathena.concat("", char_name(ctx, 0)),
              " : I think the next area needs a bit more training..."
            ),
            3
          ])

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "ein_dun03", 268, 279)
  end
end
