defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.DimensionalGap.TransportDevice do
  @moduledoc """
  Transpiled from rAthena re/warps/other/dimensional_gap.txt:9 (Transport Device#01).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "mid_camp",
        x: 210,
        y: 292,
        dir: 4,
        sprite: 10007,
        name: "Transport Device",
        unique_name: "Transport Device#01"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx |> mes("Do you wish to enter the Dimensional Gap?") |> next() |> select(["Yes", "No"])

    ctx =
      case v1 do
        1 ->
          ctx = ctx |> close() |> warp("dali", 141, 82)
          throw({:script_end, ctx})

        2 ->
          ctx = close(ctx)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
