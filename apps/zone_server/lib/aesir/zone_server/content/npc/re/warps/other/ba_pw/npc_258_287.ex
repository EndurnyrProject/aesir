defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.BaPw.Npc258287 do
  @moduledoc """
  Transpiled from rAthena re/warps/other/ba_pw.txt:35 (#ep172_cor_to_pw2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "sp_cor",
        x: 258,
        y: 287,
        dir: 0,
        sprite: 45,
        name: "",
        unique_name: "#ep172_cor_to_pw2",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :ep17_2_main, 0) < 2 do
        ctx =
          npctalk(
            ctx,
            "Rookie : Oh? I haven't opened it yet! Hold on for a while, we're doing our best to open it!",
            npc: "Rookie#rm171_2",
            target: :self
          )

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "ba_pw02", 275, 20)
  end
end
