defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.DimensionalGap.TransportDevice222200 do
  @moduledoc """
  Transpiled from rAthena re/warps/other/dimensional_gap.txt:22 (Transport Device#02).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "moc_fild22b",
        x: 222,
        y: 200,
        dir: 4,
        sprite: 10007,
        name: "Transport Device",
        unique_name: "Transport Device#02"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx |> mes("Do you wish to enter the Dimensional Gap?") |> next() |> select(["Yes", "No"])

    ctx =
      case v1 do
        1 ->
          ctx = ctx |> close() |> warp("dali", 119, 56)
          throw({:script_end, ctx})

        2 ->
          ctx = close(ctx)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
