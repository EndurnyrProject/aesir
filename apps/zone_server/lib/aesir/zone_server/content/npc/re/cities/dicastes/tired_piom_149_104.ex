defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Dicastes.TiredPiom149104 do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:297 (Tired Piom#b).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "dic_in01",
        x: 149,
        y: 104,
        dir: 5,
        sprite: 455,
        name: "Tired Piom",
        unique_name: "Tired Piom#b"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Tired Piom]")

    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        ctx =
          ctx
          |> mes("I feel so stiff...")
          |> mes("I will need another dose of Bradium.")
          |> mes("I think the effects are weaker than before,")
          |> mes("or is it just me?")
          |> emotion(:question)
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("???Ð¡? ?? ??")
    |> mes("?????? ?? ?????? ?? ??????")
    |> mes("?????? ?? ?????? ?? ????")
    |> mes("?????? ??")
    |> emotion(:question)
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
