defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Mora.BananaRafflesia do
  @moduledoc """
  Transpiled from rAthena re/cities/mora.txt:332 (Banana Rafflesia#ep14_1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "mora",
        x: 99,
        y: 65,
        dir: 6,
        sprite: 516,
        name: "Banana Rafflesia",
        scope: :renewal,
        unique_name: "Banana Rafflesia#ep14_1"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Banana Rafflesia]")
      |> mes("You want a banana?")
      |> next()
      |> select(["Yes, I do.", "No, thanks."])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Banana Rafflesia]")
            |> mes("Take it if you can.")
            |> mes("If you are successful, you can have it for free.")
            |> emotion(:ok)
            |> close()

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Banana Rafflesia]")
            |> mes("Well, I was going to give it for free,")
            |> mes("but if you don't want it, it's fine with me.")
            |> emotion(:hng)
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
