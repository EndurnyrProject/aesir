defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.Trainingzone123.TrainingInstructor do
  @moduledoc """
  Transpiled from rAthena re/warps/other/TrainingZone123.txt:10 (Training Instructor#prontera_in).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prontera",
        x: 246,
        y: 62,
        dir: 3,
        sprite: 105,
        name: "Training Instructor",
        unique_name: "Training Instructor#prontera_in"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Training Instructor]")
      |> mes(
        "The royal court has set up a training ground for adventurers who wish to test their skills. If you wish to test your skills under various conditions, we will send you to the training ground."
      )
      |> next()
      |> select([
        "??Enter Basic Training Ground",
        "??Enter 1/10 Training Ground",
        "??Enter 1/100 Training Ground",
        "??Cancel"
      ])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Training Instructor]")
            |> mes("I'll send you to the Basic Training Ground.")
            |> close()
            |> set_local(:x, Enum.random(109..110))
            |> set_local(:y, Enum.random(93..94))

          warp(ctx, "tra_fild01", get_local(ctx, :x, 0), get_local(ctx, :y, 0))

        2 ->
          ctx
          |> mes("[Training Instructor]")
          |> mes("I'll send you to the 1/10 Training Ground.")
          |> close()
          |> set_local(:x, Enum.random(109..110))
          |> set_local(:y, Enum.random(93..94))
          |> warp("tra_fild02", 109, 94)

        3 ->
          ctx =
            ctx
            |> mes("[Training Instructor]")
            |> mes("I'll send you to the 1/100 Training Ground.")
            |> close()
            |> set_local(:x, Enum.random(109..110))
            |> set_local(:y, Enum.random(93..94))

          warp(ctx, "tra_fild03", get_local(ctx, :x, 0), get_local(ctx, :y, 0))

        4 ->
          ctx

        _ ->
          ctx
      end

    ctx
  end
end
