defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.Trainingzone.TrainingInstructor11179 do
  @moduledoc """
  Transpiled from rAthena re/warps/other/TrainingZone.txt:45 (Training Instructor#sol3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "tra_fild",
        x: 111,
        y: 79,
        dir: 3,
        sprite: 105,
        name: "Training Instructor",
        unique_name: "Training Instructor#sol3"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Training Instructor]")
      |> mes("Are you sure you want to move to the upper section?")
      |> next()
      |> select(["Move", "Do not move", "Warp to Prontera."])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Training Instructor]")
            |> mes("Go to upper section!!!")
            |> close()
            |> warp("tra_fild", 112, 167)

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Training Instructor]")
            |> mes("Talk to me if you need to move.")
            |> close()

          throw({:script_end, ctx})

        3 ->
          ctx =
            ctx
            |> mes("[Training Instructor]")
            |> mes("I'll warp you.")
            |> close()
            |> warp("prontera", 246, 60)

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
