defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.Verus.C0 do
  @moduledoc """
  Transpiled from rAthena re/warps/other/verus.txt:403 (C-0#Farm).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "un_bk_q",
        x: 216,
        y: 300,
        dir: 0,
        sprite: 45,
        name: "C-0",
        unique_name: "C-0#Farm",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx = todo(ctx, :callfunc, ["F_time_limit_recorder", get_local(ctx, :cooldown_status_day, 0)])

    ctx =
      if isbegin_quest(ctx, 5359) + isbegin_quest(ctx, 5360) + isbegin_quest(ctx, 5361) +
           isbegin_quest(ctx, 5362) != 1 do
        ctx =
          ctx
          |> mes("[Bioengineer Touring]")
          |> mes("Wait a minute. You cannot enter the farm right now.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "un_bk_q", 216, 345)
  end
end
