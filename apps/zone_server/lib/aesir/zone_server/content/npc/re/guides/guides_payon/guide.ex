defmodule Aesir.ZoneServer.Content.Npc.Re.Guides.GuidesPayon.Guide do
  @moduledoc """
  Transpiled from rAthena re/guides/guides_payon.txt:11 (Guide#01payon::GuidePayon).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "payon",
        x: 160,
        y: 67,
        dir: 3,
        sprite: 708,
        name: "Guide",
        scope: :renewal,
        unique_name: "GuidePayon"
      },
      %{
        map: "payon",
        x: 151,
        y: 182,
        dir: 6,
        sprite: 708,
        name: "Guide",
        scope: :renewal,
        unique_name: "Guide#02payon"
      },
      %{
        map: "payon",
        x: 221,
        y: 85,
        dir: 6,
        sprite: 708,
        name: "Guide",
        scope: :renewal,
        unique_name: "Guide#03payon"
      },
      %{
        map: "payon",
        x: 233,
        y: 324,
        dir: 4,
        sprite: 708,
        name: "Guide",
        scope: :renewal,
        unique_name: "Guide#04payon"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    ctx
    |> cutin("pay_soldier", 2)
    |> mes("[Payon Guide]")
    |> mes("Welcome to ^8B4513Payon^000000,")
    |> mes("the Mountain Village.")
    |> mes("Do you need help navigating the village?")
    |> todo(:F_Navi, ["[Payon Guide]"])
    |> next()
    |> loop_1()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp s_l_mark(ctx, args) do
    ctx =
      ctx
      |> mes("[Payon Guide]")
      |> mes("Let me mark the location of the")
      |> mes(to_string(Enum.at(args, 0, 0)))
      |> mes("on your mini-map.")
      |> mes("Would you like to check any other locations?")

    {ctx, nil}
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop, 0)) do
      {ctx, v2} =
        select(ctx, [
          "[ Archer Village ]",
          "[ Tool Shop ]",
          "[ Weapon & Armor Shop ]",
          "[ Inn & Tavern ]",
          "[ Central Palace ]",
          "[ Palace Court ]",
          "[ Royal Kitchen ]",
          "Previous Menu"
        ])

      ctx =
        case v2 do
          1 ->
            ctx
            |> mes("[Payon Guide]")
            |> mes(
              Rathena.concat(
                Todo.call!(:F_Navi, ["Archer Village", "payon,227,328", "^B9062F"]),
                " is where"
              )
            )
            |> mes("the Archer Association resides.")
            |> mes("Please use the far north gate to enter the village.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 227, 328, 0, 16_711_680)
            |> next()

          2 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Tool Shop", "payon,144,85", "^0000FF"])])

            ctx |> viewpoint(1, 144, 85, 1, 688_895) |> next()

          3 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Weapon & Armor Shop", "payon,139,159", "^FF5400"])
              ])

            ctx |> viewpoint(1, 139, 159, 2, 16_757_760) |> next()

          4 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Inn & Tavern", "payon,220,117", "^006400"])])

            ctx |> viewpoint(1, 220, 117, 3, 11_206_400) |> next()

          5 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Central Palace", "payon,155,245", "^7F3300"])])

            ctx |> viewpoint(1, 155, 245, 4, 13_789_470) |> next()

          6 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Palace Court", "payon,107,325", "^800080"])])

            ctx |> viewpoint(1, 107, 325, 5, 14_315_734) |> next()

          7 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Royal Kitchen", "payon,154,325", "^B9062F"])])

            ctx |> viewpoint(1, 154, 325, 6, 16_711_680) |> next()

          8 ->
            ctx = set_local(ctx, :loop, 0)
            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_5(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop, 0)) do
      {ctx, v3} =
        select(ctx, [
          "[ Eden Teleport Officer ]",
          "[ Kafra Shop ]",
          "[ Repair / Armsmith ]",
          "[ Kafra Employee ]",
          "[ Trap Specialist ]",
          "[ Seiayblem ]",
          "[ Leablem ]",
          "[ Jade ]",
          "[ Pet Groomer ]",
          "[ Chef Assistant ]",
          "Previous Menu"
        ])

      ctx =
        case v3 do
          1 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Eden Teleport Officer", "payon,177,111", "^0000FF"])
              ])

            ctx |> viewpoint(1, 177, 111, 7, 688_895) |> next()

          2 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Kafra Shop Girls", "payon,164,157", "^FF5400"])
              ])

            ctx |> viewpoint(1, 164, 157, 8, 16_757_760) |> next()

          3 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Repairman & Armsmith", "payon,144,170", "^006400"])
              ])

            ctx |> viewpoint(1, 144, 170, 9, 11_206_400) |> next()

          4 ->
            {ctx, _} = s_l_mark(ctx, ["^7F3300Kafra Employees^000000"])

            ctx
            |> viewpoint(1, 181, 104, 10, 13_789_470)
            |> viewpoint(1, 175, 226, 11, 13_789_470)
            |> next()

          5 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Trap Specialist", "payon,123,109", "^800080"])])

            ctx |> viewpoint(1, 123, 109, 12, 14_315_734) |> next()

          6 ->
            ctx
            |> mes("[Payon Guide]")
            |> mes(to_string(Todo.call!(:F_Navi, ["Seiyablem", "payon,140,151", "^B9062F"])))
            |> mes("is a technician")
            |> mes("who can create ^0000FFslots^000000 on weapons and armors,")
            |> mes("but not all equipment")
            |> mes("can be slotted. Well, that's what he says.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 140, 151, 13, 16_711_680)
            |> next()

          7 ->
            ctx
            |> mes("[Payon Guide]")
            |> mes(to_string(Todo.call!(:F_Navi, ["Leablem", "payon,236,199", "^0000FF"])))
            |> mes("is a technician")
            |> mes("who can create ^0000FFslots^000000 on weapons and armors,")
            |> mes("but not all equipment")
            |> mes("can be slotted. Well, that's what he says.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 236, 199, 14, 688_895)
            |> next()

          8 ->
            ctx
            |> mes("[Payon Guide]")
            |> mes(to_string(Todo.call!(:F_Navi, ["Jade", "payon,173,238", "^FF5400"])))
            |> mes("can exchange different types of")
            |> mes("^FF5400Gemstones^000000.")
            |> mes("Would you like to check any other locations?")
            |> viewpoint(1, 173, 238, 15, 16_757_760)
            |> next()

          9 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Pet Groomer", "payon,177,131", "^006400"])])

            ctx |> viewpoint(1, 177, 131, 16, 11_206_400) |> next()

          10 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Chef Assistant", "payon,206,119", "^7F3300"])])

            ctx |> viewpoint(1, 206, 119, 17, 13_789_470) |> next()

          11 ->
            ctx = set_local(ctx, :loop, 0)
            ctx

          _ ->
            ctx
        end

      loop_5(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    {ctx, v1} =
      select(ctx, [
        "[ Main Facilities ]",
        "[ Merchants & Helpers ]",
        "Remove Marks from Mini-Map",
        "Cancel"
      ])

    ctx =
      case v1 do
        1 ->
          ctx |> set_local(:loop, 1) |> loop_3()

        2 ->
          ctx |> set_local(:loop, 1) |> loop_5()

        3 ->
          ctx
          |> mes("[Payon Guide]")
          |> mes("Sure, I'll remove all marks from your mini-map.")
          |> mes("Is there anything else I can do for you?")
          |> viewpoint(2, 1, 1, 0, 16_776_960)
          |> viewpoint(2, 1, 1, 1, 0)
          |> viewpoint(2, 1, 1, 2, 16_711_680)
          |> viewpoint(2, 1, 1, 3, 16_776_960)
          |> viewpoint(2, 1, 1, 4, 16_776_960)
          |> viewpoint(2, 1, 1, 5, 16_776_960)
          |> viewpoint(2, 1, 1, 6, 16_776_960)
          |> viewpoint(2, 1, 1, 7, 16_776_960)
          |> viewpoint(2, 1, 1, 8, 16_776_960)
          |> viewpoint(2, 1, 1, 9, 16_776_960)
          |> viewpoint(2, 1, 1, 10, 16_776_960)
          |> viewpoint(2, 1, 1, 11, 16_776_960)
          |> viewpoint(2, 1, 1, 12, 16_776_960)
          |> viewpoint(2, 1, 1, 13, 16_776_960)
          |> viewpoint(2, 1, 1, 14, 16_776_960)
          |> viewpoint(2, 1, 1, 15, 16_776_960)
          |> viewpoint(2, 1, 1, 16, 16_776_960)
          |> viewpoint(2, 1, 1, 17, 16_776_960)
          |> next()

        4 ->
          ctx =
            ctx |> mes("[Payon Guide]") |> mes("Enjoy your stay.") |> close() |> cutin("", 255)

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    loop_1(ctx)
  end
end
