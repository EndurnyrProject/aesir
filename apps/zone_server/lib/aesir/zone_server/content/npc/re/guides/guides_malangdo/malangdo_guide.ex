defmodule Aesir.ZoneServer.Content.Npc.Re.Guides.GuidesMalangdo.MalangdoGuide do
  @moduledoc """
  Transpiled from rAthena re/guides/guides_malangdo.txt:7 (Malangdo Guide#Malangdo).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :renewal,
    spawn: [
      %{
        map: "malangdo",
        x: 218,
        y: 101,
        dir: 3,
        sprite: 546,
        name: "Malangdo Guide",
        scope: :renewal,
        unique_name: "Malangdo Guide#Malangdo"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  alias Aesir.ZoneServer.Script.Todo

  @impl true
  def on_talk(ctx) do
    {ctx, _} =
      ctx
      |> mes("[Malangdo Guide]")
      |> mes("Nice to meet you, welcome to ^8C4210Malangdo^000000. The island of cat paw!")
      |> mes("Need some guidance?")
      |> todo(:F_Navi, ["[Malangdo Guide]"])
      |> loop_1()
      |> s_l_mark([])

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp s_l_mark(ctx, args) do
    ctx =
      ctx
      |> mes("[Malangdo Guide]")
      |> mes(Rathena.concat("I will show you the location of the ", Enum.at(args, 0, 0)))
      |> mes("Do you need other information?")

    {ctx, nil}
  end

  defp loop_3(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop, 0)) do
      {ctx, v2} =
        select(ctx, [
          "[ Cat Gamers Director ]",
          "[ Special Vending Machine ]",
          "[ Coin Exchanger ]",
          "[ Starry Coral ]",
          "Previous Menu"
        ])

      ctx =
        case v2 do
          1 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Cat Gamers Director", "malangdo,119,137", "FF0000"])
              ])

            ctx |> viewpoint(1, 119, 137, 0, 16_711_680) |> next()

          2 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Special Vending Machine", "malangdo,216,165", "FF4500"])
              ])

            ctx |> viewpoint(1, 216, 165, 1, 16_729_344) |> next()

          3 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Coin Exchanger", "malangdo,216,165", "DAA520"])
              ])

            ctx |> viewpoint(1, 216, 165, 2, 14_329_120) |> next()

          4 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Starry Coral", "malangdo,73,238", "7B68EE"])])

            ctx |> next() |> viewpoint(1, 73, 238, 3, 8_087_790)

          5 ->
            ctx = set_local(ctx, :loop, 0)
            ctx

          _ ->
            ctx
        end

      loop_3(ctx)
    else
      ctx
    end
  end

  defp loop_5(ctx) do
    if Rathena.truthy?(get_local(ctx, :loop, 0)) do
      {ctx, v3} =
        select(ctx, [
          "[ Chief Thomas ]",
          "[ Stinky Merchant ]",
          "[ Wandering Merchant ]",
          "[ Inn Owner ]",
          "[ Storekeeper ]",
          "[ Tool Dealer ]",
          "[ Portali ]",
          "[ Clink ]",
          "[ Holink ]",
          "[ Roving Merchant ]",
          "Previous Menu"
        ])

      ctx =
        case v3 do
          1 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Chief Thomas", "malangdo,164,163", "0A9696"])])

            ctx |> viewpoint(1, 164, 163, 4, 693_910) |> next()

          2 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Stinky Merchant", "malangdo,173,145", "4B0082"])
              ])

            ctx |> viewpoint(1, 173, 145, 5, 4_915_330) |> next()

          3 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Wandering Merchant", "malangdo,162,145", "FF4500"])
              ])

            ctx |> viewpoint(1, 162, 145, 6, 8_087_790) |> next()

          4 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Inn Owner", "malangdo,147,120", "FF6A00"])])

            ctx |> viewpoint(1, 147, 120, 7, get_char_var(ctx, :FF6A00, 0)) |> next()

          5 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Storekeeper", "malangdo,184,138", "7F3300"])])

            ctx |> viewpoint(1, 184, 138, 8, 8_336_128) |> next()

          6 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Tool Dealer", "malangdo,232,160", "7B68EE"])])

            ctx |> viewpoint(1, 232, 160, 9, 8_087_790) |> next()

          7 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Portali", "malangdo,190,167", "FF4500"])])

            ctx |> viewpoint(1, 190, 167, 10, 8_087_790) |> next()

          8 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Clink", "malangdo,225,174", "0A9696"])])

            ctx |> viewpoint(1, 225, 174, 11, 693_910) |> next()

          9 ->
            {ctx, _} =
              s_l_mark(ctx, [Todo.call!(:F_Navi, ["Holink", "malangdo,221,176", "DA70D6"])])

            ctx |> viewpoint(1, 221, 176, 12, 14_315_734) |> next()

          10 ->
            {ctx, _} =
              s_l_mark(ctx, [
                Todo.call!(:F_Navi, ["Roving Merchant", "malangdo,150,132", "3131FF"])
              ])

            ctx |> viewpoint(1, 150, 132, 13, 3_224_063) |> next()

          11 ->
            ctx = set_local(ctx, :loop, 0)
            ctx

          _ ->
            ctx
        end

      loop_5(ctx)
    else
      ctx
    end
  end

  defp loop_1(ctx) do
    {ctx, v1} =
      ctx
      |> next()
      |> select(["[ Major Facilities ]", "[ Trader & Assistant ]", "Clear Mini-Map", "Cancel"])

    ctx =
      case v1 do
        1 ->
          ctx |> set_local(:loop, 1) |> loop_3()

        2 ->
          ctx |> set_local(:loop, 1) |> loop_5()

        3 ->
          ctx
          |> mes("[Malangdo Guide]")
          |> mes("I will remove all marks from your Mini-Map.")
          |> mes("Need more help?")
          |> viewpoint(2, 1, 1, 0, 16_777_215)
          |> viewpoint(2, 1, 1, 1, 16_777_215)
          |> viewpoint(2, 1, 1, 2, 16_777_215)
          |> viewpoint(2, 1, 1, 3, 16_777_215)
          |> viewpoint(2, 1, 1, 4, 16_777_215)
          |> viewpoint(2, 1, 1, 5, 16_777_215)
          |> viewpoint(2, 1, 1, 6, 16_777_215)
          |> viewpoint(2, 1, 1, 7, 16_777_215)
          |> viewpoint(2, 1, 1, 8, 16_777_215)
          |> viewpoint(2, 1, 1, 9, 16_777_215)
          |> viewpoint(2, 1, 1, 10, 16_777_215)
          |> viewpoint(2, 1, 1, 11, 16_777_215)
          |> viewpoint(2, 1, 1, 12, 16_777_215)
          |> viewpoint(2, 1, 1, 13, 16_777_215)

        4 ->
          ctx = ctx |> mes("[Malangdo Guide]") |> mes("I wish you a pleasant trip.") |> close()
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    loop_1(ctx)
  end
end
