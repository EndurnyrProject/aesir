defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.Trainingzone.TrainingInstructor do
  @moduledoc """
  Transpiled from rAthena re/warps/other/TrainingZone.txt:9 (Training Instructor#sol1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prontera",
        x: 246,
        y: 62,
        dir: 3,
        sprite: 105,
        name: "Training Instructor",
        unique_name: "Training Instructor#sol1"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Training Instructor]")
      |> mes("If you want to train in a wider area, I'll let you in.")
      |> next()
      |> mes("[Training Instructor]")
      |> mes("The training ground is divided into two main areas.")
      |> next()
      |> mes("[Training Instructor]")
      |> mes("Upper area where you can check the applied force based on monster size.")
      |> next()
      |> mes("[Training Instructor]")
      |> mes(
        "The lower area where you can check the force applied depending on the monster's attribute and form."
      )
      |> next()
      |> mes("[Training Instructor]")
      |> mes("Where would you like to enter?")
      |> next()
      |> select(["Upper area", "Lower area", "Do not enter"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Training Instructor]")
            |> mes("Then I will guide you.")
            |> close()
            |> warp("tra_fild", 112, 167)

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Training Instructor]")
            |> mes("Then I will guide you.")
            |> close()
            |> warp("tra_fild", 111, 74)

          throw({:script_end, ctx})

        3 ->
          ctx =
            ctx
            |> mes("[Training Instructor]")
            |> mes("If you need guidance, come to me.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
