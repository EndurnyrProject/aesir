defmodule Aesir.ZoneServer.Content.Npc.Re.Cities.Malaya.Grandma do
  @moduledoc """
  Transpiled from rAthena re/cities/malaya.txt:274 (Grandma#ma01).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "malaya",
        x: 227,
        y: 311,
        dir: 4,
        sprite: 575,
        name: "Grandma",
        unique_name: "Grandma#ma01"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :malaya_hi, 0) < 10 do
        ctx =
          ctx
          |> mes("[Grandma]")
          |> mes("You're not from around here? Take care of yourself.")
          |> next()
          |> mes("[Grandma]")
          |> mes("I'm worried because my daughter-in-law is pregnant. I hope nothing goes wrong.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if get_char_var(ctx, :malaya_hi, 0) < 20 do
            ctx =
              ctx
              |> mes("[Grandma]")
              |> mes("I heard there is an outsider that is helping the village.")
              |> next()
              |> mes("[Grandma]")
              |> mes("Then my daughter-in-law will be safe.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx =
      if Enum.random(1..3) == 2 do
        ctx = mes(ctx, "[Grandma]")

        ctx =
          ctx
          |> mes(
            Rathena.concat(
              Rathena.concat(
                "Heard there is this ",
                getd(ctx, Rathena.concat(Rathena.concat("$ma_name0", Enum.random(1..6)), "$"))
              ),
              " that follows Meoneonuncle around."
            )
          )
          |> next()
          |> mes("[Grandma]")
          |> mes("I will cheer for their forbidden love.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, _} =
      ctx
      |> mes("[Grandma]")
      |> mes("I'm worried about my pregnant daughter-in-law. Hope Meoneonuncle won't bother her.")
      |> next()
      |> select(["Meoneonuncle?"])

    {ctx, _} =
      ctx
      |> mes("[Grandma]")
      |> mes(
        "Not from around here? Let me tell you why even a foreigner like you should be careful."
      )
      |> next()
      |> mes("[Grandma]")
      |> mes(
        "Meoneonuncle was a woman who lived in Port Malaya long ago. She married a decent man and also had a baby."
      )
      |> next()
      |> mes("[Grandma]")
      |> mes(
        "But happiness never lasts forever, right? The baby was miscarried from an accident..."
      )
      |> next()
      |> mes("[Grandma]")
      |> mes("She eventually passed away after grieving over her lost baby for days.")
      |> next()
      |> select(["That is a sad story."])

    ctx
    |> mes("[Grandma]")
    |> mes(
      "Meoneonuncle then started to appear in spirit and take away babies from pregnant women in the village."
    )
    |> next()
    |> mes("[Grandma]")
    |> mes("If you ever walk around Port Malaya at night...")
    |> next()
    |> mes("[Grandma]")
    |> mes(
      "And hear either tik-tik or wak-wak, be cautious because that is the sound of Meoneonuncle's wings flapping."
    )
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
