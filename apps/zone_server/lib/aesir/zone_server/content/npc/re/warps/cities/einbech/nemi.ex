defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Cities.Einbech.Nemi do
  @moduledoc """
  Transpiled from rAthena re/warps/cities/einbech.txt:10 (nemi).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "einbech", x: 215, y: 84, dir: 0, sprite: 45, name: "nemi", trigger: {1, 1}}]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :terra_gloria_main, 0) < 14 do
        ctx = ctx |> mes("You only see a cold and hard wall.") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "rebel_in", 21, 41)
  end
end
