defmodule Aesir.ZoneServer.Content.Npc.Re.Warps.Other.Verus.Verus03ToVerus01 do
  @moduledoc """
  Transpiled from rAthena re/warps/other/verus.txt:19 (verus03_to_verus01).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "verus03",
        x: 52,
        y: 254,
        dir: 0,
        sprite: 45,
        name: "verus03_to_verus01",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :ep15_2_permit, 0) < 1 do
        ctx =
          ctx
          |> mes("[Access Controller]")
          |> mes("I am sorry but only permitted members can enter from this point.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    warp(ctx, "verus01", 243, 62)
  end
end
