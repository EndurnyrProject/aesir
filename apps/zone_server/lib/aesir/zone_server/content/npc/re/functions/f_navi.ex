defmodule Aesir.ZoneServer.Content.Npc.Re.Functions.FNavi do
  @moduledoc """
  Transpiled from rAthena re/guides/navigation.txt:19 (F_Navi).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  import Aesir.ZoneServer.Script.Dsl, warn: false
  alias Aesir.ZoneServer.Script.Rathena

  @doc "Callable rAthena global function; returns `{ctx, return_value}`."
  def call(ctx, args) do
    try do
      ctx =
        if get_char_var(ctx, :PACKETVER, 0) < 20_111_010 do
          set_local(ctx, :disabled, 1)
        else
          ctx
        end

      v1 = length(args)

      case v1 do
        0 ->
          throw({:script_return, {ctx, get_local(ctx, :disabled, 0)}})

        1 ->
          ctx =
            if not Rathena.truthy?(get_local(ctx, :disabled, 0)) do
              ctx
              |> next()
              |> mes(to_string(Enum.at(args, 0, 0)))
              |> mes(
                "When you click on the ^B9062F[location name]^000000, you'll receive the most advanced ^B9062FNavigation^000000 services!"
              )
            else
              ctx
            end

          throw({:script_return, {ctx, nil}})

        _ ->
          if not Rathena.truthy?(get_local(ctx, :disabled, 0)) do
            throw(
              {:script_return,
               {ctx,
                Rathena.concat(
                  Rathena.concat(
                    Rathena.concat(Rathena.concat("<NAVI>[", Enum.at(args, 0, 0)), "]<INFO>"),
                    Enum.at(args, 1, 0)
                  ),
                  ",0,000,0</INFO></NAVI>"
                )}}
            )
          else
            if length(args) == 3 do
              throw(
                {:script_return,
                 {ctx, Rathena.concat(Enum.at(args, 2, 0) + Enum.at(args, 0, 0), "^000000")}}
              )
            else
              throw({:script_return, {ctx, Enum.at(args, 0, 0)}})
            end
          end
      end
    catch
      :throw, {:script_return, result} -> result
    end
  end
end
