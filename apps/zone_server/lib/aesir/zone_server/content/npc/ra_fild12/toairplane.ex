defmodule Aesir.ZoneServer.Content.Npc.RaFild12.Toairplane do
  @moduledoc """
  Transpiled from rAthena airports/rachel.txt:15 (toairplane#rachel).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "ra_fild12", x: 295, y: 208, dir: 0, sprite: 45, name: "toairplane", trigger: {1, 1}}
    ]

  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(_ctx) do
    exit(:normal)
  end

  def ev_ontouch(ctx) do
    {ctx, v1} =
      ctx
      |> mes("To use the airship, you are required to pay 1,200 zeny or a Free Airship Ticket.")
      |> mes("Would you like to use the service?")
      |> next()
      |> select(["Yes", "No"])

    ctx =
      if v1 == 1 do
        ctx =
          if count_item(ctx, 7311) > 0 do
            ctx |> delitem(7311, 1) |> warp("airplane_01", 245, 60)
            exit(:normal)
          else
            ctx
          end

        ctx =
          if zeny(ctx) >= 1200 do
            ctx |> pay_zeny(1200) |> warp("airplane_01", 245, 60)
            exit(:normal)
          else
            ctx
          end

        ctx
        |> mes("I am sorry, but you do not have enough money.")
        |> mes("Please remember, you are required to pay 1,200 zeny to use the service.")
        |> close()

        exit(:normal)
      else
        ctx
      end

    ctx |> mes("Thank you, please come again.") |> close()
    exit(:normal)
  end
end
