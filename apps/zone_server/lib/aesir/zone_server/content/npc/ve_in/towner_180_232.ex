defmodule Aesir.ZoneServer.Content.Npc.VeIn.Towner180232 do
  @moduledoc """
  Transpiled from rAthena cities/veins.txt:641 (Towner#ve20).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "ve_in", x: 180, y: 232, dir: 5, sprite: 946, name: "Towner"}]

  @impl true
  def on_talk(ctx) do
    ctx
    |> mes("[Towner]")
    |> mes("It's nice when a town")
    |> mes("is peaceful and quiet...")
    |> mes("But it's lame when a")
    |> mes("tavern is dead like this.")
    |> next()
    |> mes("[Towner]")
    |> mes("Well, there's good")
    |> mes("and bad points to")
    |> mes("everything. Hopefully")
    |> mes("things will pick up")
    |> mes("around here as more of")
    |> mes("you adventurers come visit.")
    |> close()
  end
end
