defmodule Aesir.ZoneServer.Content.Npc.SplIn01.SplendideGuard do
  @moduledoc """
  Transpiled from rAthena cities/splendide.txt:414 (Splendide Guard#tre).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "spl_in01", x: 281, y: 329, dir: 3, sprite: 447, name: "Splendide Guard"}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 and get_char_var(ctx, :ep13_2_rhea, 0) == 100 do
      ctx
      |> mes("[Splendide Guard]")
      |> mes(
        "If you make too much noise, the prisoners will cause trouble. So try to keep quiet at all times."
      )
      |> close()

      exit(:normal)
    else
      ctx
      |> mes("[Splendide Guard]")
      |> mes("AnduVeldRe Ko VeldReFulo So LomaurDu So\tSo ")
      |> close()

      exit(:normal)
    end
  end
end
