defmodule Aesir.ZoneServer.Content.Npc.SplIn01.Npc do
  @moduledoc """
  Transpiled from rAthena cities/splendide.txt:427 (#spl_prs).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "spl_in01", x: 287, y: 306, dir: 3, sprite: 111, name: ""}]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 and get_char_var(ctx, :ep13_2_rhea, 0) == 100 do
      ctx
      |> mes("[Manuk Prisoner]")
      |> mes("My, my body...!!")
      |> mes("Injection! Please!! Help me!")
      |> close()

      exit(:normal)
    else
      ctx
      |> mes("[Manuk Prisoner]")
      |> mes("Gi ha sd I das ")
      |> mes("Yda sod ja si dsa")
      |> close()

      exit(:normal)
    end
  end
end
