defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuAcolyte.M1stJobQuestReset do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_acolyte.txt:1763 (1st Job Quest Reset).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "sec_in02", x: 17, y: 156, dir: 3, sprite: 726, name: "1st Job Quest Reset"}]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> todo(:callfunc, ["F_GM_NPC"])
      |> mes("[1st Job Quest]")
      |> mes("Which would you like to reset?")
      |> next()
      |> select(["Swordsman", "Merchant", "Archer", "Acolyte", "Thief", "Mage"])

    ctx =
      case v1 do
        1 ->
          set_char_var(ctx, :tu_swordman, 0)

        2 ->
          set_char_var(ctx, :tu_merchant, 0)

        3 ->
          ctx |> set_char_var(:tu_archer01, 0) |> set_char_var(:tu_archer02, 0)

        4 ->
          set_char_var(ctx, :tu_acolyte01, 0)

        5 ->
          set_char_var(ctx, :tu_thief01, 0)

        6 ->
          ctx = set_char_var(ctx, :tu_magician01, 0)
          ctx

        _ ->
          ctx
      end

    ctx |> mes("Completed.") |> close()
  end
end
