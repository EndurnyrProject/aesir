defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuMaTh01.TraceOfBattle39163 do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_ma_th01.txt:149 (Trace of Battle#4).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "moc_fild11",
        x: 39,
        y: 163,
        dir: 0,
        sprite: 844,
        name: "Trace of Battle",
        unique_name: "Trace of Battle#4"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = set_local(ctx, :class_thief, Rathena.bool_int(base_class(ctx) == :thief))
    ctx = set_local(ctx, :class_mage, Rathena.bool_int(base_class(ctx) == :mage))

    ctx =
      if (get_local(ctx, :class_thief, 0) == 0 and get_local(ctx, :class_mage, 0) == 0) or
           Rathena.truthy?(:erlang.band(eaclass(ctx), 2_097_152)) do
        ctx
        |> mes("^3355FFThere are signs that show")
        |> mes("that some violent scuffle")
        |> mes("might have occurred here,")
        |> mes("but you can't tell what had")
        |> mes("happened exactly...^000000")
      else
        ctx =
          if (get_char_var(ctx, :tu_thief01, 0) < 11 and
                Rathena.truthy?(get_local(ctx, :class_thief, 0))) or
               (get_char_var(ctx, :tu_magician01, 0) < 11 and
                  Rathena.truthy?(get_local(ctx, :class_mage, 0))) do
            ctx
            |> mes("^3355FFThere are signs")
            |> mes("that many people")
            |> mes("have traveled through")
            |> mes("this particular area.^000000")
          else
            ctx =
              if (get_char_var(ctx, :tu_thief01, 0) == 11 and
                    Rathena.truthy?(get_local(ctx, :class_thief, 0))) or
                   (get_char_var(ctx, :tu_magician01, 0) == 11 and
                      Rathena.truthy?(get_local(ctx, :class_mage, 0))) do
                ctx =
                  ctx
                  |> mes("^3355FFFrom these traces,")
                  |> mes("you see that another")
                  |> mes("set of footprints has")
                  |> mes("been added. It looks like")
                  |> mes("someone else got involved.")
                  |> mes("These prints are distinctly")
                  |> mes("clearer and much smaller.^000000")

                ctx =
                  if get_char_var(ctx, :tu_magician01, 0) == 11 do
                    set_char_var(ctx, :tu_magician01, 12)
                  else
                    set_char_var(ctx, :tu_thief01, 12)
                  end

                ctx
              else
                ctx
                |> mes("^3355FFFrom these traces,")
                |> mes("you see that another")
                |> mes("person has gotten involved")
                |> mes("in this heated scuffle.^000000")
              end

            ctx
          end

        ctx
      end

    close(ctx)
  end
end
