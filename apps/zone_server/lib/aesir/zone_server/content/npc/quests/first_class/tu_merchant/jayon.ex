defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuMerchant.Jayon do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_merchant.txt:789 (Jayon).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "prontera", x: 247, y: 129, dir: 3, sprite: 85, name: "Jayon"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Jayon]")

    ctx =
      if get_char_var(ctx, :tu_merchant, 0) == 6 do
        {ctx, _} = s_l_potions(ctx, [410, 10])
        ctx
      else
        ctx =
          if get_char_var(ctx, :tu_merchant, 0) == 7 do
            {ctx, _} = s_l_potions(ctx, [400, 10])
            ctx
          else
            ctx =
              if get_char_var(ctx, :tu_merchant, 0) == 8 do
                {ctx, _} = s_l_potions(ctx, [410, 10])
                ctx
              else
                ctx
              end

            ctx
          end

        ctx
      end

    ctx
    |> mes("The Schwarzwald Republic...")
    |> mes(
      "It's a really interesting country. There's Juno and a few other places worth looking around. "
    )
    |> next()
    |> mes("[Jayon]")
    |> mes("They may not be the best places")
    |> mes(
      "for sight seeing, but I'm sure they'll offer something of interest to you adventurers."
    )
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp s_l_potions(ctx, args) do
    ctx = set_local(ctx, :cost, Enum.at(args, 0, 0))

    {ctx, v1} =
      ctx
      |> mes(
        Rathena.concat(
          Rathena.concat(
            "If you've checked out the market for Red Potions, you know that my price is the best. I'm offering 10 Red Potions for only ",
            get_local(ctx, :cost, 0)
          ),
          " Zeny!"
        )
      )
      |> next()
      |> select(["Buy", "Cancel"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Jayon]")
            |> mes("Heh heh...!")
            |> mes("It looks like")
            |> mes("you know your")
            |> mes("stuff. You're a")
            |> mes("Merchant yourself,")
            |> mes("aren't you?")

          ctx =
            if zeny(ctx) < get_local(ctx, :cost, 0) do
              ctx =
                ctx
                |> next()
                |> mes("[Jayon]")
                |> mes("But sorry buddy.")
                |> mes("Rules are rules.")
                |> mes("You gotta meet my price")
                |> mes("if you want these potions.")
                |> close()

              throw({:script_end, ctx})
            else
              ctx
            end

          ctx =
            if get_char_var(ctx, :MaxWeight, 0) - weight(ctx) < 71 do
              ctx
              |> next()
              |> mes("[Jayon]")
              |> mes("But sorry buddy. I can't let you carry more than you can handle.")
              |> mes("You ought to free up some of your inventory space when you get the chance.")
            else
              ctx
              |> pay_zeny(get_local(ctx, :cost, 0))
              |> set_char_var(:tu_merchant, Enum.at(args, 1, 0))
              |> give_item(501, 10)
            end

          ctx = close(ctx)
          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Jayon]")
            |> mes("Just looking")
            |> mes("around, eh?")
            |> mes("I understand.")
            |> mes("But you better take")
            |> mes("advantage of a real")
            |> mes("deal when you see one!")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    {ctx, nil}
  end
end
