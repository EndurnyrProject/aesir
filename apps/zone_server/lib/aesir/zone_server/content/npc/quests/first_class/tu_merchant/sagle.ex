defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuMerchant.Sagle do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_merchant.txt:596 (Sagle).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "prontera", x: 66, y: 111, dir: 3, sprite: 82, name: "Sagle"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Sagle]")

    ctx =
      if get_char_var(ctx, :tu_merchant, 0) == 6 do
        {ctx, _} = s_l_potions(ctx, [390, 9])
        ctx
      else
        ctx =
          if get_char_var(ctx, :tu_merchant, 0) == 7 do
            {ctx, _} = s_l_potions(ctx, [410, 10])
            ctx
          else
            ctx =
              if get_char_var(ctx, :tu_merchant, 0) == 8 do
                {ctx, _} = s_l_potions(ctx, [420, 10])
                ctx
              else
                ctx
              end

            ctx
          end

        ctx
      end

    ctx
    |> mes("It's on the tip")
    |> mes("of my tongue, but")
    |> mes("I can't quite remember...")
    |> next()
    |> mes("[Sagle]")
    |> mes(
      "You know, the name of the company that has all those beautiful ladies working for it. Ka... Ka-something. Anyway, I hear they're concerned about their growing competition. "
    )
    |> next()
    |> mes("[Sagle]")
    |> mes("It's none of my business, but it's good to know what's going on in")
    |> mes("the world, particularly if it'll affect the markets.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp s_l_potions(ctx, args) do
    ctx =
      ctx
      |> set_local(:cost, Enum.at(args, 0, 0))
      |> mes("Hello, hello~")
      |> mes("Why don't you buy")
      |> mes("some Red Potions?")
      |> mes(
        "They're essential for travel, and these Red Potions are the best for newer adventurers~"
      )
      |> next()
      |> mes("[Sagle]")

    {ctx, v1} =
      ctx
      |> mes(
        Rathena.concat(
          Rathena.concat("I will sell you 10 Red Potions for ^871F78", get_local(ctx, :cost, 0)),
          " zeny^000000. You'd better get them now while they're still here!"
        )
      )
      |> next()
      |> select(["Buy", "Cancel"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Sagle]")
            |> mes("Excellent!")
            |> mes(
              "You certainly have an eye for bargains. Here you go, 10 Red Potions fresh from the... Potioner..."
            )

          ctx =
            if zeny(ctx) < get_local(ctx, :cost, 0) do
              ctx =
                ctx
                |> next()
                |> mes("[Sagle]")
                |> mes("Whoa, hold on!")
                |> mes("You can't afford")
                |> mes("these potions...")
                |> mes("Please come back")
                |> mes("when you have enough")
                |> mes("Zeny, alright?")
                |> close()

              throw({:script_end, ctx})
            else
              ctx
            end

          ctx =
            if get_char_var(ctx, :MaxWeight, 0) - weight(ctx) < 71 do
              ctx
              |> next()
              |> mes("[Sagle]")
              |> mes("Whoa, hold on!")
              |> mes(
                "There's no way you carry all of this! Why don't you put some of your stuff in Kafra Storage before coming back?"
              )
            else
              ctx
              |> pay_zeny(get_local(ctx, :cost, 0))
              |> set_char_var(:tu_merchant, Enum.at(args, 1, 0))
              |> give_item(501, 10)
            end

          ctx = close(ctx)
          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Sagle]")
            |> mes("Alright...")
            |> mes("But don't")
            |> mes("complain if")
            |> mes("these potions")
            |> mes("are sold out!")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    {ctx, nil}
  end
end
