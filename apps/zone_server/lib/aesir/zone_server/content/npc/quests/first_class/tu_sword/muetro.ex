defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuSword.Muetro do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_sword.txt:2317 (Muetro).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "morocc", x: 82, y: 292, dir: 5, sprite: 84, name: "Muetro"}]

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Muetro]")

    ctx =
      if get_char_var(ctx, :tu_swordman, 0) == 15 do
        ctx =
          ctx
          |> mes("You're just")
          |> mes("like all the others.")
          |> mes("You want the code")
          |> mes("I know, right?")
          |> next()
          |> mes("[Muetro]")
          |> mes("^5D478BConBanfoevidehi^000000")
          |> mes("Happy now? Now")
          |> mes("leave me alone and")
          |> mes("catch the murderer.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx =
      if get_char_var(ctx, :tu_swordman, 0) == 14 do
        ctx =
          ctx
          |> mes("You're just")
          |> mes("like all the others.")
          |> mes("You want the code")
          |> mes("I know, right?")
          |> next()
          |> mes("[Muetro]")
          |> mes("^5D478BConBanfoevidehi^000000")
          |> mes("Happy now? Now")
          |> mes("leave me alone and")
          |> mes("catch the murderer.")
          |> set_char_var(:tu_swordman, 15)
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("^666666*Sigh*^000000")
    |> mes("I try and I try, but sometimes ridiculous things happen and")
    |> mes("I can't do anything about them.")
    |> next()
    |> mes("[Muetro]")
    |> mes("I suppose it's fate. There's no escaping what I can't possibly")
    |> mes("hope to control or understand.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
