defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuMaTh01.TraceOfBattle111303 do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_ma_th01.txt:547 (Trace of Battle#14).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "moc_fild18",
        x: 111,
        y: 303,
        dir: 0,
        sprite: 844,
        name: "Trace of Battle",
        unique_name: "Trace of Battle#14"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = set_local(ctx, :class_thief, Rathena.bool_int(base_class(ctx) == :thief))
    ctx = set_local(ctx, :class_mage, Rathena.bool_int(base_class(ctx) == :mage))

    ctx =
      if (get_local(ctx, :class_thief, 0) == 0 and get_local(ctx, :class_mage, 0) == 0) or
           Rathena.truthy?(:erlang.band(eaclass(ctx), 2_097_152)) do
        ctx
        |> mes("^3355FFThese look like")
        |> mes("traces of some kind")
        |> mes("of pursuit or battle, but")
        |> mes("you can't really tell for sure.^000000")
      else
        ctx =
          if (get_char_var(ctx, :tu_thief01, 0) < 20 and
                Rathena.truthy?(get_local(ctx, :class_thief, 0))) or
               (get_char_var(ctx, :tu_magician01, 0) < 20 and
                  Rathena.truthy?(get_local(ctx, :class_mage, 0))) do
            ctx
            |> mes("^3355FFThere are signs")
            |> mes("showing that a lot")
            |> mes("of people were in")
            |> mes("this area earlier.^000000")
          else
            ctx =
              if (get_char_var(ctx, :tu_thief01, 0) == 20 and
                    Rathena.truthy?(get_local(ctx, :class_thief, 0))) or
                   (get_char_var(ctx, :tu_magician01, 0) == 20 and
                      Rathena.truthy?(get_local(ctx, :class_mage, 0))) do
                ctx =
                  ctx
                  |> mes("^3355FFIn this area, it looks")
                  |> mes("like even more people")
                  |> mes("joined in this battle and")
                  |> mes("the pursuit clearly heads")
                  |> mes("towards the south.^000000")

                ctx =
                  if get_char_var(ctx, :tu_magician01, 0) == 20 do
                    set_char_var(ctx, :tu_magician01, 21)
                  else
                    set_char_var(ctx, :tu_thief01, 21)
                  end

                ctx
              else
                ctx
                |> mes("^3355FFThe trail of")
                |> mes("this battle heads")
                |> mes("towards the south.^000000")
              end

            ctx
          end

        ctx
      end

    close(ctx)
  end
end
