defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuAcolyte.Dog do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_acolyte.txt:942 (Dog#tu).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "prt_monk", x: 235, y: 245, dir: 5, sprite: 81, name: "Dog", unique_name: "Dog#tu"}
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = ctx |> mes("[Dog]") |> mes("^CDB79EBark bark!") |> mes("Woof woof!^000000") |> next()

    ctx =
      if get_char_var(ctx, :tu_acolyte01, 0) == 10 do
        loop_1(ctx)
      else
        ctx
      end

    ctx
    |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
    |> mes("So cute~!")
    |> close()
  end

  defp loop_1(ctx) do
    {ctx, v1} = select(ctx, ["Give a treat.", "Pet the dog.", "End actions."])

    ctx =
      case v1 do
        1 ->
          ctx = set_local(ctx, :dog_food, get_local(ctx, :dog_food, 0) + 1)

          ctx =
            if get_local(ctx, :dog_food, 0) > 4 do
              ctx
              |> mes("[Dog]")
              |> mes("^CDB79EGrrrrrr...!^000000")
              |> emotion(:fret)
              |> next()
              |> mes("^3355FFThe dog grinds")
              |> mes("its teeth and glares")
              |> mes("at you menacingly.^000000")
            else
              ctx |> mes("[Dog]") |> mes("^CDB79ERoof roof!^000000") |> emotion(:throb)
            end

          ctx

        2 ->
          ctx |> mes("[Dog]") |> mes("^CDB79ERoof roof~^000000") |> emotion(:throb)

        3 ->
          ctx = ctx |> mes("[Dog]") |> mes("^CDB79ERoof roof!^000000")
          ctx

        _ ->
          ctx
      end

    ctx |> next() |> loop_1()
  end
end
