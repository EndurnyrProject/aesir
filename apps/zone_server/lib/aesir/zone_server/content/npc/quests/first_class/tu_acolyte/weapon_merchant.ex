defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuAcolyte.WeaponMerchant do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_acolyte.txt:1277 (Weapon Merchant#tu).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prt_monk",
        x: 136,
        y: 261,
        dir: 0,
        sprite: 139,
        name: "Weapon Merchant",
        unique_name: "Weapon Merchant#tu",
        trigger: {9, 9}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :tu_acolyte01, 0) != 20 or count_item(ctx, 1081) == 0 do
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Weapon Merchant]")
    |> mes("The stuff I requested from the guild is finally here!")
    |> mes("It's pretty tough to keep everything in stock since this is such a remote place.")
    |> next()
    |> mes("[Weapon Merchant]")
    |> mes("Hey, thanks so much!")
    |> mes("Oh, and come here if")
    |> mes("you need anything, okay?")
    |> delitem(1081, 1)
    |> set_char_var(:tu_acolyte01, 21)
    |> close()
  end
end
