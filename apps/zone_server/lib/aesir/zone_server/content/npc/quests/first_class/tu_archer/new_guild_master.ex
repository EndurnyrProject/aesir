defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuArcher.NewGuildMaster do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_archer.txt:1783 (New Guild Master#tu).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prontera",
        x: 167,
        y: 281,
        dir: 3,
        sprite: 753,
        name: "New Guild Master",
        unique_name: "New Guild Master#tu"
      }
    ]

  @impl true
  def on_talk(ctx) do
    ctx =
      ctx
      |> mes("[New Guild Master]")
      |> mes("Hearken, all")
      |> mes("of you who seek")
      |> mes("fame and glory!")
      |> next()
      |> mes("[New Guild Master]")
      |> mes(
        "I hold in my hand this ^3131FFEmperium^000000, sign that I am favored by destiny! Brave ones, I bid you, lend me"
      )
      |> mes("your power!")
      |> next()
      |> mes("[New Guild Master]")
      |> mes("Nothing can stop us!")
      |> mes("So long as this jewel")
      |> mes("is in my possession,")
      |> mes("those who call themselves")
      |> mes("my comrades will never")
      |> mes("know the taste of defeat!")

    ctx =
      if get_char_var(ctx, :tu_archer02, 0) == 3 do
        set_char_var(ctx, :tu_archer02, 4)
      else
        ctx
      end

    close(ctx)
  end
end
