defmodule Aesir.ZoneServer.Content.Npc.Quests.FirstClass.TuMerchant.Aigie do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_merchant.txt:728 (Aigie).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "prt_in", x: 169, y: 11, dir: 3, sprite: 92, name: "Aigie"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Aigie]")

    ctx =
      if get_char_var(ctx, :tu_merchant, 0) == 6 do
        {ctx, _} = s_l_potions(ctx, [340, 10])
        ctx
      else
        ctx =
          if get_char_var(ctx, :tu_merchant, 0) == 7 do
            {ctx, _} = s_l_potions(ctx, [420, 10])
            ctx
          else
            ctx =
              if get_char_var(ctx, :tu_merchant, 0) == 8 do
                {ctx, _} = s_l_potions(ctx, [390, 9])
                ctx
              else
                ctx
              end

            ctx
          end

        ctx
      end

    ctx
    |> mes(
      "It's true that money isn't everything. I'm sure other things are important to have in order to be happy."
    )
    |> next()
    |> mes("[Aigie]")
    |> mes(
      "However, money can be much more dependable than some people I know. You can rely on money more than anything else in this world."
    )
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp s_l_potions(ctx, args) do
    ctx =
      ctx
      |> set_local(:cost, Enum.at(args, 0, 0))
      |> mes("Welcome to my shop.")
      |> mes(
        "I'm just a young girl who's a victim of circumstance, forced to sell potions on the street for dirt cheap prices."
      )
      |> next()
      |> mes("[Aigie]")
      |> mes("Would you like")
      |> mes("to buy 10 Red Potions")

    {ctx, v1} =
      ctx
      |> mes(
        Rathena.concat(
          Rathena.concat("for ", get_local(ctx, :cost, 0)),
          " Zeny, kind adventurer?"
        )
      )
      |> next()
      |> select(["Buy", "Cancel"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Aigie]")
            |> mes("Thank you so much.")
            |> mes("Now I can finally")
            |> mes("afford food again...")

          ctx =
            if zeny(ctx) < get_local(ctx, :cost, 0) do
              ctx =
                ctx
                |> next()
                |> mes("[Aigie]")
                |> mes("Wait, wait!")
                |> mes(
                  "I am sorry, but you don't have enough money. Would you please get some more Zeny before coming back?"
                )
                |> close()

              throw({:script_end, ctx})
            else
              ctx
            end

          ctx =
            if get_char_var(ctx, :MaxWeight, 0) - weight(ctx) < 71 do
              ctx
              |> next()
              |> mes("[Aigie]")
              |> mes("Wait, wait!")
              |> mes(
                "You can't possibly carry any more items. Why don't you put your things in Kafra Storage first?"
              )
            else
              ctx
              |> pay_zeny(get_local(ctx, :cost, 0))
              |> set_char_var(:tu_merchant, Enum.at(args, 1, 0))
              |> give_item(501, 10)
            end

          ctx = close(ctx)
          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Aigie]")
            |> mes("I understand.")
            |> mes("But I hope you")
            |> mes("see that I'm offering")
            |> mes("you a really good price.")
            |> mes("^666666*Sniff Sniff*^000000")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    {ctx, nil}
  end
end
