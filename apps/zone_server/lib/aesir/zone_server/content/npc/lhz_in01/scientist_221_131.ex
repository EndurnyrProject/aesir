defmodule Aesir.ZoneServer.Content.Npc.LhzIn01.Scientist221131 do
  @moduledoc """
  Transpiled from rAthena cities/lighthalzen.txt:3152 (Scientist#li_01).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "lhz_in01", x: 221, y: 131, dir: 7, sprite: 865, name: "Scientist"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    if Rathena.truthy?(is_equipped(ctx, 2241)) and Rathena.truthy?(is_equipped(ctx, 2243)) do
      if get_char_var(ctx, :hg_tre, 0) > 54 do
        ctx =
          ctx
          |> mes("[A Scientist]")
          |> mes(
            "What happened? All the machines are ruined and the research report are gone! The history of Regenschirm has been hacked!"
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          ctx
          |> mes("[Scientist]")
          |> mes("It takes so long for")
          |> mes("this device to process")
          |> mes("all the data and give me")
          |> mes("the results. Still, the wait")
          |> mes("heightens my anticipation...")
          |> close()

        throw({:script_end, ctx})
      end
    else
      ctx =
        ctx
        |> mes("[Scientist]")
        |> mes("What?! Guards!")
        |> mes("Hurry, there's an")
        |> mes("intruder right here!")
        |> emotion(:surprise)
        |> close()
        |> warp("lhz_in01", 33, 224)

      throw({:script_end, ctx})
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
