defmodule Aesir.ZoneServer.Content.Npc.LhzIn01.Leimi do
  @moduledoc """
  Transpiled from rAthena cities/lighthalzen.txt:2941 (Leimi#mimir).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "lhz_in01", x: 139, y: 48, dir: 7, sprite: 73, name: "Leimi"}]

  @impl true
  def on_talk(ctx) do
    ctx =
      ctx
      |> mes("[Leimi]")
      |> mes("...")
      |> mes("......")
      |> next()
      |> mes("[Leimi]")
      |> mes("Oh...!")
      |> mes("Good heavens!")
      |> mes("Um, may I help you?")
      |> emotion(:huk)

    ctx =
      if get_char_var(ctx, :BaseJob, 0) == :assassin do
        ctx =
          if sex(ctx) == get_char_var(ctx, :SEX_MALE, 0) do
            ctx
            |> next()
            |> mes("[Leimi]")
            |> mes("Oh, you're an Assassin!")
            |> mes("Oh, you boys are soooo cute!")
            |> mes("And so cool and so mysterious all at the same time! I love you!")
          else
            ctx
            |> next()
            |> mes("[Leimi]")
            |> mes("An Assassin...?")
            |> mes("Oh, you wouldn't happen")
            |> mes("to know any Assassin boys")
            |> mes("that might be single, do you?")
            |> mes("Oh-my-god, they're hunky-hot~")
          end

        ctx
      else
        ctx
      end

    close(ctx)
    exit(:normal)
  end
end
