defmodule Aesir.ZoneServer.Content.Npc.LhzIn01.Npc13847 do
  @moduledoc """
  Transpiled from rAthena cities/lighthalzen.txt:2972 (#mimir_camera).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []
  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :BaseJob, 0) == :assassin do
        ctx =
          if sex(ctx) == get_char_var(ctx, :SEX_MALE, 0) do
            ctx = ctx |> mes("^3355FF*Click*^000000") |> next()

            ctx =
              ctx
              |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
              |> mes("What the...?")
              |> mes("That sound. Did...")
              |> mes("Did someone just")
              |> mes("take my picture?")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx
  end
end
