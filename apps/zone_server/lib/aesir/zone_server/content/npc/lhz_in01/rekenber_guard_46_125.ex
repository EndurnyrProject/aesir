defmodule Aesir.ZoneServer.Content.Npc.LhzIn01.RekenberGuard46125 do
  @moduledoc """
  Transpiled from rAthena cities/lighthalzen.txt:1926 (Rekenber Guard#li03).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "lhz_in01",
        x: 46,
        y: 125,
        dir: 3,
        sprite: 867,
        name: "Rekenber Guard",
        trigger: {5, 5}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2241)) and Rathena.truthy?(is_equipped(ctx, 2243)) do
        {ctx, v1} =
          ctx
          |> mes("[Rekenber Guard]")
          |> mes("......................")
          |> next()
          |> select(["Nice day, huh?", "Cancel"])

        ctx =
          if v1 == 1 do
            ctx = ctx |> mes("[Rekenber Guard]") |> mes("...") |> emotion(:fret) |> close()
            throw({:script_end, ctx})
          else
            ctx
          end

        ctx = ctx |> mes("[Rekenber Guard]") |> mes("...") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Rekenber Guard]")
    |> mes("...!")
    |> emotion(:surprise)
    |> close()
    |> warp("lhz_in01", 33, 224)
  end
end
