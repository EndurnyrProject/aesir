defmodule Aesir.ZoneServer.Content.Npc.Brasilis.Crewman do
  @moduledoc """
  Transpiled from rAthena cities/brasilis.txt:53 (Crewman#bra1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "brasilis", x: 316, y: 57, dir: 3, sprite: 100, name: "Crewman"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Crewman]")
      |> mes("My ship is going to back to Alberta, do you want to join us?")
      |> next()
      |> select(["Go back to Alberta.", "Not yet~."])

    ctx =
      case v1 do
        1 ->
          ctx = ctx |> mes("[Crewman]") |> mes("I sure do miss home.") |> close()

          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              warp(ctx, "alberta", 245, 87)
            else
              warp(ctx, "alberta", 244, 115)
            end

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Crewman]")
            |> mes("Ok, suit yourself. We'll see you when we get back then.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
