defmodule Aesir.ZoneServer.Content.Npc.Cities.Ayothaya.YoungMan241264 do
  @moduledoc """
  Transpiled from rAthena cities/ayothaya.txt:270 (Young Man#5ayothaya2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "ayothaya",
        x: 241,
        y: 264,
        dir: 5,
        sprite: 843,
        name: "Young Man",
        scope: :shared,
        unique_name: "Young Man#5ayothaya2"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Eik]")
      |> mes("Hey...")
      |> mes("You look pretty strong")
      |> mes("You wanna challenge")
      |> mes("me to a match?")
      |> next()
      |> select(["Sure!", "Nah~"])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("[Eik]")
          |> mes("Ow ow ow!")
          |> mes("I was just")
          |> mes("kidding, man!")
          |> next()
          |> mes("[Eik]")
          |> mes("I'm not so rude as to pick fights with strangers for no reason!")
          |> emotion(:kek)
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Eik]")
    |> mes(
      "Real power is developed after having thousands of matches with other people. So, don't be afraid of fighting, okay?"
    )
    |> emotion(:hng)
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
