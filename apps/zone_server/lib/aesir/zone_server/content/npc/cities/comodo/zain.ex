defmodule Aesir.ZoneServer.Content.Npc.Cities.Comodo.Zain do
  @moduledoc """
  Transpiled from rAthena cities/comodo.txt:338 (Zain#cmd).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "cmd_fild07",
        x: 299,
        y: 83,
        dir: 4,
        sprite: 100,
        name: "Zain",
        scope: :shared,
        unique_name: "Zain#cmd"
      },
      %{
        map: "cmd_fild07",
        x: 94,
        y: 134,
        dir: 4,
        sprite: 100,
        name: "Sarumane",
        scope: :shared,
        unique_name: "Sarumane#cmd"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = set_local(ctx, :"n$", Rathena.concat(Rathena.concat("[", strnpcinfo(ctx, 1)), "]"))

    {ctx, v1} =
      ctx
      |> mes(get_local(ctx, :"n$", ""))
      |> mes("Would you like to")
      |> mes("board a ship on the")
      |> mes("Reudelus route? You")
      |> mes("can travel on Reudelus")
      |> mes("to Alberta or Izlude.")
      |> next()
      |> select(["Alberta - 600 Zeny", "Izlude - 800 Zeny", "Cancel"])

    ctx =
      try do
        case v1 do
          1 ->
            ctx =
              if zeny(ctx) < 600 do
                throw({:brk_1, ctx})
              else
                ctx
              end

            ctx = ctx |> pay_zeny(600) |> warp("alberta", 192, 169)
            throw({:script_end, ctx})

          2 ->
            ctx =
              if zeny(ctx) < 800 do
                throw({:brk_1, ctx})
              else
                ctx
              end

            ctx = pay_zeny(ctx, 800)

            ctx =
              if Rathena.truthy?(checkre(ctx, 0)) do
                warp(ctx, "izlude", 195, 212)
              else
                warp(ctx, "izlude", 176, 182)
              end

            throw({:script_end, ctx})

          3 ->
            ctx =
              ctx
              |> mes(get_local(ctx, :"n$", ""))
              |> mes("Travel by ship is")
              |> mes("still one of the safest and")
              |> mes("dependable methods of")
              |> mes("transportation. I invite you")
              |> mes("to try Reudelus travel soon~")
              |> close()

            throw({:script_end, ctx})

          _ ->
            ctx
        end
      catch
        :throw, {:brk_1, ctx} -> ctx
      end

    ctx
    |> mes(get_local(ctx, :"n$", ""))
    |> mes("I'm sorry, but you")
    |> mes("don't have enough")
    |> mes("zeny for the boarding fare.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
