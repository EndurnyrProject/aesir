defmodule Aesir.ZoneServer.Content.Npc.Cities.Jawaii.Employee107189 do
  @moduledoc """
  Transpiled from rAthena cities/jawaii.txt:315 (Employee#horoom).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "jawaii",
        x: 107,
        y: 189,
        dir: 5,
        sprite: 93,
        name: "Employee",
        unique_name: "Employee#horoom"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Sharkie Rania]")
      |> mes("I'll take you")
      |> mes("to the Honey Room.")
      |> mes("It costs 1,000 zeny.")
      |> next()
      |> mes("[Sharkie Rania]")
      |> mes("So you wanna go?")
      |> next()
      |> select(["Use.", "Cancel."])

    ctx =
      if v1 == 1 do
        ctx = mes(ctx, "[Sharkie Rania]")

        ctx =
          if zeny(ctx) > 999 do
            ctx =
              ctx
              |> mes("Eh, alright.")
              |> mes("Let's get going.")
              |> close()
              |> pay_zeny(1000)
              |> warp("jawaii_in", 86, 117)

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("You...")
          |> mes("Don't have")
          |> mes("enough money.")
          |> mes("C'mon, romance")
          |> mes("takes zeny, got it?")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx |> mes("[Sharkie Rania]") |> mes("No prob.") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
