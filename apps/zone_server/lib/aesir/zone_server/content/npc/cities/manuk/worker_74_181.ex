defmodule Aesir.ZoneServer.Content.Npc.Cities.Manuk.Worker74181 do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:539 (Worker#ep13bs2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "man_in01",
        x: 74,
        y: 181,
        dir: 3,
        sprite: 454,
        name: "Worker",
        scope: :shared,
        unique_name: "Worker#ep13bs2"
      }
    ]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 do
      ctx =
        ctx |> mes("[Worker]") |> mes("Chef Cook, how many plates should I put down?") |> close()

      throw({:script_end, ctx})
    else
      ctx = ctx |> mes("[Tee]") |> mes("We pishd bugs ouwwe iro ") |> close()
      throw({:script_end, ctx})
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
