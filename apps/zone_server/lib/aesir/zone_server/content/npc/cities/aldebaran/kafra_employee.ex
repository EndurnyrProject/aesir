defmodule Aesir.ZoneServer.Content.Npc.Cities.Aldebaran.KafraEmployee do
  @moduledoc """
  Transpiled from rAthena cities/aldebaran.txt:1054 (Kafra Employee#reserve1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "aldeba_in",
        x: 79,
        y: 161,
        dir: 7,
        sprite: 115,
        name: "Kafra Employee",
        scope: :shared,
        unique_name: "Kafra Employee#reserve1"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if checkweight(ctx, [{1201, 1}]) == 0 do
        ctx =
          ctx
          |> mes("^3355FFWait a minute! Right now,")
          |> mes("you're carrying too many items")
          |> mes("in your inventory. Please come")
          |> mes("back after storing some of")
          |> mes("your things in Kafra Storage.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx = mes(ctx, "[Kafra Employee]")

    ctx =
      ctx
      |> mes(Rathena.concat(Rathena.concat("Welcome, ", char_name(ctx, 0)), "~"))
      |> mes("Here, you can exchange")
      |> mes("the Special Reserve Points")
      |> mes("you've earned by using the")
      |> mes("Kafra Services for some")
      |> mes("neat and useful prizes~")
      |> next()
      |> mes("[Kafra Employee]")
      |> mes(
        "Please remember that each window has a different amount of special reserve points you can Use"
      )
      |> mes("You can use ^7D0781from 100p to 3000p^000000 in here.")
      |> next()
      |> mes("[Kafra Employee]")

    ctx =
      if get_char_var(ctx, :MaxWeight, 0) - weight(ctx) < 11000 do
        ctx =
          ctx
          |> mes("Um, but I don't think")
          |> mes("you're able to carry")
          |> mes("very much right now.")
          |> mes("It looks like you have")
          |> mes("too much stuff inside")
          |> mes("your inventory.")
          |> next()
          |> mes("[Kafra Employee]")
          |> mes("Please put some of")
          |> mes("your things into Kafra")
          |> mes("Storage. To use this")
          |> mes("service, we ask that you")
          |> mes("have about ^FF00001,100^000000 free units")
          |> mes("of weight in your inventory.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx = ctx |> set_local(:total, get_char_var(ctx, :RESRVPTS, 0)) |> mes("Let's see...")
    ctx = ctx |> mes(Rathena.concat(char_name(ctx, 0), "...")) |> mes("Ah, you have a total of")

    {ctx, v1} =
      ctx
      |> mes(Rathena.concat(get_local(ctx, :total, 0), " Special Reserve Points."))
      |> mes("Now what you would like")
      |> mes("to exchange them for?")
      |> next()
      |> set_local(:select_price, [
        516,
        100,
        7,
        200,
        15,
        300,
        25,
        400,
        35,
        500,
        50,
        600,
        60,
        700,
        75,
        800,
        85,
        900,
        100,
        1000
      ])
      |> select([
        "100 = Potato 7 ea",
        "200 = Potato 15 ea",
        "300 = Potato 25 ea",
        "400 = Potato 35 ea",
        "500 = Potato 50 ea",
        "600 = Potato 60 ea",
        "700 = Potato 75 ea",
        "800 = Potato 85 ea",
        "900 = Potato 100 ea",
        "1000 = 1st Lottery Chance!",
        "Next Articles",
        "Cancel"
      ])

    ctx = set_local(ctx, :s, v1)

    ctx =
      if get_local(ctx, :s, 0) == 11 do
        {ctx, v2} =
          ctx
          |> set_local(:select_price, [
            501,
            1100,
            7,
            1300,
            15,
            1500,
            25,
            1700,
            35,
            1900,
            50,
            2100,
            60,
            2300,
            75,
            2500,
            85,
            2800,
            100,
            3000
          ])
          |> select(
            String.split(
              Rathena.concat(
                "1100 = Red Potion 7 ea:1300 = Red Potion 15 ea:1500 = Red Potion 25 ea:1700 = Red Potion 35 ea:1900 = Red Potion 50 ea:2100 = Red Potion 60 ea:",
                "2300 = Red Potion 75 ea:2500 = Red Potion 85 ea:2800 = Red Potion 100 ea:3000 = 2nd Lotery Chance!::Cancel"
              ),
              ":"
            )
          )

        ctx |> set_local(:s, v2) |> set_local(:choose_sub_select, 1)
      else
        ctx
      end

    ctx =
      if get_local(ctx, :s, 0) != 12 do
        ctx = mes(ctx, "[Kafra Employee]")
        ctx = set_local(ctx, :index_points, get_local(ctx, :s, 0) * 2 - 1)
        ctx = set_local(ctx, :index_quantity, get_local(ctx, :s, 0) * 2)

        ctx =
          if get_local(ctx, :total, 0) <
               Enum.at(get_local(ctx, :select_price, []), get_local(ctx, :index_points, 0), 0) do
            ctx =
              ctx
              |> set_local(
                :points,
                Enum.at(get_local(ctx, :select_price, []), get_local(ctx, :index_points, 0), 0) -
                  get_local(ctx, :total, 0)
              )
              |> mes("I'm sorry, but you don't")
              |> mes("enough Special Reserve")
              |> mes("Points to exchange for this")
              |> mes("reward. You need at least")

            ctx =
              ctx
              |> mes(
                Rathena.concat(
                  Rathena.concat("^0000FF", get_local(ctx, :points, 0)),
                  "^000000 more points."
                )
              )
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> set_local(
            :total,
            get_local(ctx, :total, 0) -
              Enum.at(get_local(ctx, :select_price, []), get_local(ctx, :index_points, 0), 0)
          )
          |> mes("After receiving this")
          |> mes("reward, you'll have")

        {ctx, v3} =
          ctx
          |> mes(
            Rathena.concat(
              Rathena.concat("^AC0000", get_local(ctx, :total, 0)),
              "^000000 Special Reserve"
            )
          )
          |> mes("Points left. Would you")
          |> mes("like to redeem your")
          |> mes("points for this reward?")
          |> next()
          |> select(["Exchange.", "Cancel"])

        ctx =
          if v3 == 1 do
            ctx = set_char_var(ctx, :RESRVPTS, get_local(ctx, :total, 0))

            ctx =
              if get_local(ctx, :s, 0) < 10 do
                give_item(
                  ctx,
                  Enum.at(get_local(ctx, :select_price, []), 0, 0),
                  Enum.at(
                    get_local(ctx, :select_price, []),
                    get_local(ctx, :index_quantity, 0),
                    0
                  )
                )
              else
                ctx = mes(ctx, "[Kafra Employee]")

                ctx =
                  if get_local(ctx, :choose_sub_select, 0) == 0 do
                    ctx
                    |> mes("^0000FF1st Lottery Chance!!^000000")
                    |> mes("It's time to test out")
                    |> mes("your luck. Get ready!")
                  else
                    ctx
                    |> mes("Uh oh...")
                    |> mes("It's that time again~")
                    |> mes("It's Kafra Lottery Time!")
                    |> mes("Let's see how good your")
                    |> mes("luck is today. Ready?")
                  end

                ctx =
                  ctx
                  |> next()
                  |> mes("[Kafra Employee]")
                  |> mes("How many times")
                  |> mes("would you like to spin")
                  |> mes("the lottery machine?")
                  |> mes("You can spin it 1 to 5 times.")
                  |> next()
                  |> loop_1()
                  |> set_local(:choose_prize, Enum.random(1..20))
                  |> loop_2()
                  |> mes("[Kafra Employee]")

                ctx =
                  if get_local(ctx, :choose_sub_select, 0) == 0 do
                    ctx =
                      ctx
                      |> mes("Ooh, something")
                      |> mes("came out! Let's see")
                      |> mes("what you've won~")
                      |> mes("Oh goodness, it's...!")
                      |> next()
                      |> mes("[Kafra Employee]")

                    ctx =
                      if get_local(ctx, :choose_prize, 0) <= 10 do
                        ctx
                        |> give_item(516, 100)
                        |> mes("Hm? F-fourth prize?")
                        |> mes("You got the 4th prize!!")
                        |> mes("Well, that's not too bad.")
                        |> mes("That's 100 Potatoes!")
                        |> mes("When they're sliced, then fried, they make a great snack when")
                        |> mes("drinking with your friends~")
                      else
                        ctx =
                          if get_local(ctx, :choose_prize, 0) <= 15 do
                            ctx
                            |> give_item(602, 4)
                            |> mes("It's Third Prize!")
                            |> mes("4 Butterfly Wings~")
                            |> mes("When you're in trouble,")
                            |> mes("just wave one of these")
                            |> mes("to take you away...")
                            |> mes("To your safe place.")
                          else
                            ctx =
                              if get_local(ctx, :choose_prize, 0) <= 19 do
                                ctx
                                |> give_item(2403, 1)
                                |> mes("Second Prize!")
                                |> mes("A brand new shiny pair")
                                |> mes("of Shoes! Its elegant design")
                                |> mes("and durability comes with our")
                                |> mes("highest recommendation. We")
                                |> mes("hope you enjoy your new shoes~")
                              else
                                ctx =
                                  if get_local(ctx, :choose_prize, 0) == 20 do
                                    ctx
                                    |> give_item(2328, 1)
                                    |> mes("Whoa...!")
                                    |> mes("First Prize!")
                                    |> mes("Your very own")
                                    |> next()
                                    |> mes("set of Wooden Mail!")
                                    |> mes("Today must be your")
                                    |> mes("lucky day, adventurer!")
                                  else
                                    ctx
                                  end

                                ctx
                              end

                            ctx
                          end

                        ctx
                      end

                    ctx
                  else
                    ctx =
                      ctx
                      |> mes("It looks like")
                      |> mes("something came")
                      |> mes("out! What could it be?")
                      |> mes("Ooh, you just won...")
                      |> next()
                      |> mes("[Kafra Employee]")

                    ctx =
                      if get_local(ctx, :choose_prize, 0) <= 10 do
                        ctx
                        |> give_item(501, 100)
                        |> mes("F-fourth prize...?!")
                        |> mes("Boooo! 100 Red Potions.")
                        |> mes("Wait.. That's actually pretty")
                        |> mes("good! Yaaaaaay~ Now you")
                        |> mes("can look like a high roller by")
                        |> mes("sharing them with your friends!")
                      else
                        ctx =
                          if get_local(ctx, :choose_prize, 0) <= 16 do
                            ctx
                            |> give_item(2201, 1)
                            |> mes("Third Prize!")
                            |> mes("Your very own pair")
                            |> mes("of suave Sunglasses!")
                            |> mes("It'll give you an edge in")
                            |> mes("the war of looking cool,")
                            |> mes("or when playing poker~")
                          else
                            ctx =
                              if get_local(ctx, :choose_prize, 0) <= 19 do
                                ctx
                                |> give_item(2226, 1)
                                |> mes("Second Prize!")
                                |> mes("A... Cap? Hmmm,")
                                |> mes("these have pretty good")
                                |> mes("Defense, but I'm not so")
                                |> mes("sure of how fashionable")
                                |> mes("this hat is. Oh well...")
                              else
                                ctx =
                                  if get_local(ctx, :choose_prize, 0) == 20 do
                                    ctx
                                    |> give_item(505, 3)
                                    |> mes("Oh wow...!")
                                    |> mes("First Prize!")
                                    |> mes("3 Blue Potions~")
                                    |> mes("With enough of these,")
                                    |> mes("you can use your skills")
                                    |> mes("with a bit more impunity~")
                                  else
                                    ctx
                                  end

                                ctx
                              end

                            ctx
                          end

                        ctx
                      end

                    ctx
                  end

                ctx
              end

            next(ctx)
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx
    |> mes("[Kafra Employee]")
    |> mes("Alright then. Please")
    |> mes("use our services to")
    |> mes("collect more and more")
    |> mes("Special Reserve Points")
    |> mes("for even better rewards.")
    |> mes("Thank you for your patronage.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp loop_1(ctx) do
    {ctx, v4} = input(ctx, :int)
    {v5, v6} = Rathena.input_int(v4, 1, 5)
    ctx = set_local(ctx, :input, v6)

    if v5 != 0 do
      ctx
      |> mes("[Kafra Employee]")
      |> mes("Excuse me...?")
      |> mes("Please choose")
      |> mes("a number from 1 to 5.")
      |> next()
      |> loop_1()
    else
      ctx
    end
  end

  defp loop_2(ctx) do
    if get_local(ctx, :input, 0) != get_local(ctx, :random_while, 0) do
      ctx = set_local(ctx, :sound_word, Enum.random(1..3))

      ctx =
        if get_local(ctx, :sound_word, 0) == 1 do
          ctx |> mes("^3355FFDrrrrrrrrrrrrrrrrrr...") |> mes("Tuum tuum tuum!^000000")
        else
          ctx =
            if get_local(ctx, :sound_word, 0) == 2 do
              ctx |> mes("^3355FFChika chika chika") |> mes("Shooooooooooom~^000000")
            else
              ctx =
                if get_local(ctx, :sound_word, 0) == 3 do
                  ctx |> mes("^3355FFTuk tuk tuk tuk") |> mes("Flaaaaaavaaaaah~^000000")
                else
                  ctx
                end

              ctx
            end

          ctx
        end

      ctx = next(ctx)
      ctx |> set_local(:random_while, get_local(ctx, :random_while, 0) + 1) |> loop_2()
    else
      ctx
    end
  end
end
