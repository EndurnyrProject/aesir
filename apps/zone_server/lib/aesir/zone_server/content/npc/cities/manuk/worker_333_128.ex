defmodule Aesir.ZoneServer.Content.Npc.Cities.Manuk.Worker333128 do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:337 (Worker#ep13bsg4).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "man_in01",
        x: 333,
        y: 128,
        dir: 5,
        sprite: 454,
        name: "Worker",
        unique_name: "Worker#ep13bsg4"
      }
    ]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 do
      ctx = ctx |> mes("[Worker]") |> mes("My eyesight is getting weaker these days.") |> close()
      throw({:script_end, ctx})
    else
      ctx = ctx |> mes("[Worker]") |> mes("Yw I eus ia d ap s") |> close()
      throw({:script_end, ctx})
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
