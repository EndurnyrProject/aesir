defmodule Aesir.ZoneServer.Content.Npc.Cities.Izlude.Sailor do
  @moduledoc """
  Transpiled from rAthena cities/izlude.txt:505 (Sailor#2izlude).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "izlu2dun",
        x: 108,
        y: 27,
        dir: 0,
        sprite: 100,
        name: "Sailor",
        scope: :shared,
        unique_name: "Sailor#2izlude"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Sailor]")
      |> mes("Wanna")
      |> mes("head back?")
      |> next()
      |> select(["Yeah, I'm tired to death.", "Nope, I love this place!"])

    ctx =
      if v1 == 1 do
        ctx =
          if Rathena.truthy?(checkre(ctx, 0)) do
            warp(ctx, "izlude", 197, 210)
          else
            warp(ctx, "izlude", 176, 182)
          end

        throw({:script_end, ctx})
      else
        ctx
      end

    close(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
