defmodule Aesir.ZoneServer.Content.Npc.Cities.Yuno.AirshipRepresentative do
  @moduledoc """
  Transpiled from rAthena cities/yuno.txt:342 (Airship Representative).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "yuno", x: 142, y: 183, dir: 5, sprite: 100, name: "Airship Representative"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Airship Representative]")
      |> mes("Good day, I am here to inform you")
      |> mes("about the Juno Airship which")
      |> mes("we plan to operate in the near future.")
      |> next()
      |> mes("[Air ship Representative]")
      |> mes("Unfortunately, it is still in")
      |> mes("development, and we've yet")
      |> mes("to complete testing. However,")
      |> mes("we feel the need to let our")
      |> mes("eager customers know of our")
      |> mes("progress.")
      |> next()
      |> mes("[Airship Representative]")
      |> mes(
        "The Airship we're developing will provide you with convenient travel to any town. You can also enjoy the sights while aloft in the sky. Unique products from various areas will also be provided."
      )
      |> next()
      |> mes("[Airship Representative]")
      |> mes("We promise our customers an")
      |> mes("amazing travel experience")
      |> mes("once the Airship is in")
      |> mes("operation. In the meantime,")
      |> mes("we are providing a special")
      |> mes("teleport service.")
      |> next()
      |> mes("[Airship Representative]")
      |> mes(
        "The teleport fee is 1,800 zeny, regardless of where you want to go. Please let me know your desired destination."
      )
      |> next()
      |> select([
        "Prontera",
        "Izlude",
        "Geffen",
        "Morocc",
        "Payon",
        "Alberta",
        "Comodo",
        "Cancel"
      ])

    ctx =
      case v1 do
        1 ->
          {ctx, _} = s_s_warp(ctx, ["prontera", 116, 72])

          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              {ctx, _} = s_s_warp(ctx, ["izlude", 128, 98])
              ctx
            else
              {ctx, _} = s_s_warp(ctx, ["izlude", 94, 103])
              ctx
            end

          {ctx, _} = s_s_warp(ctx, ["geffen", 120, 39])
          {ctx, _} = s_s_warp(ctx, ["morocc", 156, 46])

          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              {ctx, _} = s_s_warp(ctx, ["payon", 162, 59])
              ctx
            else
              {ctx, _} = s_s_warp(ctx, ["payon", 69, 100])
              ctx
            end

          {ctx, _} = s_s_warp(ctx, ["alberta", 117, 56])
          {ctx, _} = s_s_warp(ctx, ["comodo", 209, 143])
          ctx = close(ctx)
          throw({:script_end, ctx})

        2 ->
          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              {ctx, _} = s_s_warp(ctx, ["izlude", 128, 98])
              ctx
            else
              {ctx, _} = s_s_warp(ctx, ["izlude", 94, 103])
              ctx
            end

          {ctx, _} = s_s_warp(ctx, ["geffen", 120, 39])
          {ctx, _} = s_s_warp(ctx, ["morocc", 156, 46])

          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              {ctx, _} = s_s_warp(ctx, ["payon", 162, 59])
              ctx
            else
              {ctx, _} = s_s_warp(ctx, ["payon", 69, 100])
              ctx
            end

          {ctx, _} = s_s_warp(ctx, ["alberta", 117, 56])
          {ctx, _} = s_s_warp(ctx, ["comodo", 209, 143])
          ctx = close(ctx)
          throw({:script_end, ctx})

        3 ->
          {ctx, _} = s_s_warp(ctx, ["geffen", 120, 39])
          {ctx, _} = s_s_warp(ctx, ["morocc", 156, 46])

          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              {ctx, _} = s_s_warp(ctx, ["payon", 162, 59])
              ctx
            else
              {ctx, _} = s_s_warp(ctx, ["payon", 69, 100])
              ctx
            end

          {ctx, _} = s_s_warp(ctx, ["alberta", 117, 56])
          {ctx, _} = s_s_warp(ctx, ["comodo", 209, 143])
          ctx = close(ctx)
          throw({:script_end, ctx})

        4 ->
          {ctx, _} = s_s_warp(ctx, ["morocc", 156, 46])

          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              {ctx, _} = s_s_warp(ctx, ["payon", 162, 59])
              ctx
            else
              {ctx, _} = s_s_warp(ctx, ["payon", 69, 100])
              ctx
            end

          {ctx, _} = s_s_warp(ctx, ["alberta", 117, 56])
          {ctx, _} = s_s_warp(ctx, ["comodo", 209, 143])
          ctx = close(ctx)
          throw({:script_end, ctx})

        5 ->
          ctx =
            if Rathena.truthy?(checkre(ctx, 0)) do
              {ctx, _} = s_s_warp(ctx, ["payon", 162, 59])
              ctx
            else
              {ctx, _} = s_s_warp(ctx, ["payon", 69, 100])
              ctx
            end

          {ctx, _} = s_s_warp(ctx, ["alberta", 117, 56])
          {ctx, _} = s_s_warp(ctx, ["comodo", 209, 143])
          ctx = close(ctx)
          throw({:script_end, ctx})

        6 ->
          {ctx, _} = s_s_warp(ctx, ["alberta", 117, 56])
          {ctx, _} = s_s_warp(ctx, ["comodo", 209, 143])
          ctx = close(ctx)
          throw({:script_end, ctx})

        7 ->
          {ctx, _} = s_s_warp(ctx, ["comodo", 209, 143])
          ctx = close(ctx)
          throw({:script_end, ctx})

        8 ->
          ctx = close(ctx)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    {ctx, _} = s_s_warp(ctx, [])
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  defp s_s_warp(ctx, args) do
    if zeny(ctx) >= 1800 do
      ctx =
        ctx
        |> pay_zeny(1800)
        |> todo(:warp, [Enum.at(args, 0, 0), Enum.at(args, 1, 0), Enum.at(args, 2, 0)])

      throw({:script_end, ctx})
    else
      ctx =
        ctx
        |> mes("[Airship Representative]")
        |> mes("I regret to say that you do not have enough zeny with you.")
        |> mes("Please check the amount of zeny that you have.")
        |> close()

      throw({:script_end, ctx})
    end
  end
end
