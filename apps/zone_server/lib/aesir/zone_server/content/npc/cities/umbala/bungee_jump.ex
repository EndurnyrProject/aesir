defmodule Aesir.ZoneServer.Content.Npc.Cities.Umbala.BungeeJump do
  @moduledoc """
  Transpiled from rAthena cities/umbala.txt:1023 (Bungee Jump).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, scope: :shared, spawn: []
  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    v1 = Enum.random(1..3)

    ctx =
      case v1 do
        1 ->
          ctx = percent_heal(ctx, hp: -100, sp: 0)

          ctx =
            mapannounce(
              ctx,
              "umbala",
              Rathena.concat(
                Rathena.concat("Bungee Jump: ", char_name(ctx, 0)),
                " : Kyaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa~~~~~~~"
              ),
              1
            )

          throw({:script_end, ctx})

        2 ->
          ctx = percent_heal(ctx, hp: -50, sp: 0)

          ctx =
            mapannounce(
              ctx,
              "umbala",
              Rathena.concat(
                Rathena.concat("Bungee Jump: ", char_name(ctx, 0)),
                " : Wooooooaaaaaaaaaaaaaahhhhhhhhhhhh~~~~~~!"
              ),
              1
            )

          throw({:script_end, ctx})

        3 ->
          ctx =
            if Enum.random(1..2) == 2 do
              ctx |> percent_heal(hp: -99, sp: 0) |> warp("nif_in", 69, 15)
            else
              ctx
            end

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  end
end
