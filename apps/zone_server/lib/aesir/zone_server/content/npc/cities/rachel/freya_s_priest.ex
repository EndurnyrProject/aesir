defmodule Aesir.ZoneServer.Content.Npc.Cities.Rachel.FreyaSPriest do
  @moduledoc """
  Transpiled from rAthena cities/rachel.txt:271 (Freya's Priest#play).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "rachel",
        x: 206,
        y: 30,
        dir: 3,
        sprite: 920,
        name: "Freya's Priest",
        scope: :shared,
        unique_name: "Freya's Priest#play"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Freya's Priest]")
      |> mes("This...")
      |> mes("bores me...")
      |> next()
      |> mes("[Freya's Priest]")
      |> mes("You!")
      |> mes("Entertain me!")
      |> emotion(:anger)
      |> next()
      |> select(["What? You can't tell me what to do!", "You got it."])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("[Freya's Priest]")
          |> mes("Oh. That much is")
          |> mes("true, I suppose.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, v2} =
      ctx
      |> mes("[Freya's Priest]")
      |> mes("Well, then,")
      |> mes("what will you be")
      |> mes("doing to entertain me?")
      |> next()
      |> select(["Here's a funny story...", "I will do what you want me to do."])

    ctx =
      if v2 == 1 do
        ctx =
          ctx
          |> mes("^3355FFYou told her the first")
          |> mes("funny story that you")
          |> mes("could think of.^000000")
          |> specialeffect2(:talk_frostjoke)
          |> next()
          |> mes("[Freya's Priest]")
          |> mes("That's supposed to")
          |> mes("be funny? I suppose")
          |> mes("that I don't understand")
          |> mes("hoi polloi humor.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx =
      ctx
      |> mes("[Freya's Priest]")
      |> mes("Fine, fine~")
      |> mes("Let me see what these")
      |> mes("item of yours can do...")
      |> mes("Oh? Oh! That's wonderful!")
      |> close()
      |> set_local(:play, Enum.random(1..10))

    ctx =
      if get_local(ctx, :play, 0) < 3 do
        warp(ctx, :random)
      else
        ctx =
          if get_local(ctx, :play, 0) < 5 do
            consumeitem(ctx, 12016)
          else
            consumeitem(ctx, 12017)
          end

        ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
