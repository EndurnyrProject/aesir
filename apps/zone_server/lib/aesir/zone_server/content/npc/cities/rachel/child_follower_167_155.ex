defmodule Aesir.ZoneServer.Content.Npc.Cities.Rachel.ChildFollower167155 do
  @moduledoc """
  Transpiled from rAthena cities/rachel.txt:570 (Child Follower#3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "ra_temple",
        x: 167,
        y: 155,
        dir: 4,
        sprite: 914,
        name: "Child Follower",
        scope: :shared,
        unique_name: "Child Follower#3"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Lewei]")
      |> mes("Shushh!")
      |> mes("Be quiet.")
      |> next()
      |> mes("[Lewei]")
      |> mes("......")
      |> mes(".........")
      |> next()
      |> mes("[Lewei]")
      |> mes("Go away! If someone!")
      |> mes("sees you, I'm gonna")
      |> mes("get caught, you jerk!")
      |> next()
      |> select(["What are you doing?", "Alright."])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("[Lewei]")
          |> mes("Hellooo~?")
          |> mes("Can't you see?")
          |> mes("It's called Hide-and-")
          |> mes("Go-Seek. Gee whiz!")
          |> mes("Hurry, get away!")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx |> mes("[Lewei]") |> mes("Hurry, and get") |> mes("outta here!") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
