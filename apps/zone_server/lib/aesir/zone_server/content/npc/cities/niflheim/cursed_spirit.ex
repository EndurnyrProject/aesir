defmodule Aesir.ZoneServer.Content.Npc.Cities.Niflheim.CursedSpirit do
  @moduledoc """
  Transpiled from rAthena cities/niflheim.txt:168 (Cursed Spirit#nif).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "niflheim",
        x: 350,
        y: 258,
        dir: 1,
        sprite: 802,
        name: "Cursed Spirit",
        scope: :shared,
        unique_name: "Cursed Spirit#nif"
      }
    ]

  @impl true
  def on_event("OnMyMobDead", ctx) do
    ev_onmymobdead(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> killmonster("niflheim", "Cursed Spirit#nif::OnMyMobDead")
      |> mes("[Ashe Bruce]")
      |> mes("I sense you're cursed")
      |> mes("by a powerful spell...")
      |> mes("Hmm... It's clear what")
      |> mes("you must be up to....")
      |> next()
      |> mes("[Ashe Bruce]")
      |> mes("You wish to get")
      |> mes("rid of your curse....")
      |> mes("By giving it to me!!")
      |> next()
      |> mes("[Ashe Bruce]")
      |> mes(
        "Just because I'm a cursed spirit, you adventurers think you can just dump your curses on me?!"
      )
      |> emotion(:fret)
      |> next()
      |> mes("[Ashe Bruce]")
      |> mes("I refuse to let")
      |> mes("you remain here.....")
      |> mes("Leave now, or I will")
      |> mes("remove you by force....")
      |> next()
      |> mes("[Ashe Bruce]")
      |> mes("....And...")
      |> mes("....Whatever you do...")
      |> mes("....Do NOT touch my books...")
      |> next()
      |> select([
        "Touch the first book.",
        "Touch the second book.",
        "Touch the third book.",
        "Okay, I am leaving."
      ])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> summon_mob(
              mob_id: 1478,
              map: "niflheim",
              at: {349, 259},
              event: "Cursed Spirit#nif::OnMyMobDead"
            )
            |> mes("[Ashe Bruce]")
            |> mes("...!...")
            |> mes("How dare you touch my books")
            |> mes("when I specifically said")
            |> mes("'Don't touch my books!'")
            |> next()
            |> mes("[Ashe Bruce]")
            |> mes("....!...Grrrrr!")
            |> mes("I shall tear you apart...!")
            |> mes("Be bound by an eternal curse...!")
            |> close()

          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Ashe Bruce]")
            |> mes("...!...")
            |> mes("You dare touch my books?!")
            |> mes("Right after I said not")
            |> mes("to touch them...?!")
            |> mes("Foolish mortal!")
            |> mes("...BEGONE!")
            |> close()
            |> warp("niflheim", 34, 162)

          throw({:script_end, ctx})

        3 ->
          {ctx, v2} =
            ctx
            |> mes("[Ashe Bruce]")
            |> mes("Muhahahaha....")
            |> mes("Stubborn mortal~!")
            |> mes("Fine! I will give you")
            |> mes("a fighting chance and let")
            |> mes("you cast a spell.")
            |> next()
            |> mes("[Ashe Bruce]")
            |> mes("But Blessings won't")
            |> mes("work with the curse")
            |> mes("that you have...")
            |> mes("And the spell to lift")
            |> mes("your curse has been")
            |> mes("lost to the ages~!")
            |> emotion(:kik)
            |> next()
            |> select(["Clover", "Klaatu", "Klaytos"])

          ctx =
            if v2 == 2 do
              set_local(ctx, :spell, get_local(ctx, :spell, 0) + 1)
            else
              ctx
            end

          {ctx, v3} = select(ctx, ["Verit", "Veritas", "Verata"])

          ctx =
            if v3 == 3 do
              set_local(ctx, :spell, get_local(ctx, :spell, 0) + 1)
            else
              ctx
            end

          {ctx, v4} = select(ctx, ["Necktie", "Necklace", "Nero", "^FFFFFFNictu!!!^000000"])

          ctx =
            if v4 == 4 do
              set_local(ctx, :spell, get_local(ctx, :spell, 0) + 1)
            else
              ctx
            end

          ctx =
            if get_local(ctx, :spell, 0) == 3 do
              v5 = Enum.random(1..5)

              ctx =
                case v5 do
                  1 ->
                    ctx =
                      if get_char_var(ctx, :morison_meat, 0) < 15 do
                        ctx =
                          ctx
                          |> set_char_var(:morrison_meat, 15)
                          |> mes("[Ashe Bruce]")
                          |> mes("You... You broke the curse!")
                          |> mes("How did you know that spell?!")
                          |> next()
                          |> mes("[Ashe Bruce]")
                          |> mes("I suppose you expect for me to")
                          |> mes("melt in agony about now, don't")
                          |> mes("you? Well... Sorry to disappoint")
                          |> mes("you, mortal, but I can never die!")
                          |> close()

                        throw({:script_end, ctx})
                      else
                        ctx
                      end

                    ctx =
                      ctx
                      |> mes("[Ashe Bruce]")
                      |> mes("...! You cast the correct spell?!")
                      |> mes("...!...")
                      |> mes("But...You're still cursed...")
                      |> mes("Umhaaaaaaaaaaaaaaaaa.....!")
                      |> close()

                    throw({:script_end, ctx})

                  2 ->
                    ctx =
                      if get_char_var(ctx, :thai_head, 0) == 1 do
                        ctx =
                          ctx
                          |> set_char_var(:thai_head, 2)
                          |> mes("[Ashe Bruce]")
                          |> mes("What's...")
                          |> mes("this feeling?")
                          |> next()
                          |> mes("[Ashe Bruce]")
                          |> mes("No...!")
                          |> mes("NOOOOOOOOOOOOOOOO!")
                          |> next()
                          |> mes("[Ashe Bruce]")
                          |> mes("Why did your spell have to work?!")
                          |> close()

                        throw({:script_end, ctx})
                      else
                        ctx
                      end

                    ctx =
                      ctx
                      |> mes("[Ashe Bruce]")
                      |> mes("You...")
                      |> mes("cast the correct spell?!")
                      |> next()
                      |> mes("[Ashe Bruce]")
                      |> mes("Hoho~")
                      |> mes("But you're still cursed...")
                      |> close()

                    throw({:script_end, ctx})

                  3 ->
                    ctx =
                      if get_char_var(ctx, :thai_head, 0) == 8 do
                        ctx =
                          ctx
                          |> set_char_var(:thai_head, 7)
                          |> mes("[Ashe Bruce]")
                          |> mes("You... You broke the curse!")
                          |> mes("Who taught you that spell?!")
                          |> next()
                          |> mes("[Ashe Bruce]")
                          |> mes("I suppose you expect for me to")
                          |> mes("melt in agony about now, don't")
                          |> mes("you? Well... Sorry to disappoint")
                          |> mes("you, mortal, but I can never die!")
                          |> next()
                          |> mes("[Ashe Bruce]")
                          |> mes("So long as I'm...")
                          |> mes("still...")
                          |> mes("cursed.")
                          |> next()
                          |> mes("[Ashe Bruce]")
                          |> mes("NOOOOOOOOOO!")
                          |> close()

                        throw({:script_end, ctx})
                      else
                        ctx
                      end

                    ctx =
                      ctx
                      |> mes("[Ashe Bruce]")
                      |> mes("...! You cast the correct spell?!")
                      |> mes("...!...")
                      |> mes("But...You're still cursed...")
                      |> mes("Umhaaaaaaaaaaaaaaaaa.....!")
                      |> close()

                    throw({:script_end, ctx})

                  4 ->
                    ctx =
                      ctx
                      |> mes("[Ashe Bruce]")
                      |> mes("...! You cast the correct spell?!")
                      |> mes("...!...")
                      |> mes("But...You're still cursed...")
                      |> mes("Mwahahahaaaa.....!")
                      |> close()

                    throw({:script_end, ctx})

                  _ ->
                    ctx
                end

              ctx
            else
              ctx
            end

          ctx =
            ctx
            |> summon_mob(
              mob_id: 1462,
              map: "niflheim",
              at: {345, 259},
              event: "Cursed Spirit#nif::OnMyMobDead"
            )
            |> summon_mob(
              mob_id: 1462,
              map: "niflheim",
              at: {347, 261},
              event: "Cursed Spirit#nif::OnMyMobDead"
            )
            |> summon_mob(
              mob_id: 1462,
              map: "niflheim",
              at: {344, 253},
              event: "Cursed Spirit#nif::OnMyMobDead"
            )
            |> summon_mob(
              mob_id: 1462,
              map: "niflheim",
              at: {346, 251},
              event: "Cursed Spirit#nif::OnMyMobDead"
            )
            |> summon_mob(
              mob_id: 1462,
              map: "niflheim",
              at: {349, 249},
              event: "Cursed Spirit#nif::OnMyMobDead"
            )
            |> summon_mob(
              mob_id: 1462,
              map: "niflheim",
              at: {350, 260},
              event: "Cursed Spirit#nif::OnMyMobDead"
            )
            |> summon_mob(
              mob_id: 1462,
              map: "niflheim",
              at: {353, 256},
              event: "Cursed Spirit#nif::OnMyMobDead"
            )
            |> mes("[Ashe Bruce]")
            |> mes("Muhahahahahaha!")
            |> mes("That's not the right spell!")
            |> mes("Now, death awaits you!")
            |> mes("You're eternally bound")
            |> mes("to the curse...!")
            |> close()

          throw({:script_end, ctx})

        4 ->
          ctx =
            ctx
            |> mes("[Ashe Bruce]")
            |> mes("...")
            |> mes(".....")
            |> next()
            |> mes("[Ashe Bruce]")
            |> mes("Well then.")
            |> mes("Try not to trip on")
            |> mes("your feet in your")
            |> mes("rush to leave.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_onmymobdead(ctx) do
    ctx
  end
end
