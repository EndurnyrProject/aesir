defmodule Aesir.ZoneServer.Content.Npc.Cities.Manuk.YoungVillager do
  @moduledoc """
  Transpiled from rAthena cities/manuk.txt:490 (Young Villager#ep13bs).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "manuk",
        x: 251,
        y: 180,
        dir: 5,
        sprite: 454,
        name: "Young Villager",
        unique_name: "Young Villager#ep13bs"
      }
    ]

  @impl true
  def on_talk(ctx) do
    if is_equipped(ctx, 2782) == 1 do
      ctx =
        ctx
        |> mes("[Young Villager]")
        |> mes("It's past the time of our date, why isn't she here yet!!?")
        |> close()

      throw({:script_end, ctx})
    else
      ctx = ctx |> mes("[Asd]") |> mes("Ywo di pi butfs oui Afbsu ") |> close()
      throw({:script_end, ctx})
    end
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
