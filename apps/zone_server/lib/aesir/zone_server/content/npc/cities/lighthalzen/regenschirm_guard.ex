defmodule Aesir.ZoneServer.Content.Npc.Cities.Lighthalzen.RegenschirmGuard do
  @moduledoc """
  Transpiled from rAthena cities/lighthalzen.txt:1950 (Regenschirm Guard#40).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "lhz_in01",
        x: 24,
        y: 140,
        dir: 3,
        sprite: 868,
        name: "Regenschirm Guard",
        unique_name: "Regenschirm Guard#40"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Regenschirm Guard]")

    ctx =
      if Rathena.truthy?(:erlang.band(get_char_var(ctx, :MISC_QUEST, 0), 512)) and
           count_item(ctx, 2657) > 0 do
        {ctx, v1} =
          ctx
          |> mes("Do you wish to")
          |> mes("go underground?")
          |> next()
          |> select(["Yes", "No"])

        ctx =
          if v1 == 1 do
            ctx =
              ctx
              |> mes("[Regenschirm Guard]")
              |> mes("Thank you and")
              |> mes("have a nice day.")
              |> close()
              |> warp("lhz_dun01", 149, 285)

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx =
          ctx
          |> mes("[Regenschirm Guard]")
          |> mes("Thank you and")
          |> mes("have a nice day.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("May I help you?")
    |> mes("If you would like to")
    |> mes("enter, you must first")
    |> mes("have a Laboratory Permit.")
    |> mes("Thank you for your cooperation.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
