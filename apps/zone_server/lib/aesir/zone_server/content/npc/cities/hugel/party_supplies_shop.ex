defmodule Aesir.ZoneServer.Content.Npc.Cities.Hugel.PartySuppliesShop do
  @moduledoc """
  Transpiled from rAthena cities/hugel.txt:141 (Party Supplies Shop).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "hu_in01",
        x: 23,
        y: 311,
        dir: 4,
        sprite: 898,
        name: "Party Supplies Shop",
        scope: :shared
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Shopkeeper]")
      |> mes("Welcome to the party supplies")
      |> mes("shop!")
      |> mes("Why don't you enjoy some")
      |> mes("spectacular fireworks with your")
      |> mes("friends?")
      |> mes("We can provide you with 5 of them")
      |> mes("at 500 zeny.")
      |> next()
      |> select(["Buy", "Cancel"])

    ctx =
      case v1 do
        1 ->
          ctx =
            if zeny(ctx) < 500 do
              ctx =
                ctx
                |> mes("[Shopkeeper]")
                |> mes("I am sorry, but you don't have")
                |> mes("enough money~")
                |> close()

              throw({:script_end, ctx})
            else
              ctx
            end

          ctx =
            ctx
            |> pay_zeny(500)
            |> give_item(12018, 5)
            |> mes("[Shopkeeper]")
            |> mes("Here you go!")
            |> mes("Have fun with them!")
            |> close()

          throw({:script_end, ctx})

        2 ->
          ctx = ctx |> mes("[Shopkeeper]") |> mes("Thank you, please come again.") |> close()
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
