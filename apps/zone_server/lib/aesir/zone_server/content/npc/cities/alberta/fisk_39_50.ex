defmodule Aesir.ZoneServer.Content.Npc.Cities.Alberta.Fisk3950 do
  @moduledoc """
  Transpiled from rAthena cities/alberta.txt:234 (Fisk#a2t).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    scope: :shared,
    spawn: [
      %{
        map: "alb2trea",
        x: 39,
        y: 50,
        dir: 6,
        sprite: 100,
        name: "Fisk",
        scope: :shared,
        unique_name: "Fisk#a2t"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Fisk]")
      |> mes("So you wanna head back to the mainland in Alberta, eh?")
      |> next()
      |> select(["Yes please.", "I changed my mind."])

    ctx =
      if v1 == 1 do
        warp(ctx, "alberta", 192, 169)
      else
        ctx
      end

    close(ctx)
  end
end
