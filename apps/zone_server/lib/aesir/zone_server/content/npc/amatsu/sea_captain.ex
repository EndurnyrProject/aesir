defmodule Aesir.ZoneServer.Content.Npc.Amatsu.SeaCaptain do
  @moduledoc """
  Transpiled from rAthena cities/amatsu.txt:127 (Sea Captain#ama2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "amatsu", x: 194, y: 79, dir: 5, sprite: 709, name: "Sea Captain"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Walter Moers]")
      |> mes("You came... Did you enjoy")
      |> mes("your trip to Amatsu...?")
      |> mes("Alright, I will take you")
      |> mes("back to Alberta.")
      |> next()
      |> select(["Back to Alberta", "Cancel"])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("[Walter Moers]")
          |> mes("Let's go then. You must have")
          |> mes("so many things to talk about,")
          |> mes("right? All aboard now.")
          |> close()

        _ =
          if Rathena.truthy?(checkre(ctx, 0)) do
            warp(ctx, "alberta", 245, 87)
          else
            warp(ctx, "alberta", 243, 91)
          end

        exit(:normal)
      else
        ctx
      end

    ctx
    |> mes("[Walter Moers]")
    |> mes("Well, take your time.")
    |> mes("The ship to Alberta is")
    |> mes("always ready to depart...")
    |> close()

    exit(:normal)
  end
end
