defmodule Aesir.ZoneServer.Content.Npc.Warps.Guildcastles.Aldeg415Aldeg4 do
  @moduledoc """
  Transpiled from rAthena warps/guildcastles.txt:371 (aldeg-4-15_aldeg-4-).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "aldeg_cas04",
        x: 132,
        y: 231,
        dir: 0,
        sprite: 45,
        name: "aldeg-4-15_aldeg-4-",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    v1 = Enum.random(1..5)

    ctx =
      case v1 do
        1 ->
          ctx = warp(ctx, "aldeg_cas04", 152, 210)
          throw({:script_end, ctx})

        2 ->
          ctx = warp(ctx, "aldeg_cas04", 111, 210)
          throw({:script_end, ctx})

        3 ->
          ctx = warp(ctx, "aldeg_cas04", 129, 212)
          throw({:script_end, ctx})

        4 ->
          ctx = warp(ctx, "aldeg_cas04", 129, 212)
          throw({:script_end, ctx})

        5 ->
          ctx = warp(ctx, "aldeg_cas04", 14, 196)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  end
end
