defmodule Aesir.ZoneServer.Content.Npc.Warps.Fields.Jawaii.Jawaiiwarp006 do
  @moduledoc """
  Transpiled from rAthena warps/fields/jawaii.txt:24 (jawaiiwarp006).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "jawaii_in",
        x: 27,
        y: 91,
        dir: 4,
        sprite: 45,
        name: "jawaiiwarp006",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_talk(ctx) do
    ctx =
      if getpartnerid(ctx) > 0 do
        warp(ctx, "jawaii", 192, 218)
      else
        warp(ctx, "jawaii_in", 27, 94)
      end

    ctx
  end
end
