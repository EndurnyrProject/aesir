defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.KhDun.KhDun04 do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/kh_dun.txt:29 (kh_dun04).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "kh_dun02", x: 43, y: 195, dir: 0, sprite: 45, name: "kh_dun04", trigger: {1, 1}}
    ]

  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if Enum.random(1..2) == 1 do
        warp(ctx, "kh_dun01", 41, 206)
      else
        warp(ctx, "kh_dun01", 32, 199)
      end

    ctx
  end
end
