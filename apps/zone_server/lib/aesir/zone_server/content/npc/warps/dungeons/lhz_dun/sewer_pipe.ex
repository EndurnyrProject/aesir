defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.LhzDun.SewerPipe do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/lhz_dun.txt:55 (Sewer Pipe#lt).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "lighthalzen",
        x: 313,
        y: 301,
        dir: 0,
        sprite: 111,
        name: "Sewer Pipe",
        unique_name: "Sewer Pipe#lt"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if Rathena.truthy?(:erlang.band(get_char_var(ctx, :MISC_QUEST, 0), 512)) do
        {ctx, v1} =
          ctx
          |> mes("^3355FFYou found some sort")
          |> mes("of large sewage pipe.")
          |> mes("Naturally, the stench")
          |> mes("emanating from inside")
          |> mes("the pipe is barely endurable.^000000")
          |> next()
          |> select(["Enter", "Cancel"])

        ctx =
          if v1 == 1 do
            ctx = warp(ctx, "lhz_cube", 231, 17)
            throw({:script_end, ctx})
          else
            ctx
          end

        ctx = close(ctx)
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("^3355FFYou found some sort")
    |> mes("of large sewage pipe.")
    |> mes("Naturally, the stench")
    |> mes("emanating from inside")
    |> mes("the pipe is barely endurable.^000000")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
