defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.MoskDun.Ship do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/mosk_dun.txt:214 (Ship#mos_dun).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "mosk_fild02",
        x: 198,
        y: 53,
        dir: 3,
        sprite: 111,
        name: "Ship",
        unique_name: "Ship#mos_dun"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("- You find a ship at anchor. -")
      |> next()
      |> select(["Take the ship and go back to the village.", "Explore more."])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("- You board the ship and pull at the oars. -")
          |> close()
          |> warp("moscovia", 140, 54)

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx |> mes("- That unknown area arouses your interest. -") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
