defmodule Aesir.ZoneServer.Content.Npc.Warps.Fields.Glastheim.Gl182 do
  @moduledoc """
  Transpiled from rAthena warps/fields/glastheim.txt:56 (gl18-2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "gl_prison", x: 11, y: 70, dir: 0, sprite: 45, name: "gl18-2", trigger: {1, 1}}
    ]

  @impl true
  def on_talk(ctx) do
    v1 = :rand.uniform(4) - 1

    ctx =
      case v1 do
        0 ->
          ctx = warp(ctx, "gl_cas01", 163, 193)
          throw({:script_end, ctx})

        1 ->
          ctx = warp(ctx, "gl_cas01", 238, 192)
          throw({:script_end, ctx})

        2 ->
          ctx = warp(ctx, "gl_cas01", 144, 314)
          throw({:script_end, ctx})

        3 ->
          ctx = warp(ctx, "gl_cas01", 367, 301)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
