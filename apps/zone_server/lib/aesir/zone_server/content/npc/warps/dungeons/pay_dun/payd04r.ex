defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.PayDun.Payd04r do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/pay_dun.txt:23 (payd04r).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "pay_dun03", x: 127, y: 62, dir: 4, sprite: 45, name: "payd04r", trigger: {2, 2}}
    ]

  @impl true
  def on_talk(ctx) do
    v1 = :rand.uniform(4) - 1

    ctx =
      case v1 do
        0 ->
          ctx = warp(ctx, "pay_dun04", 201, 204)
          throw({:script_end, ctx})

        1 ->
          ctx = warp(ctx, "pay_dun04", 189, 43)
          throw({:script_end, ctx})

        2 ->
          ctx = warp(ctx, "pay_dun04", 43, 40)
          throw({:script_end, ctx})

        3 ->
          ctx = warp(ctx, "pay_dun04", 34, 202)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
