defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.MoskDun.Milestone27131 do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/mosk_dun.txt:175 (Milestone#3-2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "mosk_dun03",
        x: 27,
        y: 131,
        dir: 3,
        sprite: 111,
        name: "Milestone",
        unique_name: "Milestone#3-2"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("- There is writing on a big rock. -")
      |> next()
      |> mes(
        "^3131FF- Those who stay may die but will gain honor, those who go back may get hurt but will gain courage."
      )
      |> mes("Those who have courage will go back, otherwise step forward and stay. -^000000")
      |> next()
      |> select(["Go back.", "Stay."])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("- You sense danger and decide to go back -")
          |> close()
          |> warp("mosk_dun02", 262, 118)

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx |> mes("- You decide to stay. -") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
