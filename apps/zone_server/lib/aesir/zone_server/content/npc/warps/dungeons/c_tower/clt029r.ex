defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.CTower.Clt029r do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/c_tower.txt:111 (clt029r).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "c_tower4", x: 142, y: 151, dir: 4, sprite: 45, name: "clt029r", trigger: {0, 0}}
    ]

  @impl true
  def on_talk(ctx) do
    v1 = :rand.uniform(4) - 1

    ctx =
      case v1 do
        0 ->
          ctx = warp(ctx, "c_tower3", 168, 252)
          throw({:script_end, ctx})

        1 ->
          ctx = warp(ctx, "alde_dun02", 262, 41)
          throw({:script_end, ctx})

        2 ->
          ctx = warp(ctx, "c_tower4", 73, 154)
          throw({:script_end, ctx})

        3 ->
          ctx = warp(ctx, "c_tower4", 140, 149)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
