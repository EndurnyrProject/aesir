defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.MoskDun.Milestone268117 do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/mosk_dun.txt:159 (Milestone#2-3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "mosk_dun02",
        x: 268,
        y: 117,
        dir: 3,
        sprite: 111,
        name: "Milestone",
        unique_name: "Milestone#2-3"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("- There is writing on a big rock. -")
      |> next()
      |> mes(
        "^3131FF- Those who step forward will gain honor but may die, those who step backward will have courage but may still get hurt."
      )
      |> mes("If you are truly brave, step forward. Otherwise, step back -^000000")
      |> next()
      |> select(["Step forward.", "Stay."])

    ctx =
      if v1 == 1 do
        ctx =
          ctx |> mes("- You bravely step forward. -") |> close() |> warp("mosk_dun03", 32, 135)

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx |> mes("- You decided to stay. -") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
