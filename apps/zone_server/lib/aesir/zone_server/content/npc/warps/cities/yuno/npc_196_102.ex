defmodule Aesir.ZoneServer.Content.Npc.Warps.Cities.Yuno.Npc196102 do
  @moduledoc """
  Transpiled from rAthena warps/cities/yuno.txt:102 (#yun64).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "yuno_in05",
        x: 196,
        y: 102,
        dir: 0,
        sprite: 45,
        name: "",
        unique_name: "#yun64",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_talk(ctx) do
    v1 = :rand.uniform(3) - 1

    ctx =
      case v1 do
        0 ->
          ctx = warp(ctx, "yuno_in05", 169, 102)
          throw({:script_end, ctx})

        1 ->
          ctx = warp(ctx, "yuno_in05", 128, 82)
          throw({:script_end, ctx})

        2 ->
          ctx = warp(ctx, "yuno_in05", 156, 141)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
