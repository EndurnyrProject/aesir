defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.RaSan.Sanctuary01 do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/ra_san.txt:19 (sanctuary01).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "ra_san01", x: 139, y: 13, dir: 0, sprite: 45, name: "sanctuary01", trigger: {1, 1}}
    ]

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :ra_tem_q, 0) == 21 do
        ctx = warp(ctx, "que_san04", 119, 110)
        throw({:script_end, ctx})
      else
        ctx
      end

    v1 = :rand.uniform(3) - 1

    ctx =
      case v1 do
        1 ->
          warp(ctx, "ra_san02", 213, 275)

        2 ->
          warp(ctx, "ra_san03", 119, 283)

        _ ->
          warp(ctx, "ra_san04", 119, 110)
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
