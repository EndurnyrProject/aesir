defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.PrtMaze.Npc7474 do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/prt_maze.txt:169 (#mazewarp5339).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "prt_maze03",
        x: 74,
        y: 74,
        dir: 0,
        sprite: 45,
        name: "",
        unique_name: "#mazewarp5339",
        trigger: {1, 1}
      }
    ]

  @impl true
  def on_talk(ctx) do
    v1 = :rand.uniform(4) - 1

    ctx =
      case v1 do
        0 ->
          ctx = warp(ctx, "prt_maze03", 98, 151)
          throw({:script_end, ctx})

        1 ->
          ctx = warp(ctx, "prt_maze03", 137, 128)
          throw({:script_end, ctx})

        2 ->
          ctx = warp(ctx, "prt_maze03", 14, 71)
          throw({:script_end, ctx})

        3 ->
          ctx = warp(ctx, "prt_maze03", 54, 8)
          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
