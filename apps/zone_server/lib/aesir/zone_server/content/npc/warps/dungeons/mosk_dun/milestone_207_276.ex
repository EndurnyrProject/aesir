defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.MoskDun.Milestone207276 do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/mosk_dun.txt:111 (Milestone#1-2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "mosk_dun01",
        x: 207,
        y: 276,
        dir: 3,
        sprite: 111,
        name: "Milestone",
        unique_name: "Milestone#1-2"
      }
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("- There is writing on a big rock. -")
      |> next()
      |> mes(
        "^3131FF- Those who advance may get hurt but are courageous of heart and those who move back will remain safe."
      )
      |> mes("If you have courage, step forward. Otherwise, step backward. -^000000")
      |> next()
      |> select(["Step forward.", "Stay."])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("- You follow the way before you. -")
          |> close()
          |> warp("mosk_dun02", 165, 30)

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx |> mes("- You won't advance. -") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
