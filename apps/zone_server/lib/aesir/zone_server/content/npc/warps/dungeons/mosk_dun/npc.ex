defmodule Aesir.ZoneServer.Content.Npc.Warps.Dungeons.MoskDun.Npc do
  @moduledoc """
  Transpiled from rAthena warps/dungeons/mosk_dun.txt:23 (#babayagain).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "mosk_dun02",
        x: 53,
        y: 220,
        dir: 4,
        sprite: 45,
        name: "",
        unique_name: "#babayagain",
        trigger: {1, 1}
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    if get_char_var(ctx, :mos_nowinter, 0) == 11 do
      ctx =
        ctx
        |> donpcevent("Soldier1#mos::OnEnable")
        |> donpcevent("Soldier2#mos::OnEnable")
        |> donpcevent("Soldier3#mos::OnEnable")
        |> mes("[Soldier]")
        |> mes("Halt right there!")
        |> next()
        |> mes("[Soldier]")
        |> mes("We have information")
        |> mes("that you've been secretly")
        |> mes("communicating with")
        |> mes("Baba Yaga, the Horrible.")
        |> next()
        |> mes("[Soldier]")
        |> mes("If you have something to say,")
        |> mes("speak to the Csar.")
        |> mes("Come with us.")
        |> close()
        |> donpcevent("Soldier1#mos::OnInit")
        |> donpcevent("Soldier2#mos::OnInit")
        |> donpcevent("Soldier3#mos::OnInit")
        |> set_char_var(:mos_nowinter, 12)
        |> warp("mosk_in", 125, 92)

      throw({:script_end, ctx})
    else
      if get_char_var(ctx, :mos_nowinter, 0) == 12 or get_char_var(ctx, :mos_nowinter, 0) == 13 do
        {ctx, v1} =
          ctx
          |> donpcevent("Soldier1#mos::OnEnable")
          |> donpcevent("Soldier2#mos::OnEnable")
          |> donpcevent("Soldier3#mos::OnEnable")
          |> mes("[Soldier]")
          |> mes("We expected")
          |> mes("that you would appear again.")
          |> mes("What is your business with the Csar?")
          |> next()
          |> select(["Run away.", "Explain the situation."])

        ctx =
          if v1 == 1 do
            ctx
            |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
            |> mes("I should get out of here...")
            |> close()
            |> warp("mosk_dun02", 100, 100)
          else
            ctx
            |> mes("-I explain what happend until now.-")
            |> next()
            |> mes("[Soldier]")
            |> mes("If you have something to say, speak")
            |> mes("to the Csar.")
            |> mes("If you leave to this place,")
            |> mes("you will be considered as our enemy.")
            |> close()
          end

        ctx =
          ctx
          |> donpcevent("Soldier1#mos::OnInit")
          |> donpcevent("Soldier2#mos::OnInit")
          |> donpcevent("Soldier3#mos::OnInit")

        throw({:script_end, ctx})
      else
        ctx = warp(ctx, "mosk_in", 215, 41)
        throw({:script_end, ctx})
      end
    end
  end
end
