defmodule Aesir.ZoneServer.Content.Npc.JawaiiIn.Employee30100 do
  @moduledoc """
  Transpiled from rAthena cities/jawaii.txt:672 (Employee#jaw8).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "jawaii_in", x: 30, y: 100, dir: 4, sprite: 724, name: "Employee"}]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnWelcome", ctx), do: ev_onwelcome(ctx)
  def on_event("OnSolo", ctx), do: ev_onsolo(ctx)
  @impl true
  def on_talk(ctx) do
    ctx = ctx |> mes("[Employee Yasmine]") |> mes("Welcome to Jawaii Tavern~")

    ctx =
      if Rathena.truthy?(getpartnerid(ctx)) do
        ctx
        |> mes(
          "Just like the moon and the stars, you and your partner look perfect together. I'm really happy for you!"
        )
        |> close()

        exit(:normal)
      else
        ctx =
          if not Rathena.truthy?(getpartnerid(ctx)) do
            ctx
            |> next()
            |> mes("[Employee Yasmine]")
            |> mes(
              "You don't look like you belong here, but I hope you find your perfect match one of these days. Have fun!"
            )
            |> close()

            exit(:normal)
          else
            ctx
          end

        ctx
      end

    ctx |> mes("I hope you will have a good time.") |> close()
    exit(:normal)
  end

  def ev_onwelcome(ctx) do
    _ = emotion(ctx, :chup)
    exit(:normal)
  end

  def ev_onsolo(ctx) do
    _ = emotion(ctx, :huk)
    exit(:normal)
  end
end
