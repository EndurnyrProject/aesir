defmodule Aesir.ZoneServer.Content.Npc.JawaiiIn.Employee25100 do
  @moduledoc """
  Transpiled from rAthena cities/jawaii.txt:541 (Employee#jaw4).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "jawaii_in", x: 25, y: 100, dir: 0, sprite: 724, name: "Employee"}]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnWelcome", ctx) do
    ev_onwelcome(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def on_event("OnSolo", ctx) do
    ev_onsolo(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx = ctx |> mes("[Employee Itere]") |> mes("Welcome to") |> mes("Jawaii Tavern~")

    ctx =
      if Rathena.truthy?(getpartnerid(ctx)) do
        ctx =
          ctx
          |> next()
          |> mes("[Employee Itere]")
          |> mes("Oh~")
          |> mes("You look so happy")
          |> mes("to be here with your")
          |> mes("partner! How precious~")
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if not Rathena.truthy?(getpartnerid(ctx)) do
            ctx =
              ctx
              |> mes("I hope you")
              |> mes("enjoy your st--")
              |> mes("Wait a minute...!")
              |> next()
              |> mes("[Employee Itere]")
              |> mes("You're...")
              |> mes("You better not be part of")
              |> mes("the Invincible Single Army!")
              |> next()
              |> mes("[Employee Itere]")
              |> mes("Well, whatever you do, don't despair, get drunk and then")
              |> mes("bother the married couples!")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx |> mes("I hope you will have a good time.") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_onwelcome(ctx) do
    emotion(ctx, :chup)
  end

  def ev_onsolo(ctx) do
    emotion(ctx, :huk)
  end
end
