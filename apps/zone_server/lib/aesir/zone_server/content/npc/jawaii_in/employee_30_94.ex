defmodule Aesir.ZoneServer.Content.Npc.JawaiiIn.Employee3094 do
  @moduledoc """
  Transpiled from rAthena cities/jawaii.txt:580 (Employee#jaw5).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "jawaii_in", x: 30, y: 94, dir: 4, sprite: 724, name: "Employee"}]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnWelcome", ctx) do
    ev_onwelcome(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def on_event("OnSolo", ctx) do
    ev_onsolo(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx = ctx |> mes("[Employee Tonia]") |> mes("Welcome to Jawaii Tavern~")

    ctx =
      if Rathena.truthy?(getpartnerid(ctx)) do
        ctx =
          ctx
          |> next()
          |> mes("[Employee Tonia]")
          |> mes("Congratulations to both of you!")
          |> next()
          |> mes("[Employee Tonia]")
          |> mes("^666666*Sigh...*^000000")
          |> mes(
            "^333333I hope those Single Army morons don't get drunk and do something stupid again..."
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx =
          if not Rathena.truthy?(getpartnerid(ctx)) do
            ctx =
              ctx
              |> mes("Wait a sec. You're...!")
              |> next()
              |> mes("[Employee Tonia]")
              |> mes("Hey--!")
              |> mes("You're not welcome here!")
              |> mes("S-Stop drinking! Right this instant!")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      end

    ctx |> mes("I hope you will have a good time.") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_onwelcome(ctx) do
    emotion(ctx, :chup)
  end

  def ev_onsolo(ctx) do
    emotion(ctx, :huk)
  end
end
