defmodule Aesir.ZoneServer.Content.Npc.Dicastes01.TrainingGalten do
  @moduledoc """
  Transpiled from rAthena re/cities/dicastes.txt:80 (Training Galten#a).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "dicastes01", x: 234, y: 158, dir: 0, sprite: 450, name: "Training Galten"}]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx = mes(ctx, "[Training Galten]")

    ctx =
      if Rathena.truthy?(is_equipped(ctx, 2782)) do
        ctx =
          ctx
          |> mes("I'll be ready for anything")
          |> mes("if I keep up with my training every day.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("?????? ?? ?????? ??")
    |> mes("?? ??")
    |> next()
    |> mes("Don't know what it means.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
