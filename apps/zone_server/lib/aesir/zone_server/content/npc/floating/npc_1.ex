defmodule Aesir.ZoneServer.Content.Npc.Floating.Npc1 do
  @moduledoc """
  Transpiled from rAthena quests/first_class/tu_archer.txt:1894 (#sound_tu_).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []

  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if get_char_var(ctx, :tu_archer02, 0) == 6 do
        ctx =
          ctx
          |> mes("[Minister]")
          |> mes("Contact the")
          |> mes("Prontera Church.")
          |> mes("Hurry, this is urgent!")
          |> set_char_var(:tu_archer02, 7)
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
  end
end
