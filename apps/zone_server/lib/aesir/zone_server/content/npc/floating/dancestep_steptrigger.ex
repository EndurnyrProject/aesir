defmodule Aesir.ZoneServer.Content.Npc.Floating.DancestepSteptrigger do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/dancer.txt:1293 (dancestep::StepTrigger).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, scope: :shared, spawn: []
  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx), do: ev_ontouch(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_ontouch(ctx) do
    ctx = donpcevent(ctx, "Backdancer#1::OnOmg")

    ctx
    |> mapannounce(
      "job_duncer",
      Rathena.concat(
        Rathena.concat(" ", char_name(ctx, 0)),
        ", you lack rhythm... Your timing was too late!"
      ),
      1
    )
    |> set_char_var(:DANC_Q, 8)
    |> donpcevent("Bijou#dance_timer::OnDisable")
    |> donpcevent("Waiting Room#dance::OnEnable")
    |> warp("comodo", 188, 162)
  end
end
