defmodule Aesir.ZoneServer.Content.Npc.Floating.BifrostField0000 do
  @moduledoc """
  Transpiled from rAthena re/warps/fields/bif_fild.txt:23 (bifrost_field0000).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "bif_fild02",
        x: 285,
        y: 332,
        dir: 0,
        sprite: 844,
        name: "Small Hole",
        unique_name: "Small Hole#ep14_mora1"
      },
      %{
        map: "bif_fild02",
        x: 95,
        y: 310,
        dir: 0,
        sprite: 844,
        name: "Small Hole",
        unique_name: "Small Hole#ep14_mora2"
      },
      %{
        map: "bif_fild02",
        x: 174,
        y: 162,
        dir: 0,
        sprite: 844,
        name: "Small Hole",
        unique_name: "Small Hole#ep14_mora3"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("At the end of the small path through the bright flower garden")
      |> mes("is an entrance to something that looks like a small hill.")
      |> next()
      |> select(["Knock-knock", "Is this a wormhole?"])

    ctx =
      if v1 == 1 do
        ctx =
          ctx
          |> mes("When you knock on the door-like thing just for fun,")
          |> mes("the door clicks open and you feel some mysterious force pulling you inside.")
          |> close()

        v2 = Rathena.atoi(Rathena.charat(strnpcinfo(ctx, 2), 9))

        ctx =
          case v2 do
            1 ->
              ctx = warp(ctx, "mora", 179, 74)
              throw({:script_end, ctx})

            2 ->
              ctx = warp(ctx, "mora", 22, 157)
              throw({:script_end, ctx})

            3 ->
              ctx = warp(ctx, "mora", 58, 27)
              throw({:script_end, ctx})

            _ ->
              ctx
          end

        ctx
      else
        ctx
      end

    close(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    specialeffect(ctx, :level99_4)
  end
end
