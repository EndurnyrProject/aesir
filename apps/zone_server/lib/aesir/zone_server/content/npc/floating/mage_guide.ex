defmodule Aesir.ZoneServer.Content.Npc.Floating.MageGuide do
  @moduledoc """
  Transpiled from rAthena re/jobs/novice/novice.txt:1962 (Mage Guide#newbe10::NvMage).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "new_1-3",
        x: 117,
        y: 41,
        dir: 3,
        sprite: 123,
        name: "Mage Guide",
        unique_name: "Mage Guide#nv1"
      },
      %{
        map: "new_2-3",
        x: 117,
        y: 41,
        dir: 3,
        sprite: 123,
        name: "Mage Guide",
        unique_name: "Mage Guide#nv2"
      },
      %{
        map: "new_3-3",
        x: 117,
        y: 41,
        dir: 3,
        sprite: 123,
        name: "Mage Guide",
        unique_name: "Mage Guide#nv3"
      },
      %{
        map: "new_4-3",
        x: 117,
        y: 41,
        dir: 3,
        sprite: 123,
        name: "Mage Guide",
        unique_name: "Mage Guide#nv4"
      },
      %{
        map: "new_5-3",
        x: 117,
        y: 41,
        dir: 3,
        sprite: 123,
        name: "Mage Guide",
        unique_name: "Mage Guide#nv5"
      }
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    ctx =
      if get_char_var(ctx, :job_novice_q, 0) < 15 do
        ctx =
          ctx
          |> mes("[Mage Guild Member]")
          |> mes("You are still in the process of training with Instructor Brade.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    {ctx, v1} =
      ctx
      |> mes("[Mage Guild Member]")
      |> mes("What can I help you with?")
      |> next()
      |> select(["Tell me about Mages", "I want to be a Mage", "Give me a Quest", "Cancel"])

    ctx =
      case v1 do
        1 ->
          ctx =
            ctx
            |> mes("[Mage Guild Member]")
            |> mes("Mages are those who can use magic when they need to protect their friends.")
            |> mes(
              "Mages can curse enemies using the elements of fire, water, earth and thunder magic."
            )
            |> next()
            |> mes("[Mage Guild Member]")
            |> mes("However they cannot equip other weapons but staffs and books.")
            |> mes("Because they are too delicate")
            |> mes("to carry heavy weapons.")
            |> next()
            |> mes("[Mage Guild Member]")
            |> mes("But they can cover this weakness")
            |> mes("by their remarkable magic skills.")
            |> mes("That is why many people want")
            |> mes("to become a Mage!")
            |> next()
            |> mes("[Mage Guild Member]")
            |> mes("Mages have to be in their guild for guild's power and honor.")
            |> mes("This is one of reasons why Mages are popular in any parties and guilds.")
            |> mes("Why don't you become a Mage?")
            |> next()
            |> mes("[Mage Guild Member]")
            |> mes("^8E2323Mages can transform to Wizards")
            |> mes("^8E2323or Sages as their second job^000000.")
            |> next()

          ctx =
            if Rathena.truthy?(is_equipped(ctx, 2824)) or Rathena.truthy?(count_item(ctx, 2824)) do
              ctx =
                ctx
                |> mes("[Mage Guild Member]")
                |> mes("I think you should test Mage skills before you decide what to do.")
                |> mes("I can provide you with our test skill registration form.")
                |> close()

              throw({:script_end, ctx})
            else
              ctx
            end

          {ctx, v2} =
            ctx
            |> mes("[Mage Guild Member]")
            |> mes("Do you want to register to experience how it is to be a Mage?")
            |> mes("Would you like to test Mage skills?")
            |> next()
            |> select(["I want to test Mage skills.", "I don't want to test it."])

          ctx =
            case v2 do
              1 ->
                {ctx, _} =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("Here is the Mage Manual.")
                  |> mes("I will take the other job registration forms and manuals from you.")
                  |> next()
                  |> Aesir.ZoneServer.Content.Npc.Functions.FNverase.call([])

                ctx =
                  ctx
                  |> give_item(2824, 1)
                  |> next()
                  |> mes("[Mage Guild Member]")
                  |> mes("After equipping the Manual, open your Skill menu by pressing ALT+S.")
                  |> mes(
                    "Make sure to minimize your Skill tree by clicking the '-' button on the top right corner of the Skill Tree Window."
                  )
                  |> close()

                throw({:script_end, ctx})

              2 ->
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("Hum. Do you think so.")
                  |> mes("Ok, I agree with you. Choosing a job is very important in our life.")
                  |> mes("Just talk to me whenever you want to experience Mage skills.")
                  |> close()

                throw({:script_end, ctx})

              _ ->
                ctx
            end

          {ctx, v3} =
            ctx
            |> mes("[Mage Guild Member]")
            |> mes("You want to be a Mage?")
            |> mes("Ok, then, I will send you to the Mage Guild union in Geffen right away.")
            |> next()
            |> mes("[Mage Guild Member]")
            |> mes("Wait, have you checked your Job level?")
            |> mes(
              "Do you want to complete your Novice training and go straight to the Mage Guild's union?"
            )
            |> next()
            |> select(["I will think about it again.", "Yes, complete my training."])

          ctx =
            case v3 do
              1 ->
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("Please test what Mage skills are all about before you decide.")
                  |> mes("We are always welcome to have you!")
                  |> close()

                throw({:script_end, ctx})

              2 ->
                {ctx, _} =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("That's great.")
                  |> mes(
                    "Then I will end the Training Process and send you to the Mage Guild union in Geffen right away."
                  )
                  |> Aesir.ZoneServer.Content.Npc.Functions.FNverase.call([1])

                ctx = ctx |> close() |> savepoint("geffen", 119, 37) |> warp("geffen_in", 163, 98)
                throw({:script_end, ctx})

              _ ->
                ctx
            end

          ctx = set_local(ctx, :quest, checkquest(ctx, 7124))

          if get_local(ctx, :quest, 0) == -1 do
            ctx =
              ctx
              |> mes("[Mage Guild Member]")
              |> mes("Huh? Quest?")
              |> mes("Hmm... What would be suited for you...?")
              |> mes("Would you like to hunt Lunatics by using Bolt skills?")
              |> next()
              |> mes("^4d4dffYou have received a Hunting quest from the Mage Guide.")
              |> mes("You can get more information about the quest from the quest window.^000000")
              |> setquest(7124)
              |> next()
              |> mes("[Mage Guild Member]")
              |> mes("You are able to use 'Fire Bolt' skill that is shown on the Mage manual.")
              |> mes("I will wait for you here.")
              |> mes("Good luck!")
              |> close()

            throw({:script_end, ctx})
          else
            if get_local(ctx, :quest, 0) == 0 or get_local(ctx, :quest, 0) == 1 do
              if checkquest(ctx, 7124, :hunting) == 2 do
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("You have done an excellent job.")
                  |> mes("So, did you find any interests about Mages?")
                  |> mes("Alright. I will give something...")
                  |> mes("Here are Novice Butterfly Wings, please take them.")
                  |> completequest(7124)
                  |> give_item(12324, 30)
                  |> getexp(5000, 100)
                  |> close()

                throw({:script_end, ctx})
              else
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("The quest is that you have to")
                  |> mes("hunt Lunatics and bring 5 clovers.")
                  |> close()

                throw({:script_end, ctx})
              end
            else
              if get_local(ctx, :quest, 0) == 2 do
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("You have already completed the quest.")
                  |> mes(
                    "Once you enter the main world, you can get various exciting quests there!"
                  )
                  |> next()
                  |> mes("[Mage Guild Member]")
                  |> mes("This should be enough for your training.")
                  |> close()

                throw({:script_end, ctx})
              else
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("I'm sorry but I don't have any quests to give you right now.")
                  |> close()

                throw({:script_end, ctx})
              end
            end
          end

        2 ->
          {ctx, v4} =
            ctx
            |> mes("[Mage Guild Member]")
            |> mes("You want to be a Mage?")
            |> mes("Ok, then, I will send you to the Mage Guild union in Geffen right away.")
            |> next()
            |> mes("[Mage Guild Member]")
            |> mes("Wait, have you checked your Job level?")
            |> mes(
              "Do you want to complete your Novice training and go straight to the Mage Guild's union?"
            )
            |> next()
            |> select(["I will think about it again.", "Yes, complete my training."])

          ctx =
            case v4 do
              1 ->
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("Please test what Mage skills are all about before you decide.")
                  |> mes("We are always welcome to have you!")
                  |> close()

                throw({:script_end, ctx})

              2 ->
                {ctx, _} =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("That's great.")
                  |> mes(
                    "Then I will end the Training Process and send you to the Mage Guild union in Geffen right away."
                  )
                  |> Aesir.ZoneServer.Content.Npc.Functions.FNverase.call([1])

                ctx = ctx |> close() |> savepoint("geffen", 119, 37) |> warp("geffen_in", 163, 98)
                throw({:script_end, ctx})

              _ ->
                ctx
            end

          ctx = set_local(ctx, :quest, checkquest(ctx, 7124))

          if get_local(ctx, :quest, 0) == -1 do
            ctx =
              ctx
              |> mes("[Mage Guild Member]")
              |> mes("Huh? Quest?")
              |> mes("Hmm... What would be suited for you...?")
              |> mes("Would you like to hunt Lunatics by using Bolt skills?")
              |> next()
              |> mes("^4d4dffYou have received a Hunting quest from the Mage Guide.")
              |> mes("You can get more information about the quest from the quest window.^000000")
              |> setquest(7124)
              |> next()
              |> mes("[Mage Guild Member]")
              |> mes("You are able to use 'Fire Bolt' skill that is shown on the Mage manual.")
              |> mes("I will wait for you here.")
              |> mes("Good luck!")
              |> close()

            throw({:script_end, ctx})
          else
            if get_local(ctx, :quest, 0) == 0 or get_local(ctx, :quest, 0) == 1 do
              if checkquest(ctx, 7124, :hunting) == 2 do
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("You have done an excellent job.")
                  |> mes("So, did you find any interests about Mages?")
                  |> mes("Alright. I will give something...")
                  |> mes("Here are Novice Butterfly Wings, please take them.")
                  |> completequest(7124)
                  |> give_item(12324, 30)
                  |> getexp(5000, 100)
                  |> close()

                throw({:script_end, ctx})
              else
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("The quest is that you have to")
                  |> mes("hunt Lunatics and bring 5 clovers.")
                  |> close()

                throw({:script_end, ctx})
              end
            else
              if get_local(ctx, :quest, 0) == 2 do
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("You have already completed the quest.")
                  |> mes(
                    "Once you enter the main world, you can get various exciting quests there!"
                  )
                  |> next()
                  |> mes("[Mage Guild Member]")
                  |> mes("This should be enough for your training.")
                  |> close()

                throw({:script_end, ctx})
              else
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("I'm sorry but I don't have any quests to give you right now.")
                  |> close()

                throw({:script_end, ctx})
              end
            end
          end

        3 ->
          ctx = set_local(ctx, :quest, checkquest(ctx, 7124))

          if get_local(ctx, :quest, 0) == -1 do
            ctx =
              ctx
              |> mes("[Mage Guild Member]")
              |> mes("Huh? Quest?")
              |> mes("Hmm... What would be suited for you...?")
              |> mes("Would you like to hunt Lunatics by using Bolt skills?")
              |> next()
              |> mes("^4d4dffYou have received a Hunting quest from the Mage Guide.")
              |> mes("You can get more information about the quest from the quest window.^000000")
              |> setquest(7124)
              |> next()
              |> mes("[Mage Guild Member]")
              |> mes("You are able to use 'Fire Bolt' skill that is shown on the Mage manual.")
              |> mes("I will wait for you here.")
              |> mes("Good luck!")
              |> close()

            throw({:script_end, ctx})
          else
            if get_local(ctx, :quest, 0) == 0 or get_local(ctx, :quest, 0) == 1 do
              if checkquest(ctx, 7124, :hunting) == 2 do
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("You have done an excellent job.")
                  |> mes("So, did you find any interests about Mages?")
                  |> mes("Alright. I will give something...")
                  |> mes("Here are Novice Butterfly Wings, please take them.")
                  |> completequest(7124)
                  |> give_item(12324, 30)
                  |> getexp(5000, 100)
                  |> close()

                throw({:script_end, ctx})
              else
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("The quest is that you have to")
                  |> mes("hunt Lunatics and bring 5 clovers.")
                  |> close()

                throw({:script_end, ctx})
              end
            else
              if get_local(ctx, :quest, 0) == 2 do
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("You have already completed the quest.")
                  |> mes(
                    "Once you enter the main world, you can get various exciting quests there!"
                  )
                  |> next()
                  |> mes("[Mage Guild Member]")
                  |> mes("This should be enough for your training.")
                  |> close()

                throw({:script_end, ctx})
              else
                ctx =
                  ctx
                  |> mes("[Mage Guild Member]")
                  |> mes("I'm sorry but I don't have any quests to give you right now.")
                  |> close()

                throw({:script_end, ctx})
              end
            end
          end

        4 ->
          ctx =
            ctx
            |> mes("[Mage Guild Member]")
            |> mes("If you have any questions about Mages, feel free to ask me.")
            |> mes("I think you could be a intelligent Mage...")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
