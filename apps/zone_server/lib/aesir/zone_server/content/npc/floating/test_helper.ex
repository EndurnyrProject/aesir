defmodule Aesir.ZoneServer.Content.Npc.Floating.TestHelper do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/sage.txt:2865 (Test Helper#sg).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, scope: :shared, spawn: []

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  def on_event("OnTimer2000", ctx), do: ev_ontimer2000(ctx)
  def on_event("OnTimer4000", ctx), do: ev_ontimer4000(ctx)
  def on_event("OnTimer5000", ctx), do: ev_ontimer5000(ctx)
  def on_event("OnTimer7000", ctx), do: ev_ontimer7000(ctx)
  def on_event("OnTimer9000", ctx), do: ev_ontimer9000(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    disablenpc(ctx, "Test Helper#sg")
  end

  def ev_onenable(ctx) do
    initnpctimer(ctx)
  end

  def ev_ontimer2000(ctx) do
    mapannounce(
      ctx,
      "job_sage",
      "Please go back to where you came from and finish the rest of your job change quest.",
      1
    )
  end

  def ev_ontimer4000(ctx) do
    mapannounce(
      ctx,
      "job_sage",
      "This is the end of practical examination. Next candidate, please get ready.",
      1
    )
  end

  def ev_ontimer5000(ctx) do
    areawarp(ctx, "job_sage", 100, 82, 131, 113, "yuno_in03", 163, 180)
  end

  def ev_ontimer7000(ctx) do
    mapannounce(ctx, "job_sage", "Next candidate, please enter.", 1)
  end

  def ev_ontimer9000(ctx) do
    ctx
    |> disablenpc("Test Helper#sg")
    |> donpcevent("Waiting Room#sg::OnEnable")
    |> stopnpctimer()
  end
end
