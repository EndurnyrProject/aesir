defmodule Aesir.ZoneServer.Content.Npc.Floating.Odgnalam do
  @moduledoc """
  Transpiled from rAthena re/cities/malangdo.txt:162 (::Odgnalam).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "alberta", x: 200, y: 151, dir: 4, sprite: 554, name: "Odgnalam"},
      %{map: "izlude", x: 182, y: 218, dir: 4, sprite: 554, name: "Odgnalam"},
      %{map: "izlude_a", x: 182, y: 218, dir: 4, sprite: 554, name: "Odgnalam"},
      %{map: "izlude_b", x: 182, y: 218, dir: 4, sprite: 554, name: "Odgnalam"},
      %{map: "izlude_c", x: 182, y: 218, dir: 4, sprite: 554, name: "Odgnalam"},
      %{map: "izlude_d", x: 182, y: 218, dir: 4, sprite: 554, name: "Odgnalam"}
    ]

  alias Aesir.ZoneServer.Script.Rathena

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes(
        Rathena.concat(
          Rathena.concat(
            "This cat is sleeping in the warm ",
            if(strnpcinfo(ctx, 2) == "albe", do: "Alberta", else: "Izlude")
          ),
          " sun."
        )
      )
      |> next()
      |> select(["Let him sleep", "Talk to him"])

    ctx =
      if v1 == 1 do
        ctx = close(ctx)
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx = ctx |> mes("[Odgnalam]") |> mes("Meow? Huh?") |> next()

    ctx =
      if get_char_var(ctx, :ep13_yong1, 0) == 0 do
        ctx
        |> set_local(:price, 1000)
        |> set_local(
          :"s1$",
          "Huh! You are so annoying. Let me send you to ^A2314BMalangdo^000000 for ^0000FF1000 Zeny^000000."
        )
        |> set_local(
          :"s2$",
          "Money? Where is the money? You can't go anywhere without the money. Shame on you."
        )
      else
        ctx =
          if get_char_var(ctx, :ep13_yong1, 0) < 60 do
            ctx
            |> set_local(:price, 999)
            |> set_local(
              :"s1$",
              "You have a little relationship with fleet of cat. I'll give you special discount price ^0000FF999 Zeny^000000 to ^A2314BMalangdo^000000, because you have relationship with fleet."
            )
            |> set_local(:"s2$", "Where is the money? You don't have a sense of honor.")
          else
            ctx =
              if get_char_var(ctx, :ep13_yong1, 0) > 59 and get_char_var(ctx, :ep13_yong1, 0) < 80 do
                ctx
                |> set_local(:price, 500)
                |> set_local(
                  :"s1$",
                  "You gave several help to our cat's fleet so let me give you special that with 50% discount price ^0000FF500 Zeny^000000. I'll send you to ^A2314BMalangdo^000000."
                )
                |> set_local(
                  :"s2$",
                  "You don't have any money for paying 50% discount price? I can't believe it."
                )
              else
                {ctx, v2} =
                  ctx
                  |> mes("[Odgnalam]")
                  |> mes("You are a hero of cat's fleet. Your activity is the legend between us.")
                  |> next()
                  |> mes("[Odgnalam]")
                  |> mes(
                    "As you are a hero of the cat fleet let me send you to ^A2314BMalangdo^000000 for free."
                  )
                  |> next()
                  |> select(["Send me to Malangdo!", "Do not go."])

                ctx =
                  if v2 == 1 do
                    ctx
                    |> mes("[Odgnalam]")
                    |> mes("Have a comfortable trip... Alright let's go~")
                    |> warp("malangdo", 217, 85)
                  else
                    ctx
                  end

                ctx = close(ctx)
                throw({:script_end, ctx})
              end

            ctx
          end

        ctx
      end

    ctx = mes(ctx, "[Odgnalam]")

    {ctx, v3} =
      ctx
      |> mes(get_local(ctx, :"s1$", ""))
      |> next()
      |> select(["Send me to Malangdo!", "Do not go."])

    ctx =
      if v3 == 2 do
        ctx = close(ctx)
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx =
      if zeny(ctx) < get_local(ctx, :price, 0) do
        ctx =
          ctx
          |> mes("[Odgnalam]")
          |> mes(
            "Money? Where is the money? You can't go anywhere without the money. Shame on you."
          )
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> pay_zeny(get_local(ctx, :price, 0))
    |> mes("[Odgnalam]")
    |> mes("Let me send you right away. Let's go~")
    |> warp("malangdo", 217, 85)
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
