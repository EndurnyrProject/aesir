defmodule Aesir.ZoneServer.Content.Npc.Floating.AirshipStaffIzlude do
  @moduledoc """
  Transpiled from rAthena airports/izlude.txt:15 (::Airship_Staff_izlude).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Airship Staff]")
      |> mes("Welcome! Would you like")
      |> mes("to board the Airship that")
      |> mes("departs on the flight which stops")
      |> mes("in Juno and Rachel?")
      |> next()
      |> select(["Board the Airship to Juno/Rachel.", "Cancel."])

    ctx =
      if v1 == 1 do
        {ctx, v2} =
          ctx
          |> mes("[Airship Staff]")
          |> mes("The boarding fee is")
          |> mes("1,200 zeny. However, this")
          |> mes("charged is waived if you use")
          |> mes("a Free Ticket for Airship. Now,")
          |> mes("would you still like to board?")
          |> next()
          |> select(["Yes", "No"])

        ctx =
          if v2 == 1 do
            ctx =
              if count_item(ctx, 7311) > 0 do
                ctx = ctx |> delitem(7311, 1) |> warp("airplane_01", 244, 58)
                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              if zeny(ctx) >= 1200 do
                ctx = ctx |> pay_zeny(1200) |> warp("airplane_01", 244, 58)
                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              ctx
              |> mes("[Airship Staff]")
              |> mes("I'm sorry, but you don't")
              |> mes("have 1,200 zeny to pay")
              |> mes("for the boarding fee.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx
    |> mes("[Airship Staff]")
    |> mes("Thank you and")
    |> mes("please come again.")
    |> mes("Have a good day~")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
