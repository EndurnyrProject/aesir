defmodule Aesir.ZoneServer.Content.Npc.Floating.Arena2 do
  @moduledoc """
  Transpiled from rAthena jobs/2-2/sage.txt:2848 (Arena#3).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc, spawn: []

  @impl true
  def on_event("OnInit", ctx), do: ev_oninit(ctx)
  def on_event("OnEnable", ctx), do: ev_onenable(ctx)
  def on_event("OnDisable", ctx), do: ev_ondisable(ctx)
  @impl true
  def on_talk(ctx) do
    ctx
  end

  def ev_oninit(ctx) do
    disablenpc(ctx, "Arena#3")
  end

  def ev_onenable(ctx) do
    ctx
    |> summon_mob(mob_id: 1185, map: "job_sage", at: {107, 106})
    |> summon_mob(mob_id: 1185, map: "job_sage", at: {124, 106})
    |> summon_mob(mob_id: 1185, map: "job_sage", at: {107, 89})
    |> summon_mob(mob_id: 1185, map: "job_sage", at: {124, 89})
  end

  def ev_ondisable(ctx) do
    killmonsterall(ctx, "job_sage")
  end
end
