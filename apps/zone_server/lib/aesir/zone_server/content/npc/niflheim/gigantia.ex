defmodule Aesir.ZoneServer.Content.Npc.Niflheim.Gigantia do
  @moduledoc """
  Transpiled from rAthena cities/niflheim.txt:38 (Gigantia#nif).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{map: "niflheim", x: 195, y: 211, dir: 6, sprite: 796, name: "Gigantia", trigger: {3, 3}}
    ]

  alias Aesir.ZoneServer.Script.Rathena
  @impl true
  def on_event("OnTouch", ctx) do
    ev_ontouch(ctx)
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  @impl true
  def on_talk(ctx) do
    ctx =
      if Rathena.truthy?(is_equipped(ctx, 5038)) or Rathena.truthy?(is_equipped(ctx, 2257)) or
           Rathena.truthy?(is_equipped(ctx, 2256)) do
        ctx =
          ctx
          |> mes(Rathena.concat(Rathena.concat("[", char_name(ctx, 0)), "]"))
          |> mes("What's up?")
          |> next()
          |> mes("[Gigantia]")
          |> mes("Just...")
          |> mes("Come over here.")
          |> mes("I have something")
          |> mes("I must do for you.")
          |> next()
          |> mes("[Gigantia]")
          |> mes("Your horn is crooked.")
          |> mes("Always make sure your horn")
          |> mes("is worn straight and neat.")
          |> mes("The Lord of Death is always")
          |> mes("looking at you.")
          |> close()

        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
    |> mes("[Gigantia]")
    |> mes("The Lord of Death knows")
    |> mes("and sees all. It's useless")
    |> mes("to hide, and escape from")
    |> mes("Death's sweet embrace.")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end

  def ev_ontouch(ctx) do
    ctx =
      if Rathena.truthy?(is_equipped(ctx, 5038)) or Rathena.truthy?(is_equipped(ctx, 2257)) or
           Rathena.truthy?(is_equipped(ctx, 2256)) do
        ctx = ctx |> mes("[Gigantia]") |> mes("Hey, wait!") |> close()
        throw({:script_end, ctx})
      else
        ctx
      end

    ctx
  end
end
