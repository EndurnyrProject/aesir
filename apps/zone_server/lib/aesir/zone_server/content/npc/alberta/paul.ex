defmodule Aesir.ZoneServer.Content.Npc.Alberta.Paul do
  @moduledoc """
  Transpiled from rAthena cities/alberta.txt:243 (Paul).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [%{map: "alberta", x: 195, y: 151, dir: 2, sprite: 86, name: "Paul"}]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Paul]")
      |> mes("Good day~")
      |> mes("Would you like")
      |> mes("to join the")
      |> mes("exploration team")
      |> mes("of the Sunken Ship?")
      |> next()
      |> mes("[Paul]")
      |> mes(
        "Oh! Before you join, I must warn you. If you're not that strong, you may not want to go."
      )
      |> next()
      |> mes("[Paul]")
      |> mes("So, want")
      |> mes("to sign up?")
      |> mes("The admission")
      |> mes("fee is only")
      |> mes("200 Zeny.")
      |> next()
      |> select(["Sign me up!", "Uh, no thanks."])

    ctx =
      case v1 do
        1 ->
          ctx =
            if zeny(ctx) < 200 do
              ctx =
                ctx
                |> mes("[Paul]")
                |> mes(
                  "It seems you don't have the money, my friend. But please come back when you're able to pay."
                )
                |> close()

              throw({:script_end, ctx})
            else
              ctx
            end

          ctx = ctx |> pay_zeny(200) |> warp("alb2trea", 62, 69) |> close()
          throw({:script_end, ctx})

        2 ->
          ctx =
            ctx
            |> mes("[Paul]")
            |> mes("Alright, well...")
            |> mes("I'll be around")
            |> mes("if you change")
            |> mes("your mind.")
            |> close()

          throw({:script_end, ctx})

        _ ->
          ctx
      end

    ctx
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
