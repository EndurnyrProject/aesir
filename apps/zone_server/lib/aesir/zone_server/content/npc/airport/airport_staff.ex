defmodule Aesir.ZoneServer.Content.Npc.Airport.AirportStaff do
  @moduledoc """
  Transpiled from rAthena airports/einbroch.txt:15 (Airport Staff#airport1a::airport1).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "airport",
        x: 143,
        y: 43,
        dir: 5,
        sprite: 90,
        name: "Airport Staff",
        unique_name: "airport1"
      },
      %{map: "airport", x: 158, y: 43, dir: 5, sprite: 90, name: "Airport Staff"},
      %{map: "airport", x: 126, y: 43, dir: 5, sprite: 90, name: "Airport Staff"}
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Airport Staff]")
      |> mes("Welcome to the")
      |> mes("Einbroch Airport,")
      |> mes("where we offer nonstop")
      |> mes("flights to the cities of")
      |> mes("Juno, Lighthalzen and Hugel.")
      |> next()
      |> select(["Board the Airship", "Cancel"])

    ctx =
      if v1 == 1 do
        {ctx, v2} =
          ctx
          |> mes("[Airport Staff]")
          |> mes("The Airship boarding fee")
          |> mes("is 1,200 zeny, but if you've")
          |> mes("got a Free Ticket for Airship,")
          |> mes("the fee will be waived. Will")
          |> mes("you board the Airship?")
          |> next()
          |> select(["Yes", "No"])

        ctx =
          if v2 == 1 do
            ctx =
              if count_item(ctx, 7311) > 0 do
                ctx = ctx |> delitem(7311, 1) |> warp("airport", 148, 51)
                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              if zeny(ctx) >= 1200 do
                ctx = ctx |> pay_zeny(1200) |> warp("airport", 148, 51)
                throw({:script_end, ctx})
              else
                ctx
              end

            ctx =
              ctx
              |> mes("[Airport Staff]")
              |> mes("I'm sorry, but you don't")
              |> mes("have a Free Ticket for")
              |> mes("Airship and you don't have")
              |> mes("enough zeny for boarding")
              |> mes("the Airship. Remember, the")
              |> mes("boarding fee is 1,200 zeny.")
              |> close()

            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx |> mes("[Airport Staff]") |> mes("Thank you and") |> mes("have a nice day.") |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
