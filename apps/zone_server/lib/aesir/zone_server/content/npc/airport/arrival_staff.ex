defmodule Aesir.ZoneServer.Content.Npc.Airport.ArrivalStaff do
  @moduledoc """
  Transpiled from rAthena airports/einbroch.txt:61 (Arrival Staff#airport2a::airport2).

  Regenerated by `mix aesir.import.npcs`; hand edits are detected through
  the transpile manifest and future imports land in `_conflicts/` instead
  of overwriting them.
  """

  use Aesir.ZoneServer.Npc,
    spawn: [
      %{
        map: "airport",
        x: 143,
        y: 49,
        dir: 3,
        sprite: 90,
        name: "Arrival Staff",
        unique_name: "airport2"
      },
      %{map: "airport", x: 126, y: 51, dir: 3, sprite: 90, name: "Arrival Staff"},
      %{map: "airport", x: 158, y: 50, dir: 3, sprite: 90, name: "Arrival Staff"}
    ]

  @impl true
  def on_talk(ctx) do
    {ctx, v1} =
      ctx
      |> mes("[Arrival Staff]")
      |> mes("Welcome to Einbroch Airport.")
      |> mes("If you are arriving from your")
      |> mes("flight, let me guide you to the")
      |> mes("main terminal. Otherwise, please board the Airship to depart to")
      |> mes("Juno, Lighthalzen and Hugel.")
      |> next()
      |> select(["Exit to main terminal.", "Cancel."])

    ctx =
      if v1 == 1 do
        {ctx, v2} =
          ctx
          |> mes("[Arrival Staff]")
          |> mes("Once you're in the main terminal, you will need to pay the fee again")
          |> mes("to board an Airship. You should")
          |> mes("only exit if the city of Einbroch")
          |> mes("is your intended destination.")
          |> mes("Proceed to the main terminal?")
          |> next()
          |> select(["Yes", "No"])

        ctx =
          if v2 == 1 do
            ctx = warp(ctx, "airport", 142, 40)
            throw({:script_end, ctx})
          else
            ctx
          end

        ctx
      else
        ctx
      end

    ctx
    |> mes("[Arrival Staff]")
    |> mes("Alright, thank you")
    |> mes("for your patronage")
    |> mes("and I hope you have")
    |> mes("a pleasant flight~")
    |> close()
  catch
    :throw, {:script_end, ctx} -> ctx
  end
end
