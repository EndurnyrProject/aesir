---
- job: merchant
  inherit:
    - novice
  tree:
    - name: MC_DISCOUNT
      max_level: 10
    - name: MC_OVERCHARGE
      max_level: 10
    - name: MC_PUSHCART
      max_level: 10
    - name: MC_IDENTIFY
      max_level: 1
    - name: MC_CHANGECART
      max_level: 1
      # Change Cart requires a mounted cart, represented by the Pushcart prerequisite.
      requires:
        - name: MC_PUSHCART
          level: 1
    - name: MC_CARTREVOLUTION
      max_level: 1
      # Cart Revolution requires a mounted cart, represented by the level 5 Pushcart gate.
      requires:
        - name: MC_PUSHCART
          level: 5
    - name: MC_VENDING
      max_level: 10
      # Vending requires a mounted cart, represented by the Pushcart prerequisite.
      requires:
        - name: MC_PUSHCART
          level: 1
    - name: MC_INCCARRY
      max_level: 10
    - name: MC_MAMMONITE
      max_level: 10
    - name: MC_LOUD
      max_level: 1
