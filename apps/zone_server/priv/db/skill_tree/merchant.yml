---
- job: merchant
  inherit:
    - novice
  tree:
    - name: MC_PUSHCART
      max_level: 10
    - name: MC_CHANGECART
      max_level: 1
      # rAthena gates Change Cart via `State: Cart` (must have a cart equipped),
      # not a skill prereq; we model that as the tree prerequisite MC_PUSHCART.
      requires:
        - name: MC_PUSHCART
          level: 1
