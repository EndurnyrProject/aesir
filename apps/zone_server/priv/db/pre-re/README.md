# Pre-renewal data

This is the pre-renewal data seam. No data is shipped yet; booting with `AESIR_DB_MODE=pre_renewal` fails until the required data is imported. Future population uses the mode-aware importers, for example `mix aesir.import.items --mode pre-re`.
