# Fetch deps when a worktree is created
pre-start = "mix deps.get"

# Quality gates before committing
[pre-commit]
format = "mix format --check-formatted"
credo = "mix credo --strict"

# Full test suite before merging
pre-merge = "mix test"
